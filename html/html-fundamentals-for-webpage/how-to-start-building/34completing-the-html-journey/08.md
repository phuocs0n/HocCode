Quiz: Why do developers finalize and validate HTML before starting CSS?

Tap an option below to answer:

Because validators improve page performance

##### Incorrect, give it another shot!

Validation is about the correctness of your code, not speed or performance.

Validation is about the correctness of your code, not speed or performance.

Because HTML can't be changed later

##### Incorrect, give it another shot!

HTML can always be refactored.

HTML can always be refactored.

Because browsers require validation

##### Incorrect, give it another shot!

Browsers don't enforce validation.

Browsers don't enforce validation.

Because clean structure prevents styling issues later

##### Correct Answer!

Effective styling with CSS depends heavily on reliable HTML.

Effective styling with CSS depends heavily on reliable HTML.

## 📝 Quiz

**Quiz: Why do developers finalize and validate HTML before starting CSS?**

⬚ A. A:Because validators improve page performance
⬚ B. Because validators improve page performance
⬚ C. B:Because HTML can't be changed later
⬚ D. Because HTML can't be changed later
⬚ E. C:Because browsers require validation
⬚ F. Because browsers require validation
⬚ G. D:Because clean structure prevents styling issues later
⬚ H. Because clean structure prevents styling issues later

