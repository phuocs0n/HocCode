Quiz: When should you use a table instead of a list?

Tap an option below to answer:

When content is very long

##### Incorrect, give it another shot!

Length doesn't determine table usage.

Length doesn't determine table usage.

When displaying structured data with rows and columns

##### Correct Answer!

Tables represent relationships between data.

Tables represent relationships between data.

When content needs spacing

##### Incorrect, give it another shot!

Spacing is not a table's purpose.

Spacing is not a table's purpose.

When building page layout

##### Incorrect, give it another shot!

Tables should not be used for layout.

Tables should not be used for layout.

## 📝 Quiz

**Quiz: When should you use a table instead of a list?**

⬚ A. A:When content is very long
⬚ B. When content is very long
⬚ C. B:When displaying structured data with rows and columns
⬚ D. When displaying structured data with rows and columns
⬚ E. C:When content needs spacing
⬚ F. When content needs spacing
⬚ G. D:When building page layout
⬚ H. When building page layout

