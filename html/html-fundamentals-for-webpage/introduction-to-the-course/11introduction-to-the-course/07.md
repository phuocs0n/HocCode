Quiz: Which of the following is not possible to achieve using just HTML?

Tap an option below to answer:

Create headings and paragraphs.

##### Incorrect, give it another shot!

You can use HTML to define headings and create paragraphs on webpages.

You can use HTML to define headings and create paragraphs on webpages.

Insert images.

##### Incorrect, give it another shot!

You can use HTML to create images.

You can use HTML to create images.

Create tables.

##### Incorrect, give it another shot!

You can use HTML to create tables.

You can use HTML to create tables.

Changing the background color of the entire webpage when a button is clicked.

##### Correct Answer!

To perform this task, we need to use CSS and JavaScript. It is not possible to do this using only HTML.

To perform this task, we need to use CSS and JavaScript. It is not possible to do this using only HTML.

## 📝 Quiz

**Quiz: Which of the following is not possible to achieve using just HTML?**

⬚ A. A:Create headings and paragraphs.
⬚ B. Create headings and paragraphs.
⬚ C. B:Insert images.
⬚ D. Insert images.
⬚ E. C:Create tables.
⬚ F. Create tables.
⬚ G. D:Changing the background color of the entire webpage when a button is clicked.
⬚ H. Changing the background color of the entire webpage when a button is clicked.

