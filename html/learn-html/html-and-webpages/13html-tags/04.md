Quiz: If the start tag of an HTML element is <table>, which of the following could be its end tag?

Tap an option below to answer:

<table>

##### Incorrect, give it another shot!

The <table> is a start tag and cannot be used as the end tag.

The <table> is a start tag and cannot be used as the end tag.

<table/>

##### Incorrect, give it another shot!

The end tag for an HTML element should have a forward slash after the angle bracket, not after the tag name.

The end tag for an HTML element should have a forward slash after the angle bracket, not after the tag name.

</table>

##### Correct Answer!

The end tag of <table> is </table>.

The end tag of <table> is </table>.

<table-end>

##### Incorrect, give it another shot!

<table-end> is not a valid HTML tag format and does not follow the standard closing tag syntax.

<table-end> is not a valid HTML tag format and does not follow the standard closing tag syntax.

##### Incorrect, give it another shot!

The <table> is a start tag and cannot be used as the end tag.

The <table> is a start tag and cannot be used as the end tag.

##### Incorrect, give it another shot!

The end tag for an HTML element should have a forward slash after the angle bracket, not after the tag name.

The end tag for an HTML element should have a forward slash after the angle bracket, not after the tag name.

##### Correct Answer!

The end tag of <table> is </table>.

The end tag of <table> is </table>.

##### Incorrect, give it another shot!

<table-end> is not a valid HTML tag format and does not follow the standard closing tag syntax.

<table-end> is not a valid HTML tag format and does not follow the standard closing tag syntax.

## 📝 Quiz

**Quiz: If the start tag of an HTML element is <table>, which of the following could be its end tag?**

⬚ A. A:<table>
⬚ B. <table>
⬚ C. B:<table/>
⬚ D. <table/>
⬚ E. C:</table>
⬚ F. </table>
⬚ G. D:<table-end>
⬚ H. <table-end>

