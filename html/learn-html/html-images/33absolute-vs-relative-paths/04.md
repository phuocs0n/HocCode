Quiz: If the current URL is https://example.com/hello, where will the link be directed to in the following code?

```html
<a href="abc">
    Programiz PRO website
</a>
```

Tap an option below to answer:

```html
https://example.com/abc
```

##### Incorrect, give it another shot!

We have used the relative path abc in our code. Therefore, abc will be appended to the current path.

We have used the relative path abc in our code. Therefore, abc will be appended to the current path.

```html
https://example.com
```

##### Incorrect, give it another shot!

The relative path abc doesn't point to the domain root.

The relative path abc doesn't point to the domain root.

```html
https://example.com/hello/abc
```

##### Correct Answer!

The relative path abc is appended to the current path.

The relative path abc is appended to the current path.

```html
https://programiz.pro/hello/abc
```

##### Incorrect, give it another shot!

Since the domain of the current page is https://example.com, the link is not directed to a different domain.

Since the domain of the current page is https://example.com, the link is not directed to a different domain.

## 📝 Quiz

**Quiz: If the current URL is https://example.com/hello, where will the link be directed to in the following code?**

⬚ A. A:https://example.com/abc
⬚ B. https://example.com/abc
⬚ C. B:https://example.com
⬚ D. https://example.com
⬚ E. C:https://example.com/hello/abc
⬚ F. https://example.com/hello/abc
⬚ G. D:https://programiz.pro/hello/abc
⬚ H. https://programiz.pro/hello/abc

