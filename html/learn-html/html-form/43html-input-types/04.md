Quiz: You need a form where users can choose all the hobbies they are interested in from a list.

Which input type should you use?

Tap an option below to answer:

```html
<input type="radio">
```

##### Incorrect, give it another shot!

Radio buttons are used when you want the user to select only one option from a group.

Radio buttons are used when you want the user to select only one option from a group.

```html
<input type="checkbox">
```

##### Correct Answer!

Checkboxes allow users to select multiple options from a list.

Checkboxes allow users to select multiple options from a list.

```html
<input type="password">
```

##### Incorrect, give it another shot!

Password fields are used for entering sensitive information that needs to be masked.

Password fields are used for entering sensitive information that needs to be masked.

```html
<input type="file">
```

##### Incorrect, give it another shot!

File input fields are used for uploading files. They are not suitable for selecting multiple hobbies from a list of options.

File input fields are used for uploading files. They are not suitable for selecting multiple hobbies from a list of options.

## 📝 Quiz

**Quiz: You need a form where users can choose all the hobbies they are interested in from a list.**

⬚ A. A:<input type="radio">
⬚ B. <input type="radio">
⬚ C. B:<input type="checkbox">
⬚ D. <input type="checkbox">
⬚ E. C:<input type="password">
⬚ F. <input type="password">
⬚ G. D:<input type="file">
⬚ H. <input type="file">

