Quiz: Which of the following is true regarding the processing of user inputs in HTML forms?

Tap an option below to answer:

HTML alone can process user inputs.

##### Incorrect, give it another shot!

HTML can only create the form structure; it cannot process user inputs.

HTML can only create the form structure; it cannot process user inputs.

To process form inputs, we must use a programming language.

##### Correct Answer!

HTML forms can collect user inputs, but to process these inputs (e.g., validate and store them in a database), we need to use programming logic.

HTML forms can collect user inputs, but to process these inputs (e.g., validate and store them in a database), we need to use programming logic.

CSS can process user inputs

##### Incorrect, give it another shot!

CSS is used to style webpages and cannot process user inputs.

CSS is used to style webpages and cannot process user inputs.

User inputs do not need processing.

##### Incorrect, give it another shot!

User inputs usually need to be processed for validation, storage, and various other functionalities.

User inputs usually need to be processed for validation, storage, and various other functionalities.

## 📝 Quiz

**Quiz: Which of the following is true regarding the processing of user inputs in HTML forms?**

⬚ A. A:HTML alone can process user inputs.
⬚ B. HTML alone can process user inputs.
⬚ C. B:To process form inputs, we must use a programming language.
⬚ D. To process form inputs, we must use a programming language.
⬚ E. C:CSS can process user inputs
⬚ F. CSS can process user inputs
⬚ G. D:User inputs do not need processing.
⬚ H. User inputs do not need processing.

