Quiz: Which of the following is a good situation to use the <pre> tag?

Tap an option below to answer:

To emphasize important phrases within a paragraph.

##### Incorrect, give it another shot!

We use the <em> for this.

We use the <em> for this.

To display a block of Python code where whitespaces are important.

##### Correct Answer!

Since whitespaces are crucial in Python code, using the <pre> tag is ideal in this situation.

Since whitespaces are crucial in Python code, using the <pre> tag is ideal in this situation.

To create a list of items.

##### Incorrect, give it another shot!

We use either <ul> or <ol> tags to create a list of items.

We use either <ul> or <ol> tags to create a list of items.

To display prices neatly in a table format.

##### Incorrect, give it another shot!

We use HTML tables for it.

We use HTML tables for it.

## 📝 Quiz

**Quiz: Which of the following is a good situation to use the <pre> tag?**

⬚ A. A:To emphasize important phrases within a paragraph.
⬚ B. To emphasize important phrases within a paragraph.
⬚ C. B:To display a block of Python code where whitespaces are important.
⬚ D. To display a block of Python code where whitespaces are important.
⬚ E. C:To create a list of items.
⬚ F. To create a list of items.
⬚ G. D:To display prices neatly in a table format.
⬚ H. To display prices neatly in a table format.

