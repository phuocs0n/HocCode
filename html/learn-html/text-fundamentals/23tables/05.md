Quiz: Which of the following is the correct way to create an HTML table?

Tap an option below to answer:

```html
<table>
  <td>
    <tr>Item 3</tr>
    <tr>Item 4</tr>
  </td>
</table>
```

##### Incorrect, give it another shot!

Here, the table cells, <td>, are incorrectly placed directly under the <table> tag, and the table rows, <tr>, are incorrectly nested within. This is not a correct way to structure an HTML table.

Here, the table cells, <td>, are incorrectly placed directly under the <table> tag, and the table rows, <tr>, are incorrectly nested within. This is not a correct way to structure an HTML table.

```html
<table>
  <tr>
    <td>Item 3</td>
    <td>Item 4</td>
  </tr>
</table>
```

##### Correct Answer!

This is the proper way to structure an HTML table, with the table rows (<tr>) nested within the <table> tag, and the table cells (<td>) nested within the <tr> tags.

This is the proper way to structure an HTML table, with the table rows (<tr>) nested within the <table> tag, and the table cells (<td>) nested within the <tr> tags.

```html
<table>
  <td>Item 3</td>
  <td>Item 4</td>
</table>
```

##### Incorrect, give it another shot!

The <td> tags are not wrapped inside <tr> tags. Therefore, it is not a valid HTML code.

The <td> tags are not wrapped inside <tr> tags. Therefore, it is not a valid HTML code.

```html
<table>
  <tr>Item 3</tr>
  <tr>Item 4</tr>
</table>
```

##### Incorrect, give it another shot!

The <tr> tags are incorrectly used without containing <td> tags. Therefore, it is not a valid HTML code.

The <tr> tags are incorrectly used without containing <td> tags. Therefore, it is not a valid HTML code.

## 📝 Quiz

**Quiz: Which of the following is the correct way to create an HTML table?**

⬚ A. A:<table>
  <td>
    <tr>Item 3</tr>
    <tr>Item 4</tr>
  </td>
</table>
⬚ B. <table>
  <td>
    <tr>Item 3</tr>
    <tr>Item 4</tr>
  </td>
</table>
⬚ C. B:<table>
  <tr>
    <td>Item 3</td>
    <td>Item 4</td>
  </tr>
</table>
⬚ D. <table>
  <tr>
    <td>Item 3</td>
    <td>Item 4</td>
  </tr>
</table>
⬚ E. C:<table>
  <td>Item 3</td>
  <td>Item 4</td>
</table>
⬚ F. <table>
  <td>Item 3</td>
  <td>Item 4</td>
</table>
⬚ G. D:<table>
  <tr>Item 3</tr>
  <tr>Item 4</tr>
</table>
⬚ H. <table>
  <tr>Item 3</tr>
  <tr>Item 4</tr>
</table>

