Quiz: Suppose you need a list to describe the steps for installing software.

Which type of list should you use in this case?

Tap an option below to answer:

Unordered list (<ul>)

##### Incorrect, give it another shot!

Since the order of items is important in this case, we should not use an unordered list.

Since the order of items is important in this case, we should not use an unordered list.

Ordered list (<ol>)

##### Correct Answer!

Since the order of items is important in this case, we should use an ordered list.

Since the order of items is important in this case, we should use an ordered list.

Magic list (<ml>)

##### Incorrect, give it another shot!

There is no such list in HTML.

There is no such list in HTML.

None of the above.

##### Incorrect, give it another shot!

One of the above answers is correct.

One of the above answers is correct.

##### Incorrect, give it another shot!

Since the order of items is important in this case, we should not use an unordered list.

Since the order of items is important in this case, we should not use an unordered list.

##### Correct Answer!

Since the order of items is important in this case, we should use an ordered list.

Since the order of items is important in this case, we should use an ordered list.

##### Incorrect, give it another shot!

There is no such list in HTML.

There is no such list in HTML.

##### Incorrect, give it another shot!

One of the above answers is correct.

One of the above answers is correct.

## 📝 Quiz

**Quiz: Suppose you need a list to describe the steps for installing software.**

⬚ A. A:Unordered list (<ul>)
⬚ B. Unordered list (<ul>)
⬚ C. B:Ordered list (<ol>)
⬚ D. Ordered list (<ol>)
⬚ E. C:Magic list (<ml>)
⬚ F. Magic list (<ml>)
⬚ G. D:None of the above.
⬚ H. None of the above.

