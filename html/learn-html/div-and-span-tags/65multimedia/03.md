Quiz: In HTML code, which tag is used to embed a media player that supports audio playback on a webpage?

Tap an option below to answer:

```html
<img>
```

##### Incorrect, give it another shot!

The <img> tag is used to insert an image, not to embed a media player that can play audio files.

The <img> tag is used to insert an image, not to embed a media player that can play audio files.

```html
<audio>
```

##### Correct Answer!

The <audio> tag, along with the source attribute, is used to embed a media player to play audio files.

The <audio> tag, along with the source attribute, is used to embed a media player to play audio files.

```html
<video>
```

##### Incorrect, give it another shot!

The <video> tag is used to embed a media player that can play video files, not audio files.

The <video> tag is used to embed a media player that can play video files, not audio files.

```html
<source>
```

##### Incorrect, give it another shot!

The <source> tag is used within <audio> or <video> tags to provide the source of the media content, not to embed the media player itself.

The <source> tag is used within <audio> or <video> tags to provide the source of the media content, not to embed the media player itself.

## 📝 Quiz

**Quiz: In HTML code, which tag is used to embed a media player that supports audio playback on a webpage?**

⬚ A. A:<img>
⬚ B. <img>
⬚ C. B:<audio>
⬚ D. <audio>
⬚ E. C:<video>
⬚ F. <video>
⬚ G. D:<source>
⬚ H. <source>

