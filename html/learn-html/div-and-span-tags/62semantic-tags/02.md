Quiz: Which HTML tag should you use to group the main content of a webpage for better semantic meaning?

Tap an option below to answer:

<header>

##### Incorrect, give it another shot!

The <header> tag represents the introductory content of a webpage, not the main content.

The <header> tag represents the introductory content of a webpage, not the main content.

<nav>

##### Incorrect, give it another shot!

The <nav> tag is used for navigation links, not the main content.

The <nav> tag is used for navigation links, not the main content.

<main>

##### Correct Answer!

The <main> tag is used to group the main content of a webpage for better semantics.

The <main> tag is used to group the main content of a webpage for better semantics.

<footer>

##### Incorrect, give it another shot!

The <footer> tag is used for the footer section of a document, not the main content.

The <footer> tag is used for the footer section of a document, not the main content.

##### Incorrect, give it another shot!

The <header> tag represents the introductory content of a webpage, not the main content.

The <header> tag represents the introductory content of a webpage, not the main content.

##### Incorrect, give it another shot!

The <nav> tag is used for navigation links, not the main content.

The <nav> tag is used for navigation links, not the main content.

##### Correct Answer!

The <main> tag is used to group the main content of a webpage for better semantics.

The <main> tag is used to group the main content of a webpage for better semantics.

##### Incorrect, give it another shot!

The <footer> tag is used for the footer section of a document, not the main content.

The <footer> tag is used for the footer section of a document, not the main content.

## 📝 Quiz

**Quiz: Which HTML tag should you use to group the main content of a webpage for better semantic meaning?**

⬚ A. A:<header>
⬚ B. <header>
⬚ C. B:<nav>
⬚ D. <nav>
⬚ E. C:<main>
⬚ F. <main>
⬚ G. D:<footer>
⬚ H. <footer>

