Quiz: What is the purpose of an iframe in HTML?

Tap an option below to answer:

To create a new window inside an HTML document.

##### Incorrect, give it another shot!

The iframe tag allows embedding another HTML document within the current HTML document. It doesn't create a new window.

The iframe tag allows embedding another HTML document within the current HTML document. It doesn't create a new window.

To embed another HTML document within the current document.

##### Correct Answer!

The iframe tag is used for embedding an HTML document within the current HTML document.

The iframe tag is used for embedding an HTML document within the current HTML document.

To insert an image in an HTML document.

##### Incorrect, give it another shot!

The img tag is used for adding images, not the iframe tag.

The img tag is used for adding images, not the iframe tag.

To add CSS styles to an HTML document.

##### Incorrect, give it another shot!

The iframe is used to embed a document, not to add CSS styles.

The iframe is used to embed a document, not to add CSS styles.

## 📝 Quiz

**Quiz: What is the purpose of an iframe in HTML?**

⬚ A. A:To create a new window inside an HTML document.
⬚ B. To create a new window inside an HTML document.
⬚ C. B:To embed another HTML document within the current document.
⬚ D. To embed another HTML document within the current document.
⬚ E. C:To insert an image in an HTML document.
⬚ F. To insert an image in an HTML document.
⬚ G. D:To add CSS styles to an HTML document.
⬚ H. To add CSS styles to an HTML document.

