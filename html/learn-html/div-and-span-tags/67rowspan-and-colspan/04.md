Quiz: Which of the following codes is used to create a table where a cell spans across four rows?

Tap an option below to answer:

<td colspan="4">...</td>

##### Incorrect, give it another shot!

<td colspan="4">...</td> makes a cell span across four columns, not rows.

<td colspan="4">...</td> makes a cell span across four columns, not rows.

<td rowspan="4">...</td>

##### Correct Answer!

The rowspan attribute makes a cell span across four rows.

The rowspan attribute makes a cell span across four rows.

<table colspan="4">...</table>

##### Incorrect, give it another shot!

The colspan attribute is not used with tables, rather, it is used with a data cell element.

The colspan attribute is not used with tables, rather, it is used with a data cell element.

<table rows="4">...</table>

##### Incorrect, give it another shot!

The rowspan attribute is not used with tables, rather, it is used with a data cell element.

The rowspan attribute is not used with tables, rather, it is used with a data cell element.

## 📝 Quiz

**Quiz: Which of the following codes is used to create a table where a cell spans across four rows?**

⬚ A. A:<td colspan="4">...</td>
⬚ B. <td colspan="4">...</td>
⬚ C. B:<td rowspan="4">...</td>
⬚ D. <td rowspan="4">...</td>
⬚ E. C:<table colspan="4">...</table>
⬚ F. <table colspan="4">...</table>
⬚ G. D:<table rows="4">...</table>
⬚ H. <table rows="4">...</table>

