Quiz: Which of the following is the correct way to write the title of a webpage?

Tap an option below to answer:

```html
<h1>Title Content</h1>
```

##### Incorrect, give it another shot!

The <h1> tag defines the heading 1 element of a webpage.

The <h1> tag defines the heading 1 element of a webpage.

```html
<head>Title Content</head>
```

##### Incorrect, give it another shot!

The <head> tag defines the head section of a page. We define the title element inside it.

The <head> tag defines the head section of a page. We define the title element inside it.

```html
<title>Title Content</title>
```

##### Correct Answer!

We use the <title> tag to define the title of a page.

We use the <title> tag to define the title of a page.

```html
<t1>Title Content</t1>
```

##### Incorrect, give it another shot!

There is no such tag as <t1> in HTML.

There is no such tag as <t1> in HTML.

## 📝 Quiz

**Quiz: Which of the following is the correct way to write the title of a webpage?**

⬚ A. A:<h1>Title Content</h1>
⬚ B. <h1>Title Content</h1>
⬚ C. B:<head>Title Content</head>
⬚ D. <head>Title Content</head>
⬚ E. C:<title>Title Content</title>
⬚ F. <title>Title Content</title>
⬚ G. D:<t1>Title Content</t1>
⬚ H. <t1>Title Content</t1>

