Quiz: What is the primary purpose of a link tag in HTML?

Tap an option below to answer:

To create a hyperlink.

##### Incorrect, give it another shot!

The tag is used to create hyperlinks, not a link tag.

The tag is used to create hyperlinks, not a link tag.

To define the relationship between the current document and an external resource.

##### Correct Answer!

The link tag in HTML is used to link an external resource like a CSS file or a favicon.

The link tag in HTML is used to link an external resource like a CSS file or a favicon.

To display images in the document.

##### Incorrect, give it another shot!

The img tag is used to display images in HTML documents.

The img tag is used to display images in HTML documents.

To mark a section in the document.

##### Incorrect, give it another shot!

The link tag is used for linking external resources, not for marking sections of a document.

The link tag is used for linking external resources, not for marking sections of a document.

## 📝 Quiz

**Quiz: What is the primary purpose of a link tag in HTML?**

⬚ A. A:To create a hyperlink.
⬚ B. To create a hyperlink.
⬚ C. B:To define the relationship between the current document and an external resource.
⬚ D. To define the relationship between the current document and an external resource.
⬚ E. C:To display images in the document.
⬚ F. To display images in the document.
⬚ G. D:To mark a section in the document.
⬚ H. To mark a section in the document.

