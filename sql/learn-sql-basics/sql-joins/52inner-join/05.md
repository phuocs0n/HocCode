Quiz: What does the following SQL code do?

```sql
SELECT e.*, d.*
FROM Employees e
JOIN Departments d
ON e.department_id = d.department_id;
```

Tap an option below to answer:

Results in an error because department_id and department columns are joined.

##### Incorrect, give it another shot!

We can join two tables even if they have different column names.As long as the Employees table has a department column and the Departments table has a depratment_id column, the SQL query will not result in an error.

We can join two tables even if they have different column names.

As long as the Employees table has a department column and the Departments table has a depratment_id column, the SQL query will not result in an error.

Joins the Employees and Departments tables and returns all the columns from the Employees table.

##### Incorrect, give it another shot!

This SQL code returns all the columns from both tables, not just the Employees table.

This SQL code returns all the columns from both tables, not just the Employees table.

Joins the Employees and Departments tables and returns all columns from the Departments table.

##### Incorrect, give it another shot!

This SQL code returns all the columns from both tables, not just the Departments table.

This SQL code returns all the columns from both tables, not just the Departments table.

Joins the Employees and Departments tables and returns all columns from both tables.

##### Correct Answer!

The e.* notation selects all the columns from the Employees table, and the d.* notation selects all the columns from the Departments table.By the way, we could have replaced e.*, d.* with * in the above code.

The e.* notation selects all the columns from the Employees table, and the d.* notation selects all the columns from the Departments table.

By the way, we could have replaced e.*, d.* with * in the above code.

## 📝 Quiz

**Quiz: What does the following SQL code do?**

⬚ A. A:Results in an error because department_id and department columns are joined.
⬚ B. Results in an error because department_id and department columns are joined.
⬚ C. B:Joins the Employees and Departments tables and returns all the columns from the Employees table.
⬚ D. Joins the Employees and Departments tables and returns all the columns from the Employees table.
⬚ E. C:Joins the Employees and Departments tables and returns all columns from the Departments table.
⬚ F. Joins the Employees and Departments tables and returns all columns from the Departments table.
⬚ G. D:Joins the Employees and Departments tables and returns all columns from both tables.
⬚ H. Joins the Employees and Departments tables and returns all columns from both tables.

