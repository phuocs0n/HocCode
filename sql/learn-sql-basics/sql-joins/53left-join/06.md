Quiz: Which table is the left table in the following SQL statement?

```sql
SELECT e.first_name, e.last_name, d.manager
FROM Employees e
JOIN Departments d
ON e.department = d.department_name;
```

Tap an option below to answer:

The Customers table is the left table.

##### Incorrect, give it another shot!

We haven't used the Customers table in the above query.

We haven't used the Customers table in the above query.

The Employees table is the left table.

##### Correct Answer!

Employees is the left table because it is the first table in the above query.

Employees is the left table because it is the first table in the above query.

The Departments table is the left table.

##### Incorrect, give it another shot!

Departments is not the left table because it is not the first table in the above query.

Departments is not the left table because it is not the first table in the above query.

None of the above.

##### Incorrect, give it another shot!

One of the above answers is correct.

One of the above answers is correct.

## 📝 Quiz

**Quiz: Which table is the left table in the following SQL statement?**

⬚ A. A:The Customers table is the left table.
⬚ B. The Customers table is the left table.
⬚ C. B:The Employees table is the left table.
⬚ D. The Employees table is the left table.
⬚ E. C:The Departments table is the left table.
⬚ F. The Departments table is the left table.
⬚ G. D:None of the above.
⬚ H. None of the above.

