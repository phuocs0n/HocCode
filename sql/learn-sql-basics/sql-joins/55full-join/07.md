Quiz: In which of the following scenarios would a FULL JOIN be the most appropriate type of JOIN to use?

Tap an option below to answer:

When you want to return only unique rows from both tables.

##### Incorrect, give it another shot!

A FULL JOIN is not the right choice if you want to return only unique rows from both tables.

A FULL JOIN is not the right choice if you want to return only unique rows from both tables.

When you want to return all rows from both tables, whether there is a match in the other table or not.

##### Correct Answer!

A FULL JOIN is useful if we want to return all rows from both tables, whether there is a match in the other table or not.

A FULL JOIN is useful if we want to return all rows from both tables, whether there is a match in the other table or not.

When you want to return only matching rows from both tables.

##### Incorrect, give it another shot!

It is better to use INNER JOIN if you want to return only matching rows from both tables.

It is better to use INNER JOIN if you want to return only matching rows from both tables.

None of the above.

##### Incorrect, give it another shot!

One of the above answers is correct.

One of the above answers is correct.

## 📝 Quiz

**Quiz: In which of the following scenarios would a FULL JOIN be the most appropriate type of JOIN to use?**

⬚ A. A:When you want to return only unique rows from both tables.
⬚ B. When you want to return only unique rows from both tables.
⬚ C. B:When you want to return all rows from both tables, whether there is a match in the other table or not.
⬚ D. When you want to return all rows from both tables, whether there is a match in the other table or not.
⬚ E. C:When you want to return only matching rows from both tables.
⬚ F. When you want to return only matching rows from both tables.
⬚ G. D:None of the above.
⬚ H. None of the above.

