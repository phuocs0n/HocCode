Quiz: What does a RIGHT JOIN do in SQL?

Tap an option below to answer:

Returns all the rows from the left table and matching rows from the right table

##### Incorrect, give it another shot!

That is what a LEFT JOIN does, not a RIGHT JOIN.

That is what a LEFT JOIN does, not a RIGHT JOIN.

Returns all the rows from the right table and matching rows from the left table.

##### Correct Answer!

A RIGHT JOIN selects all rows from the right table and matching rows from the left table.

A RIGHT JOIN selects all rows from the right table and matching rows from the left table.

Returns all rows from both tables, regardless of whether they match or not.

##### Incorrect, give it another shot!

That is what a FULL JOIN does, which we'll learn in the next lesson.

That is what a FULL JOIN does, which we'll learn in the next lesson.

Returns rows that match between the two tables.

##### Incorrect, give it another shot!

That is what an INNER JOIN does, not a RIGHT JOIN.

That is what an INNER JOIN does, not a RIGHT JOIN.

## 📝 Quiz

**Quiz: What does a RIGHT JOIN do in SQL?**

⬚ A. A:Returns all the rows from the left table and matching rows from the right table
⬚ B. Returns all the rows from the left table and matching rows from the right table
⬚ C. B:Returns all the rows from the right table and matching rows from the left table.
⬚ D. Returns all the rows from the right table and matching rows from the left table.
⬚ E. C:Returns all rows from both tables, regardless of whether they match or not.
⬚ F. Returns all rows from both tables, regardless of whether they match or not.
⬚ G. D:Returns rows that match between the two tables.
⬚ H. Returns rows that match between the two tables.

