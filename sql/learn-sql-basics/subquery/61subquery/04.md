Quiz: In the following SQL query, which section is the subquery?

```sql
SELECT name
FROM Students
WHERE id IN (
    SELECT id
    FROM Students
    WHERE grade = 'A'
);
```

Tap an option below to answer:

```sql
SELECT name FROM Students
```

##### Incorrect, give it another shot!

This is part of the main query that retrieves data from the Students table, but it doesn't contain the subquery itself.

This is part of the main query that retrieves data from the Students table, but it doesn't contain the subquery itself.

```sql
WHERE id IN (SELECT id FROM Students WHERE grade = 'A')
```

##### Incorrect, give it another shot!

This clause uses the result of a subquery to filter data. Look inside the parentheses to find the subquery.

This clause uses the result of a subquery to filter data. Look inside the parentheses to find the subquery.

```sql
WHERE grade = 'A'
```

##### Incorrect, give it another shot!

This is part of the subquery's WHERE clause, but it's not the subquery itself. The subquery is the entire block inside the parentheses.

This is part of the subquery's WHERE clause, but it's not the subquery itself. The subquery is the entire block inside the parentheses.

```sql
SELECT id FROM Students WHERE grade = 'A';
```

##### Correct Answer!

This is a subquery because it is a query nested inside another query, and its result is used by the outer query to filter or modify the main query's results.

This is a subquery because it is a query nested inside another query, and its result is used by the outer query to filter or modify the main query's results.

## 📝 Quiz

**Quiz: In the following SQL query, which section is the subquery?**

⬚ A. A:SELECT name FROM Students
⬚ B. SELECT name FROM Students
⬚ C. B:WHERE id IN (SELECT id FROM Students WHERE grade = 'A')
⬚ D. WHERE id IN (SELECT id FROM Students WHERE grade = 'A')
⬚ E. C:WHERE grade = 'A'
⬚ F. WHERE grade = 'A'
⬚ G. D:SELECT id FROM Students WHERE grade = 'A';
⬚ H. SELECT id FROM Students WHERE grade = 'A';

