Quiz: What is the alternative syntax for the following query?

```sql
SELECT * FROM Employees
ORDER BY Country ASC;
```

Tap an option below to answer:

```sql
SELECT * FROM Employees
ORDER BY Country DESC;
```

##### Incorrect, give it another shot!

This query sorts the output in descending order by the Country column.

This query sorts the output in descending order by the Country column.

```sql
SELECT * FROM Employees
ORDER BY Country;
```

##### Correct Answer!

This query sorts the output by the Country column in ascending order. It is because the ASC keyword is optional if we want to sort the output in ascending order.

This query sorts the output by the Country column in ascending order. It is because the ASC keyword is optional if we want to sort the output in ascending order.

```sql
SELECT * FROM Employees;
```

##### Incorrect, give it another shot!

The query does not sort the output. The order of rows in the output will be the same as the order of rows in our table.

The query does not sort the output. The order of rows in the output will be the same as the order of rows in our table.

None of the above.

##### Incorrect, give it another shot!

One of the above answers is correct.

One of the above answers is correct.

## 📝 Quiz

**Quiz: What is the alternative syntax for the following query?**

⬚ A. A:SELECT * FROM Employees
ORDER BY Country DESC;
⬚ B. SELECT * FROM Employees
ORDER BY Country DESC;
⬚ C. B:SELECT * FROM Employees
ORDER BY Country;
⬚ D. SELECT * FROM Employees
ORDER BY Country;
⬚ E. C:SELECT * FROM Employees;
⬚ F. SELECT * FROM Employees;
⬚ G. D:None of the above.
⬚ H. None of the above.

