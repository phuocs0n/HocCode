Quiz: Suppose you have a table named Employees with the fields: id, salary, name, email, and city.

How can you select the names and emails of employees whose salary is greater than or equal to 5000?

Tap an option below to answer:

```sql
SELECT *
FROM Employees
WHERE salary >= 5000;
```

##### Incorrect, give it another shot!

This query selects all the columns, but you just need the name and email columns.

This query selects all the columns, but you just need the name and email columns.

```sql
SELECT name, email
FROM Employees
WHERE salary > 5000;
```

##### Incorrect, give it another shot!

This query selects the name and email columns of employees whose salary is greater than 5000.If their salary is exactly equal to 5000, those rows are ignored.

This query selects the name and email columns of employees whose salary is greater than 5000.

If their salary is exactly equal to 5000, those rows are ignored.

```sql
SELECT name, email
FROM Employees
WHERE salary >= 5000;
```

##### Correct Answer!

This query selects the name and email columns of employees whose salary is greater than or equal to 5000.

This query selects the name and email columns of employees whose salary is greater than or equal to 5000.

```sql
SELECT name, email
FROM Employees
WHERE salary <= 5000;
```

##### Incorrect, give it another shot!

This query selects the name and email columns of those employees whose salary is less than or equal to 5000. However, you want records of employees whose salary is greater than or equal to 5000.

This query selects the name and email columns of those employees whose salary is less than or equal to 5000. However, you want records of employees whose salary is greater than or equal to 5000.

## 📝 Quiz

**Quiz: Suppose you have a table named Employees with the fields: id, salary, name, email, and city.**

⬚ A. A:SELECT *
FROM Employees
WHERE salary >= 5000;
⬚ B. SELECT *
FROM Employees
WHERE salary >= 5000;
⬚ C. B:SELECT name, email
FROM Employees
WHERE salary > 5000;
⬚ D. SELECT name, email
FROM Employees
WHERE salary > 5000;
⬚ E. C:SELECT name, email
FROM Employees
WHERE salary >= 5000;
⬚ F. SELECT name, email
FROM Employees
WHERE salary >= 5000;
⬚ G. D:SELECT name, email
FROM Employees
WHERE salary <= 5000;
⬚ H. SELECT name, email
FROM Employees
WHERE salary <= 5000;

