Quiz: Consider this SQL query:

```sql
SELECT age
FROM Customers
WHERE age IN (22, 28);
```

Which of the following is equivalent to the query given above?

Tap an option below to answer:

```sql
SELECT age
FROM Customers
WHERE age > 22 OR age < 28;
```

##### Incorrect, give it another shot!

This query selects customers with ages greater than 22 or less than 28.

This query selects customers with ages greater than 22 or less than 28.

```sql
SELECT age
FROM Customers
WHERE age = 22 AND age = 28;
```

##### Incorrect, give it another shot!

This query selects customers with ages that are both 22 and 28, which is not possible. Hence, the result set will be empty.

This query selects customers with ages that are both 22 and 28, which is not possible. Hence, the result set will be empty.

```sql
SELECT age
FROM Customers
WHERE age = 22 OR age = 28;
```

##### Correct Answer!

This query selects customers with ages of either 22 or 28.

This query selects customers with ages of either 22 or 28.

None of the above.

##### Incorrect, give it another shot!

One of the above answers is correct.

One of the above answers is correct.

## 📝 Quiz

**Quiz: Consider this SQL query:**

⬚ A. A:SELECT age
FROM Customers
WHERE age > 22 OR age < 28;
⬚ B. SELECT age
FROM Customers
WHERE age > 22 OR age < 28;
⬚ C. B:SELECT age
FROM Customers
WHERE age = 22 AND age = 28;
⬚ D. SELECT age
FROM Customers
WHERE age = 22 AND age = 28;
⬚ E. C:SELECT age
FROM Customers
WHERE age = 22 OR age = 28;
⬚ F. SELECT age
FROM Customers
WHERE age = 22 OR age = 28;
⬚ G. D:None of the above.
⬚ H. None of the above.

