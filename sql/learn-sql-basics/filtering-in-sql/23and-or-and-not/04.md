Quiz: As a data analyst, you need to retrieve all customers' names and email addresses in California or Texas.

Which of the following queries will return the correct result set?

Tap an option below to answer:

```sql
SELECT name, email
FROM Customers
WHERE state = 'California' OR state = 'Texas';
```

##### Correct Answer!

This query uses the OR operator correctly to include customers from California or Texas.

This query uses the OR operator correctly to include customers from California or Texas.

```sql
SELECT name, email
FROM Customers
WHERE state = 'California' OR 'Texas';
```

##### Incorrect, give it another shot!

The condition after the OR operator should be state = 'Texas' instead of just 'Texas'.

The condition after the OR operator should be state = 'Texas' instead of just 'Texas'.

```sql
SELECT name, email
From Customers
WHERE state = California OR state = Texas;
```

##### Incorrect, give it another shot!

Since California and Texas are strings, they must be enclosed inside quotes.

Since California and Texas are strings, they must be enclosed inside quotes.

```sql
SELECT name, email
From Customers
WHERE state = 'California' AND state = 'Texas';
```

##### Incorrect, give it another shot!

This query uses AND, which would never be true, as a customer cannot live in California and Texas simultaneously.

This query uses AND, which would never be true, as a customer cannot live in California and Texas simultaneously.

## 📝 Quiz

**Quiz: As a data analyst, you need to retrieve all customers' names and email addresses in California or Texas.**

⬚ A. A:SELECT name, email
FROM Customers
WHERE state = 'California' OR state = 'Texas';
⬚ B. SELECT name, email
FROM Customers
WHERE state = 'California' OR state = 'Texas';
⬚ C. B:SELECT name, email
FROM Customers
WHERE state = 'California' OR 'Texas';
⬚ D. SELECT name, email
FROM Customers
WHERE state = 'California' OR 'Texas';
⬚ E. C:SELECT name, email
From Customers
WHERE state = California OR state = Texas;
⬚ F. SELECT name, email
From Customers
WHERE state = California OR state = Texas;
⬚ G. D:SELECT name, email
From Customers
WHERE state = 'California' AND state = 'Texas';
⬚ H. SELECT name, email
From Customers
WHERE state = 'California' AND state = 'Texas';

