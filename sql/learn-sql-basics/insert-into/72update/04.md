Quiz: What is the result of the following query?

```sql
UPDATE Students
SET name = 'David Lee';
```

Tap an option below to answer:

The name of the student with a GPA of 3.95 will be updated to "David Lee".

##### Incorrect, give it another shot!

The query will update all the rows, not just the one with a GPA of 3.95.

The query will update all the rows, not just the one with a GPA of 3.95.

Only the student named "David Lee" will have their name updated.

##### Incorrect, give it another shot!

The query will update all students' names to "David Lee" because, without a WHERE clause, the update is applied to every row in the table.

The query will update all students' names to "David Lee" because, without a WHERE clause, the update is applied to every row in the table.

Every student's name will be updated to "David Lee".

##### Correct Answer!

Without a WHERE clause, this will apply the update to every row in the table.

Without a WHERE clause, this will apply the update to every row in the table.

The query will result in an error since the WHERE clause is missing.

##### Incorrect, give it another shot!

The query will run successfully; it just updates every row in the table.

The query will run successfully; it just updates every row in the table.

## 📝 Quiz

**Quiz: What is the result of the following query?**

⬚ A. A:The name of the student with a GPA of 3.95 will be updated to "David Lee".
⬚ B. The name of the student with a GPA of 3.95 will be updated to "David Lee".
⬚ C. B:Only the student named "David Lee" will have their name updated.
⬚ D. Only the student named "David Lee" will have their name updated.
⬚ E. C:Every student's name will be updated to "David Lee".
⬚ F. Every student's name will be updated to "David Lee".
⬚ G. D:The query will result in an error since the WHERE clause is missing.
⬚ H. The query will result in an error since the WHERE clause is missing.

