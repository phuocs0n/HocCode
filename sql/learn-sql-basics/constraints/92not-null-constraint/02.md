Quiz: Suppose we used the following code to create a table.

```sql
CREATE TABLE Customers (
  id INTEGER,
  name VARCHAR(50) NOT NULL,
  phone VARCHAR(50)
);
```

Which of the following statements will insert a row into the Customers table?

Tap an option below to answer:

```sql
INSERT INTO Customers (id, name, phone)
VALUES (5, 'Julie');
```

##### Incorrect, give it another shot!

Even though the phone column does not have a NOT NULL constraint, you cannot specify it in the column list without providing a corresponding value. To omit it, you must leave it out of the column list entirely.

Even though the phone column does not have a NOT NULL constraint, you cannot specify it in the column list without providing a corresponding value. To omit it, you must leave it out of the column list entirely.

```sql
INSERT INTO Customers (id, phone)
VALUES (5, '98233593');
```

##### Incorrect, give it another shot!

This code results in an error because the name column has a NOT NULL constraint. Since it is missing from the column list, it will default to NULL, which is not allowed.

This code results in an error because the name column has a NOT NULL constraint. Since it is missing from the column list, it will default to NULL, which is not allowed.

```sql
INSERT INTO Customers (id, name, phone)
VALUES (5, NULL, '98233593');
```

##### Incorrect, give it another shot!

This code results in an error because it explicitly inserts NULL into the name column, which is not allowed due to the NOT NULL constraint.

This code results in an error because it explicitly inserts NULL into the name column, which is not allowed due to the NOT NULL constraint.

```sql
INSERT INTO Customers (id, name, phone)
VALUES (5, 'Julie', '98233593');
```

##### Correct Answer!

This code successfully inserts a row into the table because all required columns are provided with valid values. The NOT NULL constraint on the name column is satisfied.

This code successfully inserts a row into the table because all required columns are provided with valid values. The NOT NULL constraint on the name column is satisfied.

## 📝 Quiz

**Quiz: Suppose we used the following code to create a table.**

⬚ A. A:INSERT INTO Customers (id, name, phone)
VALUES (5, 'Julie');
⬚ B. INSERT INTO Customers (id, name, phone)
VALUES (5, 'Julie');
⬚ C. B:INSERT INTO Customers (id, phone)
VALUES (5, '98233593');
⬚ D. INSERT INTO Customers (id, phone)
VALUES (5, '98233593');
⬚ E. C:INSERT INTO Customers (id, name, phone)
VALUES (5, NULL, '98233593');
⬚ F. INSERT INTO Customers (id, name, phone)
VALUES (5, NULL, '98233593');
⬚ G. D:INSERT INTO Customers (id, name, phone)
VALUES (5, 'Julie', '98233593');
⬚ H. INSERT INTO Customers (id, name, phone)
VALUES (5, 'Julie', '98233593');

