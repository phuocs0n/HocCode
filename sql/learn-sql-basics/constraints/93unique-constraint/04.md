Quiz: Suppose we used the following code to create a table.

```sql
CREATE TABLE Customers (
  id INT UNIQUE,
  name VARCHAR(50),
  email VARCHAR(50)
);
```

Which of the following statements produces an error?

Tap an option below to answer:

```sql
INSERT INTO Customers (name, email)
VALUES ('Julie', 'julie@example.com');
```

##### Incorrect, give it another shot!

This statement inserts a row successfully. While the id column has a UNIQUE constraint, it doesn't have a NOT NULL constraint, meaning NULL values are allowed and do not violate uniqueness rules.

This statement inserts a row successfully. While the id column has a UNIQUE constraint, it doesn't have a NOT NULL constraint, meaning NULL values are allowed and do not violate uniqueness rules.

```sql
INSERT INTO Customers (id, name, email)
VALUES (1, 'Julie', 'julie@example.com'), 
(1, 'Namita', 'namita@example.com');
```

##### Correct Answer!

This statement produces an error because it attempts to insert the same value (1) into the id column twice, violating the UNIQUE constraint.

This statement produces an error because it attempts to insert the same value (1) into the id column twice, violating the UNIQUE constraint.

```sql
INSERT INTO Customers (id, name, email)
VALUES (1, 'Julie', 'julie@example.com');  

INSERT INTO Customers (id, name, email)
VALUES (2, 'Namita', 'namita@example.com');
```

##### Incorrect, give it another shot!

This statement does not produce an error because both id values are unique (1 and 2), which satisfies the UNIQUE constraint.

This statement does not produce an error because both id values are unique (1 and 2), which satisfies the UNIQUE constraint.

```sql
INSERT INTO Customers (id, name, email)
VALUES (NULL, 'Julie', 'julie@example.com');

INSERT INTO Customers (id, name, email)
VALUES (NULL, 'Namita', 'namita@example.com');
```

##### Incorrect, give it another shot!

This statement does not produce an error because the id column has a UNIQUE constraint, but NULL values are allowed.

This statement does not produce an error because the id column has a UNIQUE constraint, but NULL values are allowed.

## 📝 Quiz

**Quiz: Suppose we used the following code to create a table.**

⬚ A. A:INSERT INTO Customers (name, email)
VALUES ('Julie', 'julie@example.com');
⬚ B. INSERT INTO Customers (name, email)
VALUES ('Julie', 'julie@example.com');
⬚ C. B:INSERT INTO Customers (id, name, email)
VALUES (1, 'Julie', 'julie@example.com'), 
(1, 'Namita', 'namita@example.com');
⬚ D. INSERT INTO Customers (id, name, email)
VALUES (1, 'Julie', 'julie@example.com'), 
(1, 'Namita', 'namita@example.com');
⬚ E. C:INSERT INTO Customers (id, name, email)
VALUES (1, 'Julie', 'julie@example.com');  

INSERT INTO Customers (id, name, email)
VALUES (2, 'Namita', 'namita@example.com');
⬚ F. INSERT INTO Customers (id, name, email)
VALUES (1, 'Julie', 'julie@example.com');  

INSERT INTO Customers (id, name, email)
VALUES (2, 'Namita', 'namita@example.com');
⬚ G. D:INSERT INTO Customers (id, name, email)
VALUES (NULL, 'Julie', 'julie@example.com');

INSERT INTO Customers (id, name, email)
VALUES (NULL, 'Namita', 'namita@example.com');
⬚ H. INSERT INTO Customers (id, name, email)
VALUES (NULL, 'Julie', 'julie@example.com');

INSERT INTO Customers (id, name, email)
VALUES (NULL, 'Namita', 'namita@example.com');

