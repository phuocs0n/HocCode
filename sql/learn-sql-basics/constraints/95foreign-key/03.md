Quiz: Which of the following statements is true about foreign keys?

Tap an option below to answer:

A foreign key is a column in a table that uniquely identifies each row in that table.

##### Incorrect, give it another shot!

A PRIMARY KEY uniquely identifies each row in a table.

A PRIMARY KEY uniquely identifies each row in a table.

A foreign key is a column in a table that references a column in another table.

##### Correct Answer!

Foreign keys establish a link between two tables to prevent data inconsistencies.

Foreign keys establish a link between two tables to prevent data inconsistencies.

A foreign key is a column in a table that allows null values.

##### Incorrect, give it another shot!

If we do not add any constraints to a column, the column allows null values by default.

If we do not add any constraints to a column, the column allows null values by default.

A foreign key is a column not used to establish relationships between tables.

##### Incorrect, give it another shot!

Foreign keys are used to establish relationships between tables to prevent data inconsistencies.

Foreign keys are used to establish relationships between tables to prevent data inconsistencies.

## 📝 Quiz

**Quiz: Which of the following statements is true about foreign keys?**

⬚ A. A:A foreign key is a column in a table that uniquely identifies each row in that table.
⬚ B. A foreign key is a column in a table that uniquely identifies each row in that table.
⬚ C. B:A foreign key is a column in a table that references a column in another table.
⬚ D. A foreign key is a column in a table that references a column in another table.
⬚ E. C:A foreign key is a column in a table that allows null values.
⬚ F. A foreign key is a column in a table that allows null values.
⬚ G. D:A foreign key is a column not used to establish relationships between tables.
⬚ H. A foreign key is a column not used to establish relationships between tables.

