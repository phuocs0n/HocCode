Quiz: Suppose you have a table named Employees with the following fields: id, name, email, and city.

How can you select the name and email of all the employees from the Employees table?

Tap an option below to answer:

```sql
SELECT name and email
FROM Employees;
```

##### Incorrect, give it another shot!

SQL doesn't use "and" to select multiple columns; it uses commas.

SQL doesn't use "and" to select multiple columns; it uses commas.

```sql
SELECT email, name
FROM Employees;
```

##### Incorrect, give it another shot!

This selects the correct columns but in the wrong order. The order of the columns matters.

This selects the correct columns but in the wrong order.

The order of the columns matters.

```sql
SELECT name, email
FROM Employee;
```

##### Incorrect, give it another shot!

This selects name and email from the Employee table, not the Employees table. Using the wrong table name is a common mistake.

This selects name and email from the Employee table, not the Employees table.

Using the wrong table name is a common mistake.

```sql
SELECT name, email
FROM Employees;
```

##### Correct Answer!

This correctly selects the name and email columns from the Employees table.

This correctly selects the name and email columns from the Employees table.

## 📝 Quiz

**Quiz: Suppose you have a table named Employees with the following fields: id, name, email, and city.**

⬚ A. A:SELECT name and email
FROM Employees;
⬚ B. SELECT name and email
FROM Employees;
⬚ C. B:SELECT email, name
FROM Employees;
⬚ D. SELECT email, name
FROM Employees;
⬚ E. C:SELECT name, email
FROM Employee;
⬚ F. SELECT name, email
FROM Employee;
⬚ G. D:SELECT name, email
FROM Employees;
⬚ H. SELECT name, email
FROM Employees;

