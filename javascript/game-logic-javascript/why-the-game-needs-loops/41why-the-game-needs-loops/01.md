Quiz: In the quiz we have

```javascript
while (i < total) { 
  ... prompt,
  check answer,
  score++; 
  i++;
}
```

What happens if we forget to write i++; inside the loop?

Tap an option below to answer:

The loop gets stuck on the first question and never finishes.

##### Correct Answer!

Correct. Without i++, i never changes, so i < total stays true and the loop keeps repeating the same question.

Correct. Without i++, i never changes, so i < total stays true and the loop keeps repeating the same question.

The score never increases.

##### Incorrect, give it another shot!

Score increases with score++ when the answer is correct. The real issue is that the loop doesn't move forward.

Score increases with score++ when the answer is correct. The real issue is that the loop doesn't move forward.

The player can only answer once.

##### Incorrect, give it another shot!

The player can keep answering, but the game keeps showing the same question because the counter never moves.

The player can keep answering, but the game keeps showing the same question because the counter never moves.

Pass or fail will be wrong.

##### Incorrect, give it another shot!

Pass/fail happens after the loop. If the loop never ends, the game never reaches that part.

Pass/fail happens after the loop. If the loop never ends, the game never reaches that part.

## 📝 Quiz

**Quiz: In the quiz we have**

⬚ A. A:The loop gets stuck on the first question and never finishes.
⬚ B. The loop gets stuck on the first question and never finishes.
⬚ C. B:The score never increases.
⬚ D. The score never increases.
⬚ E. C:The player can only answer once.
⬚ F. The player can only answer once.
⬚ G. D:Pass or fail will be wrong.
⬚ H. Pass or fail will be wrong.

