Quiz: Why do we use a variable like passMark instead of typing 40 everywhere in the code?

Tap an option below to answer:

So we can change the value in one place and keep the game consistent.

##### Correct Answer!

Correct. Storing the pass mark in a variable means we update it once and every place that uses passMark gets the new value. This keeps the game easy to maintain.

Correct. Storing the pass mark in a variable means we update it once and every place that uses passMark gets the new value. This keeps the game easy to maintain.

Because JavaScript does not allow typing numbers in console.log().

##### Incorrect, give it another shot!

JavaScript allows numbers and strings in console.log(). We use variables for clarity and to avoid repeating the same value in many places.

JavaScript allows numbers and strings in console.log(). We use variables for clarity and to avoid repeating the same value in many places.

Because variables run faster than numbers.

##### Incorrect, give it another shot!

Variables do not make the code faster. We use them so the code is easier to read and change, and we don't repeat the same value everywhere.

Variables do not make the code faster. We use them so the code is easier to read and change, and we don't repeat the same value everywhere.

Because the pass mark must be 40 and variables enforce that.

##### Incorrect, give it another shot!

Variables don't enforce values - they just store them. We use passMark so we can change 40 in one place if we want a different pass mark later.

Variables don't enforce values - they just store them. We use passMark so we can change 40 in one place if we want a different pass mark later.

## 📝 Quiz

**Quiz: Why do we use a variable like passMark instead of typing 40 everywhere in the code?**

⬚ A. A:So we can change the value in one place and keep the game consistent.
⬚ B. So we can change the value in one place and keep the game consistent.
⬚ C. B:Because JavaScript does not allow typing numbers in console.log().
⬚ D. Because JavaScript does not allow typing numbers in console.log().
⬚ E. C:Because variables run faster than numbers.
⬚ F. Because variables run faster than numbers.
⬚ G. D:Because the pass mark must be 40 and variables enforce that.
⬚ H. Because the pass mark must be 40 and variables enforce that.

