Quiz: What does prompt() return when the player types an answer?

Tap an option below to answer:

The text the player typed, as a string, so we can store it and use it in conditions.

##### Correct Answer!

Correct. prompt() returns whatever the player entered. We store it in a variable and use it in if/else to decide whether it is correct or wrong.

Correct. prompt() returns whatever the player entered. We store it in a variable and use it in if/else to decide whether it is correct or wrong.

A number so we can add it to the score.

##### Incorrect, give it another shot!

prompt() always returns a string. If we need a number we convert it (e.g. Number()). We still store the result in a variable and use it in our logic.

prompt() always returns a string. If we need a number we convert it (e.g. Number()). We still store the result in a variable and use it in our logic.

True or false depending on whether the player clicked OK.

##### Incorrect, give it another shot!

True/false would not tell us what they typed. prompt() returns the actual input as a string so we can compare it to the correct answer.

True/false would not tell us what they typed. prompt() returns the actual input as a string so we can compare it to the correct answer.

Nothing; it only waits for input in the console.

##### Incorrect, give it another shot!

prompt() does wait for the player to type in the console, but it also returns the value they typed. We need that return value to decide if the answer was correct.

prompt() does wait for the player to type in the console, but it also returns the value they typed. We need that return value to decide if the answer was correct.

## 📝 Quiz

**Quiz: What does prompt() return when the player types an answer?**

⬚ A. A:The text the player typed, as a string, so we can store it and use it in conditions.
⬚ B. The text the player typed, as a string, so we can store it and use it in conditions.
⬚ C. B:A number so we can add it to the score.
⬚ D. A number so we can add it to the score.
⬚ E. C:True or false depending on whether the player clicked OK.
⬚ F. True or false depending on whether the player clicked OK.
⬚ G. D:Nothing; it only waits for input in the console.
⬚ H. Nothing; it only waits for input in the console.

