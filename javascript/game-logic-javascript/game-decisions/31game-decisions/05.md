Quiz: In the quiz game, what does the condition (percentage >= passMark) tell us?

Tap an option below to answer:

How many questions the player answered.

##### Incorrect, give it another shot!

The number of questions is in total. The condition checks whether the player reached the pass mark, not how many they answered.

The number of questions is in total. The condition checks whether the player reached the pass mark, not how many they answered.

What the pass mark value is.

##### Incorrect, give it another shot!

passMark is already set (e.g. 40). The condition checks whether the percentage is at least that value - it doesn't set or change the pass mark.

passMark is already set (e.g. 40). The condition checks whether the percentage is at least that value - it doesn't set or change the pass mark.

Whether the game should stop.

##### Incorrect, give it another shot!

Stopping the game would be a different kind of decision (e.g. a loop condition). This condition only decides which message to show: pass or fail.

Stopping the game would be a different kind of decision (e.g. a loop condition). This condition only decides which message to show: pass or fail.

Whether the player's score is high enough to pass, so we can show the right message.

##### Correct Answer!

Correct. We compare the player's percentage to the pass mark. If it's greater than or equal, we show the pass message; otherwise we show the fail message.

Correct. We compare the player's percentage to the pass mark. If it's greater than or equal, we show the pass message; otherwise we show the fail message.

## 📝 Quiz

**Quiz: In the quiz game, what does the condition (percentage >= passMark) tell us?**

⬚ A. A:How many questions the player answered.
⬚ B. How many questions the player answered.
⬚ C. B:What the pass mark value is.
⬚ D. What the pass mark value is.
⬚ E. C:Whether the game should stop.
⬚ F. Whether the game should stop.
⬚ G. D:Whether the player's score is high enough to pass, so we can show the right message.
⬚ H. Whether the player's score is high enough to pass, so we can show the right message.

