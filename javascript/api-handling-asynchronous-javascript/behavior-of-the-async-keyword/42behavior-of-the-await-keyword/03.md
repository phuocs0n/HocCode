Quiz: What does await do when it encounters a promise?

Tap an option below to answer:

It blocks all JavaScript execution until the promise resolves

##### Incorrect, give it another shot!

await pauses the function, not all of JavaScript.

await pauses the function, not all of JavaScript.

It pauses the async function and waits for the promise to resolve

##### Correct Answer!

The function pauses, but the rest of JavaScript continues.

The function pauses, but the rest of JavaScript continues.

It converts the promise into a synchronous operation

##### Incorrect, give it another shot!

The operation stays asynchronous, await just makes it look synchronous.

The operation stays asynchronous, await just makes it look synchronous.

It automatically handles errors in the promise

##### Incorrect, give it another shot!

await doesn't handle errors automatically; you need try...catch to do that.

await doesn't handle errors automatically; you need try...catch to do that.

## 📝 Quiz

**Quiz: What does await do when it encounters a promise?**

⬚ A. A:It blocks all JavaScript execution until the promise resolves
⬚ B. It blocks all JavaScript execution until the promise resolves
⬚ C. B:It pauses the async function and waits for the promise to resolve
⬚ D. It pauses the async function and waits for the promise to resolve
⬚ E. C:It converts the promise into a synchronous operation
⬚ F. It converts the promise into a synchronous operation
⬚ G. D:It automatically handles errors in the promise
⬚ H. It automatically handles errors in the promise

