Quiz: What does an async function always return?

Tap an option below to answer:

The value you explicitly return

##### Incorrect, give it another shot!

Async functions wrap return values automatically.

Async functions wrap return values automatically.

A promise

##### Correct Answer!

Even plain values get wrapped in Promise.resolve().

Even plain values get wrapped in Promise.resolve().

undefined if you don't return anything

##### Incorrect, give it another shot!

Even undefined gets wrapped in a Promise.

Even undefined gets wrapped in a Promise.

An async object

##### Incorrect, give it another shot!

There is no "async object" type in JavaScript

There is no "async object" type in JavaScript

## 📝 Quiz

**Quiz: What does an async function always return?**

⬚ A. A:The value you explicitly return
⬚ B. The value you explicitly return
⬚ C. B:A promise
⬚ D. A promise
⬚ E. C:undefined if you don't return anything
⬚ F. undefined if you don't return anything
⬚ G. D:An async object
⬚ H. An async object

