Quiz: What does it mean when we say JavaScript is single-threaded?

Tap an option below to answer:

JavaScript runs one task at a time

##### Correct Answer!

Only one piece of code executes at any moment.

Only one piece of code executes at any moment.

JavaScript cannot handle asynchronous work

##### Incorrect, give it another shot!

Asynchronous work is handled via Web APIs.

Asynchronous work is handled via Web APIs.

JavaScript can only open one file

##### Incorrect, give it another shot!

Files have nothing to do with threads.

Files have nothing to do with threads.

JavaScript always freezes the browser

##### Incorrect, give it another shot!

Async exists to avoid freezing.

Async exists to avoid freezing.

## 📝 Quiz

**Quiz: What does it mean when we say JavaScript is single-threaded?**

⬚ A. A:JavaScript runs one task at a time
⬚ B. JavaScript runs one task at a time
⬚ C. B:JavaScript cannot handle asynchronous work
⬚ D. JavaScript cannot handle asynchronous work
⬚ E. C:JavaScript can only open one file
⬚ F. JavaScript can only open one file
⬚ G. D:JavaScript always freezes the browser
⬚ H. JavaScript always freezes the browser

