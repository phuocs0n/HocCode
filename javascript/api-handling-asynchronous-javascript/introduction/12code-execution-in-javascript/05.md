Quiz: In the code below, which function completes its execution first?

```javascript
function startMorningRoutine() {
  brushTeeth();
}

function brushTeeth() {
  console.log("Teeth brushed");
}

startMorningRoutine();
```

Tap an option below to answer:

startMorningRoutine()

##### Incorrect, give it another shot!

This function runs first, but it calls brushTeeth() inside it. So, it won't complete its execution unless brushTeeth() has been executed.

This function runs first, but it calls brushTeeth() inside it. So, it won't complete its execution unless brushTeeth() has been executed.

brushTeeth()

##### Correct Answer!

Since startMorningRoutine() calls brushTeeth() and nothing else, the latter is the first function that completes execution.

Since startMorningRoutine() calls brushTeeth() and nothing else, the latter is the first function that completes execution.

console.log()

##### Incorrect, give it another shot!

While this function runs briefly, brushTeeth() is the active function running from the call stack.

While this function runs briefly, brushTeeth() is the active function running from the call stack.

startMorningRoutine() and brushTeeth() are both on top at the same time

##### Incorrect, give it another shot!

Both functions are on the call stack, but only one can be on top at any given moment.

Both functions are on the call stack, but only one can be on top at any given moment.

## 📝 Quiz

**Quiz: In the code below, which function completes its execution first?**

⬚ A. A:startMorningRoutine()
⬚ B. startMorningRoutine()
⬚ C. B:brushTeeth()
⬚ D. brushTeeth()
⬚ E. C:console.log()
⬚ F. console.log()
⬚ G. D:startMorningRoutine() and brushTeeth() are both on top at the same time
⬚ H. startMorningRoutine() and brushTeeth() are both on top at the same time

