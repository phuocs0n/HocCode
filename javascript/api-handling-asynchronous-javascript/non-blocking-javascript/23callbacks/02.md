Quiz: What will be the output of the following code?

```javascript
function greet(name, callback) {
  console.log('Hello ' + name);
  callback();
}

function callMe() {
  console.log('I am a callback function');
}

greet('Alice', callMe);
```

Tap an option below to answer:

Hello Alice

##### Incorrect, give it another shot!

The greet() function prints "Hello Alice" first, but it also calls the callback function after.

The greet() function prints "Hello Alice" first, but it also calls the callback function after.

I am a callback function

##### Incorrect, give it another shot!

The callMe() function prints "I am a callback function", but it is called after "Hello Alice".

The callMe() function prints "I am a callback function", but it is called after "Hello Alice".

Hello Alice

I am a callback function

##### Correct Answer!

The greet() function prints "Hello Alice" and then calls the callback function callMe(), which prints "I am a callback function".

The greet() function prints "Hello Alice" and then calls the callback function callMe(), which prints "I am a callback function".

I am a callback function

Hello Alice

##### Incorrect, give it another shot!

The callback function is called after the greeting message.

The callback function is called after the greeting message.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:Hello Alice
⬚ B. Hello Alice
⬚ C. B:I am a callback function
⬚ D. I am a callback function
⬚ E. C:Hello AliceI am a callback function
⬚ F. Hello AliceI am a callback function
⬚ G. D:I am a callback functionHello Alice
⬚ H. I am a callback functionHello Alice

