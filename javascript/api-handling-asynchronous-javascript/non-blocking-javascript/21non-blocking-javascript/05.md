Quiz: What is the output of this code?

```javascript
setTimeout(() => {
  console.log("A");
}, 0);

console.log("B");
```

Tap an option below to answer:

##### Incorrect, give it another shot!

setTimeout() never runs before the synchronous code, i.e., console.log("B"), even with a 0ms delay.

setTimeout() never runs before the synchronous code, i.e., console.log("B"), even with a 0ms delay.

##### Correct Answer!

console.log("B") runs immediately because it's synchronous code. The setTimeout() callback is queued and runs after the current call stack finishes.

console.log("B") runs immediately because it's synchronous code. The setTimeout() callback is queued and runs after the current call stack finishes.

Only A

##### Incorrect, give it another shot!

"B" is logged to the console right away, so it will always appear.

"B" is logged to the console right away, so it will always appear.

Only B

##### Incorrect, give it another shot!

"A" still prints later because the timeout callback will execute once the stack is clear (assuming the environment keeps running).

"A" still prints later because the timeout callback will execute once the stack is clear (assuming the environment keeps running).

## 📝 Quiz

**Quiz: What is the output of this code?**

⬚ A. A:AB
⬚ B. AB
⬚ C. B:BA
⬚ D. BA
⬚ E. C:Only A
⬚ F. Only A
⬚ G. D:Only B
⬚ H. Only B

