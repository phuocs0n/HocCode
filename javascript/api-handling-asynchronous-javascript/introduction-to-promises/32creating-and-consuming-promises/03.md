Quiz: Which syntax is used to create a new Promise?

Tap an option below to answer:

```javascript
const p = Promise();
```

##### Incorrect, give it another shot!

Promise is a constructor. Calling it like a normal function will not work

Promise is a constructor. Calling it like a normal function will not work

```javascript
const p = new Promise();
```

##### Incorrect, give it another shot!

This uses new, which is good — but Promises require a function with resolve and reject.

This uses new, which is good — but Promises require a function with resolve and reject.

```javascript
const p = new Promise((resolve, reject) => {});
```

##### Correct Answer!

This correctly uses new and provides a function where async code runs.

This correctly uses new and provides a function where async code runs.

```javascript
const p = Promise((resolve, reject) => {});
```

##### Incorrect, give it another shot!

The function is there, but new is missing.

The function is there, but new is missing.

## 📝 Quiz

**Quiz: Which syntax is used to create a new Promise?**

⬚ A. A:const p = Promise();
⬚ B. const p = Promise();
⬚ C. B:const p = new Promise();
⬚ D. const p = new Promise();
⬚ E. C:const p = new Promise((resolve, reject) => {});
⬚ F. const p = new Promise((resolve, reject) => {});
⬚ G. D:const p = Promise((resolve, reject) => {});
⬚ H. const p = Promise((resolve, reject) => {});

