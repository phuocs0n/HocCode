Quiz: Why does the previous fetch() example work as a clean promise chain?

Tap an option below to answer:

Because each async step returns a promise that connects to the next

##### Correct Answer!

Returning Promises links all steps into one flow.

Returning Promises links all steps into one flow.

Because fetch() runs synchronously

##### Incorrect, give it another shot!

In JavaScript, fetch() is asynchronous.

In JavaScript, fetch() is asynchronous.

Because .then() blocks execution

##### Incorrect, give it another shot!

.then() does not block execution.

.then() does not block execution.

Because .catch() retries requests automatically

##### Incorrect, give it another shot!

Error handling does not imply retries.

Error handling does not imply retries.

## 📝 Quiz

**Quiz: Why does the previous fetch() example work as a clean promise chain?**

⬚ A. A:Because each async step returns a promise that connects to the next
⬚ B. Because each async step returns a promise that connects to the next
⬚ C. B:Because fetch() runs synchronously
⬚ D. Because fetch() runs synchronously
⬚ E. C:Because .then() blocks execution
⬚ F. Because .then() blocks execution
⬚ G. D:Because .catch() retries requests automatically
⬚ H. Because .catch() retries requests automatically

