Quiz: What does ${2 + 3} evaluate to inside a template literal?

Tap an option below to answer:

${2 + 3}

##### Incorrect, give it another shot!

Template literals evaluate expressions inside ${}. The text is not printed literally.

Template literals evaluate expressions inside ${}. The text is not printed literally.

23

##### Incorrect, give it another shot!

JavaScript evaluates the expression as math, not string concatenation. 2 + 3 performs addition.

JavaScript evaluates the expression as math, not string concatenation. 2 + 3 performs addition.

##### Correct Answer!

Correct! Any valid expression inside ${} is evaluated at runtime. 2 + 3 produces 5.

Correct! Any valid expression inside ${} is evaluated at runtime. 2 + 3 produces 5.

undefined

##### Incorrect, give it another shot!

The expression 2 + 3 is valid arithmetic. It returns 5, not undefined.

The expression 2 + 3 is valid arithmetic. It returns 5, not undefined.

##### Incorrect, give it another shot!

Template literals evaluate expressions inside ${}. The text is not printed literally.

Template literals evaluate expressions inside ${}. The text is not printed literally.

##### Incorrect, give it another shot!

JavaScript evaluates the expression as math, not string concatenation. 2 + 3 performs addition.

JavaScript evaluates the expression as math, not string concatenation. 2 + 3 performs addition.

##### Correct Answer!

Correct! Any valid expression inside ${} is evaluated at runtime. 2 + 3 produces 5.

Correct! Any valid expression inside ${} is evaluated at runtime. 2 + 3 produces 5.

##### Incorrect, give it another shot!

The expression 2 + 3 is valid arithmetic. It returns 5, not undefined.

The expression 2 + 3 is valid arithmetic. It returns 5, not undefined.

## 📝 Quiz

**Quiz: What does ${2 + 3} evaluate to inside a template literal?**

⬚ A. A:${2 + 3}
⬚ B. ${2 + 3}
⬚ C. B:23
⬚ D. 23
⬚ E. C:5
⬚ F. 5
⬚ G. D:undefined
⬚ H. undefined

