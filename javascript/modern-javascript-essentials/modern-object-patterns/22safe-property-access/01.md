Quiz: What does player.stats?.highScore return when a player has no stats property?

Tap an option below to answer:

It throws a TypeError

##### Incorrect, give it another shot!

That is what happens without ?.. Optional chaining prevents the error by short-circuiting.

That is what happens without ?.. Optional chaining prevents the error by short-circuiting.

It returns null

##### Incorrect, give it another shot!

Optional chaining returns undefined, not null, when a link in the chain is missing.

Optional chaining returns undefined, not null, when a link in the chain is missing.

It returns undefined

##### Correct Answer!

Correct! ?. short-circuits to undefined when the property before it does not exist.

Correct! ?. short-circuits to undefined when the property before it does not exist.

It returns 0

##### Incorrect, give it another shot!

?. does not substitute a default value. It returns undefined when the chain breaks. Use ?? for a fallback.

?. does not substitute a default value. It returns undefined when the chain breaks. Use ?? for a fallback.

##### Incorrect, give it another shot!

That is what happens without ?.. Optional chaining prevents the error by short-circuiting.

That is what happens without ?.. Optional chaining prevents the error by short-circuiting.

##### Incorrect, give it another shot!

Optional chaining returns undefined, not null, when a link in the chain is missing.

Optional chaining returns undefined, not null, when a link in the chain is missing.

##### Correct Answer!

Correct! ?. short-circuits to undefined when the property before it does not exist.

Correct! ?. short-circuits to undefined when the property before it does not exist.

##### Incorrect, give it another shot!

?. does not substitute a default value. It returns undefined when the chain breaks. Use ?? for a fallback.

?. does not substitute a default value. It returns undefined when the chain breaks. Use ?? for a fallback.

## 📝 Quiz

**Quiz: What does player.stats?.highScore return when a player has no stats property?**

⬚ A. A:It throws a TypeError
⬚ B. It throws a TypeError
⬚ C. B:It returns null
⬚ D. It returns null
⬚ E. C:It returns undefined
⬚ F. It returns undefined
⬚ G. D:It returns 0
⬚ H. It returns 0

