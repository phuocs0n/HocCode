Quiz: What does the map method return?

Tap an option below to answer:

undefined

##### Incorrect, give it another shot!

That is what forEach returns. map always returns a new array.

That is what forEach returns. map always returns a new array.

The original array, modified in place

##### Incorrect, give it another shot!

map never mutates the original. It creates and returns a brand-new array.

map never mutates the original. It creates and returns a brand-new array.

A new array with the results of calling the function on each element

##### Correct Answer!

Correct! map builds a new array by applying the callback to every element.

Correct! map builds a new array by applying the callback to every element.

A single value reduced from all elements

##### Incorrect, give it another shot!

That describes reduce. map returns an array, not a single value.

That describes reduce. map returns an array, not a single value.

## 📝 Quiz

**Quiz: What does the map method return?**

⬚ A. A:undefined
⬚ B. undefined
⬚ C. B:The original array, modified in place
⬚ D. The original array, modified in place
⬚ E. C:A new array with the results of calling the function on each element
⬚ F. A new array with the results of calling the function on each element
⬚ G. D:A single value reduced from all elements
⬚ H. A single value reduced from all elements

