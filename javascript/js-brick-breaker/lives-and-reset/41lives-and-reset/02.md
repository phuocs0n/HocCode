# Detect the Ball Falling Out ⬇️

## Detect the Ball Falling Out ⬇️

Add the out-of-bounds check so losing the ball actually has a consequence.

In game.js, add a checkBallOutOfBounds() method that calls this.loseLife() when this.ball.y - this.ball.radius > CANVAS_HEIGHT.

- Call this.checkBallOutOfBounds() at the end of Game.update().
- Add an empty loseLife() stub for now — it will be implemented in the next step.

- entities.js
- game.js
- index.html
- style.css

```unknown
"use strict";
```

