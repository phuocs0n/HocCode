Quiz: You want to add a "Back to top" button that only appears after the user scrolls down 200 pixels.

Which pattern from this course fits best?

Tap an option below to answer:

Form validation and feedback.

##### Incorrect, give it another shot!

Form validation is used for inputs and submit handling, not for reacting to scroll position.

Form validation is used for inputs and submit handling, not for reacting to scroll position.

Modal open and close functions.

##### Incorrect, give it another shot!

Modals are for overlay dialogs, not scroll-based visibility.

Modals are for overlay dialogs, not scroll-based visibility.

Slider state with currentSlideIndex.

##### Incorrect, give it another shot!

Slider state controls which slide is visible. It does not relate to scroll-based UI changes.

Slider state controls which slide is visible. It does not relate to scroll-based UI changes.

Scroll event, threshold, and toggling a class.

##### Correct Answer!

Listen for scroll on window, read scrollY, compare it to a threshold like 200, and toggle a class when the condition is true.

Listen for scroll on window, read scrollY, compare it to a threshold like 200, and toggle a class when the condition is true.

## 📝 Quiz

**Quiz: You want to add a "Back to top" button that only appears after the user scrolls down 200 pixels.**

⬚ A. A:Form validation and feedback.
⬚ B. Form validation and feedback.
⬚ C. B:Modal open and close functions.
⬚ D. Modal open and close functions.
⬚ E. C:Slider state with currentSlideIndex.
⬚ F. Slider state with currentSlideIndex.
⬚ G. D:Scroll event, threshold, and toggling a class.
⬚ H. Scroll event, threshold, and toggling a class.

