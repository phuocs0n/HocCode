Quiz: Why do we add the <script> tag just before the closing </body> tag?

Tap an option below to answer:

So the HTML elements are already created when the script runs.

##### Correct Answer!

The browser reads the page from top to bottom. Placing the script at the end ensures the HTML exists before JavaScript runs.

The browser reads the page from top to bottom. Placing the script at the end ensures the HTML exists before JavaScript runs.

So the page loads faster.

##### Incorrect, give it another shot!

Page speed depends on many factors. Script placement is not mainly about speed.

Page speed depends on many factors. Script placement is not mainly about speed.

So CSS loads before JavaScript.

##### Incorrect, give it another shot!

CSS loading is separate from where the script tag is placed.

CSS loading is separate from where the script tag is placed.

So the script runs only once.

##### Incorrect, give it another shot!

The script runs once either way. Placement changes when it runs, not how many times.

The script runs once either way. Placement changes when it runs, not how many times.

## 📝 Quiz

**Quiz: Why do we add the <script> tag just before the closing </body> tag?**

⬚ A. A:So the HTML elements are already created when the script runs.
⬚ B. So the HTML elements are already created when the script runs.
⬚ C. B:So the page loads faster.
⬚ D. So the page loads faster.
⬚ E. C:So CSS loads before JavaScript.
⬚ F. So CSS loads before JavaScript.
⬚ G. D:So the script runs only once.
⬚ H. So the script runs only once.

