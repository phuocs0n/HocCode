Quiz: When should you use textContent instead of innerHTML?

Tap an option below to answer:

When the element has no child elements.

##### Incorrect, give it another shot!

Having children or not doesn't decide. We choose based on whether we need to insert HTML or just text.

Having children or not doesn't decide. We choose based on whether we need to insert HTML or just text.

When you only need to display plain text.

##### Correct Answer!

Use textContent for plain text; the browser won't interpret tags. Use innerHTML when you need actual HTML (e.g. links).

Use textContent for plain text; the browser won't interpret tags. Use innerHTML when you need actual HTML (e.g. links).

When you want the content to load faster.

##### Incorrect, give it another shot!

Performance isn't the main reason. The choice is about meaning: plain text vs. HTML.

Performance isn't the main reason. The choice is about meaning: plain text vs. HTML.

When the text is longer than one line.

##### Incorrect, give it another shot!

Length doesn't determine which property to use. Both can handle long or short strings.

Length doesn't determine which property to use. Both can handle long or short strings.

## 📝 Quiz

**Quiz: When should you use textContent instead of innerHTML?**

⬚ A. A:When the element has no child elements.
⬚ B. When the element has no child elements.
⬚ C. B:When you only need to display plain text.
⬚ D. When you only need to display plain text.
⬚ E. C:When you want the content to load faster.
⬚ F. When you want the content to load faster.
⬚ G. D:When the text is longer than one line.
⬚ H. When the text is longer than one line.

