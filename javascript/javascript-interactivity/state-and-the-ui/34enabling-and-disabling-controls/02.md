Quiz: Where is the best place to update the Prev and Next buttons disabled state so they stay correct after every slide change?

Tap an option below to answer:

Only in the Prev button click handler.

##### Incorrect, give it another shot!

Updating only in Prev would miss slide changes from Next. The disabled state could get out of sync.

Updating only in Prev would miss slide changes from Next. The disabled state could get out of sync.

Only in the Next button click handler.

##### Incorrect, give it another shot!

Only updating in Next leaves Prev wrong when you go back. The disabled state could get out of sync.

Only updating in Next leaves Prev wrong when you go back. The disabled state could get out of sync.

Inside showSlide()|single function that runs whenever the slide changes, so one place keeps state and button state in sync.

##### Correct Answer!

Whenever the slide changes, showSlide() runs. Set prevBtn.disabled and nextBtn.disabled there based on currentSlideIndex. One source of truth.

Whenever the slide changes, showSlide() runs. Set prevBtn.disabled and nextBtn.disabled there based on currentSlideIndex. One source of truth.

In a separate function that runs once on page load.

##### Incorrect, give it another shot!

On load you start on the first slide, but the buttons must update again after every click. A one-time update will not stay correct.

On load you start on the first slide, but the buttons must update again after every click. A one-time update will not stay correct.

## 📝 Quiz

**Quiz: Where is the best place to update the Prev and Next buttons disabled state so they stay correct after every slide change?**

⬚ A. A:Only in the Prev button click handler.
⬚ B. Only in the Prev button click handler.
⬚ C. B:Only in the Next button click handler.
⬚ D. Only in the Next button click handler.
⬚ E. C:Inside showSlide()|single function that runs whenever the slide changes, so one place keeps state and button state in sync.
⬚ F. Inside showSlide()|single function that runs whenever the slide changes, so one place keeps state and button state in sync.
⬚ G. D:In a separate function that runs once on page load.
⬚ H. In a separate function that runs once on page load.

