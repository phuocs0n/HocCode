Quiz: Why do we keep a variable like currentSlideIndex for a slider, instead of only changing the DOM when the user clicks Next or Previous?

Tap an option below to answer:

So the browser can animate the transition between slides.

##### Incorrect, give it another shot!

Animation comes from CSS or how we update the DOM. The variable just tracks which slide is active.

Animation comes from CSS or how we update the DOM. The variable just tracks which slide is active.

So we have one source of truth for which slide is visible, and we can update the variable and then update the DOM to match.

##### Correct Answer!

The variable stores the current slide, and the DOM is updated based on it. That keeps everything in sync.

The variable stores the current slide, and the DOM is updated based on it. That keeps everything in sync.

So we can support more than two slides.

##### Incorrect, give it another shot!

You can support many slides either way. The variable helps you track which one is active.

You can support many slides either way. The variable helps you track which one is active.

So the user can use the keyboard to change slides.

##### Incorrect, give it another shot!

Keyboard support is separate. The same variable helps, but it's not the main reason

Keyboard support is separate. The same variable helps, but it's not the main reason

## 📝 Quiz

**Quiz: Why do we keep a variable like currentSlideIndex for a slider, instead of only changing the DOM when the user clicks Next or Previous?**

⬚ A. A:So the browser can animate the transition between slides.
⬚ B. So the browser can animate the transition between slides.
⬚ C. B:So we have one source of truth for which slide is visible, and we can update the variable and then update the DOM to match.
⬚ D. So we have one source of truth for which slide is visible, and we can update the variable and then update the DOM to match.
⬚ E. C:So we can support more than two slides.
⬚ F. So we can support more than two slides.
⬚ G. D:So the user can use the keyboard to change slides.
⬚ H. So the user can use the keyboard to change slides.

