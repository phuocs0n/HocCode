Quiz: For a "Read more" section that expands and collapses smoothly, why is it better to toggle a CSS class instead of setting element.style.height directly in JavaScript?

Tap an option below to answer:

So the content loads faster.

##### Incorrect, give it another shot!

Changing styles this way does not affect loading speed. The benefit is structure and maintainability.

Changing styles this way does not affect loading speed. The benefit is structure and maintainability.

So we can use a CSS transition and let the browser handle the animation while keeping styling inside CSS.

##### Correct Answer!

JavaScript changes the state by toggling a class. CSS handles how that state looks and animates.

JavaScript changes the state by toggling a class. CSS handles how that state looks and animates.

So screen readers can detect the content.

##### Incorrect, give it another shot!

Accessibility is important, but that is not the main reason for choosing classes here.

Accessibility is important, but that is not the main reason for choosing classes here.

So the button text can change between "Read more" and "Show less."

##### Incorrect, give it another shot!

The button text can change either way. The key advantage is separating behavior from styling.

The button text can change either way. The key advantage is separating behavior from styling.

## 📝 Quiz

**Quiz: For a "Read more" section that expands and collapses smoothly, why is it better to toggle a CSS class instead of setting element.style.height directly in JavaScript?**

⬚ A. A:So the content loads faster.
⬚ B. So the content loads faster.
⬚ C. B:So we can use a CSS transition and let the browser handle the animation while keeping styling inside CSS.
⬚ D. So we can use a CSS transition and let the browser handle the animation while keeping styling inside CSS.
⬚ E. C:So screen readers can detect the content.
⬚ F. So screen readers can detect the content.
⬚ G. D:So the button text can change between "Read more" and "Show less."
⬚ H. So the button text can change between "Read more" and "Show less."

