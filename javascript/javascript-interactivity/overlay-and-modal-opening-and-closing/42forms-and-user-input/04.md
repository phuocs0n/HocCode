Quiz: Why do we call event.preventDefault() in a form submit handler?

Tap an option below to answer:

So the form values are not sent to the server.

##### Incorrect, give it another shot!

The goal is not to block sending forever. The goal is to control the submit flow in JavaScript first.

The goal is not to block sending forever. The goal is to control the submit flow in JavaScript first.

So the page does not reload and we can handle validation, loading, and success in JavaScript.

##### Correct Answer!

By default, form submit reloads the page. preventDefault() stops that so you can control validation and feedback.

By default, form submit reloads the page. preventDefault() stops that so you can control validation and feedback.

So the user cannot submit the form twice.

##### Incorrect, give it another shot!

Preventing double submit requires disabling the button or tracking state.

Preventing double submit requires disabling the button or tracking state.

So the modal stays open after submit.

##### Incorrect, give it another shot!

Keeping the modal open is a separate choice. preventDefault() is about stopping the reload.

Keeping the modal open is a separate choice. preventDefault() is about stopping the reload.

## 📝 Quiz

**Quiz: Why do we call event.preventDefault() in a form submit handler?**

⬚ A. A:So the form values are not sent to the server.
⬚ B. So the form values are not sent to the server.
⬚ C. B:So the page does not reload and we can handle validation, loading, and success in JavaScript.
⬚ D. So the page does not reload and we can handle validation, loading, and success in JavaScript.
⬚ E. C:So the user cannot submit the form twice.
⬚ F. So the user cannot submit the form twice.
⬚ G. D:So the modal stays open after submit.
⬚ H. So the modal stays open after submit.

