Quiz: When the user clicks inside the modal, why do we check event.target === overlay before calling closeModal()?

Tap an option below to answer:

So the modal does not close when the user clicks the close button.

##### Incorrect, give it another shot!

The close button has its own click handler. This check is for the overlay click handler.

The close button has its own click handler. This check is for the overlay click handler.

So the modal closes only when the user clicks the overlay, not when they click inside the modal content.

##### Correct Answer!

Clicking the overlay should close the modal. Clicking inside the modal should not, otherwise the modal would close while the user is trying to interact with the form.

Clicking the overlay should close the modal. Clicking inside the modal should not, otherwise the modal would close while the user is trying to interact with the form.

So we can support multiple modals.

##### Incorrect, give it another shot!

Multiple modals are a different design. This check is about separating overlay clicks from modal clicks.

Multiple modals are a different design. This check is about separating overlay clicks from modal clicks.

So the Escape key still works.

##### Incorrect, give it another shot!

Escape is handled in a keydown listener. It does not depend on the overlay click check.

Escape is handled in a keydown listener. It does not depend on the overlay click check.

## 📝 Quiz

**Quiz: When the user clicks inside the modal, why do we check event.target === overlay before calling closeModal()?**

⬚ A. A:So the modal does not close when the user clicks the close button.
⬚ B. So the modal does not close when the user clicks the close button.
⬚ C. B:So the modal closes only when the user clicks the overlay, not when they click inside the modal content.
⬚ D. So the modal closes only when the user clicks the overlay, not when they click inside the modal content.
⬚ E. C:So we can support multiple modals.
⬚ F. So we can support multiple modals.
⬚ G. D:So the Escape key still works.
⬚ H. So the Escape key still works.

