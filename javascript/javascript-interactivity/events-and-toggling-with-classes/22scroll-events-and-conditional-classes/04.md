Quiz: You want to add a class to the header only when the user has scrolled more than 80 pixels.

What should your scroll handler do?

Tap an option below to answer:

Add the class when scrollY < 80 and remove it when scrollY >= 80.

##### Incorrect, give it another shot!

That would add the class near the top and remove it after scrolling down. For a sticky header we want the opposite: sticky only when past the threshold.

That would add the class near the top and remove it after scrolling down. For a sticky header we want the opposite: sticky only when past the threshold.

Add the class when scrollY > 80 and remove it when scrollY <= 80.

##### Correct Answer!

When scrollY > 80, the user has passed the threshold, so you add the class.When they scroll back up and scrollY <= 80, you remove it.

When scrollY > 80, the user has passed the threshold, so you add the class.

When they scroll back up and scrollY <= 80, you remove it.

Add the class once when the page loads

##### Incorrect, give it another shot!

Header state should depend on current scroll position. We re-check on every scroll (or throttle if needed).

Header state should depend on current scroll position. We re-check on every scroll (or throttle if needed).

Add the class every time the user scrolls

##### Incorrect, give it another shot!

Adding the class regardless of position would keep the header sticky always. We need to remove the class when they scroll back up above the threshold.

Adding the class regardless of position would keep the header sticky always. We need to remove the class when they scroll back up above the threshold.

## 📝 Quiz

**Quiz: You want to add a class to the header only when the user has scrolled more than 80 pixels.**

⬚ A. A:Add the class when scrollY < 80 and remove it when scrollY >= 80.
⬚ B. Add the class when scrollY < 80 and remove it when scrollY >= 80.
⬚ C. B:Add the class when scrollY > 80 and remove it when scrollY <= 80.
⬚ D. Add the class when scrollY > 80 and remove it when scrollY <= 80.
⬚ E. C:Add the class once when the page loads
⬚ F. Add the class once when the page loads
⬚ G. D:Add the class every time the user scrolls
⬚ H. Add the class every time the user scrolls

