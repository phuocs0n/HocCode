Quiz: You want a navigation menu to open and close each time the user clicks the menu button.

Which approach is best?

Tap an option below to answer:

Use classList.add('is-open') inside the click handler every time.

##### Incorrect, give it another shot!

Adding the class every time would keep the menu open and never hide it. You need to switch between two states, not only add.

Adding the class every time would keep the menu open and never hide it. You need to switch between two states, not only add.

Use classList.remove('is-open') inside the click handler every time.

##### Incorrect, give it another shot!

Removing the class every time would keep the menu closed. It doesn't alternate between states.

Removing the class every time would keep the menu closed. It doesn't alternate between states.

Use classList.toggle('is-open') inside the click handler.

##### Correct Answer!

toggle adds the class if it's missing and removes it if present. One click opens, the next closes.

toggle adds the class if it's missing and removes it if present. One click opens, the next closes.

Set element.style.display directly inside JavaScript.

##### Incorrect, give it another shot!

You could use inline styles, but toggling a class is cleaner: CSS handles the look, JS only toggles the class.

You could use inline styles, but toggling a class is cleaner: CSS handles the look, JS only toggles the class.

## 📝 Quiz

**Quiz: You want a navigation menu to open and close each time the user clicks the menu button.**

⬚ A. A:Use classList.add('is-open') inside the click handler every time.
⬚ B. Use classList.add('is-open') inside the click handler every time.
⬚ C. B:Use classList.remove('is-open') inside the click handler every time.
⬚ D. Use classList.remove('is-open') inside the click handler every time.
⬚ E. C:Use classList.toggle('is-open') inside the click handler.
⬚ F. Use classList.toggle('is-open') inside the click handler.
⬚ G. D:Set element.style.display directly inside JavaScript.
⬚ H. Set element.style.display directly inside JavaScript.

