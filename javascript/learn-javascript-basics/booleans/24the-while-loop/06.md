Quiz: What will be the output of the following code?

```javascript
let counter = 1;
while (counter < 5) {
    console.log("Programiz");
}
```

Tap an option below to answer:

Programiz will be displayed once.

##### Incorrect, give it another shot!

The counter variable is not being updated inside the loop. So, the loop condition stays true and continues even after the first iteration.

The counter variable is not being updated inside the loop. So, the loop condition stays true and continues even after the first iteration.

Programiz will be displayed four times.

##### Incorrect, give it another shot!

The counter variable is not being updated in the loop. So, it won't reach the condition where counter is no longer less than 5.

The counter variable is not being updated in the loop. So, it won't reach the condition where counter is no longer less than 5.

The program will enter an infinite loop and continuously print Programiz.

##### Correct Answer!

In the program, counter < 5 never evaluates to false because we've not updated the value of counter inside the loop.Therefore, this results in an infinite loop.

In the program, counter < 5 never evaluates to false because we've not updated the value of counter inside the loop.

Therefore, this results in an infinite loop.

No output will be produced.

##### Incorrect, give it another shot!

The loop executes infinitely and prints Programiz in each iteration.

The loop executes infinitely and prints Programiz in each iteration.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:Programiz will be displayed once.
⬚ B. Programiz will be displayed once.
⬚ C. B:Programiz will be displayed four times.
⬚ D. Programiz will be displayed four times.
⬚ E. C:The program will enter an infinite loop and continuously print Programiz.
⬚ F. The program will enter an infinite loop and continuously print Programiz.
⬚ G. D:No output will be produced.
⬚ H. No output will be produced.

