Quiz: How many times is the text "Hello" displayed in the following program?

```javascript
for (let i = 1; i <= 10; i++) {
    console.log("Hello");

    if (i == 5) {
        break;
    }
}
```

Tap an option below to answer:

10

##### Incorrect, give it another shot!

The loop does not complete all 10 iterations. The loop ends due to the break statement when i is 5.

The loop does not complete all 10 iterations. The loop ends due to the break statement when i is 5.

##### Correct Answer!

"Hello" is displayed 5 times as the loop iterates from i = 1 to i = 5. However, when i = 5, the condition in the if statement evaluates to true, causing the break statement to execute and terminating the loop.

"Hello" is displayed 5 times as the loop iterates from i = 1 to i = 5. However, when i = 5, the condition in the if statement evaluates to true, causing the break statement to execute and terminating the loop.

##### Incorrect, give it another shot!

Before the condition in the if statement evaluates to true, causing the break statement to execute, the loop executes 5 times.

Before the condition in the if statement evaluates to true, causing the break statement to execute, the loop executes 5 times.

##### Incorrect, give it another shot!

Even if the break statement executes in the first iteration, "Hello" is displayed at least 1 time as console.log() executes before the if statement in each iteration.

Even if the break statement executes in the first iteration, "Hello" is displayed at least 1 time as console.log() executes before the if statement in each iteration.

##### Incorrect, give it another shot!

The loop does not complete all 10 iterations. The loop ends due to the break statement when i is 5.

The loop does not complete all 10 iterations. The loop ends due to the break statement when i is 5.

##### Correct Answer!

"Hello" is displayed 5 times as the loop iterates from i = 1 to i = 5. However, when i = 5, the condition in the if statement evaluates to true, causing the break statement to execute and terminating the loop.

"Hello" is displayed 5 times as the loop iterates from i = 1 to i = 5. However, when i = 5, the condition in the if statement evaluates to true, causing the break statement to execute and terminating the loop.

##### Incorrect, give it another shot!

Before the condition in the if statement evaluates to true, causing the break statement to execute, the loop executes 5 times.

Before the condition in the if statement evaluates to true, causing the break statement to execute, the loop executes 5 times.

##### Incorrect, give it another shot!

Even if the break statement executes in the first iteration, "Hello" is displayed at least 1 time as console.log() executes before the if statement in each iteration.

Even if the break statement executes in the first iteration, "Hello" is displayed at least 1 time as console.log() executes before the if statement in each iteration.

## 📝 Quiz

**Quiz: How many times is the text "Hello" displayed in the following program?**

⬚ A. A:10
⬚ B. 10
⬚ C. B:5
⬚ D. 5
⬚ E. C:4
⬚ F. 4
⬚ G. D:0
⬚ H. 0

