Quiz: Which of the following is a valid way to write an if statement?

Tap an option below to answer:

```javascript
age < 18 {
    console.log("You are less than 18 years old");
}
```

##### Incorrect, give it another shot!

This code is not valid since it's missing the if keyword.

This code is not valid since it's missing the if keyword.

```javascript
if (age < 18) {
    console.log("You are less than 18 years old");
}
```

##### Correct Answer!

This is a valid if statement. It begins with the if keyword followed by a valid boolean expression and the statements begin with curly braces.

This is a valid if statement. It begins with the if keyword followed by a valid boolean expression and the statements begin with curly braces.

```javascript
if {
    console.log("You are less than 18 years old");
}
```

##### Incorrect, give it another shot!

This code is not valid since it's missing a condition after if.

This code is not valid since it's missing a condition after if.

```javascript
if {age < 18} (
    console.log("You are less than 18 years old");
)
```

##### Incorrect, give it another shot!

This code is not valid because it uses incorrect syntax.

This code is not valid because it uses incorrect syntax.

## 📝 Quiz

**Quiz: Which of the following is a valid way to write an if statement?**

⬚ A. A:age < 18 {
    console.log("You are less than 18 years old");
}
⬚ B. age < 18 {
    console.log("You are less than 18 years old");
}
⬚ C. B:if (age < 18) {
    console.log("You are less than 18 years old");
}
⬚ D. if (age < 18) {
    console.log("You are less than 18 years old");
}
⬚ E. C:if {
    console.log("You are less than 18 years old");
}
⬚ F. if {
    console.log("You are less than 18 years old");
}
⬚ G. D:if {age < 18} (
    console.log("You are less than 18 years old");
)
⬚ H. if {age < 18} (
    console.log("You are less than 18 years old");
)

