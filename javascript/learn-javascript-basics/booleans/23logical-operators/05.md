Quiz: Given the following code:

```javascript
let eligible = (age >= 18) && (gpa >= 3.5);
```

Which scenario makes eligible evaluate to true?

Tap an option below to answer:

Age is 18, GPA is 3.4

##### Incorrect, give it another shot!

While the age >= 18 condition is satisfied, remember both conditions must be true. The condition gpa >= 3.5 is not satisfied.

While the age >= 18 condition is satisfied, remember both conditions must be true. The condition gpa >= 3.5 is not satisfied.

Age is 17, GPA is 3.5

##### Incorrect, give it another shot!

The GPA satisfies gpa >= 3.5, but doesn't satisfy age >= 1. If either condition fails, the entire expression is false.

The GPA satisfies gpa >= 3.5, but doesn't satisfy age >= 1. If either condition fails, the entire expression is false.

Age is 22, GPA is 3.5

##### Correct Answer!

Both conditions, age >= 18 and gpa >= 3.5, are both satisfied for this scenario.

Both conditions, age >= 18 and gpa >= 3.5, are both satisfied for this scenario.

Age is 16, GPA is 3.6

##### Incorrect, give it another shot!

The GPA satisfies gpa >= 3.5, but doesn't satisfy the age >= 18 condition. If one condition fails, the result of and will always be false.

The GPA satisfies gpa >= 3.5, but doesn't satisfy the age >= 18 condition. If one condition fails, the result of and will always be false.

##### Incorrect, give it another shot!

While the age >= 18 condition is satisfied, remember both conditions must be true. The condition gpa >= 3.5 is not satisfied.

While the age >= 18 condition is satisfied, remember both conditions must be true. The condition gpa >= 3.5 is not satisfied.

##### Incorrect, give it another shot!

The GPA satisfies gpa >= 3.5, but doesn't satisfy age >= 1. If either condition fails, the entire expression is false.

The GPA satisfies gpa >= 3.5, but doesn't satisfy age >= 1. If either condition fails, the entire expression is false.

##### Correct Answer!

Both conditions, age >= 18 and gpa >= 3.5, are both satisfied for this scenario.

Both conditions, age >= 18 and gpa >= 3.5, are both satisfied for this scenario.

##### Incorrect, give it another shot!

The GPA satisfies gpa >= 3.5, but doesn't satisfy the age >= 18 condition. If one condition fails, the result of and will always be false.

The GPA satisfies gpa >= 3.5, but doesn't satisfy the age >= 18 condition. If one condition fails, the result of and will always be false.

## 📝 Quiz

**Quiz: Given the following code:**

⬚ A. A:Age is 18, GPA is 3.4
⬚ B. Age is 18, GPA is 3.4
⬚ C. B:Age is 17, GPA is 3.5
⬚ D. Age is 17, GPA is 3.5
⬚ E. C:Age is 22, GPA is 3.5
⬚ F. Age is 22, GPA is 3.5
⬚ G. D:Age is 16, GPA is 3.6
⬚ H. Age is 16, GPA is 3.6

