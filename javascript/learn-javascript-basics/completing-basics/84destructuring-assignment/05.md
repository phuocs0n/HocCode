Quiz: Consider the following code:

```javascript
const animals = ["lion", "tiger", "elephant", "giraffe", "zebra"];

let [animal1, animal2, animal3, animal4, animal5] = animals;
```

After destructuring animals, which value will the animal3 variable store after destructuring?

Tap an option below to answer:

"lion"

##### Incorrect, give it another shot!

The animal3 variable is the third variable, but "lion" is the first value in the array.

The animal3 variable is the third variable, but "lion" is the first value in the array.

"tiger"

##### Incorrect, give it another shot!

"tiger" is the second value in the array, and animal2 stores the second element.

"tiger" is the second value in the array, and animal2 stores the second element.

"elephant"

##### Correct Answer!

Destructuring assigns the first value to animal1, the second value to animal2, and the third value to animal3. Therefore, animal3 will store the third element in the array, which is "elephant".

Destructuring assigns the first value to animal1, the second value to animal2, and the third value to animal3. Therefore, animal3 will store the third element in the array, which is "elephant".

"giraffe"

##### Incorrect, give it another shot!

"giraffe" is the fourth element in the array, so animal4 stores that value.

"giraffe" is the fourth element in the array, so animal4 stores that value.

##### Incorrect, give it another shot!

The animal3 variable is the third variable, but "lion" is the first value in the array.

The animal3 variable is the third variable, but "lion" is the first value in the array.

##### Incorrect, give it another shot!

"tiger" is the second value in the array, and animal2 stores the second element.

"tiger" is the second value in the array, and animal2 stores the second element.

##### Correct Answer!

Destructuring assigns the first value to animal1, the second value to animal2, and the third value to animal3. Therefore, animal3 will store the third element in the array, which is "elephant".

Destructuring assigns the first value to animal1, the second value to animal2, and the third value to animal3. Therefore, animal3 will store the third element in the array, which is "elephant".

##### Incorrect, give it another shot!

"giraffe" is the fourth element in the array, so animal4 stores that value.

"giraffe" is the fourth element in the array, so animal4 stores that value.

## 📝 Quiz

**Quiz: Consider the following code:**

⬚ A. A:"lion"
⬚ B. "lion"
⬚ C. B:"tiger"
⬚ D. "tiger"
⬚ E. C:"elephant"
⬚ F. "elephant"
⬚ G. D:"giraffe"
⬚ H. "giraffe"

