Quiz: In the given code snippet, what does item represent when looping through the person object?

```javascript
for (let item in person) {
    console.log(item);
}
```

Tap an option below to answer:

item represents the values of the object properties.

##### Incorrect, give it another shot!

item represents the property names (keys), not the values. To access the values, you would use person[item].

item represents the property names (keys), not the values. To access the values, you would use person[item].

item represents the property names of the object.

##### Correct Answer!

In the for...in loop, item refers to the names of the properties (like "name", "age", and "city").

In the for...in loop, item refers to the names of the properties (like "name", "age", and "city").

item represents the index of the object properties.

##### Incorrect, give it another shot!

Unlike arrays, objects don't have indices. The for...in loop iterates through the property names (keys), not indices.

Unlike arrays, objects don't have indices. The for...in loop iterates through the property names (keys), not indices.

item represents the number of properties in the object.

##### Incorrect, give it another shot!

item represents the property names (keys), not the number of properties.

item represents the property names (keys), not the number of properties.

## 📝 Quiz

**Quiz: In the given code snippet, what does item represent when looping through the person object?**

⬚ A. A:item represents the values of the object properties.
⬚ B. item represents the values of the object properties.
⬚ C. B:item represents the property names of the object.
⬚ D. item represents the property names of the object.
⬚ E. C:item represents the index of the object properties.
⬚ F. item represents the index of the object properties.
⬚ G. D:item represents the number of properties in the object.
⬚ H. item represents the number of properties in the object.

