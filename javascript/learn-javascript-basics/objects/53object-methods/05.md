Quiz: What will be the output of the given code snippet?

```javascript
let person = {
    name: "Alice",
    age: 30,
    occupation: "Engineer"
};

const personKeys = Object.keys(person);

console.log(personKeys);
```

Tap an option below to answer:

[ 'name', 'age', 'occupation' ]

##### Correct Answer!

This is the list of keys of the person object.

This is the list of keys of the person object.

[ 'Alice', 30, 'Engineer' ]

##### Incorrect, give it another shot!

This is an array of values of the person object instead of keys.

This is an array of values of the person object instead of keys.

[ 'name': 'Alice', 'age': 30, 'occupation': 'Engineer' ]

##### Incorrect, give it another shot!

This is a mix of keys with values which is not how Object.keys() works.

This is a mix of keys with values which is not how Object.keys() works.

[ 'name', 'age' ]

##### Incorrect, give it another shot!

This omits the occupation key from the person object.

This omits the occupation key from the person object.

## 📝 Quiz

**Quiz: What will be the output of the given code snippet?**

⬚ A. A:[ 'name', 'age', 'occupation' ]
⬚ B. [ 'name', 'age', 'occupation' ]
⬚ C. B:[ 'Alice', 30, 'Engineer' ]
⬚ D. [ 'Alice', 30, 'Engineer' ]
⬚ E. C:[ 'name': 'Alice', 'age': 30, 'occupation': 'Engineer' ]
⬚ F. [ 'name': 'Alice', 'age': 30, 'occupation': 'Engineer' ]
⬚ G. D:[ 'name', 'age' ]
⬚ H. [ 'name', 'age' ]

