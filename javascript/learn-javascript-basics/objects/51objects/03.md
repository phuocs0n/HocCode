Quiz: What is the correct syntax for creating an object with a key named name and the value "Alice"?

Tap an option below to answer:

const person = { name: "Alice" };

##### Correct Answer!

This syntax correctly uses curly braces {} to define the object and key-value pairs.

This syntax correctly uses curly braces {} to define the object and key-value pairs.

const person = new Object(name: "Alice");

##### Incorrect, give it another shot!

This option is incorrect because new Object() does not take arguments in this way.

This option is incorrect because new Object() does not take arguments in this way.

const person = { "name" = "Alice" };

##### Incorrect, give it another shot!

This option is incorrect because = is not used inside curly braces. Also, keys don't require quotation marks.

This option is incorrect because = is not used inside curly braces. Also, keys don't require quotation marks.

const person = { name => "Alice" };

##### Incorrect, give it another shot!

This option is incorrect because => is not used for key-value pairs in objects.

This option is incorrect because => is not used for key-value pairs in objects.

## 📝 Quiz

**Quiz: What is the correct syntax for creating an object with a key named name and the value "Alice"?**

⬚ A. A:const person = { name: "Alice" };
⬚ B. const person = { name: "Alice" };
⬚ C. B:const person = new Object(name: "Alice");
⬚ D. const person = new Object(name: "Alice");
⬚ E. C:const person = { "name" = "Alice" };
⬚ F. const person = { "name" = "Alice" };
⬚ G. D:const person = { name => "Alice" };
⬚ H. const person = { name => "Alice" };

