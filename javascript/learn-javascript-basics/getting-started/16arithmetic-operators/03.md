Quiz: What is the output of the following code?

```javascript
let x = -1;
let y = 10;

console.log(y + x);
```

Tap an option below to answer:

```text
11
```

##### Incorrect, give it another shot!

The addition of 10 and -1 is not 11. Think carefully about how negatives affect the sum.

The addition of 10 and -1 is not 11. Think carefully about how negatives affect the sum.

```text
10 - 1
```

##### Incorrect, give it another shot!

JavaScript calculates the sum of y and x, not a subtraction in this format. The result is not 10 - 1.

JavaScript calculates the sum of y and x, not a subtraction in this format. The result is not 10 - 1.

```text
9
```

##### Correct Answer!

The sum of 10 and -1 is 9. Consider how addition with negative numbers works.

The sum of 10 and -1 is 9. Consider how addition with negative numbers works.

```text
-9
```

##### Incorrect, give it another shot!

Adding 10 and -1 results in a positive value, not a negative one like -9.

Adding 10 and -1 results in a positive value, not a negative one like -9.

##### Incorrect, give it another shot!

The addition of 10 and -1 is not 11. Think carefully about how negatives affect the sum.

The addition of 10 and -1 is not 11. Think carefully about how negatives affect the sum.

##### Incorrect, give it another shot!

JavaScript calculates the sum of y and x, not a subtraction in this format. The result is not 10 - 1.

JavaScript calculates the sum of y and x, not a subtraction in this format. The result is not 10 - 1.

##### Correct Answer!

The sum of 10 and -1 is 9. Consider how addition with negative numbers works.

The sum of 10 and -1 is 9. Consider how addition with negative numbers works.

##### Incorrect, give it another shot!

Adding 10 and -1 results in a positive value, not a negative one like -9.

Adding 10 and -1 results in a positive value, not a negative one like -9.

## 📝 Quiz

**Quiz: What is the output of the following code?**

⬚ A. A:11
⬚ B. 11
⬚ C. B:10 - 1
⬚ D. 10 - 1
⬚ E. C:9
⬚ F. 9
⬚ G. D:-9
⬚ H. -9

