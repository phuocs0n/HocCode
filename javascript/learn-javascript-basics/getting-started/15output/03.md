Quiz: What is the output of the following code?

```javascript
let favoriteActor = "Andrew";
console.log("Favorite actor:", favoriteActor);
```

Tap an option below to answer:

```text
Favorite actor:, Andrew
```

##### Incorrect, give it another shot!

The comma symbol separates items in console.log(). It is not displayed in the output.

The comma symbol separates items in console.log(). It is not displayed in the output.

```text
Favorite actor: favoriteActor
```

##### Incorrect, give it another shot!

favoriteActor is a variable, so its value (Andrew) is displayed.

favoriteActor is a variable, so its value (Andrew) is displayed.

```text
"Favorite actor:" Andrew
```

##### Incorrect, give it another shot!

The quotation marks (" ") used for strings inside the console.log() statement are not printed in the output.

The quotation marks (" ") used for strings inside the console.log() statement are not printed in the output.

```text
Favorite actor: Andrew
```

##### Correct Answer!

The above code outputs the "Favorite actor" string, followed by the value stored in the favoriteActor variable.

The above code outputs the "Favorite actor" string, followed by the value stored in the favoriteActor variable.

## 📝 Quiz

**Quiz: What is the output of the following code?**

⬚ A. A:Favorite actor:, Andrew
⬚ B. Favorite actor:, Andrew
⬚ C. B:Favorite actor: favoriteActor
⬚ D. Favorite actor: favoriteActor
⬚ E. C:"Favorite actor:" Andrew
⬚ F. "Favorite actor:" Andrew
⬚ G. D:Favorite actor: Andrew
⬚ H. Favorite actor: Andrew

