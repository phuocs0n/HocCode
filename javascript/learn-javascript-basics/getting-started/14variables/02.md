Quiz: In the code below, which part is the variable?

```javascript
let jack = "name";
```

Tap an option below to answer:

jack

##### Correct Answer!

The word that follows the let keyword is the variable.

The word that follows the let keyword is the variable.

"name"

##### Incorrect, give it another shot!

"name" is a string that's being assigned to the variable.

"name" is a string that's being assigned to the variable.

name

##### Incorrect, give it another shot!

name is the value of the string being assigned to the variable.

name is the value of the string being assigned to the variable.

##### Incorrect, give it another shot!

The = symbol assigns the value to the variable.

The = symbol assigns the value to the variable.

##### Correct Answer!

The word that follows the let keyword is the variable.

The word that follows the let keyword is the variable.

##### Incorrect, give it another shot!

"name" is a string that's being assigned to the variable.

"name" is a string that's being assigned to the variable.

##### Incorrect, give it another shot!

name is the value of the string being assigned to the variable.

name is the value of the string being assigned to the variable.

##### Incorrect, give it another shot!

The = symbol assigns the value to the variable.

The = symbol assigns the value to the variable.

## 📝 Quiz

**Quiz: In the code below, which part is the variable?**

⬚ A. A:jack
⬚ B. jack
⬚ C. B:"name"
⬚ D. "name"
⬚ E. C:name
⬚ F. name
⬚ G. D:=
⬚ H. =

