Quiz: What will be the output of the following code?

```javascript
let num = -5;

try {
    if (num < 0) {
        throw new Error("Negative numbers not allowed");
    }
    console.log(`Valid number: ${num}`);
} catch (error) {
    console.log(`Error caught: ${error.message}`);
}
```

Tap an option below to answer:

Valid number: -5

##### Incorrect, give it another shot!

The try block throws an error when if number is negative, so console.log(`Valid number: ${num}`); doesn't execute.

The try block throws an error when if number is negative, so console.log(`Valid number: ${num}`); doesn't execute.

Error caught: Negative numbers not allowed

##### Correct Answer!

Since num is -5, the throw statement executes, and the error is caught in the catch block, printing "Error caught: Negative numbers not allowed".

Since num is -5, the throw statement executes, and the error is caught in the catch block, printing "Error caught: Negative numbers not allowed".

undefined

##### Incorrect, give it another shot!

The try block either displays a valid string or throws an error, but it does not return undefined in any case.

The try block either displays a valid string or throws an error, but it does not return undefined in any case.

SyntaxError

##### Incorrect, give it another shot!

There is no syntax error in the given code.

There is no syntax error in the given code.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:Valid number: -5
⬚ B. Valid number: -5
⬚ C. B:Error caught: Negative numbers not allowed
⬚ D. Error caught: Negative numbers not allowed
⬚ E. C:undefined
⬚ F. undefined
⬚ G. D:SyntaxError
⬚ H. SyntaxError

