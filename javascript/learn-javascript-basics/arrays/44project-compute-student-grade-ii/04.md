# Using Array to Store Scores

#### Course: Learn JavaScript Basics

## Using Array to Store Scores

Instead of passing five separate numbers as arguments, you can simplify the function by passing an array directly.

Consider this is the input array:

```javascript
const scores = [55, 64, 75, 80, 65];
```

Now, you can modify the function to accept the array and calculate the average score:

```javascript
// Find the average score and return it
const getAverageScore = (scores) => {

    // Calculate total score

    // Compute average score
    let averageScore = total / scores.length;
    
    return averageScore;
}
```

Notice that we've used scores.length to ensure the function works for arrays of any size, not just five elements.

Let's now implement the code to calculate the total score.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


