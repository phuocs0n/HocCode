# Implementing reduce()

#### Course: Learn JavaScript Basics

## Implementing reduce()

Here's the updated code:

```javascript
1// Function to add scores
2const addScore = (sum, score) => {
3    return sum + score;
4};
5
6// Find the average score and return it
7const getAverageScore = (scores) => {
8
9    // Calculate total score
10    let total = scores.reduce(addScore, 0);
11    
12    // Compute average score
13    let averageScore = total / scores.length;
14    
15    return averageScore;
16}
17
18const scores = [55, 64, 75, 80, 65];
19
20let averageScore = getAverageScore(scores);
21
22console.log(averageScore);    // Output: 67.8
```

Here, we've placed the addScore() function outside getAverageScore(). While the position of the function doesn't matter, what's important is that it is called correctly.

Also, now that getAverageScore() works with arrays of any size, let's test it using an array of a different size with the final version of the code.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


