Quiz: Which symbol is used to define arrays?

Tap an option below to answer:

( )

##### Incorrect, give it another shot!

Parentheses ( ) are used for creating and calling functions. They aren't used in arrays.

Parentheses ( ) are used for creating and calling functions. They aren't used in arrays.

{ }

##### Incorrect, give it another shot!

Curly braces { } are used to define the body of conditional statements, such as if statements, and the body of functions.

Curly braces { } are used to define the body of conditional statements, such as if statements, and the body of functions.

[ ]

##### Correct Answer!

Square brackets [ ] enclose the elements of an array, with each element separated by a comma (,).

Square brackets [ ] enclose the elements of an array, with each element separated by a comma (,).

< >

##### Incorrect, give it another shot!

Angle brackets < > are used as comparison operators to compare values.

Angle brackets < > are used as comparison operators to compare values.

##### Incorrect, give it another shot!

Parentheses ( ) are used for creating and calling functions. They aren't used in arrays.

Parentheses ( ) are used for creating and calling functions. They aren't used in arrays.

##### Incorrect, give it another shot!

Curly braces { } are used to define the body of conditional statements, such as if statements, and the body of functions.

Curly braces { } are used to define the body of conditional statements, such as if statements, and the body of functions.

##### Correct Answer!

Square brackets [ ] enclose the elements of an array, with each element separated by a comma (,).

Square brackets [ ] enclose the elements of an array, with each element separated by a comma (,).

##### Incorrect, give it another shot!

Angle brackets < > are used as comparison operators to compare values.

Angle brackets < > are used as comparison operators to compare values.

## 📝 Quiz

**Quiz: Which symbol is used to define arrays?**

⬚ A. A:( )
⬚ B. ( )
⬚ C. B:{ }
⬚ D. { }
⬚ E. C:[ ]
⬚ F. [ ]
⬚ G. D:< >
⬚ H. < >

