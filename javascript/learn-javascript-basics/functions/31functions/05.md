Quiz: What is the output of the following code?

```javascript
function addNumbers() {
    console.log(5 + 3);
}

addNumbers();
```

Tap an option below to answer:

```text
5 + 3
```

##### Incorrect, give it another shot!

The function performs a calculation, so the result should be the outcome of the calculation.

The function performs a calculation, so the result should be the outcome of the calculation.

```text
8
```

##### Correct Answer!

When addNumbers() is called, console.log(5 + 3) is executed, displaying 8 on the console.

When addNumbers() is called, console.log(5 + 3) is executed, displaying 8 on the console.

```text
addNumbers
```

##### Incorrect, give it another shot!

The function does not print the function name. The function prints the sum of 5 and 3.

The function does not print the function name. The function prints the sum of 5 and 3.

```text
console.log
```

##### Incorrect, give it another shot!

The console.log() method displays the content that's inside ( ) not its own name.

The console.log() method displays the content that's inside ( ) not its own name.

##### Incorrect, give it another shot!

The function performs a calculation, so the result should be the outcome of the calculation.

The function performs a calculation, so the result should be the outcome of the calculation.

##### Correct Answer!

When addNumbers() is called, console.log(5 + 3) is executed, displaying 8 on the console.

When addNumbers() is called, console.log(5 + 3) is executed, displaying 8 on the console.

##### Incorrect, give it another shot!

The function does not print the function name. The function prints the sum of 5 and 3.

The function does not print the function name. The function prints the sum of 5 and 3.

##### Incorrect, give it another shot!

The console.log() method displays the content that's inside ( ) not its own name.

The console.log() method displays the content that's inside ( ) not its own name.

## 📝 Quiz

**Quiz: What is the output of the following code?**

⬚ A. A:5 + 3
⬚ B. 5 + 3
⬚ C. B:8
⬚ D. 8
⬚ E. C:addNumbers
⬚ F. addNumbers
⬚ G. D:console.log
⬚ H. console.log

