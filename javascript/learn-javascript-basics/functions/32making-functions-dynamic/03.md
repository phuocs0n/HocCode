Quiz: What's the output of the following code?

```javascript
function addFive(number) {
    let result = number + 5;
    console.log(result);
}

addFive(100);
addFive(20);
```

Tap an option below to answer:

110

30

##### Incorrect, give it another shot!

The function adds 5 to the argument passed. Not 10.

The function adds 5 to the argument passed. Not 10.

105

25

##### Correct Answer!

The function adds 5 to the argument and prints it.

The function adds 5 to the argument and prints it.

ERROR!

##### Incorrect, give it another shot!

The code is logically correct and has no errors.

The code is logically correct and has no errors.

130

##### Incorrect, give it another shot!

We've called the function twice. So the function will print value twice.

We've called the function twice. So the function will print value twice.

## 📝 Quiz

**Quiz: What's the output of the following code?**

⬚ A. A:11030
⬚ B. 11030
⬚ C. B:10525
⬚ D. 10525
⬚ E. C:ERROR!
⬚ F. ERROR!
⬚ G. D:130
⬚ H. 130

