Quiz: What will be the output of the following code?

```javascript
function greet() {
    let greeting = "Hello";
}

console.log(greeting);
```

Tap an option below to answer:

```text
Hello
```

##### Incorrect, give it another shot!

The greeting variable inside the console.log() statement is not defined since it is local to the greet() function only. In other words, the variable doesn't exist outside the greet() function.

The greeting variable inside the console.log() statement is not defined since it is local to the greet() function only. In other words, the variable doesn't exist outside the greet() function.

```text
ReferenceError
```

##### Correct Answer!

The greeting variable is not defined since it only exists inside the greet() function.

The greeting variable is not defined since it only exists inside the greet() function.

```text
greeting
```

##### Incorrect, give it another shot!

You're printing the greeting variable, not the "greeting" string.

You're printing the greeting variable, not the "greeting" string.

None of the above

##### Incorrect, give it another shot!

One of the above options is correct.

One of the above options is correct.

##### Incorrect, give it another shot!

The greeting variable inside the console.log() statement is not defined since it is local to the greet() function only. In other words, the variable doesn't exist outside the greet() function.

The greeting variable inside the console.log() statement is not defined since it is local to the greet() function only. In other words, the variable doesn't exist outside the greet() function.

##### Correct Answer!

The greeting variable is not defined since it only exists inside the greet() function.

The greeting variable is not defined since it only exists inside the greet() function.

##### Incorrect, give it another shot!

You're printing the greeting variable, not the "greeting" string.

You're printing the greeting variable, not the "greeting" string.

##### Incorrect, give it another shot!

One of the above options is correct.

One of the above options is correct.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:Hello
⬚ B. Hello
⬚ C. B:ReferenceError
⬚ D. ReferenceError
⬚ E. C:greeting
⬚ F. greeting
⬚ G. D:None of the above
⬚ H. None of the above

