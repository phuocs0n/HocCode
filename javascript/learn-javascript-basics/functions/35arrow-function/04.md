Quiz: What is the correct syntax for an arrow function that returns the sum of two numbers?

Tap an option below to answer:

const add = (a, b) = a + b;

##### Incorrect, give it another shot!

The syntax isn't correct because there needs to be => after the parameters and not =.

The syntax isn't correct because there needs to be => after the parameters and not =.

const add = (a, b) => a + b;

##### Correct Answer!

This is the correct syntax for an arrow function with a concise body.

This is the correct syntax for an arrow function with a concise body.

const add = (a, b) => { a + b; }

##### Incorrect, give it another shot!

Code inside curly braces {} needs to explicitly return a value using return statement. This doesn't return any value.

Code inside curly braces {} needs to explicitly return a value using return statement. This doesn't return any value.

const add = (a, b) -> { return a + b; };

##### Incorrect, give it another shot!

The -> syntax is incorrect for JavaScript arrow functions. JavaScript uses => for defining arrow functions.

The -> syntax is incorrect for JavaScript arrow functions. JavaScript uses => for defining arrow functions.

## 📝 Quiz

**Quiz: What is the correct syntax for an arrow function that returns the sum of two numbers?**

⬚ A. A:const add = (a, b) = a + b;
⬚ B. const add = (a, b) = a + b;
⬚ C. B:const add = (a, b) => a + b;
⬚ D. const add = (a, b) => a + b;
⬚ E. C:const add = (a, b) => { a + b; }
⬚ F. const add = (a, b) => { a + b; }
⬚ G. D:const add = (a, b) -> { return a + b; };
⬚ H. const add = (a, b) -> { return a + b; };

