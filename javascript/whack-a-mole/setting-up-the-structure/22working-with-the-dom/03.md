# Find the Game Board 🎮

## Find the Game Board 🎮

Your first DOM task is to tell JavaScript to find the game board div you created in the HTML.

You will use querySelector to select the element by its ID, store it in a variable, then log it to the console to confirm it worked.

Create a file named script.js.

- Create a function called main().Inside main(), use querySelector to select the element with ID "game-board" and store it in a variable called gameBoard.
- Then log the gameBoard variable to the console.
- Finally, call main() to run the code.

- index.html
- script.js
- style.css

```unknown
<!DOCTYPE html>
```

