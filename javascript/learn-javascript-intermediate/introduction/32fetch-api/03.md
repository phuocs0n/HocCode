Quiz: Why is the fetch() function used asynchronously in JavaScript?

Tap an option below to answer:

Because it sends a request and blocks the rest of your code until a response is received.

##### Incorrect, give it another shot!

fetch() doesn't block the rest of your code.

fetch() doesn't block the rest of your code.

Because it sends a request and doesn't block the rest of your code from running while it waits for a response.

##### Correct Answer!

fetch() sends a request and runs the rest of the code asynchronously.

fetch() sends a request and runs the rest of the code asynchronously.

Because it always requires a callback function to handle the response.

##### Incorrect, give it another shot!

fetch() doesn't always require callback. It sends the request and runs the rest of the code asynchronously.

fetch() doesn't always require callback. It sends the request and runs the rest of the code asynchronously.

Because it automatically retries the request if it fails.

##### Incorrect, give it another shot!

fetch() doesn't automatically retry the request if it fails. It runs the rest of the code after sending a request.

fetch() doesn't automatically retry the request if it fails. It runs the rest of the code after sending a request.

## 📝 Quiz

**Quiz: Why is the fetch() function used asynchronously in JavaScript?**

⬚ A. A:Because it sends a request and blocks the rest of your code until a response is received.
⬚ B. Because it sends a request and blocks the rest of your code until a response is received.
⬚ C. B:Because it sends a request and doesn't block the rest of your code from running while it waits for a response.
⬚ D. Because it sends a request and doesn't block the rest of your code from running while it waits for a response.
⬚ E. C:Because it always requires a callback function to handle the response.
⬚ F. Because it always requires a callback function to handle the response.
⬚ G. D:Because it automatically retries the request if it fails.
⬚ H. Because it automatically retries the request if it fails.

