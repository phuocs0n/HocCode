Quiz: What does the async keyword do when placed before a function?

Tap an option below to answer:

Makes the function return a Promise.

##### Correct Answer!

The async keyword ensures that the function returns a Promise, allowing asynchronous operations inside it using await.

The async keyword ensures that the function returns a Promise, allowing asynchronous operations inside it using await.

Converts the function into a synchronous function.

##### Incorrect, give it another shot!

The async keyword does not convert a function into a synchronous function. It allows asynchronous operations to be handled within a synchronous-looking structure.

The async keyword does not convert a function into a synchronous function. It allows asynchronous operations to be handled within a synchronous-looking structure.

Automatically resolves the function.

##### Incorrect, give it another shot!

The async keyword does not automatically resolve a function.

The async keyword does not automatically resolve a function.

Makes the function run faster.

##### Incorrect, give it another shot!

The async keyword does not affect the speed of function execution. It manages asynchronous operations.

The async keyword does not affect the speed of function execution. It manages asynchronous operations.

## 📝 Quiz

**Quiz: What does the async keyword do when placed before a function?**

⬚ A. A:Makes the function return a Promise.
⬚ B. Makes the function return a Promise.
⬚ C. B:Converts the function into a synchronous function.
⬚ D. Converts the function into a synchronous function.
⬚ E. C:Automatically resolves the function.
⬚ F. Automatically resolves the function.
⬚ G. D:Makes the function run faster.
⬚ H. Makes the function run faster.

