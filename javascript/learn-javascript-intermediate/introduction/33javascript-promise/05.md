Quiz: What does the resolve function do in a Promise?

Tap an option below to answer:

Rejects the Promise

##### Incorrect, give it another shot!

The resolve function is used to pass the result, not to reject the Promise.

The resolve function is used to pass the result, not to reject the Promise.

Passes the result to be handled.

##### Correct Answer!

The resolve function is used to pass the result to the next .then() handler.

The resolve function is used to pass the result to the next .then() handler.

Creates a new Promise

##### Incorrect, give it another shot!

Creating a Promise involves the new Promise, not the resolve function.

Creating a Promise involves the new Promise, not the resolve function.

Prints an error message

##### Incorrect, give it another shot!

The resolve function does not handle errors; the reject function does that.

The resolve function does not handle errors; the reject function does that.

## 📝 Quiz

**Quiz: What does the resolve function do in a Promise?**

⬚ A. A:Rejects the Promise
⬚ B. Rejects the Promise
⬚ C. B:Passes the result to be handled.
⬚ D. Passes the result to be handled.
⬚ E. C:Creates a new Promise
⬚ F. Creates a new Promise
⬚ G. D:Prints an error message
⬚ H. Prints an error message

