Quiz: What will be the output of the following code?

```javascript
console.log('Start');
setTimeout(() => {
  console.log('Middle');
}, 1000);
console.log('End');
```

Tap an option below to answer:

Start

End

Middle

##### Correct Answer!

The setTimeout() function prints "Middle" 1 second, allowing the next console.log('End') to execute immediately.

The setTimeout() function prints "Middle" 1 second, allowing the next console.log('End') to execute immediately.

Start

Middle

End

##### Incorrect, give it another shot!

The "Middle" will not appear immediately because it is inside a setTimeout() with a delay.

The "Middle" will not appear immediately because it is inside a setTimeout() with a delay.

Middle

Start

End

##### Incorrect, give it another shot!

"Middle" cannot appear first because of the setTimeout() delay.

"Middle" cannot appear first because of the setTimeout() delay.

End

Start

Middle

##### Incorrect, give it another shot!

JavaScript executes code synchronously by default, so "Start" will always appear first.

JavaScript executes code synchronously by default, so "Start" will always appear first.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:StartEndMiddle
⬚ B. StartEndMiddle
⬚ C. B:StartMiddleEnd
⬚ D. StartMiddleEnd
⬚ E. C:MiddleStartEnd
⬚ F. MiddleStartEnd
⬚ G. D:EndStartMiddle
⬚ H. EndStartMiddle

