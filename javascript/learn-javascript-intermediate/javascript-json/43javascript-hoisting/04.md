Quiz: Consider the following code snippet.

```javascript
console.log(a);
var a = 10;
```

What will it print?

Tap an option below to answer:

10

##### Incorrect, give it another shot!

Only variable declaration is moved to the top of its scope, not variable initialization. So, it doesn't print 10.

Only variable declaration is moved to the top of its scope, not variable initialization. So, it doesn't print 10.

undefined

##### Correct Answer!

Correct! Although var a; is hoisted to the top, a is not yet assigned a value 10 at the time console.log(a); is executed. Therefore, a is undefined.

Correct! Although var a; is hoisted to the top, a is not yet assigned a value 10 at the time console.log(a); is executed. Therefore, a is undefined.

ReferenceError: a is not defined

##### Incorrect, give it another shot!

Variable a is indeed defined due to hoisting, so we don't get a ReferenceError.

Variable a is indeed defined due to hoisting, so we don't get a ReferenceError.

TypeError: Cannot access 'a' before initialization

##### Incorrect, give it another shot!

TypeError occurs when trying to access a const or let variable before it is initialized, not with var.

TypeError occurs when trying to access a const or let variable before it is initialized, not with var.

## 📝 Quiz

**Quiz: Consider the following code snippet.**

⬚ A. A:10
⬚ B. 10
⬚ C. B:undefined
⬚ D. undefined
⬚ E. C:ReferenceError: a is not defined
⬚ F. ReferenceError: a is not defined
⬚ G. D:TypeError: Cannot access 'a' before initialization
⬚ H. TypeError: Cannot access 'a' before initialization

