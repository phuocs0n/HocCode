Quiz: What is the output of the following JavaScript code?

```javascript
function getMessage() {
    var message = "Hello";
}

getMessage();
console.log(message);
```

Tap an option below to answer:

ReferenceError: message is not defined

##### Correct Answer!

Correct! Variables declared inside a function are local to that function. Attempting to access message variable outside its scope results in a ReferenceError.

Correct! Variables declared inside a function are local to that function. Attempting to access message variable outside its scope results in a ReferenceError.

undefined

##### Incorrect, give it another shot!

The message variable is initialized inside the function, so it won't print undefined.

The message variable is initialized inside the function, so it won't print undefined.

Hello

##### Incorrect, give it another shot!

The variable defined inside a function can not be accessed outside its scope, so it doesn't print Hello.

The variable defined inside a function can not be accessed outside its scope, so it doesn't print Hello.

message

##### Incorrect, give it another shot!

Here, message is a variable, not a string.

Here, message is a variable, not a string.

## 📝 Quiz

**Quiz: What is the output of the following JavaScript code?**

⬚ A. A:ReferenceError: message is not defined
⬚ B. ReferenceError: message is not defined
⬚ C. B:undefined
⬚ D. undefined
⬚ E. C:Hello
⬚ F. Hello
⬚ G. D:message
⬚ H. message

