Quiz: Which of the following statements about querySelector() is true?

Tap an option below to answer:

It returns all elements that match the selector in an array.

##### Incorrect, give it another shot!

querySelector() does not return all matching elements; it returns only one element.

querySelector() does not return all matching elements; it returns only one element.

It returns only the first element that matches the selector.

##### Correct Answer!

querySelector() selects only the first element that matches the selector.

querySelector() selects only the first element that matches the selector.

It returns the last element that matches the selector.

##### Incorrect, give it another shot!

querySelector() does not return the last matching element.

querySelector() does not return the last matching element.

It throws an error if multiple elements match the selector.

##### Incorrect, give it another shot!

querySelector() does not throw an error if multiple elements match the selector.

querySelector() does not throw an error if multiple elements match the selector.

## 📝 Quiz

**Quiz: Which of the following statements about querySelector() is true?**

⬚ A. A:It returns all elements that match the selector in an array.
⬚ B. It returns all elements that match the selector in an array.
⬚ C. B:It returns only the first element that matches the selector.
⬚ D. It returns only the first element that matches the selector.
⬚ E. C:It returns the last element that matches the selector.
⬚ F. It returns the last element that matches the selector.
⬚ G. D:It throws an error if multiple elements match the selector.
⬚ H. It throws an error if multiple elements match the selector.

