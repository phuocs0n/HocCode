Quiz: What does the parentElement property return?

Tap an option below to answer:

All ancestor elements of the current element.

##### Incorrect, give it another shot!

parentElement does not traverse up the DOM tree beyond the immediate parent.

parentElement does not traverse up the DOM tree beyond the immediate parent.

The first child of the current element.

##### Incorrect, give it another shot!

The children property is used to access child elements.

The children property is used to access child elements.

All sibling elements of the current element.

##### Incorrect, give it another shot!

parentElement does not deal with siblings.

parentElement does not deal with siblings.

The immediate parent element of the current element.

##### Correct Answer!

parentElement only returns the direct parent in the DOM tree.

parentElement only returns the direct parent in the DOM tree.

## 📝 Quiz

**Quiz: What does the parentElement property return?**

⬚ A. A:All ancestor elements of the current element.
⬚ B. All ancestor elements of the current element.
⬚ C. B:The first child of the current element.
⬚ D. The first child of the current element.
⬚ E. C:All sibling elements of the current element.
⬚ F. All sibling elements of the current element.
⬚ G. D:The immediate parent element of the current element.
⬚ H. The immediate parent element of the current element.

