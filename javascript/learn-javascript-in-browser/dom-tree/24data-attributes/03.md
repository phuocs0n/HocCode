Quiz: What will this code do?

```javascriptDOM
const image = document.querySelector('img');
const imgSrc = image.getAttribute('src');
console.log(imgSrc);
```

Tap an option below to answer:

It logs the text content of the image.

##### Incorrect, give it another shot!

getAttribute retrieves the value of an attribute, not the text content of an element.

getAttribute retrieves the value of an attribute, not the text content of an element.

It retrieves the src attribute value and logs it.

##### Correct Answer!

getAttribute('src') specifically retrieves the value of the src attribute.

getAttribute('src') specifically retrieves the value of the src attribute.

It sets the src attribute of the image.

##### Incorrect, give it another shot!

Setting an attribute is done using setAttribute, not getAttribute.

Setting an attribute is done using setAttribute, not getAttribute.

It removes the src attribute from the image.

##### Incorrect, give it another shot!

Removing attributes is done with removeAttribute, not getAttribute.

Removing attributes is done with removeAttribute, not getAttribute.

## 📝 Quiz

**Quiz: What will this code do?**

⬚ A. A:It logs the text content of the image.
⬚ B. It logs the text content of the image.
⬚ C. B:It retrieves the src attribute value and logs it.
⬚ D. It retrieves the src attribute value and logs it.
⬚ E. C:It sets the src attribute of the image.
⬚ F. It sets the src attribute of the image.
⬚ G. D:It removes the src attribute from the image.
⬚ H. It removes the src attribute from the image.

