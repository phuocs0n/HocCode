Quiz: How would you access the second child element of an element with the class list?

Tap an option below to answer:

document.querySelector(".list").children[1]

##### Correct Answer!

children[1] accesses the second child in the children collection.

children[1] accesses the second child in the children collection.

document.querySelector("#list").children[1]

##### Incorrect, give it another shot!

The way to select classes is .list not #list.

The way to select classes is .list not #list.

document.querySelector(".list").querySelector("li:nth-child(2)")

##### Incorrect, give it another shot!

querySelector retrieves the second <li> but not via the children property.

querySelector retrieves the second <li> but not via the children property.

document.querySelector(".list").lastChild

##### Incorrect, give it another shot!

lastChild retrieves the last child node, not necessarily the second.

lastChild retrieves the last child node, not necessarily the second.

## 📝 Quiz

**Quiz: How would you access the second child element of an element with the class list?**

⬚ A. A:document.querySelector(".list").children[1]
⬚ B. document.querySelector(".list").children[1]
⬚ C. B:document.querySelector("#list").children[1]
⬚ D. document.querySelector("#list").children[1]
⬚ E. C:document.querySelector(".list").querySelector("li:nth-child(2)")
⬚ F. document.querySelector(".list").querySelector("li:nth-child(2)")
⬚ G. D:document.querySelector(".list").lastChild
⬚ H. document.querySelector(".list").lastChild

