Quiz: In the DOM tree, what is the parent of the <ul> element in the following structure?

```javascriptDOM
<div>
  <ul>
    <li>Item 1</li>
  </ul>
</div>
```

Tap an option below to answer:

The <html> element.

##### Incorrect, give it another shot!

The <html> is the root element and not the immediate parent of <ul>.

The <html> is the root element and not the immediate parent of <ul>.

The <body> element.

##### Incorrect, give it another shot!

The <body> is higher up in the DOM hierarchy.

The <body> is higher up in the DOM hierarchy.

The <li> element.

##### Incorrect, give it another shot!

The <li> is a child of the <ul>, not its parent.

The <li> is a child of the <ul>, not its parent.

The <div> element.

##### Correct Answer!

The <div> directly contains the <ul>.

The <div> directly contains the <ul>.

##### Incorrect, give it another shot!

The <html> is the root element and not the immediate parent of <ul>.

The <html> is the root element and not the immediate parent of <ul>.

##### Incorrect, give it another shot!

The <body> is higher up in the DOM hierarchy.

The <body> is higher up in the DOM hierarchy.

##### Incorrect, give it another shot!

The <li> is a child of the <ul>, not its parent.

The <li> is a child of the <ul>, not its parent.

##### Correct Answer!

The <div> directly contains the <ul>.

The <div> directly contains the <ul>.

## 📝 Quiz

**Quiz: In the DOM tree, what is the parent of the <ul> element in the following structure?**

⬚ A. A:The <html> element.
⬚ B. The <html> element.
⬚ C. B:The <body> element.
⬚ D. The <body> element.
⬚ E. C:The <li> element.
⬚ F. The <li> element.
⬚ G. D:The <div> element.
⬚ H. The <div> element.

