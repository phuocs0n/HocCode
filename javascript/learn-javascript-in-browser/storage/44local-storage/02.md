Quiz: If the object user = { name: "John", role: "admin" } is stored as a string using JSON.stringify, how do you retrieve it in its original object form?

Tap an option below to answer:

localStorage.getItem('user');

##### Incorrect, give it another shot!

This retrieves the value as a string but does not convert it back into an object.

This retrieves the value as a string but does not convert it back into an object.

localStorage.getObject('user');

##### Incorrect, give it another shot!

localStorage has no getObject() method; you must manually parse the stored string using JSON.parse.

localStorage has no getObject() method; you must manually parse the stored string using JSON.parse.

JSON.parse(localStorage.getItem('user'));

##### Correct Answer!

JSON.parse converts the JSON string back into the original object format.

JSON.parse converts the JSON string back into the original object format.

JSON.stringify(localStorage.getItem('user'));

##### Incorrect, give it another shot!

This re-encodes the string as JSON instead of converting it back into an object.

This re-encodes the string as JSON instead of converting it back into an object.

## 📝 Quiz

**Quiz: If the object user = { name: "John", role: "admin" } is stored as a string using JSON.stringify, how do you retrieve it in its original object form?**

⬚ A. A:localStorage.getItem('user');
⬚ B. localStorage.getItem('user');
⬚ C. B:localStorage.getObject('user');
⬚ D. localStorage.getObject('user');
⬚ E. C:JSON.parse(localStorage.getItem('user'));
⬚ F. JSON.parse(localStorage.getItem('user'));
⬚ G. D:JSON.stringify(localStorage.getItem('user'));
⬚ H. JSON.stringify(localStorage.getItem('user'));

