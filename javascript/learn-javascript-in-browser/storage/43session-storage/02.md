Quiz: What happens to session storage data when the browser tab is closed?

Tap an option below to answer:

It persists across browser restarts.

##### Incorrect, give it another shot!

Session storage is designed to be temporary and does not survive browser restarts or tab closures.

Session storage is designed to be temporary and does not survive browser restarts or tab closures.

It is deleted when the tab is closed.

##### Correct Answer!

Session storage is specific to a single browser tab and is cleared when the tab is closed.

Session storage is specific to a single browser tab and is cleared when the tab is closed.

It is shared across all tabs.

##### Incorrect, give it another shot!

Session storage is not shared across tabs.

Session storage is not shared across tabs.

It is stored permanently unless explicitly cleared.

##### Incorrect, give it another shot!

Permanent storage is a characteristic of local storage, not session storage.

Permanent storage is a characteristic of local storage, not session storage.

## 📝 Quiz

**Quiz: What happens to session storage data when the browser tab is closed?**

⬚ A. A:It persists across browser restarts.
⬚ B. It persists across browser restarts.
⬚ C. B:It is deleted when the tab is closed.
⬚ D. It is deleted when the tab is closed.
⬚ E. C:It is shared across all tabs.
⬚ F. It is shared across all tabs.
⬚ G. D:It is stored permanently unless explicitly cleared.
⬚ H. It is stored permanently unless explicitly cleared.

