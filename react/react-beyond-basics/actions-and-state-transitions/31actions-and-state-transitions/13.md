Quiz: What happens when you call the following function?

```react
dispatch({ type: "SET_ENTRIES", payload: data })
```

Tap an option below to answer:

React calls setState() internally and merges the payload into the existing state

##### Incorrect, give it another shot!

useReducer() has no automatic merging. The reducer decides the new state shape entirely.

useReducer() has no automatic merging. The reducer decides the new state shape entirely.

React re-renders with a new state

##### Correct Answer!

When you call dispatch(), React passes the current state and the action object to the reducer, which returns a new state. React then re-renders with this new state.

When you call dispatch(), React passes the current state and the action object to the reducer, which returns a new state. React then re-renders with this new state.

The action object is stored in a queue and processed on the next render cycle

##### Incorrect, give it another shot!

dispatch() triggers the reducer synchronously. The result is scheduled for re-render immediately.

dispatch() triggers the reducer synchronously. The result is scheduled for re-render immediately.

The action type is matched against the component's props to decide what to update

##### Incorrect, give it another shot!

Props have nothing to do with it. The reducer is a plain function that reads state and action.

Props have nothing to do with it. The reducer is a plain function that reads state and action.

## 📝 Quiz

**Quiz: What happens when you call the following function?**

⬚ A. A:React calls setState() internally and merges the payload into the existing state
⬚ B. React calls setState() internally and merges the payload into the existing state
⬚ C. B:React re-renders with a new state
⬚ D. React re-renders with a new state
⬚ E. C:The action object is stored in a queue and processed on the next render cycle
⬚ F. The action object is stored in a queue and processed on the next render cycle
⬚ G. D:The action type is matched against the component's props to decide what to update
⬚ H. The action type is matched against the component's props to decide what to update

