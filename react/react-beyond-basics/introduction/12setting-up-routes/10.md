Quiz: What does the Routes component do?

Tap an option below to answer:

It fetches data from the server based on the current URL

##### Incorrect, give it another shot!

Routes doesn't interact with the server at all.

Routes doesn't interact with the server at all.

It renders all Route children at the same time

##### Incorrect, give it another shot!

Only one Route matches at a time.

Only one Route matches at a time.

It looks at the current URL and renders the first Route whose path matches

##### Correct Answer!

Routes acts as a switcher that picks the best match.

Routes acts as a switcher that picks the best match.

It wraps the app and listens to the browser history API

##### Incorrect, give it another shot!

That's what BrowserRouter does, not Routes.

That's what BrowserRouter does, not Routes.

## 📝 Quiz

**Quiz: What does the Routes component do?**

⬚ A. A:It fetches data from the server based on the current URL
⬚ B. It fetches data from the server based on the current URL
⬚ C. B:It renders all Route children at the same time
⬚ D. It renders all Route children at the same time
⬚ E. C:It looks at the current URL and renders the first Route whose path matches
⬚ F. It looks at the current URL and renders the first Route whose path matches
⬚ G. D:It wraps the app and listens to the browser history API
⬚ H. It wraps the app and listens to the browser history API

