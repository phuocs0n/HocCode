Quiz: What does useParams() return when the route is /entries/:id and the URL is /entries/7?

Tap an option below to answer:

The number 7

##### Incorrect, give it another shot!

useParams() always returns an object whose key-value pair consists of a string value, never a number.

useParams() always returns an object whose key-value pair consists of a string value, never a number.

An array: ["id", "7"]

##### Incorrect, give it another shot!

useParams() returns an object, not an array.

useParams() returns an object, not an array.

An object: { id: "7" }

##### Correct Answer!

The key matches the segment name, the value is always a string.

The key matches the segment name, the value is always a string.

The full URL string "/entries/7"

##### Incorrect, give it another shot!

useParams() returns only the captured segment values, not the full URL.

useParams() returns only the captured segment values, not the full URL.

## 📝 Quiz

**Quiz: What does useParams() return when the route is /entries/:id and the URL is /entries/7?**

⬚ A. A:The number 7
⬚ B. The number 7
⬚ C. B:An array: ["id", "7"]
⬚ D. An array: ["id", "7"]
⬚ E. C:An object: { id: "7" }
⬚ F. An object: { id: "7" }
⬚ G. D:The full URL string "/entries/7"
⬚ H. The full URL string "/entries/7"

