Quiz: What does the value prop on ThemeContext.Provider control?

Tap an option below to answer:

It sets the default value used when createContext() is first called

##### Incorrect, give it another shot!

The default value in createContext() only applies when there is no Provider above the consumer.

The default value in createContext() only applies when there is no Provider above the consumer.

It defines what any useContext(ThemeContext) call in the tree receives

##### Correct Answer!

The value prop is the data contract. Whatever you put there is exactly what consumers get back.

The value prop is the data contract. Whatever you put there is exactly what consumers get back.

It determines which components are allowed to consume the context

##### Incorrect, give it another shot!

Any component inside the Provider can consume the context.

Any component inside the Provider can consume the context.

It prevents the Provider from re-rendering when state changes

##### Incorrect, give it another shot!

Changing state in the Provider causes a re-render, which updates the value for all consumers.

Changing state in the Provider causes a re-render, which updates the value for all consumers.

## 📝 Quiz

**Quiz: What does the value prop on ThemeContext.Provider control?**

⬚ A. A:It sets the default value used when createContext() is first called
⬚ B. It sets the default value used when createContext() is first called
⬚ C. B:It defines what any useContext(ThemeContext) call in the tree receives
⬚ D. It defines what any useContext(ThemeContext) call in the tree receives
⬚ E. C:It determines which components are allowed to consume the context
⬚ F. It determines which components are allowed to consume the context
⬚ G. D:It prevents the Provider from re-rendering when state changes
⬚ H. It prevents the Provider from re-rendering when state changes

