# Creating the Context File

#### Course: React Beyond Basics

## Creating the Context File

We'll start by creating a new file at /context/ThemeContext.jsx.

This file will:

- hold the context object,
- a Provider component,
- and a custom hook for reading the value.

```react
import { createContext, useContext, useState } from "react";

const ThemeContext = createContext(null);

export function ThemeProvider({ children }) {
  const [theme, setTheme] = useState("light");
  function toggleTheme() {
    setTheme(function (t) { return t === "light" ? "dark" : "light"; });
  }

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

export function useTheme() {
  return useContext(ThemeContext);
}
```

There are a few things worth understanding here. Let's walk through them.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


