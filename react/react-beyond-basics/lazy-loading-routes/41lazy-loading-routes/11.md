Quiz: What happens if we use lazy() without a Suspense boundary?

Tap an option below to answer:

React throws an error because no fallback UI was specified

##### Correct Answer!

React requires a Suspense boundary to handle the loading state.

React requires a Suspense boundary to handle the loading state.

The lazy component renders normally with no fallback shown

##### Incorrect, give it another shot!

React has no way to know what to render while the chunk loads.

React has no way to know what to render while the chunk loads.

The browser silently waits until the chunk is downloaded

##### Incorrect, give it another shot!

React does not silently wait. It needs explicit instruction.

React does not silently wait. It needs explicit instruction.

The static import is used as a fallback automatically

##### Incorrect, give it another shot!

There is no static import when using lazy().

There is no static import when using lazy().

## 📝 Quiz

**Quiz: What happens if we use lazy() without a Suspense boundary?**

⬚ A. A:React throws an error because no fallback UI was specified
⬚ B. React throws an error because no fallback UI was specified
⬚ C. B:The lazy component renders normally with no fallback shown
⬚ D. The lazy component renders normally with no fallback shown
⬚ E. C:The browser silently waits until the chunk is downloaded
⬚ F. The browser silently waits until the chunk is downloaded
⬚ G. D:The static import is used as a fallback automatically
⬚ H. The static import is used as a fallback automatically

