Quiz: A user visits /entries/1 for the first time and sees the skeleton. They navigate away and come back. What happens the second time?

Tap an option below to answer:

The app throws an error because the chunk was already imported

##### Incorrect, give it another shot!

Importing a cached module is valid and happens transparently.

Importing a cached module is valid and happens transparently.

The skeleton shows again because lazy() always re-fetches

##### Incorrect, give it another shot!

lazy() relies on the browser's module cache, not its own re-fetch logic.

lazy() relies on the browser's module cache, not its own re-fetch logic.

The skeleton shows for exactly 300ms as a minimum delay

##### Incorrect, give it another shot!

<Suspense> has no built-in minimum delay. It resolves as soon as the chunk is ready.

<Suspense> has no built-in minimum delay. It resolves as soon as the chunk is ready.

The page renders immediately with no fallback because the chunk is cached

##### Correct Answer!

The browser caches the chunk after the first download. So, subsequent renders are synchronous.

The browser caches the chunk after the first download. So, subsequent renders are synchronous.

## 📝 Quiz

**Quiz: A user visits /entries/1 for the first time and sees the skeleton. They navigate away and come back. What happens the second time?**

⬚ A. A:The app throws an error because the chunk was already imported
⬚ B. The app throws an error because the chunk was already imported
⬚ C. B:The skeleton shows again because lazy() always re-fetches
⬚ D. The skeleton shows again because lazy() always re-fetches
⬚ E. C:The skeleton shows for exactly 300ms as a minimum delay
⬚ F. The skeleton shows for exactly 300ms as a minimum delay
⬚ G. D:The page renders immediately with no fallback because the chunk is cached
⬚ H. The page renders immediately with no fallback because the chunk is cached

