Quiz: You're building a user dashboard that shows a list of tasks, a calendar, and notifications. All three update when the user completes a task. Why might React be a good choice here?

Tap an option below to answer:

React automatically updates all three sections when the task data changes

##### Correct Answer!

React keeps the UI in sync with your data.

React keeps the UI in sync with your data.

React makes the page load faster than plain JavaScript

##### Incorrect, give it another shot!

Speed is not React's main advantage.

Speed is not React's main advantage.

React prevents you from writing any JavaScript

##### Incorrect, give it another shot!

React is a JavaScript library, not a replacement for it.

React is a JavaScript library, not a replacement for it.

React eliminates the need for HTML and CSS

##### Incorrect, give it another shot!

React uses JSX (which looks like HTML) and still needs CSS.

React uses JSX (which looks like HTML) and still needs CSS.

## 📝 Quiz

**Quiz: You're building a user dashboard that shows a list of tasks, a calendar, and notifications. All three update when the user completes a task. Why might React be a good choice here?**

⬚ A. A:React automatically updates all three sections when the task data changes
⬚ B. React automatically updates all three sections when the task data changes
⬚ C. B:React makes the page load faster than plain JavaScript
⬚ D. React makes the page load faster than plain JavaScript
⬚ E. C:React prevents you from writing any JavaScript
⬚ F. React prevents you from writing any JavaScript
⬚ G. D:React eliminates the need for HTML and CSS
⬚ H. React eliminates the need for HTML and CSS

