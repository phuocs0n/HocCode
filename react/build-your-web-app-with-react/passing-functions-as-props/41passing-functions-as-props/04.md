Quiz: Why does EntryCard call onDelete(id) instead of updating the entries list itself?

Tap an option below to answer:

So that the Delete button works without re-rendering

##### Incorrect, give it another shot!

The list re-renders whenever the parent updates the state.

The list re-renders whenever the parent updates the state.

Because EntryCard is not allowed to use useState

##### Incorrect, give it another shot!

EntryCard could use useState for other things. The issue is who owns the entries list.

EntryCard could use useState for other things. The issue is who owns the entries list.

Because passing the id would not work from inside EntryCard

##### Incorrect, give it another shot!

Passing id works; the reason is about where the state lives.

Passing id works; the reason is about where the state lives.

Because App owns the entries state, not EntryCard

##### Correct Answer!

The state lives in the component that owns it; i.e., inside the parent component App. Children thus need to use callbacks to request changes.

The state lives in the component that owns it; i.e., inside the parent component App. Children thus need to use callbacks to request changes.

## 📝 Quiz

**Quiz: Why does EntryCard call onDelete(id) instead of updating the entries list itself?**

⬚ A. A:So that the Delete button works without re-rendering
⬚ B. So that the Delete button works without re-rendering
⬚ C. B:Because EntryCard is not allowed to use useState
⬚ D. Because EntryCard is not allowed to use useState
⬚ E. C:Because passing the id would not work from inside EntryCard
⬚ F. Because passing the id would not work from inside EntryCard
⬚ G. D:Because App owns the entries state, not EntryCard
⬚ H. Because App owns the entries state, not EntryCard

