Quiz: What is a React Fragment used for?

Tap an option below to answer:

Grouping multiple elements without adding a wrapper DOM node

##### Correct Answer!

Fragments let you group multiple elements without adding an extra DOM node like a <div>.

Fragments let you group multiple elements without adding an extra DOM node like a <div>.

Breaking up long components into smaller pieces

##### Incorrect, give it another shot!

That's what components are for, not fragments.

That's what components are for, not fragments.

Rendering components conditionally

##### Incorrect, give it another shot!

That's conditional rendering, not fragments.

That's conditional rendering, not fragments.

Adding CSS styles to multiple elements

##### Incorrect, give it another shot!

Styling is done with className or CSS, not fragments.

Styling is done with className or CSS, not fragments.

## 📝 Quiz

**Quiz: What is a React Fragment used for?**

⬚ A. A:Grouping multiple elements without adding a wrapper DOM node
⬚ B. Grouping multiple elements without adding a wrapper DOM node
⬚ C. B:Breaking up long components into smaller pieces
⬚ D. Breaking up long components into smaller pieces
⬚ E. C:Rendering components conditionally
⬚ F. Rendering components conditionally
⬚ G. D:Adding CSS styles to multiple elements
⬚ H. Adding CSS styles to multiple elements

