Quiz: Why must component names start with a capital letter?

Tap an option below to answer:

To make the code look more professional

##### Incorrect, give it another shot!

This is a technical requirement, not a style choice.

This is a technical requirement, not a style choice.

Because JavaScript requires it

##### Incorrect, give it another shot!

JavaScript doesn't care about capitalization for function names.

JavaScript doesn't care about capitalization for function names.

To prevent naming conflicts with CSS classes

##### Incorrect, give it another shot!

CSS classes are unrelated to component names.

CSS classes are unrelated to component names.

So React can distinguish components from regular HTML tags

##### Correct Answer!

React will interpret components beginning with lowercase as HTML tags. Think about <div> vs <EntryCard>.

React will interpret components beginning with lowercase as HTML tags. Think about <div> vs <EntryCard>.

## 📝 Quiz

**Quiz: Why must component names start with a capital letter?**

⬚ A. A:To make the code look more professional
⬚ B. To make the code look more professional
⬚ C. B:Because JavaScript requires it
⬚ D. Because JavaScript requires it
⬚ E. C:To prevent naming conflicts with CSS classes
⬚ F. To prevent naming conflicts with CSS classes
⬚ G. D:So React can distinguish components from regular HTML tags
⬚ H. So React can distinguish components from regular HTML tags

