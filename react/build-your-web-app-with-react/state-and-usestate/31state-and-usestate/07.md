Quiz: Why do we use useState instead of a normal variable for the list of entries?

Tap an option below to answer:

To improve performance

##### Incorrect, give it another shot!

useState is all about tracking change and re-rendering, not raw performance.

useState is all about tracking change and re-rendering, not raw performance.

To make the code shorter

##### Incorrect, give it another shot!

useState does not exist to shorten code.

useState does not exist to shorten code.

To pass data to child components

##### Incorrect, give it another shot!

You pass data with props; state is about change over time.

You pass data with props; state is about change over time.

So React can re-render the UI when the list changes

##### Correct Answer!

React only re-renders when state (or props) change.

React only re-renders when state (or props) change.

## 📝 Quiz

**Quiz: Why do we use useState instead of a normal variable for the list of entries?**

⬚ A. A:To improve performance
⬚ B. To improve performance
⬚ C. B:To make the code shorter
⬚ D. To make the code shorter
⬚ E. C:To pass data to child components
⬚ F. To pass data to child components
⬚ G. D:So React can re-render the UI when the list changes
⬚ H. So React can re-render the UI when the list changes

