Quiz: Why do we pass key={entry.id} instead of key={index} when rendering the list?

Tap an option below to answer:

Using index would make the list render in reverse order

##### Incorrect, give it another shot!

Index does not reverse the order.

Index does not reverse the order.

React requires keys to be numbers

##### Incorrect, give it another shot!

Keys can be strings or numbers; the important thing is uniqueness and stability.

Keys can be strings or numbers; the important thing is uniqueness and stability.

A unique id stays the same for that entry even if the list is reordered or items are removed

##### Correct Answer!

Index changes when the list changes, but a unique id is stable per entry.

Index changes when the list changes, but a unique id is stable per entry.

entry.id is required by EntryCard

##### Incorrect, give it another shot!

The array key is used internally by React; it's not for the component's props.

The array key is used internally by React; it's not for the component's props.

## 📝 Quiz

**Quiz: Why do we pass key={entry.id} instead of key={index} when rendering the list?**

⬚ A. A:Using index would make the list render in reverse order
⬚ B. Using index would make the list render in reverse order
⬚ C. B:React requires keys to be numbers
⬚ D. React requires keys to be numbers
⬚ E. C:A unique id stays the same for that entry even if the list is reordered or items are removed
⬚ F. A unique id stays the same for that entry even if the list is reordered or items are removed
⬚ G. D:entry.id is required by EntryCard
⬚ H. entry.id is required by EntryCard

