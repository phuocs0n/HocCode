Quiz: What is the main reason to show an explicit No entries yet message instead of leaving the list area blank?

Tap an option below to answer:

To improve performance when the list is empty

##### Incorrect, give it another shot!

The reason is user experience, not performance.

The reason is user experience, not performance.

So the user knows the app is working and what to do next

##### Correct Answer!

Empty space can look like an error; a message fixes this issue by giving the appropriate feedback.

Empty space can look like an error; a message fixes this issue by giving the appropriate feedback.

Because React requires something to be rendered in every branch

##### Incorrect, give it another shot!

React allows null or empty. Instead, the message is there to provide clarity for the user.

React allows null or empty. Instead, the message is there to provide clarity for the user.

To prevent errors when entries is an empty array

##### Incorrect, give it another shot!

JavaScript map returns [] on an empty array. Instead; the reason is improved user experience.

JavaScript map returns [] on an empty array. Instead; the reason is improved user experience.

## 📝 Quiz

**Quiz: What is the main reason to show an explicit No entries yet message instead of leaving the list area blank?**

⬚ A. A:To improve performance when the list is empty
⬚ B. To improve performance when the list is empty
⬚ C. B:So the user knows the app is working and what to do next
⬚ D. So the user knows the app is working and what to do next
⬚ E. C:Because React requires something to be rendered in every branch
⬚ F. Because React requires something to be rendered in every branch
⬚ G. D:To prevent errors when entries is an empty array
⬚ H. To prevent errors when entries is an empty array

