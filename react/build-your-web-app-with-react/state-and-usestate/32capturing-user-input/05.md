Quiz: In a controlled input, what is the role of the value attribute?

Tap an option below to answer:

To set the initial value only when the component first loads

##### Incorrect, give it another shot!

value is updated on every render, not just the first.

value is updated on every render, not just the first.

To show the current value from state and keep the input in sync with it

##### Correct Answer!

The input displays the current state and updates the state on change.

The input displays the current state and updates the state on change.

To validate the input before the user submits

##### Incorrect, give it another shot!

Validation is separate; value is about what is displayed.

Validation is separate; value is about what is displayed.

To prevent the user from typing more than a certain length

##### Incorrect, give it another shot!

You could do that with maxLength; value is about syncing with state.

You could do that with maxLength; value is about syncing with state.

## 📝 Quiz

**Quiz: In a controlled input, what is the role of the value attribute?**

⬚ A. A:To set the initial value only when the component first loads
⬚ B. To set the initial value only when the component first loads
⬚ C. B:To show the current value from state and keep the input in sync with it
⬚ D. To show the current value from state and keep the input in sync with it
⬚ E. C:To validate the input before the user submits
⬚ F. To validate the input before the user submits
⬚ G. D:To prevent the user from typing more than a certain length
⬚ H. To prevent the user from typing more than a certain length

