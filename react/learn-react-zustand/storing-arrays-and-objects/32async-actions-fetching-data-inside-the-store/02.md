Quiz: What is the main advantage of putting fetchEntries inside the Zustand store instead of inside a component?

Tap an option below to answer:

It makes the fetch run faster

##### Incorrect, give it another shot!

Where the fetch lives does not affect how fast it runs.

Where the fetch lives does not affect how fast it runs.

Any component can trigger the fetch and any component can read the result without prop drilling

##### Correct Answer!

Correct! When fetchEntries lives in the store, any component can call it and any component can read entries, isLoading, or error — all without passing props.

Correct! When fetchEntries lives in the store, any component can call it and any component can read entries, isLoading, or error — all without passing props.

It removes the need for async/await

##### Incorrect, give it another shot!

You still use async/await inside the store action. The store does not change how async code works.

You still use async/await inside the store action. The store does not change how async code works.

It prevents the component from re-rendering during the fetch

##### Incorrect, give it another shot!

Components still re-render when store state changes. That is how they show the loading state.

Components still re-render when store state changes. That is how they show the loading state.

## 📝 Quiz

**Quiz: What is the main advantage of putting fetchEntries inside the Zustand store instead of inside a component?**

⬚ A. A:It makes the fetch run faster
⬚ B. It makes the fetch run faster
⬚ C. B:Any component can trigger the fetch and any component can read the result without prop drilling
⬚ D. Any component can trigger the fetch and any component can read the result without prop drilling
⬚ E. C:It removes the need for async/await
⬚ F. It removes the need for async/await
⬚ G. D:It prevents the component from re-rendering during the fetch
⬚ H. It prevents the component from re-rendering during the fetch

