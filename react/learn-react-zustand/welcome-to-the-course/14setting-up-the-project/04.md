# What the Starter Files Look Like

#### Course: Learn React Zustand: The Smarter Way to Handle Data

## What the Starter Files Look Like

Go ahead and run the project.

```javascript
1import { create } from 'zustand'
2 
3const useJournalStore = create(() => ({
4  theme: 'light',
5}))
6 
7export default useJournalStore
```

```undefined
1import useJournalStore from './store/useJournalStore'
2 
3export default function App() {
4  const theme = useJournalStore((state) => state.theme)
5  return <div>My Daily Journal — Zustand is working!</div>
6}
```

```undefined
1import React from 'react'
2import { createRoot } from 'react-dom/client'
3import App from './App'
4 
5createRoot(document.getElementById('root')).render(
6  <React.StrictMode>
7    <App />
8  </React.StrictMode>
9)
```

```html
1<!DOCTYPE html>
2<html lang="en">
3  <title>My Daily Journal</title>
4  <div id="root"></div>
5  <script type="module" src="/src/index.jsx"></script>
6</html>
```

You will see the heading My Daily Journal — Zustand is working! and nothing else. No entries.

No form. No theme toggle. No search.

That is expected. The components are empty shells. The store has some starter state but the components are not connected to it yet.

Have a look through the files before moving on. Get a feel for where things live.

By the end of Chapter 2 the store will be alive and journal entries will be showing on screen.

Let us go build it. Click Next Chapter to continue.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


