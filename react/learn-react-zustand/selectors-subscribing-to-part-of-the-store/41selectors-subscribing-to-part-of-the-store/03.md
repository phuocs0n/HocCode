Quiz: What is the purpose of a selector function in Zustand?

Tap an option below to answer:

It filters entries in the database

##### Incorrect, give it another shot!

A selector does not touch a database. It picks state from the store.

A selector does not touch a database. It picks state from the store.

It picks a specific piece of state from the store and can compute derived values from it

##### Correct Answer!

Correct! A selector tells Zustand which piece of state a component needs. It can also compute derived values like filtered entries.

Correct! A selector tells Zustand which piece of state a component needs. It can also compute derived values like filtered entries.

It replaces the need for actions

##### Incorrect, give it another shot!

Selectors and actions do different things. Selectors read state. Actions update it.

Selectors and actions do different things. Selectors read state. Actions update it.

It connects two stores together

##### Incorrect, give it another shot!

Zustand does not connect stores with selectors. A selector just reads from one store.

Zustand does not connect stores with selectors. A selector just reads from one store.

## 📝 Quiz

**Quiz: What is the purpose of a selector function in Zustand?**

⬚ A. A:It filters entries in the database
⬚ B. It filters entries in the database
⬚ C. B:It picks a specific piece of state from the store and can compute derived values from it
⬚ D. It picks a specific piece of state from the store and can compute derived values from it
⬚ E. C:It replaces the need for actions
⬚ F. It replaces the need for actions
⬚ G. D:It connects two stores together
⬚ H. It connects two stores together

