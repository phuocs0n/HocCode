Quiz: Why does pagedEntries have filteredEntries in its dependency array?

Tap an option below to answer:

To ensure the page number resets to 1 when the search changes

##### Incorrect, give it another shot!

The page reset happens in handleQueryChange(). The dependency array only controls when useMemo() recomputes.

The page reset happens in handleQueryChange(). The dependency array only controls when useMemo() recomputes.

Because pagedEntries is a slice of filteredEntries; if the filter result changes, the page slice must recompute

##### Correct Answer!

pagedEntries is derived from filteredEntries, so it must be a dependency.

pagedEntries is derived from filteredEntries, so it must be a dependency.

Because useMemo() always requires the previous useMemo() result as a dependency

##### Incorrect, give it another shot!

useMemo() dependencies are whatever values the function actually uses, not an automatic chain.

useMemo() dependencies are whatever values the function actually uses, not an automatic chain.

To prevent stale entries from appearing on the current page

##### Incorrect, give it another shot!

Preventing staleness is a side effect of listing the right dependencies, not the reason filteredEntries is listed.

Preventing staleness is a side effect of listing the right dependencies, not the reason filteredEntries is listed.

## 📝 Quiz

**Quiz: Why does pagedEntries have filteredEntries in its dependency array?**

⬚ A. A:To ensure the page number resets to 1 when the search changes
⬚ B. To ensure the page number resets to 1 when the search changes
⬚ C. B:Because pagedEntries is a slice of filteredEntries; if the filter result changes, the page slice must recompute
⬚ D. Because pagedEntries is a slice of filteredEntries; if the filter result changes, the page slice must recompute
⬚ E. C:Because useMemo() always requires the previous useMemo() result as a dependency
⬚ F. Because useMemo() always requires the previous useMemo() result as a dependency
⬚ G. D:To prevent stale entries from appearing on the current page
⬚ H. To prevent stale entries from appearing on the current page

