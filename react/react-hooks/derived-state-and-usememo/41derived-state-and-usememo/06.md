Quiz: When does useMemo() recompute its value?

Tap an option below to answer:

On every render, the same as regular code in the component body

##### Incorrect, give it another shot!

That would defeat the purpose since useMemo() exists precisely to avoid recomputing on every render.

That would defeat the purpose since useMemo() exists precisely to avoid recomputing on every render.

Whenever the component's parent re-renders

##### Incorrect, give it another shot!

useMemo() depends on its own dependency array, not on the parent's render cycle.

useMemo() depends on its own dependency array, not on the parent's render cycle.

Only when one of the values in its dependency array has changed

##### Correct Answer!

React caches the result and only calls the function again when a dependency has changed.

React caches the result and only calls the function again when a dependency has changed.

Only once, when the component first mounts

##### Incorrect, give it another shot!

useMemo() reruns whenever a dependency changes — not only on mount.

useMemo() reruns whenever a dependency changes — not only on mount.

## 📝 Quiz

**Quiz: When does useMemo() recompute its value?**

⬚ A. A:On every render, the same as regular code in the component body
⬚ B. On every render, the same as regular code in the component body
⬚ C. B:Whenever the component's parent re-renders
⬚ D. Whenever the component's parent re-renders
⬚ E. C:Only when one of the values in its dependency array has changed
⬚ F. Only when one of the values in its dependency array has changed
⬚ G. D:Only once, when the component first mounts
⬚ H. Only once, when the component first mounts

