Quiz: What is the minimum requirement for a function to be a valid custom hook?

Tap an option below to answer:

It must be defined in a separate file in a hooks/ folder

##### Incorrect, give it another shot!

File location does not matter. The naming convention and internal hook call are what define a custom hook.

File location does not matter. The naming convention and internal hook call are what define a custom hook.

It must accept props as its first argument

##### Incorrect, give it another shot!

Custom hooks can take any parameters you choose — not necessarily props.

Custom hooks can take any parameters you choose — not necessarily props.

Its name must start with use and it must call at least one hook inside it

##### Correct Answer!

The use prefix is required for React to apply hook rules, and calling a hook is what makes it a hook.

The use prefix is required for React to apply hook rules, and calling a hook is what makes it a hook.

It must return an object with at least one property

##### Incorrect, give it another shot!

A custom hook can return any value — a primitive, array, object, function, or nothing at all.

A custom hook can return any value — a primitive, array, object, function, or nothing at all.

## 📝 Quiz

**Quiz: What is the minimum requirement for a function to be a valid custom hook?**

⬚ A. A:It must be defined in a separate file in a hooks/ folder
⬚ B. It must be defined in a separate file in a hooks/ folder
⬚ C. B:It must accept props as its first argument
⬚ D. It must accept props as its first argument
⬚ E. C:Its name must start with use and it must call at least one hook inside it
⬚ F. Its name must start with use and it must call at least one hook inside it
⬚ G. D:It must return an object with at least one property
⬚ H. It must return an object with at least one property

