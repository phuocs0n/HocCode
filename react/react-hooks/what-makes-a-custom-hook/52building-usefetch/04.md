Quiz: Why does useFetch() return data as the raw API response rather than mapped journal entries?

Tap an option below to answer:

Because custom hooks cannot return arrays

##### Incorrect, give it another shot!

Custom hooks can return any type, including arrays.

Custom hooks can return any type, including arrays.

Because useMemo() cannot process data returned from a custom hook

##### Incorrect, give it another shot!

useMemo() works with any value, including data returned from a custom hook.

useMemo() works with any value, including data returned from a custom hook.

Because the mapping happens inside useEffect(), not outside it

##### Incorrect, give it another shot!

The mapping is done in a useMemo() inside App, not inside useEffect().

The mapping is done in a useMemo() inside App, not inside useEffect().

Because useFetch() is generic and works with any URL

##### Correct Answer!

A generic hook should not know about journal entries. The component that calls it is responsible for mapping the data to whatever shape it needs.

A generic hook should not know about journal entries. The component that calls it is responsible for mapping the data to whatever shape it needs.

## 📝 Quiz

**Quiz: Why does useFetch() return data as the raw API response rather than mapped journal entries?**

⬚ A. A:Because custom hooks cannot return arrays
⬚ B. Because custom hooks cannot return arrays
⬚ C. B:Because useMemo() cannot process data returned from a custom hook
⬚ D. Because useMemo() cannot process data returned from a custom hook
⬚ E. C:Because the mapping happens inside useEffect(), not outside it
⬚ F. Because the mapping happens inside useEffect(), not outside it
⬚ G. D:Because useFetch() is generic and works with any URL
⬚ H. Because useFetch() is generic and works with any URL

