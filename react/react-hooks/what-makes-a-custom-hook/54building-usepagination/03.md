# Replace in App.jsx

#### Course: React Hooks: Level Up Your First Web App

## Replace in App.jsx

This is the final form of App.jsx, i.e., the complete composed version using all three custom hooks.

```undefined
1const { currentPage, pagedEntries, totalPages, handleNext, handlePrev, reset } = usePagination(
2  filteredEntries,
3  ENTRIES_PER_PAGE
4);
5
6function handleQueryChange(newQuery) {
7  setQuery(newQuery);
8  reset();
9}
10
11
12// The above codes replace the codes below
13
14
15// const [currentPage, setCurrentPage] = useState(1);
16
17// const totalPages = useMemo(function () {
18//   return Math.ceil(filteredEntries.length / ENTRIES_PER_PAGE);
19// }, [filteredEntries]);
20
21// const pagedEntries = useMemo(function () {
22//   const start = (currentPage - 1) * ENTRIES_PER_PAGE;
23//   return filteredEntries.slice(start, start + ENTRIES_PER_PAGE);
24// }, [filteredEntries, currentPage]);
25
26// function handleNext() {
27//   setCurrentPage(function (p) { return Math.min(p + 1, totalPages); });
28// }
29
30// function handlePrev() {
31//   setCurrentPage(function (p) { return Math.max(p - 1, 1); });
32// }
```

```undefined
1import { useRef, useEffect } from "react";
2
3function Header({ query, onQueryChange }) {
4  const searchRef = useRef(null);
5  useEffect(function () {
6    searchRef.current.focus();
7  }, []);
8
9  function handleClear() {
10    onQueryChange("");
11    searchRef.current.focus();
12  }
13
14  return (
15    <header className="header">
16      <h2 className="title-primary">My Daily Journal</h2>
17      <p className="subtitle">Capture your thoughts, one day at a time.</p>
18
19      <input
20        ref={searchRef}
21        className="search-input"
22        type="text"
23        placeholder="Search entries..."
24        value={query}
25        onChange={(e) => onQueryChange(e.target.value)}
26      />
27      {query && (
28        <button className="btn-clear" onClick={handleClear}>Clear</button>
29      )}
30
31    </header>
32  );
33}
34
35export default Header;
```

```undefined
1import { useRef, useEffect } from "react";
2
3function Drawer({ entry, onClose }) {
4  const drawerRef = useRef(null);
5
6  useEffect(function () {
7    function handleMouseDown(event) {
8      if (drawerRef.current && !drawerRef.current.contains(event.target)) {
9        onClose();
10      }
11    }
12
13    document.addEventListener("mousedown", handleMouseDown);
14
15    return function () {
16      document.removeEventListener("mousedown", handleMouseDown);
17    };
18  }, [onClose]);
19
20  if (!entry) return null;
21
22  return (
23    <div className="drawer-overlay">
24      <div className="drawer" ref={drawerRef}>
25        <button className="drawer-close" onClick={onClose}>×</button>
26        <h2>{entry.title}</h2>
27        <p className="drawer-meta">{entry.date} · {entry.author}</p>
28        <p className="drawer-content">{entry.content}</p>
29      </div>
30    </div>
31  );
32}
33
34export default Drawer;
```

```undefined
1function EntryCard({ id, title, date, author, content, onSelect }) {
2  return (
3    <div className="entry-card" onClick={function () { onSelect({ id, title, date, author, content }); }}>
4      <p className="entry-date">{date}</p>
5      <h2 className="entry-title">{title}</h2>
6      <p className="entry-author">by {author}</p>
7      <p className="entry-content">{content}</p>
8    </div>
9  );
10}
11
12export default EntryCard;
```

```javascript
1import { useState, useEffect } from "react";
2
3function useFetch(url) {
4  const [data, setData] = useState(null);
5  const [isLoading, setIsLoading] = useState(true);
6  const [error, setError] = useState(null);
7
8  useEffect(function () {
9    async function fetchData() {
10      setIsLoading(true);
11      setError(null);
12
13      try {
14        const res = await fetch(url);
15        if (!res.ok) {
16          throw new Error("Could not load data. Status: " + res.status);
17        }
18        const json = await res.json();
19        setData(json);
20        setIsLoading(false);
21      }
22
23      catch (err) {
24        setError(err.message);
25        setIsLoading(false);
26      }
27    }
28
29    fetchData();
30  }, [url]);
31
32  return { data, isLoading, error };
33}
34
35export default useFetch;
```

```javascript
1import { useState, useMemo } from "react";
2
3function useSearch(entries) {
4  const [query, setQuery] = useState("");
5
6  const filteredEntries = useMemo(function () {
7    if (!query.trim()) return entries;
8    const lower = query.toLowerCase();
9
10    return entries.filter(function (entry) {
11      return (
12        entry.title.toLowerCase().includes(lower) ||
13        entry.content.toLowerCase().includes(lower)
14      );
15    });
16  }, [entries, query]);
17
18  return { query, setQuery, filteredEntries };
19}
20
21export default useSearch;
```

```javascript
1import { useState, useMemo } from "react";
2
3function usePagination(entries, pageSize) {
4  const [currentPage, setCurrentPage] = useState(1);
5
6  const totalPages = useMemo(function () {
7    return Math.ceil(entries.length / pageSize);
8  }, [entries, pageSize]);
9
10  const pagedEntries = useMemo(function () {
11    const start = (currentPage - 1) * pageSize;
12    return entries.slice(start, start + pageSize);
13  }, [entries, currentPage, pageSize]);
14
15  function handleNext() {
16    setPage(function (p) { return Math.min(p + 1, totalPages); });
17  }
18
19  function handlePrev() {
20    setPage(function (p) { return Math.max(p - 1, 1); });
21  }
22
23  function reset() {
24    setPage(1);
25  }
26
27  return { currentPage, pagedEntries, totalPages, handleNext, handlePrev, reset };
28}
29
30export default usePagination;
```

```css
1/* Daily Journal App — React Hooks edition css goes here */
```

Click Run Code. The app behaves exactly as before, even though a lot has changed.

Next, we'll list out the major changes you can see in App.jsx.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


