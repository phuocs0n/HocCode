# Implementing the Changes

#### Course: React Hooks: Level Up Your First Web App

## Implementing the Changes

Here's the full code for the changes we've made to our app.

You can view the changes we've made to App.jsx and EntryCard.jsx through the comments in the code.

You'll also find that JournalForm.jsx has been deleted:

```undefined
1import { useState, useEffect } from "react";
2
3import Header from "./components/Header";
4import EntryCard from "./components/EntryCard";
5
6// Removed Header import from here
7
8function App() {
9  const [entries, setEntries] = useState([]);
10
11  // Implementing useEffect() with named async
12  useEffect(function () {
13    async function loadEntries() {...}
14    loadEntries();
15  }, []);
16
17  return (
18    <div className="main-container">
19      <Header />
20
21      {/* Removed <JournalForm /> from here */}
22
23      <section className="entries-section">
24        {/* Code for entries section */}
25      </section>
26    </div>
27  );
28}
29
30export default App;
```

```undefined
1function Header() {
2
3  return (
4    <header className="header">
5      <h2 className="title-primary">My Daily Journal</h2>
6      <p className="subtitle">Capture your thoughts, one day at a time.</p>
7    </header>
8  );
9}
10
11export default Header;
```

```undefined
1function EntryCard({ id, title, date, author, content }) {
2
3  // Removed states like isEditing, draftTitle, etc
4
5  // Also removed handlers for edit, save, cancel
6
7  // Finally, removed if (isEditing) condition
8
9  return (
10    <div className="entry-card">
11      <p className="entry-date">{date}</p>
12      <h2 className="entry-title">{title}</h2>
13      <p className="entry-author">by {author}</p>
14      <p className="entry-content">{content}</p>
15    </div>
16  );
17}
18
19export default EntryCard;
```

```css
1/* Daily Journal App — React Hooks edition css goes here */
```

Click Run Code. After a brief pause, ten entries should appear in the list.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


