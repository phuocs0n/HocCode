# Update App.jsx

#### Course: React Hooks: Level Up Your First Web App

## Update App.jsx

Now, our App component gains

- A refreshKey state
- A Refresh button in the entry list header
- [refreshKey] in the dependency array

The error display also expands from a single paragraph to a two-line banner.

```undefined
1function App() {
2  ... ... ...
3
4  // refreshKey state
5  const [refreshKey, setRefreshKey] = useState(0);
6
7  // refreshKey is in the dependency array
8  useEffect(function () {
9    // Effect code
10  }, [refreshKey]);
11
12  return (
13
14          ... ... ...
15
16          // Refresh button
17          <button
18            className="btn btn-secondary"
19            onClick={() => setRefreshKey((k) => k + 1)}
20          >
21            Refresh
22          </button>
23
24        ... ... ...
25
26  );
27}
```

```undefined
1function Header() {
2  return (
3    <header className="header">
4      <h2 className="title-primary">My Daily Journal</h2>
5      <p className="subtitle">Capture your thoughts, one day at a time.</p>
6    </header>
7  );
8}
9
10export default Header;
```

```undefined
1function EntryCard({ id, title, date, author, content }) {
2  return (
3    <div className="entry-card">
4      <p className="entry-date">{date}</p>
5      <h2 className="entry-title">{title}</h2>
6      <p className="entry-author">by {author}</p>
7      <p className="entry-content">{content}</p>
8    </div>
9  );
10}
11
12export default EntryCard;
```

```css
1/* Daily Journal App — React Hooks edition css goes here */
```

Click Run Code to run the app. Click Refresh to trigger a new fetch. The list reloads.

To test the error state: temporarily change the fetch URL to something invalid, then click Refresh. You should see the error banner.

Change the URL back when done.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


