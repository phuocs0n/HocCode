# A Final Prerequisite

#### Course: React Hooks: Level Up Your First Web App

## A Final Prerequisite

Finally, you'll also need a basic understanding of async/await, since that's how this course fetches data from an API.

If this code looks readable to you, you are ready:

```react
async function loadData() {
  try {
    const res = await fetch("https://api.example.com/entries");
    const data = await res.json();
    console.log(data);
  }
  catch (err) {
    console.error(err);
  }
}
```

If it does not, spend a short time with JavaScript async / await before continuing — it'll make the first chapter much easier.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


