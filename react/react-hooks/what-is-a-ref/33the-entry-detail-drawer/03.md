# Update App.jsx

#### Course: React Hooks: Level Up Your First Web App

## Update App.jsx

Let's continue with our changes:

4. Import 'Drawer' and render it in the JSX.

```react
import Drawer from "./components/Drawer";

... ... ...

// Inside App()
<Drawer
  entry={selectedEntry}
  onClose={() => setSelectedEntry(null)}
/>
```

5. Reset the UI in 'loadEntries()'.

Let's go back to the effect and ensure the UI is reset before each load:

```react
useEffect(function () {
    async function loadEntries() {

      // Reset the UI
      setIsLoading(true);
      setError(null);
      ... ... ...
  }, [refreshKey]);
```

Here,

- setIsLoading(true) ensures the loading indicator appears again when the user clicks Refresh.
- setError(null) clears any previous error banner so it doesn't linger through a successful reload.

Next, let's combine this all in a non-executable code snippet so you can get a better picture of where the changes were made.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


