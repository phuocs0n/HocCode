# A Snippet of the Full Changes

#### Course: React Hooks: Level Up Your First Web App

## A Snippet of the Full Changes

Here's a snippet of App.jsx with all the changes we just made:

```react
import Drawer from "./components/Drawer";

... ... ...

function App() {
  ... ... ...

  // Create a selectedEntry state
  const [selectedEntry, setSelectedEntry] = useState(null);

  useEffect(function () {
    async function loadEntries() {

      // Reset the UI
      setIsLoading(true);
      setError(null);

      ... ... ...

    }
    loadEntries();
  }, [refreshKey]);


  // Function to set the state
  function handleSelectEntry(entry) {
    setSelectedEntry(entry);
  }

  return (
    <div className="main-container">
      <Header />
      <section className="entries-section">
        ... ... ...

        {isLoading ? (...)
        : error ? (...)
        : entries.length === 0 ? (...)
        : (
          entries.map((entry) => (
            <EntryCard
              ... ... ...

              {/* Pass onSelect to each entry card */}
              onSelect={handleSelectEntry}
            />
          ))
        )}
      </section>

      {/* Render the drawer */}
      <Drawer
        entry={selectedEntry}
        onClose={() => setSelectedEntry(null)}
      />
    </div>
  );
}

export default App;
```

Next, let's discuss how these changes work.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


