Quiz: Why do we update the cache directly in onSuccess instead of calling invalidateQueries?

Tap an option below to answer:

Because invalidateQueries only works with useQuery not useMutation

##### Incorrect, give it another shot!

invalidateQueries works fine with mutations. That's its most common use case.

invalidateQueries works fine with mutations. That's its most common use case.

Because DummyJSON doesn't actually persist changes so refetching returns the original data

##### Correct Answer!

Correct. DummyJSON pretends to accept changes but doesn't save them. Refetching would undo everything.

Correct. DummyJSON pretends to accept changes but doesn't save them. Refetching would undo everything.

Because setQueryData is always faster than invalidateQueries

##### Incorrect, give it another shot!

Speed isn't the reason here. It's about what data DummyJSON actually returns.

Speed isn't the reason here. It's about what data DummyJSON actually returns.

Because onSuccess doesn't support invalidateQueries

##### Incorrect, give it another shot!

onSuccess supports both. You can call invalidateQueries inside onSuccess just fine.

onSuccess supports both. You can call invalidateQueries inside onSuccess just fine.

## 📝 Quiz

**Quiz: Why do we update the cache directly in onSuccess instead of calling invalidateQueries?**

⬚ A. A:Because invalidateQueries only works with useQuery not useMutation
⬚ B. Because invalidateQueries only works with useQuery not useMutation
⬚ C. B:Because DummyJSON doesn't actually persist changes so refetching returns the original data
⬚ D. Because DummyJSON doesn't actually persist changes so refetching returns the original data
⬚ E. C:Because setQueryData is always faster than invalidateQueries
⬚ F. Because setQueryData is always faster than invalidateQueries
⬚ G. D:Because onSuccess doesn't support invalidateQueries
⬚ H. Because onSuccess doesn't support invalidateQueries

