Quiz: What is the difference between useQuery and useMutation?

Tap an option below to answer:

useQuery runs automatically on load, useMutation only runs when you call .mutate()

##### Correct Answer!

Correct. useQuery fires the moment the component mounts. useMutation sits and waits until you trigger it.

Correct. useQuery fires the moment the component mounts. useMutation sits and waits until you trigger it.

useQuery is for reading data and useMutation is for reading data faster

##### Incorrect, give it another shot!

useMutation is not about speed. It's specifically for sending changes to the server.

useMutation is not about speed. It's specifically for sending changes to the server.

useMutation caches data and useQuery doesn't

##### Incorrect, give it another shot!

It's actually the other way around. useQuery caches. useMutation doesn't.

It's actually the other way around. useQuery caches. useMutation doesn't.

useQuery handles errors and useMutation doesn't

##### Incorrect, give it another shot!

Both handle errors. useMutation has onError just like useQuery.

Both handle errors. useMutation has onError just like useQuery.

## 📝 Quiz

**Quiz: What is the difference between useQuery and useMutation?**

⬚ A. A:useQuery runs automatically on load, useMutation only runs when you call .mutate()
⬚ B. useQuery runs automatically on load, useMutation only runs when you call .mutate()
⬚ C. B:useQuery is for reading data and useMutation is for reading data faster
⬚ D. useQuery is for reading data and useMutation is for reading data faster
⬚ E. C:useMutation caches data and useQuery doesn't
⬚ F. useMutation caches data and useQuery doesn't
⬚ G. D:useQuery handles errors and useMutation doesn't
⬚ H. useQuery handles errors and useMutation doesn't

