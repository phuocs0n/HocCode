Quiz: What does staleTime: 0 mean?

Tap an option below to answer:

Data is cached forever and never refetches

##### Incorrect, give it another shot!

staleTime: 0 is the opposite — data goes stale immediately.

staleTime: 0 is the opposite — data goes stale immediately.

Queries are disabled and won't run

##### Incorrect, give it another shot!

staleTime has nothing to do with disabling queries. Use enabled: false for that.

staleTime has nothing to do with disabling queries. Use enabled: false for that.

Data goes stale instantly and React Query refetches aggressively

##### Correct Answer!

Correct. With staleTime: 0 data is considered stale the moment it arrives so React Query refetches whenever it gets a chance.

Correct. With staleTime: 0 data is considered stale the moment it arrives so React Query refetches whenever it gets a chance.

Data never refetches after the first load

##### Incorrect, give it another shot!

staleTime: 0 actually causes more refetching not less.

staleTime: 0 actually causes more refetching not less.

## 📝 Quiz

**Quiz: What does staleTime: 0 mean?**

⬚ A. A:Data is cached forever and never refetches
⬚ B. Data is cached forever and never refetches
⬚ C. B:Queries are disabled and won't run
⬚ D. Queries are disabled and won't run
⬚ E. C:Data goes stale instantly and React Query refetches aggressively
⬚ F. Data goes stale instantly and React Query refetches aggressively
⬚ G. D:Data never refetches after the first load
⬚ H. Data never refetches after the first load

