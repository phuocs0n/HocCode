Quiz: Which value from useQuery tells you the fetch is still in progress?

Tap an option below to answer:

isError

##### Incorrect, give it another shot!

isError tells you something went wrong, not that it's still loading.

isError tells you something went wrong, not that it's still loading.

isFetching

##### Incorrect, give it another shot!

Close, but isFetching covers background refetches too. isLoading is specifically for the very first fetch.

Close, but isFetching covers background refetches too. isLoading is specifically for the very first fetch.

isLoading

##### Correct Answer!

isLoading is true while queryFn is still running for the first time.

isLoading is true while queryFn is still running for the first time.

isPending

##### Incorrect, give it another shot!

isPending is not a value returned by useQuery.

isPending is not a value returned by useQuery.

##### Incorrect, give it another shot!

isError tells you something went wrong, not that it's still loading.

isError tells you something went wrong, not that it's still loading.

##### Incorrect, give it another shot!

Close, but isFetching covers background refetches too. isLoading is specifically for the very first fetch.

Close, but isFetching covers background refetches too. isLoading is specifically for the very first fetch.

##### Correct Answer!

isLoading is true while queryFn is still running for the first time.

isLoading is true while queryFn is still running for the first time.

##### Incorrect, give it another shot!

isPending is not a value returned by useQuery.

isPending is not a value returned by useQuery.

## 📝 Quiz

**Quiz: Which value from useQuery tells you the fetch is still in progress?**

⬚ A. A:isError
⬚ B. isError
⬚ C. B:isFetching
⬚ D. isFetching
⬚ E. C:isLoading
⬚ F. isLoading
⬚ G. D:isPending
⬚ H. isPending

