Quiz: A component uses queryKey: ['tasks'] but fetches tasks filtered by status. The user changes the filter. What happens?

Tap an option below to answer:

React Query refetches automatically because the filter changed

##### Incorrect, give it another shot!

React Query only knows to refetch when the key changes. The key is still ['tasks'] here.

React Query only knows to refetch when the key changes. The key is still ['tasks'] here.

React Query returns the old cached data because the key didn't change

##### Correct Answer!

The key never changed so React Query serves whatever is sitting in the cache.

The key never changed so React Query serves whatever is sitting in the cache.

React Query throws an error about a mismatched key

##### Incorrect, give it another shot!

No error is thrown. The wrong data just shows up silently, which is the real danger.

No error is thrown. The wrong data just shows up silently, which is the real danger.

The component re-renders but isLoading stays false

##### Incorrect, give it another shot!

The component may re-render but it will show the wrong data, not trigger a new fetch.

The component may re-render but it will show the wrong data, not trigger a new fetch.

## 📝 Quiz

**Quiz: A component uses queryKey: ['tasks'] but fetches tasks filtered by status. The user changes the filter. What happens?**

⬚ A. A:React Query refetches automatically because the filter changed
⬚ B. React Query refetches automatically because the filter changed
⬚ C. B:React Query returns the old cached data because the key didn't change
⬚ D. React Query returns the old cached data because the key didn't change
⬚ E. C:React Query throws an error about a mismatched key
⬚ F. React Query throws an error about a mismatched key
⬚ G. D:The component re-renders but isLoading stays false
⬚ H. The component re-renders but isLoading stays false

