Quiz: Why do we use a query key factory instead of writing keys directly in each component?

Tap an option below to answer:

Because React Query requires it for mutations to work

##### Incorrect, give it another shot!

React Query doesn't require a factory. It's a pattern you choose to adopt.

React Query doesn't require a factory. It's a pattern you choose to adopt.

Because it keeps all keys in one place so typos and mismatches can't silently break your cache

##### Correct Answer!

Correct. One source of truth means if a key changes you fix it once and everything stays in sync.

Correct. One source of truth means if a key changes you fix it once and everything stays in sync.

Because query keys only work when they're imported from a separate file

##### Incorrect, give it another shot!

Keys work fine written inline. The factory is about maintainability not functionality.

Keys work fine written inline. The factory is about maintainability not functionality.

Because the factory automatically invalidates queries when data changes

##### Incorrect, give it another shot!

The factory just generates keys. Invalidation is still your job.

The factory just generates keys. Invalidation is still your job.

## 📝 Quiz

**Quiz: Why do we use a query key factory instead of writing keys directly in each component?**

⬚ A. A:Because React Query requires it for mutations to work
⬚ B. Because React Query requires it for mutations to work
⬚ C. B:Because it keeps all keys in one place so typos and mismatches can't silently break your cache
⬚ D. Because it keeps all keys in one place so typos and mismatches can't silently break your cache
⬚ E. C:Because query keys only work when they're imported from a separate file
⬚ F. Because query keys only work when they're imported from a separate file
⬚ G. D:Because the factory automatically invalidates queries when data changes
⬚ H. Because the factory automatically invalidates queries when data changes

