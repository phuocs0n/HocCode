Quiz: What happens when enabled is set to false on a useQuery call?

Tap an option below to answer:

The query runs once and then stops

##### Incorrect, give it another shot!

The query doesn't run at all when enabled is false.

The query doesn't run at all when enabled is false.

The query throws an error

##### Incorrect, give it another shot!

No error is thrown. The query just sits idle.

No error is thrown. The query just sits idle.

The query doesn't run until enabled becomes true

##### Correct Answer!

Correct. React Query watches the enabled value and fires the query the moment it becomes true.

Correct. React Query watches the enabled value and fires the query the moment it becomes true.

The query runs but ignores the result

##### Incorrect, give it another shot!

The query doesn't run at all. It's completely paused.

The query doesn't run at all. It's completely paused.

## 📝 Quiz

**Quiz: What happens when enabled is set to false on a useQuery call?**

⬚ A. A:The query runs once and then stops
⬚ B. The query runs once and then stops
⬚ C. B:The query throws an error
⬚ D. The query throws an error
⬚ E. C:The query doesn't run until enabled becomes true
⬚ F. The query doesn't run until enabled becomes true
⬚ G. D:The query runs but ignores the result
⬚ H. The query runs but ignores the result

