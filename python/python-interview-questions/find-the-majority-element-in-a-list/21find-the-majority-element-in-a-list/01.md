# Thought Process

#### Course: Python Interview Questions

## Thought Process

As mentioned before, the majority element occurs more than n / 2 times in a list of size n.

So, if we want to find the majority element, we first need to find the number of times each element occurs in the list.

Python provides a built-in method count() to count the number of occurrences of the specified element.

```python
list.count(element)
```

Now, let's see how we can solve this problem.

1. Loop through each element of the list using a for loop.
2. In each iteration of the loop, we will use the count() function to count the number of occurrences of every element.
3. We will then use an if...else statement to check if the value of count() for each element is greater than n // 2 (where n is the size of our list).
4. If the condition is true, we will return the element as the majority element.

Next, use the same process to solve the question.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


