# Step 2: Add New Student Data from User Input

#### Course: Python Numpy for Data Science

## Step 2: Add New Student Data from User Input

So far, we worked on the dataset we created. Next, we will add new student records from user input.

For this step, we'll use the concept of indexing and np.append().

First we will read the name of the student.

```python
studentName = input('Student Name : ')
```

Next, we'll read the marks. First, we initialize an empty marks array for new students.

```python
marks = np.empty(len(subjectArray), dtype=int)
```

Now we take user input for each subject.

```python
for i in range(len(subjectArray)):
        marks[i] = int(input(f"Enter marks for {subjectArray[i]}: "))
```

Finally, we append student to student array and marks to marks array.

```python
studentArray = np.append(studentArray, studentName)
marksArray = np.vstack((marksArray, marks))
```

Putting it all together,

```python
# function that takes user input for new record
def add_record(studentArray, subjectArray, marksArray):
    # get student's name
    studentName = input('Student Name : ')
    # initialize an empty marks array of same length as subject
    marks = np.empty(len(subjectArray), dtype=int)
    
    # get inputs from the user
    for i in range(len(subjectArray)):
        marks[i] = int(input(f"Enter marks for {subjectArray[i]}: "))
    # append new student to studentArray
    studentArray = np.append(studentArray, studentName)
    # append marks of new student to studentArray
    marksArray = np.vstack((marksArray, marks))
    
    return studentArray, marksArray
```

Now let's see the whole code.

```python
1import numpy as np
2
3# function to initialize and store data
4def initialize_data():
5    subject = np.array(['English', 'Maths', 'Science', 'Social', 'History'])
6    students = np.array(['Alice', 'Bob', 'Charlie', 'Dan'])
7    marks = np.array([[81, 78, 80, 60, 63],
8                      [50, 60, 65, 48, 55],
9                      [30, 83, 97, 79, 75],
10                      [99, 55, 60, 48, 30]])
11    return subject, students, marks
12
13# function that takes user input for new record
14def add_record(studentArray, subjectArray, marksArray):
15
16    # get student's name
17    studentName = input('Student Name : ')
18
19    # initialize an empty marks array of same length as subject
20    marks = np.empty(len(subjectArray), dtype=int)
21    
22    # get inputs from the user
23    for i in range(len(subjectArray)):
24        marks[i] = int(input(f"Enter marks for {subjectArray[i]}: "))
25
26    # append new student to studentArray
27    studentArray = np.append(studentArray, studentName)
28
29    # append marks of new student to studentArray
30    marksArray = np.vstack((marksArray, marks))
31    
32    return studentArray, marksArray
33    
34def main():
35    subject, students, marks = initialize_data()
36    students, marks = add_record(students, subject, marks)
37    print(subject)
38    print(students)
39    print(marks)
40
41main()
```

Sample Output

```text
Student Name : Eve
Enter marks for English: 60
Enter marks for Maths: 48
Enter marks for Science: 50
Enter marks for Social: 77
Enter marks for History: 80
['English' 'Maths' 'Science' 'Social' 'History']
['Alice' 'Bob' 'Charlie' 'Dan' 'Eve']
[[81 78 80 60 63]
 [50 60 65 48 55]
 [30 83 97 79 75]
 [99 55 60 48 30]
 [60 48 50 77 80]]
```

Next, we'll display these data in a visually pleasing format.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


