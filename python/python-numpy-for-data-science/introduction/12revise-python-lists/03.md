Quiz: What will be the output of the following code?

```python
even_numbers = [2, 4, 6, 8]
print(even_numbers[2])
```

Tap an option below to answer:

##### Incorrect, give it another shot!

even_numbers[2] gives the third item, not the first item.

even_numbers[2] gives the third item, not the first item.

##### Incorrect, give it another shot!

Index starts from 0, not 1. Hence, even_numbers[2] gives the third item, not the second item.

Index starts from 0, not 1. Hence, even_numbers[2] gives the third item, not the second item.

##### Correct Answer!

even_numbers[2] gives the third item because the list index starts from 0.

even_numbers[2] gives the third item because the list index starts from 0.

##### Incorrect, give it another shot!

even_numbers[2] gives the third item, not the fourth item.

even_numbers[2] gives the third item, not the fourth item.

##### Incorrect, give it another shot!

even_numbers[2] gives the third item, not the first item.

even_numbers[2] gives the third item, not the first item.

##### Incorrect, give it another shot!

Index starts from 0, not 1. Hence, even_numbers[2] gives the third item, not the second item.

Index starts from 0, not 1. Hence, even_numbers[2] gives the third item, not the second item.

##### Correct Answer!

even_numbers[2] gives the third item because the list index starts from 0.

even_numbers[2] gives the third item because the list index starts from 0.

##### Incorrect, give it another shot!

even_numbers[2] gives the third item, not the fourth item.

even_numbers[2] gives the third item, not the fourth item.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:2
⬚ B. 2
⬚ C. B:4
⬚ D. 4
⬚ E. C:6
⬚ F. 6
⬚ G. D:8
⬚ H. 8

