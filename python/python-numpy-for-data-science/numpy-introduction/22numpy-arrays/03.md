Quiz: What is the correct syntax for creating a NumPy array from a Python list?

Tap an option below to answer:

np.array()

##### Incorrect, give it another shot!

You can create a NumPy array by passing the list variable to the np.array() method.However, this is not a dedicated syntax to create a NumPy array.

You can create a NumPy array by passing the list variable to the np.array() method.

However, this is not a dedicated syntax to create a NumPy array.

np.array([])

##### Correct Answer!

The list elements go inside [].

The list elements go inside [].

np.create_array()

##### Incorrect, give it another shot!

There is no such function called create_array() in NumPy.

There is no such function called create_array() in NumPy.

np.create_array([])

##### Incorrect, give it another shot!

There is no such function called create_array() in NumPy.

There is no such function called create_array() in NumPy.

##### Incorrect, give it another shot!

You can create a NumPy array by passing the list variable to the np.array() method.However, this is not a dedicated syntax to create a NumPy array.

You can create a NumPy array by passing the list variable to the np.array() method.

However, this is not a dedicated syntax to create a NumPy array.

##### Correct Answer!

The list elements go inside [].

The list elements go inside [].

##### Incorrect, give it another shot!

There is no such function called create_array() in NumPy.

There is no such function called create_array() in NumPy.

##### Incorrect, give it another shot!

There is no such function called create_array() in NumPy.

There is no such function called create_array() in NumPy.

## 📝 Quiz

**Quiz: What is the correct syntax for creating a NumPy array from a Python list?**

⬚ A. A:np.array()
⬚ B. np.array()
⬚ C. B:np.array([])
⬚ D. np.array([])
⬚ E. C:np.create_array()
⬚ F. np.create_array()
⬚ G. D:np.create_array([])
⬚ H. np.create_array([])

