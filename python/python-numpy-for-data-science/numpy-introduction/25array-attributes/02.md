Quiz: What is the shape of a 1D NumPy array with 20 elements?

Tap an option below to answer:

(20, 0)

##### Incorrect, give it another shot!

This shape indicates a 2D array with 20 rows and 0 columns, which is not applicable for a 1D array.

This shape indicates a 2D array with 20 rows and 0 columns, which is not applicable for a 1D array.

(1, 21)

##### Incorrect, give it another shot!

This shape indicates a 2D array with 1 row and 21 columns, which is not applicable for a 1D array.

This shape indicates a 2D array with 1 row and 21 columns, which is not applicable for a 1D array.

(20, )

##### Correct Answer!

A 1D NumPy array with 20 elements has a shape of (20,).

A 1D NumPy array with 20 elements has a shape of (20,).

(19, )

##### Incorrect, give it another shot!

This shape indicates a 1D array with 19 elements.

This shape indicates a 1D array with 19 elements.

##### Incorrect, give it another shot!

This shape indicates a 2D array with 20 rows and 0 columns, which is not applicable for a 1D array.

This shape indicates a 2D array with 20 rows and 0 columns, which is not applicable for a 1D array.

##### Incorrect, give it another shot!

This shape indicates a 2D array with 1 row and 21 columns, which is not applicable for a 1D array.

This shape indicates a 2D array with 1 row and 21 columns, which is not applicable for a 1D array.

##### Correct Answer!

A 1D NumPy array with 20 elements has a shape of (20,).

A 1D NumPy array with 20 elements has a shape of (20,).

##### Incorrect, give it another shot!

This shape indicates a 1D array with 19 elements.

This shape indicates a 1D array with 19 elements.

## 📝 Quiz

**Quiz: What is the shape of a 1D NumPy array with 20 elements?**

⬚ A. A:(20, 0)
⬚ B. (20, 0)
⬚ C. B:(1, 21)
⬚ D. (1, 21)
⬚ E. C:(20, )
⬚ F. (20, )
⬚ G. D:(19, )
⬚ H. (19, )

