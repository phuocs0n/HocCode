Quiz: Which of the following best describes a universal function (ufunc) in NumPy?

Tap an option below to answer:

Works with arrays of any size and shape

##### Incorrect, give it another shot!

ufuncs can work with arrays of any size and shape, but this option doesn't best describe ufunc.

ufuncs can work with arrays of any size and shape, but this option doesn't best describe ufunc.

Performs element-wise operations on arrays

##### Correct Answer!

ufunc applies function to each element of the array individually.

ufunc applies function to each element of the array individually.

Converts arrays into lists

##### Incorrect, give it another shot!

ufunc does not convert arrays into lists.

ufunc does not convert arrays into lists.

Only works with one-dimensional arrays

##### Incorrect, give it another shot!

ufunc can work with arrays of any dimensionality, not just one-dimensional arrays.

ufunc can work with arrays of any dimensionality, not just one-dimensional arrays.

## 📝 Quiz

**Quiz: Which of the following best describes a universal function (ufunc) in NumPy?**

⬚ A. A:Works with arrays of any size and shape
⬚ B. Works with arrays of any size and shape
⬚ C. B:Performs element-wise operations on arrays
⬚ D. Performs element-wise operations on arrays
⬚ E. C:Converts arrays into lists
⬚ F. Converts arrays into lists
⬚ G. D:Only works with one-dimensional arrays
⬚ H. Only works with one-dimensional arrays

