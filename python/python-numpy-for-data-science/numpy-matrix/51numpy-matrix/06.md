Quiz: Which methods are used for the following matrix operations?

- dot product between two matrices
- transpose of a matrix
- create identity matrix

Tap an option below to answer:

np.dot()

np.transpose()

np.identity()

##### Incorrect, give it another shot!

Remember the methods to create an identity matrix.

Remember the methods to create an identity matrix.

np.dot()

np.transpose()

np.eye()

##### Incorrect, give it another shot!

Remember the methods to create an identity matrix.

Remember the methods to create an identity matrix.

np.transpose()

np.identity()

np.dot()

##### Incorrect, give it another shot!

np.transpose() doesn't find the dot product, identity() doesn't find the transpose and so on.

np.transpose() doesn't find the dot product, identity() doesn't find the transpose and so on.

Both A and B

##### Correct Answer!

np.dot() and np.transpose() finds the dot product and transpose of the matrix. To create an identity matrix, we can use both np.identity() and np.eye().

np.dot() and np.transpose() finds the dot product and transpose of the matrix. To create an identity matrix, we can use both np.identity() and np.eye().

## 📝 Quiz

**Quiz: Which methods are used for the following matrix operations?**

⬚ A. dot product between two matrices
⬚ B. transpose of a matrix
⬚ C. create identity matrix
⬚ D. A:np.dot()np.transpose()np.identity()
⬚ E. np.dot()np.transpose()np.identity()
⬚ F. B:np.dot()np.transpose()np.eye()
⬚ G. np.dot()np.transpose()np.eye()
⬚ H. C:np.transpose()np.identity()np.dot()
⬚ I. np.transpose()np.identity()np.dot()
⬚ J. D:Both A and B
⬚ K. Both A and B

