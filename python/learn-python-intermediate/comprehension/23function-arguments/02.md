Quiz: What will be the output of the following code?

```python
def subtract(first, second):
    result = first - second
    return result

result = subtract(second = 20, first = 10)
print(result)
```

Tap an option below to answer:

10

##### Incorrect, give it another shot!

In the function definition, first will be 10 and second will be 20. It's because we are passing arguments based on names.

In the function definition, first will be 10 and second will be 20. It's because we are passing arguments based on names.

-10

##### Correct Answer!

In the function definition, first will be 10 and second will be 20. It's because we are passing arguments based on names.

In the function definition, first will be 10 and second will be 20. It's because we are passing arguments based on names.

30

##### Incorrect, give it another shot!

We are not adding arguments inside the function definition.

We are not adding arguments inside the function definition.

-100

##### Incorrect, give it another shot!

We are not multiplying arguments inside the function definition.

We are not multiplying arguments inside the function definition.

##### Incorrect, give it another shot!

In the function definition, first will be 10 and second will be 20. It's because we are passing arguments based on names.

In the function definition, first will be 10 and second will be 20. It's because we are passing arguments based on names.

##### Correct Answer!

In the function definition, first will be 10 and second will be 20. It's because we are passing arguments based on names.

In the function definition, first will be 10 and second will be 20. It's because we are passing arguments based on names.

##### Incorrect, give it another shot!

We are not adding arguments inside the function definition.

We are not adding arguments inside the function definition.

##### Incorrect, give it another shot!

We are not multiplying arguments inside the function definition.

We are not multiplying arguments inside the function definition.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:10
⬚ B. 10
⬚ C. B:-10
⬚ D. -10
⬚ E. C:30
⬚ F. 30
⬚ G. D:-100
⬚ H. -100

