Quiz: Suppose a Dog class is derived from an Animal class. Then, which of the following statements is correct?

Tap an option below to answer:

The Animal class inherits all the attributes and methods of the Dog class.

##### Incorrect, give it another shot!

Derived classes (child classes) inherit the methods and attributes of the base class (parent class), not the other way around.

Derived classes (child classes) inherit the methods and attributes of the base class (parent class), not the other way around.

Both classes inherit each other's attributes and methods.

##### Incorrect, give it another shot!

Only the derived class inherits the base class's methods and attributes.

Only the derived class inherits the base class's methods and attributes.

The Dog class inherits all the attributes and methods of the Animal class.

##### Correct Answer!

Derived classes (child classes) inherit the methods and attributes of the base class (parent class).

Derived classes (child classes) inherit the methods and attributes of the base class (parent class).

None of the above.

##### Incorrect, give it another shot!

One of the answers is correct.

One of the answers is correct.

## 📝 Quiz

**Quiz: Suppose a Dog class is derived from an Animal class. Then, which of the following statements is correct?**

⬚ A. A:The Animal class inherits all the attributes and methods of the Dog class.
⬚ B. The Animal class inherits all the attributes and methods of the Dog class.
⬚ C. B:Both classes inherit each other's attributes and methods.
⬚ D. Both classes inherit each other's attributes and methods.
⬚ E. C:The Dog class inherits all the attributes and methods of the Animal class.
⬚ F. The Dog class inherits all the attributes and methods of the Animal class.
⬚ G. D:None of the above.
⬚ H. None of the above.

