Quiz: Suppose we have a code like this.

```python
class Test:
    def print_hello(self):
        print('Hello')

test1 = Test()
```

How can we call the print_hello() method using the test1 object?

Tap an option below to answer:

test1.print_hello('Hello')

##### Incorrect, give it another shot!

The self argument in the print_hello() method takes the value of the test1 object. Since we are calling the print_hello() method with one argument, it cannot be assigned to anything. Hence, this code results in an error.

The self argument in the print_hello() method takes the value of the test1 object. Since we are calling the print_hello() method with one argument, it cannot be assigned to anything. Hence, this code results in an error.

print_hello()

##### Incorrect, give it another shot!

We need to specify which object is calling the print_hello() method. Hence, this code results in an error.

We need to specify which object is calling the print_hello() method. Hence, this code results in an error.

test1.print_hello()

##### Correct Answer!

Here, we are calling the printHello() method using the test1 object. The self argument in the printHello() method takes the value of the test1 object.

Here, we are calling the printHello() method using the test1 object. The self argument in the printHello() method takes the value of the test1 object.

test1.print_text()

##### Incorrect, give it another shot!

The Test class doesn't have a method named print_text().

The Test class doesn't have a method named print_text().

##### Incorrect, give it another shot!

The self argument in the print_hello() method takes the value of the test1 object. Since we are calling the print_hello() method with one argument, it cannot be assigned to anything. Hence, this code results in an error.

The self argument in the print_hello() method takes the value of the test1 object. Since we are calling the print_hello() method with one argument, it cannot be assigned to anything. Hence, this code results in an error.

##### Incorrect, give it another shot!

We need to specify which object is calling the print_hello() method. Hence, this code results in an error.

We need to specify which object is calling the print_hello() method. Hence, this code results in an error.

##### Correct Answer!

Here, we are calling the printHello() method using the test1 object. The self argument in the printHello() method takes the value of the test1 object.

Here, we are calling the printHello() method using the test1 object. The self argument in the printHello() method takes the value of the test1 object.

##### Incorrect, give it another shot!

The Test class doesn't have a method named print_text().

The Test class doesn't have a method named print_text().

## 📝 Quiz

**Quiz: Suppose we have a code like this.**

⬚ A. A:test1.print_hello('Hello')
⬚ B. test1.print_hello('Hello')
⬚ C. B:print_hello()
⬚ D. print_hello()
⬚ E. C:test1.print_hello()
⬚ F. test1.print_hello()
⬚ G. D:test1.print_text()
⬚ H. test1.print_text()

