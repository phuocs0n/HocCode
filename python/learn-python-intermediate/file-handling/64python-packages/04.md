Quiz: Suppose we have an empty directory named Test. What should we do to make this directory a package?

Tap an option below to answer:

By adding a file named main.py inside the Test directory.

##### Incorrect, give it another shot!

Creating a file named main.py won't make a package.

Creating a file named main.py won't make a package.

By adding a file named main.py and creating a class with __init__() method inside it.

##### Incorrect, give it another shot!

Creating a file named main.py won't make a package.

Creating a file named main.py won't make a package.

By adding a file named __init__.py.

##### Correct Answer!

Adding a file named __init__.py will tell Python that this directory is a package.

Adding a file named __init__.py will tell Python that this directory is a package.

We do not need to do anything.

##### Incorrect, give it another shot!

If a directory has a file with a specific name, only then that directory is considered a package.

If a directory has a file with a specific name, only then that directory is considered a package.

## 📝 Quiz

**Quiz: Suppose we have an empty directory named Test. What should we do to make this directory a package?**

⬚ A. A:By adding a file named main.py inside the Test directory.
⬚ B. By adding a file named main.py inside the Test directory.
⬚ C. B:By adding a file named main.py and creating a class with __init__() method inside it.
⬚ D. By adding a file named main.py and creating a class with __init__() method inside it.
⬚ E. C:By adding a file named __init__.py.
⬚ F. By adding a file named __init__.py.
⬚ G. D:We do not need to do anything.
⬚ H. We do not need to do anything.

