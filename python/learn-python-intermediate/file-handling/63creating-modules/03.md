Quiz: Suppose we have a file named person.py and inside this file, a function named get_name() has been defined. How can we call this function from a different file?

Tap an option below to answer:

import person

get_name()

##### Incorrect, give it another shot!

We need to define the module name while calling the get_name() function. Otherwise this code tries to call the get_name() function of the current file.

We need to define the module name while calling the get_name() function. Otherwise this code tries to call the get_name() function of the current file.

person.get_name()

##### Incorrect, give it another shot!

We need to import the person module first.

We need to import the person module first.

import person

person.get_name()

##### Correct Answer!

We have imported the person module. Then, the get_name() function of the person module is called.

We have imported the person module. Then, the get_name() function of the person module is called.

import person

get_name().person

##### Incorrect, give it another shot!

This code results in an error.

This code results in an error.

## 📝 Quiz

**Quiz: Suppose we have a file named person.py and inside this file, a function named get_name() has been defined. How can we call this function from a different file?**

⬚ A. A:import personget_name()
⬚ B. import personget_name()
⬚ C. B:person.get_name()
⬚ D. person.get_name()
⬚ E. C:import personperson.get_name()
⬚ F. import personperson.get_name()
⬚ G. D:import personget_name().person
⬚ H. import personget_name().person

