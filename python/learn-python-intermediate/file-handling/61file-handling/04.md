Quiz: Assuming the hello.txt file exists, what does the following code do?

```python
f = open('hello.txt', 'r')

content = f.read()
print(content)

f.close()
```

Tap an option below to answer:

Prints the first line of the line.

##### Incorrect, give it another shot!

f.read() reads the whole content of the file, not just the first line.

f.read() reads the whole content of the file, not just the first line.

Prints the whole content of the file.

##### Correct Answer!

f.read() reads the whole content of the file which is assigned to the content variable.

f.read() reads the whole content of the file which is assigned to the content variable.

Prints the first 10 characters of the file.

##### Incorrect, give it another shot!

f.read() reads the whole content of the file, not just the first 10 characters.

f.read() reads the whole content of the file, not just the first 10 characters.

Creates a new line.

##### Incorrect, give it another shot!

We have opened the hello.txt in the read mode. The read mode doesn't create a new file.

We have opened the hello.txt in the read mode. The read mode doesn't create a new file.

## 📝 Quiz

**Quiz: Assuming the hello.txt file exists, what does the following code do?**

⬚ A. A:Prints the first line of the line.
⬚ B. Prints the first line of the line.
⬚ C. B:Prints the whole content of the file.
⬚ D. Prints the whole content of the file.
⬚ E. C:Prints the first 10 characters of the file.
⬚ F. Prints the first 10 characters of the file.
⬚ G. D:Creates a new line.
⬚ H. Creates a new line.

