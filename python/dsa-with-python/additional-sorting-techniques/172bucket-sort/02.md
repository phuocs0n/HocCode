Quiz: Given an array of elements ranging from 1 to 20, what should be the approximate range of each bucket?

Tap an option below to answer:

0-6, 6-12, 12-20

##### Correct Answer!

Since we are dividing the elements into three approximately equal ranges, these would be the suitable ranges for the buckets.

Since we are dividing the elements into three approximately equal ranges, these would be the suitable ranges for the buckets.

0-5, 5-15, 15-20

##### Incorrect, give it another shot!

The distribution needs to be uniform for each bucket. This choice leads to uneven distribution.

The distribution needs to be uniform for each bucket. This choice leads to uneven distribution.

1-10, 10-20, 20-30

##### Incorrect, give it another shot!

The values in the range 20-30 are greater than the maximum value present in the list, hence it's not a correct range.

The values in the range 20-30 are greater than the maximum value present in the list, hence it's not a correct range.

1-20, 20-40, 40-60

##### Incorrect, give it another shot!

Range of buckets depends on the range of input values. If we create three buckets like this all of our input values will fall in the first bucket and the rest of the buckets will be empty. This undermines the efficiency of Bucket sort.

Range of buckets depends on the range of input values. If we create three buckets like this all of our input values will fall in the first bucket and the rest of the buckets will be empty.

This undermines the efficiency of Bucket sort.

##### Correct Answer!

Since we are dividing the elements into three approximately equal ranges, these would be the suitable ranges for the buckets.

Since we are dividing the elements into three approximately equal ranges, these would be the suitable ranges for the buckets.

##### Incorrect, give it another shot!

The distribution needs to be uniform for each bucket. This choice leads to uneven distribution.

The distribution needs to be uniform for each bucket. This choice leads to uneven distribution.

##### Incorrect, give it another shot!

The values in the range 20-30 are greater than the maximum value present in the list, hence it's not a correct range.

The values in the range 20-30 are greater than the maximum value present in the list, hence it's not a correct range.

##### Incorrect, give it another shot!

Range of buckets depends on the range of input values. If we create three buckets like this all of our input values will fall in the first bucket and the rest of the buckets will be empty. This undermines the efficiency of Bucket sort.

Range of buckets depends on the range of input values. If we create three buckets like this all of our input values will fall in the first bucket and the rest of the buckets will be empty.

This undermines the efficiency of Bucket sort.

## 📝 Quiz

**Quiz: Given an array of elements ranging from 1 to 20, what should be the approximate range of each bucket?**

⬚ A. A:0-6, 6-12, 12-20
⬚ B. 0-6, 6-12, 12-20
⬚ C. B:0-5, 5-15, 15-20
⬚ D. 0-5, 5-15, 15-20
⬚ E. C:1-10, 10-20, 20-30
⬚ F. 1-10, 10-20, 20-30
⬚ G. D:1-20, 20-40, 40-60
⬚ H. 1-20, 20-40, 40-60

