Quiz: What is a stack?

Tap an option below to answer:

An algorithm for sorting data.

##### Incorrect, give it another shot!

Stack is a data structure; it's not an algorithm to sort data.

Stack is a data structure; it's not an algorithm to sort data.

A linear data structure that follows the first-in-first-out (FIFO) principle.

##### Incorrect, give it another shot!

In a stack, the first element added is the last to be removed (not the first to be removed).

In a stack, the first element added is the last to be removed (not the first to be removed).

A linear data structure that follows the last-in-first-out (LIFO) principle.

##### Correct Answer!

In a stack, the last element added is the first to be removed.

In a stack, the last element added is the first to be removed.

A physical storage device used in computer systems.

##### Incorrect, give it another shot!

Stacks are not physical devices; they are abstract concepts for data management in computer science.

Stacks are not physical devices; they are abstract concepts for data management in computer science.

## 📝 Quiz

**Quiz: What is a stack?**

⬚ A. A:An algorithm for sorting data.
⬚ B. An algorithm for sorting data.
⬚ C. B:A linear data structure that follows the first-in-first-out (FIFO) principle.
⬚ D. A linear data structure that follows the first-in-first-out (FIFO) principle.
⬚ E. C:A linear data structure that follows the last-in-first-out (LIFO) principle.
⬚ F. A linear data structure that follows the last-in-first-out (LIFO) principle.
⬚ G. D:A physical storage device used in computer systems.
⬚ H. A physical storage device used in computer systems.

