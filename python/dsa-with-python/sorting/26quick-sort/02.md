Quiz: If we use quick sort to arrange elements in ascending order, elements that are less than the pivot are moved to the ___ sublist.

Tap an option below to answer:

left

##### Correct Answer!

Elements less than the pivot are moved to the left sublist during the partitioning step.

Elements less than the pivot are moved to the left sublist during the partitioning step.

right

##### Incorrect, give it another shot!

Elements greater than the pivot are moved to the right sublist during the partitioning step.

Elements greater than the pivot are moved to the right sublist during the partitioning step.

center

##### Incorrect, give it another shot!

Quick sort does not use a center sublist during its sorting process.

Quick sort does not use a center sublist during its sorting process.

bottom

##### Incorrect, give it another shot!

Quick sort does not use a bottom sublist during its sorting process.

Quick sort does not use a bottom sublist during its sorting process.

##### Correct Answer!

Elements less than the pivot are moved to the left sublist during the partitioning step.

Elements less than the pivot are moved to the left sublist during the partitioning step.

##### Incorrect, give it another shot!

Elements greater than the pivot are moved to the right sublist during the partitioning step.

Elements greater than the pivot are moved to the right sublist during the partitioning step.

##### Incorrect, give it another shot!

Quick sort does not use a center sublist during its sorting process.

Quick sort does not use a center sublist during its sorting process.

##### Incorrect, give it another shot!

Quick sort does not use a bottom sublist during its sorting process.

Quick sort does not use a bottom sublist during its sorting process.

## 📝 Quiz

**Quiz: If we use quick sort to arrange elements in ascending order, elements that are less than the pivot are moved to the ___ sublist.**

⬚ A. A:left
⬚ B. left
⬚ C. B:right
⬚ D. right
⬚ E. C:center
⬚ F. center
⬚ G. D:bottom
⬚ H. bottom

