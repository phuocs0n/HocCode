Quiz: Suppose we are sorting elements in ascending order using selection sort. After sorting the smallest element, what is the next action in selection sort?

Tap an option below to answer:

Re-sort the entire list.

##### Incorrect, give it another shot!

This is inefficient and not how selection sort works.

This is inefficient and not how selection sort works.

Find the second-smallest element and swap it with the element at the first position.

##### Incorrect, give it another shot!

The second-smallest element should be swapped with the element in the second position.

The second-smallest element should be swapped with the element in the second position.

Find the smallest element and swap it with the element at the second position.

##### Incorrect, give it another shot!

After placing the smallest element in the first position, it's unnecessary to find that smallest element again to place in the second position for sorting.

After placing the smallest element in the first position, it's unnecessary to find that smallest element again to place in the second position for sorting.

Find the second-smallest element and swap it with the element at the second position.

##### Correct Answer!

The second-smallest element should be swapped with the element in the second position.

The second-smallest element should be swapped with the element in the second position.

## 📝 Quiz

**Quiz: Suppose we are sorting elements in ascending order using selection sort. After sorting the smallest element, what is the next action in selection sort?**

⬚ A. A:Re-sort the entire list.
⬚ B. Re-sort the entire list.
⬚ C. B:Find the second-smallest element and swap it with the element at the first position.
⬚ D. Find the second-smallest element and swap it with the element at the first position.
⬚ E. C:Find the smallest element and swap it with the element at the second position.
⬚ F. Find the smallest element and swap it with the element at the second position.
⬚ G. D:Find the second-smallest element and swap it with the element at the second position.
⬚ H. Find the second-smallest element and swap it with the element at the second position.

