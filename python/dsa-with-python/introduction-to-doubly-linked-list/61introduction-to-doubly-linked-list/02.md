Quiz: What is a doubly linked list?

Tap an option below to answer:

A data structure where each node contains a reference to the next node.

##### Incorrect, give it another shot!

This describes a singly linked list where each node has a reference to the next node.

This describes a singly linked list where each node has a reference to the next node.

A data structure where each node contains a reference to the previous node.

##### Incorrect, give it another shot!

This describes a characteristic of a doubly linked list, but it is not a complete description.

This describes a characteristic of a doubly linked list, but it is not a complete description.

A data structure where each node contains both a reference to the next node and the previous node.

##### Correct Answer!

This accurately describes a doubly linked list with references to both the next and previous nodes.

This accurately describes a doubly linked list with references to both the next and previous nodes.

A data structure where each node contains only the data value.

##### Incorrect, give it another shot!

This describes a simple data value, not a linked list structure.

This describes a simple data value, not a linked list structure.

## 📝 Quiz

**Quiz: What is a doubly linked list?**

⬚ A. A:A data structure where each node contains a reference to the next node.
⬚ B. A data structure where each node contains a reference to the next node.
⬚ C. B:A data structure where each node contains a reference to the previous node.
⬚ D. A data structure where each node contains a reference to the previous node.
⬚ E. C:A data structure where each node contains both a reference to the next node and the previous node.
⬚ F. A data structure where each node contains both a reference to the next node and the previous node.
⬚ G. D:A data structure where each node contains only the data value.
⬚ H. A data structure where each node contains only the data value.

