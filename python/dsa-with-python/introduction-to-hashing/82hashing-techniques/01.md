Quiz: Suppose we have a hash function, H(x) = x mod 12.

Considering x is a non-negative integer, what will be the range of hash values we get from this function?

Tap an option below to answer:

1 to 12

##### Incorrect, give it another shot!

Since we are performing mod 12, the output can contain 0.

Since we are performing mod 12, the output can contain 0.

0 to 12

##### Incorrect, give it another shot!

Since we are performing mod 12, the output cannot contain 12.

Since we are performing mod 12, the output cannot contain 12.

0 to 11

##### Correct Answer!

Since we are performing mod 12, the output can contain the values from 0-11.

Since we are performing mod 12, the output can contain the values from 0-11.

0 to 10

##### Incorrect, give it another shot!

Since we are performing mod 12, the output can contain 11.

Since we are performing mod 12, the output can contain 11.

##### Incorrect, give it another shot!

Since we are performing mod 12, the output can contain 0.

Since we are performing mod 12, the output can contain 0.

##### Incorrect, give it another shot!

Since we are performing mod 12, the output cannot contain 12.

Since we are performing mod 12, the output cannot contain 12.

##### Correct Answer!

Since we are performing mod 12, the output can contain the values from 0-11.

Since we are performing mod 12, the output can contain the values from 0-11.

##### Incorrect, give it another shot!

Since we are performing mod 12, the output can contain 11.

Since we are performing mod 12, the output can contain 11.

## 📝 Quiz

**Quiz: Suppose we have a hash function, H(x) = x mod 12.**

⬚ A. A:1 to 12
⬚ B. 1 to 12
⬚ C. B:0 to 12
⬚ D. 0 to 12
⬚ E. C:0 to 11
⬚ F. 0 to 11
⬚ G. D:0 to 10
⬚ H. 0 to 10

