Quiz: Which of these options best describes a linked list?

Tap an option below to answer:

A fixed-size list where each element is directly accessible.

##### Incorrect, give it another shot!

Linked lists do not have a fixed size and do not allow direct access to elements.

Linked lists do not have a fixed size and do not allow direct access to elements.

A dynamic data structure that grows and shrinks as needed.

##### Incorrect, give it another shot!

This is a general description that could apply to several data structures, including built-in data structures such as lists and dictionaries.

This is a general description that could apply to several data structures, including built-in data structures such as lists and dictionaries.

A series of connected nodes, where each node stores data and the address of the next node.

##### Correct Answer!

This is the most accurate description of a linked list.

This is the most accurate description of a linked list.

A collection of data elements where the order is based on their values.

##### Incorrect, give it another shot!

This sounds more like a list, not necessarily a linked list.

This sounds more like a list, not necessarily a linked list.

## 📝 Quiz

**Quiz: Which of these options best describes a linked list?**

⬚ A. A:A fixed-size list where each element is directly accessible.
⬚ B. A fixed-size list where each element is directly accessible.
⬚ C. B:A dynamic data structure that grows and shrinks as needed.
⬚ D. A dynamic data structure that grows and shrinks as needed.
⬚ E. C:A series of connected nodes, where each node stores data and the address of the next node.
⬚ F. A series of connected nodes, where each node stores data and the address of the next node.
⬚ G. D:A collection of data elements where the order is based on their values.
⬚ H. A collection of data elements where the order is based on their values.

