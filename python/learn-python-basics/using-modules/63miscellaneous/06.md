Quiz: What will be the output of the following code?

```python
n = 10
if n == 10 and 100:
    print("Hello")
```

Tap an option below to answer:

Hello

##### Correct Answer!

Here, n == 10 and 100 evaluates to True because both n == 10 and 100 evaluate to True (non-zero numbers always evaluate to True). Thus, the print("Hello") statement is executed.

Here, n == 10 and 100 evaluates to True because both n == 10 and 100 evaluate to True (non-zero numbers always evaluate to True). Thus, the print("Hello") statement is executed.

(no output)

##### Incorrect, give it another shot!

Here, n == 10 and 100 evaluates to True because both n == 10 and 100 evaluate to True (non-zero numbers always evaluate to True). Hence, the body of the if statement is executed.

Here, n == 10 and 100 evaluates to True because both n == 10 and 100 evaluate to True (non-zero numbers always evaluate to True). Hence, the body of the if statement is executed.

(error code)

##### Incorrect, give it another shot!

The program is valid and will not cause any errors.

The program is valid and will not cause any errors.

Hello

Hello

##### Incorrect, give it another shot!

We have printed "Hello" only once if the test condition is True.

We have printed "Hello" only once if the test condition is True.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:Hello
⬚ B. Hello
⬚ C. B:(no output)
⬚ D. (no output)
⬚ E. C:(error code)
⬚ F. (error code)
⬚ G. D:HelloHello
⬚ H. HelloHello

