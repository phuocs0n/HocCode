Quiz: What will be the output of the following code?

```python
for i in range(2):
    for i in range(1):
        print("*")
```

Tap an option below to answer:

```text
*
*
```

##### Correct Answer!

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

```text
*
```

##### Incorrect, give it another shot!

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

```text
*
*
*
*
```

##### Incorrect, give it another shot!

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

(No output)

##### Incorrect, give it another shot!

This program produces a visible output.

This program produces a visible output.

##### Correct Answer!

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

##### Incorrect, give it another shot!

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

##### Incorrect, give it another shot!

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

The outer loop iterates two times from i equals 0 to 1. In each iteration of the outer loop, the inner loop iterates once and prints *.

##### Incorrect, give it another shot!

This program produces a visible output.

This program produces a visible output.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:*
*
⬚ B. *
*
⬚ C. B:*
⬚ D. *
⬚ E. C:*
*
*
*
⬚ F. *
*
*
*
⬚ G. D:(No output)
⬚ H. (No output)

