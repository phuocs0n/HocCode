Quiz: What happens when you import the same module twice?

```python
import math
import math

result = math.sqrt(9)
print(result)
```

Tap an option below to answer:

An error occurs because math was already imported.

##### Incorrect, give it another shot!

Python allows a module to be imported multiple times. However, the module is only loaded once during execution, so importing it again has no effect.

Python allows a module to be imported multiple times.

However, the module is only loaded once during execution, so importing it again has no effect.

The math module will be loaded twice, which could cause unexpected behavior.

##### Incorrect, give it another shot!

Python caches imported modules to avoid reloading them each time they are imported. Therefore, the second import has no effect.

Python caches imported modules to avoid reloading them each time they are imported. Therefore, the second import has no effect.

The program will work as expected and print 3.0.

##### Correct Answer!

The program will work as expected. The second import of math does not cause any issues, and math.sqrt(9) will correctly return 3.0.

The program will work as expected. The second import of math does not cause any issues, and math.sqrt(9) will correctly return 3.0.

The program will run indefinitely in a loop.

##### Incorrect, give it another shot!

Importing the same module twice will not cause the program to loop.

Importing the same module twice will not cause the program to loop.

## 📝 Quiz

**Quiz: What happens when you import the same module twice?**

⬚ A. A:An error occurs because math was already imported.
⬚ B. An error occurs because math was already imported.
⬚ C. B:The math module will be loaded twice, which could cause unexpected behavior.
⬚ D. The math module will be loaded twice, which could cause unexpected behavior.
⬚ E. C:The program will work as expected and print 3.0.
⬚ F. The program will work as expected and print 3.0.
⬚ G. D:The program will run indefinitely in a loop.
⬚ H. The program will run indefinitely in a loop.

