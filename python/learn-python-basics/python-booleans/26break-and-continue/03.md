Quiz: How many times is the print statement executed in the following program?

```python
for i in range(1, 11):
    print("Hello")

    if i == 5:
        break
```

Tap an option below to answer:

10 times

##### Incorrect, give it another shot!

The loop does not complete all 10 iterations. The loop ends due to the break statement when i is 5.

The loop does not complete all 10 iterations. The loop ends due to the break statement when i is 5.

5 times

##### Correct Answer!

The print statement executes five times from i = 1 to i = 5. Only then the break statement is encountered, which ends the loop.

The print statement executes five times from i = 1 to i = 5. Only then the break statement is encountered, which ends the loop.

4 times

##### Incorrect, give it another shot!

The print statement executes five times from i = 1 to 5. Only then the break statement is encountered, which ends the loop.

The print statement executes five times from i = 1 to 5. Only then the break statement is encountered, which ends the loop.

(error)

##### Incorrect, give it another shot!

The program is valid and will not cause any errors.

The program is valid and will not cause any errors.

##### Incorrect, give it another shot!

The loop does not complete all 10 iterations. The loop ends due to the break statement when i is 5.

The loop does not complete all 10 iterations. The loop ends due to the break statement when i is 5.

##### Correct Answer!

The print statement executes five times from i = 1 to i = 5. Only then the break statement is encountered, which ends the loop.

The print statement executes five times from i = 1 to i = 5. Only then the break statement is encountered, which ends the loop.

##### Incorrect, give it another shot!

The print statement executes five times from i = 1 to 5. Only then the break statement is encountered, which ends the loop.

The print statement executes five times from i = 1 to 5. Only then the break statement is encountered, which ends the loop.

##### Incorrect, give it another shot!

The program is valid and will not cause any errors.

The program is valid and will not cause any errors.

## 📝 Quiz

**Quiz: How many times is the print statement executed in the following program?**

⬚ A. A:10 times
⬚ B. 10 times
⬚ C. B:5 times
⬚ D. 5 times
⬚ E. C:4 times
⬚ F. 4 times
⬚ G. D:(error)
⬚ H. (error)

