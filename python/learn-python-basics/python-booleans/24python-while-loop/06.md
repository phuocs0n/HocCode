Quiz: How many times is print() executed in the following program?

```python
number = 1

while number <= 10:
    print(f"Number: {number}")
    number = number + 2
```

Tap an option below to answer:

##### Incorrect, give it another shot!

The loop doesn't execute only once. It continues until the condition number <= 10 evaluates to False.

The loop doesn't execute only once. It continues until the condition number <= 10 evaluates to False.

10

##### Incorrect, give it another shot!

The loop does not execute 10 times because we have incremented the value of number by 2 in each iteration, not 1.

The loop does not execute 10 times because we have incremented the value of number by 2 in each iteration, not 1.

Infinite times

##### Incorrect, give it another shot!

The loop does not run infinitely. It terminates when the value of number exceeds 10.

The loop does not run infinitely. It terminates when the value of number exceeds 10.

##### Correct Answer!

The loop runs five times.The value of number is 1 in the first iteration, 3 in the second, 5 in the third, 7 in the fourth, and 9 in the fifth.When number becomes 11, number <= 10 evaluates to False and the loop terminates.

The loop runs five times.

The value of number is 1 in the first iteration, 3 in the second, 5 in the third, 7 in the fourth, and 9 in the fifth.When number becomes 11, number <= 10 evaluates to False and the loop terminates.

##### Incorrect, give it another shot!

The loop doesn't execute only once. It continues until the condition number <= 10 evaluates to False.

The loop doesn't execute only once. It continues until the condition number <= 10 evaluates to False.

##### Incorrect, give it another shot!

The loop does not execute 10 times because we have incremented the value of number by 2 in each iteration, not 1.

The loop does not execute 10 times because we have incremented the value of number by 2 in each iteration, not 1.

##### Incorrect, give it another shot!

The loop does not run infinitely. It terminates when the value of number exceeds 10.

The loop does not run infinitely. It terminates when the value of number exceeds 10.

##### Correct Answer!

The loop runs five times.The value of number is 1 in the first iteration, 3 in the second, 5 in the third, 7 in the fourth, and 9 in the fifth.When number becomes 11, number <= 10 evaluates to False and the loop terminates.

The loop runs five times.

The value of number is 1 in the first iteration, 3 in the second, 5 in the third, 7 in the fourth, and 9 in the fifth.When number becomes 11, number <= 10 evaluates to False and the loop terminates.

## 📝 Quiz

**Quiz: How many times is print() executed in the following program?**

⬚ A. A:1
⬚ B. 1
⬚ C. B:10
⬚ D. 10
⬚ E. C:Infinite times
⬚ F. Infinite times
⬚ G. D:5
⬚ H. 5

