Quiz: Which of the following is a valid way to write an if statement.?

Tap an option below to answer:

```python
age = 16

if age < 18
    print("You are less than 18 years old")
```

##### Incorrect, give it another shot!

This code is not valid since it's missing a colon : after the boolean expression.

This code is not valid since it's missing a colon : after the boolean expression.

```python
age = 16

if age < 18:
    print("You are less than 18 years old")
```

##### Correct Answer!

The if statement is valid because it has the proper colon : after the condition. The indented print() statement correctly belongs to the if block and will execute when the condition is True.

The if statement is valid because it has the proper colon : after the condition. The indented print() statement correctly belongs to the if block and will execute when the condition is True.

```python
age = 16

if:
    print("You are less than 18 years old")
```

##### Incorrect, give it another shot!

This code is not valid since it's missing a boolean expression after if.

This code is not valid since it's missing a boolean expression after if.

```python
age = 16

if age < 18:
print("You are less than 18 years old")
```

##### Incorrect, give it another shot!

This code is not valid since it's missing proper indentation after the colon :. The indentation indicates which statements are part of the if block.

This code is not valid since it's missing proper indentation after the colon :. The indentation indicates which statements are part of the if block.

## 📝 Quiz

**Quiz: Which of the following is a valid way to write an if statement.?**

⬚ A. A:age = 16

if age < 18
    print("You are less than 18 years old")
⬚ B. age = 16

if age < 18
    print("You are less than 18 years old")
⬚ C. B:age = 16

if age < 18:
    print("You are less than 18 years old")
⬚ D. age = 16

if age < 18:
    print("You are less than 18 years old")
⬚ E. C:age = 16

if:
    print("You are less than 18 years old")
⬚ F. age = 16

if:
    print("You are less than 18 years old")
⬚ G. D:age = 16

if age < 18:
print("You are less than 18 years old")
⬚ H. age = 16

if age < 18:
print("You are less than 18 years old")

