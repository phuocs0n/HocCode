Quiz: Which of the following variables is assigned a boolean value in this Python code?

```python
var1 = "True"
var2 = false
```

Tap an option below to answer:

Both variables are assigned boolean values.

##### Incorrect, give it another shot!

var1 holds a string because "True" is in quotes. var2 holds an undefined value because the initial in false has not been capitalized. Therefore, it is not a Python boolean.

var1 holds a string because "True" is in quotes.

var2 holds an undefined value because the initial in false has not been capitalized. Therefore, it is not a Python boolean.

var2

##### Incorrect, give it another shot!

var2 doesn't store a boolean value because the initials of the boolean values should be capitalized.In other words, True and False are boolean values but true and false are not.

var2 doesn't store a boolean value because the initials of the boolean values should be capitalized.

In other words, True and False are boolean values but true and false are not.

var1

##### Incorrect, give it another shot!

var1 doesn't store a boolean value because boolean values aren't enclosed within quotation marks. So, "True" is actually a string.

var1 doesn't store a boolean value because boolean values aren't enclosed within quotation marks. So, "True" is actually a string.

None of the variables are assigned a boolean value.

##### Correct Answer!

var1 contains a string since "True" is in quotes. var2 is assigned false, which is not a boolean in Python because its initial is not capitalized. So, it's treated as an undefined variable.

var1 contains a string since "True" is in quotes.

var2 is assigned false, which is not a boolean in Python because its initial is not capitalized. So, it's treated as an undefined variable.

## 📝 Quiz

**Quiz: Which of the following variables is assigned a boolean value in this Python code?**

⬚ A. A:Both variables are assigned boolean values.
⬚ B. Both variables are assigned boolean values.
⬚ C. B:var2
⬚ D. var2
⬚ E. C:var1
⬚ F. var1
⬚ G. D:None of the variables are assigned a boolean value.
⬚ H. None of the variables are assigned a boolean value.

