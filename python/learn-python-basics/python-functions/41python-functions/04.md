Quiz: Which of the following statements is correct?

Tap an option below to answer:

Unless we call a function, the code inside its function definition will not be executed.

##### Correct Answer!

We need to call a function using the function name in order to execute the code defined inside it.

We need to call a function using the function name in order to execute the code defined inside it.

We can call a function only once.

##### Incorrect, give it another shot!

We can invoke (call) a function as many times as we want.

We can invoke (call) a function as many times as we want.

The program terminates once a function is called and executed.

##### Incorrect, give it another shot!

When a function is called, the program's control jumps to the function's definition.Once all the statements are executed, the control of the program returns to where the function was called. Then, the program continues executing the next statement outside the function.

When a function is called, the program's control jumps to the function's definition.

Once all the statements are executed, the control of the program returns to where the function was called. Then, the program continues executing the next statement outside the function.

We can define a function using the function keyword.

##### Incorrect, give it another shot!

We use the def keyword to define a function.

We use the def keyword to define a function.

## 📝 Quiz

**Quiz: Which of the following statements is correct?**

⬚ A. A:Unless we call a function, the code inside its function definition will not be executed.
⬚ B. Unless we call a function, the code inside its function definition will not be executed.
⬚ C. B:We can call a function only once.
⬚ D. We can call a function only once.
⬚ E. C:The program terminates once a function is called and executed.
⬚ F. The program terminates once a function is called and executed.
⬚ G. D:We can define a function using the function keyword.
⬚ H. We can define a function using the function keyword.

