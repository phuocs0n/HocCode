Quiz: How many items does this set have?

```python
numbers = {1, 1, 2, 1}
```

Tap an option below to answer:

four

##### Incorrect, give it another shot!

A set cannot contain duplicate items. Hence, the duplicate items of the set must be counted as a single item.

A set cannot contain duplicate items. Hence, the duplicate items of the set must be counted as a single item.

three

##### Incorrect, give it another shot!

This set doesn't have three items.

This set doesn't have three items.

two

##### Correct Answer!

A set cannot contain duplicate items. Since there are only two unique items in this set, it thus contains only two items.

A set cannot contain duplicate items. Since there are only two unique items in this set, it thus contains only two items.

five

##### Incorrect, give it another shot!

This set doesn't have five items.

This set doesn't have five items.

##### Incorrect, give it another shot!

A set cannot contain duplicate items. Hence, the duplicate items of the set must be counted as a single item.

A set cannot contain duplicate items. Hence, the duplicate items of the set must be counted as a single item.

##### Incorrect, give it another shot!

This set doesn't have three items.

This set doesn't have three items.

##### Correct Answer!

A set cannot contain duplicate items. Since there are only two unique items in this set, it thus contains only two items.

A set cannot contain duplicate items. Since there are only two unique items in this set, it thus contains only two items.

##### Incorrect, give it another shot!

This set doesn't have five items.

This set doesn't have five items.

## 📝 Quiz

**Quiz: How many items does this set have?**

⬚ A. A:four
⬚ B. four
⬚ C. B:three
⬚ D. three
⬚ E. C:two
⬚ F. two
⬚ G. D:five
⬚ H. five

