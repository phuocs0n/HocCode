Quiz: What will be the output of the following code?

```python
numbers = (1, 2, 3)
numbers = list(numbers)

print(numbers)
```

Tap an option below to answer:

(1, 2, 3)

##### Incorrect, give it another shot!

In the program, the (1, 2, 3) tuple is converted to a list.

In the program, the (1, 2, 3) tuple is converted to a list.

{1, 2, 3}

##### Incorrect, give it another shot!

In the program, the (1, 2, 3) tuple is not converted to a set. Instead, it's converted to a list.

In the program, the (1, 2, 3) tuple is not converted to a set. Instead, it's converted to a list.

[1, 2, 3]

##### Correct Answer!

In the program, the (1, 2, 3) tuple is converted to a list.

In the program, the (1, 2, 3) tuple is converted to a list.

(Error)

##### Incorrect, give it another shot!

The program is valid and will not cause any errors.

The program is valid and will not cause any errors.

##### Incorrect, give it another shot!

In the program, the (1, 2, 3) tuple is converted to a list.

In the program, the (1, 2, 3) tuple is converted to a list.

##### Incorrect, give it another shot!

In the program, the (1, 2, 3) tuple is not converted to a set. Instead, it's converted to a list.

In the program, the (1, 2, 3) tuple is not converted to a set. Instead, it's converted to a list.

##### Correct Answer!

In the program, the (1, 2, 3) tuple is converted to a list.

In the program, the (1, 2, 3) tuple is converted to a list.

##### Incorrect, give it another shot!

The program is valid and will not cause any errors.

The program is valid and will not cause any errors.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:(1, 2, 3)
⬚ B. (1, 2, 3)
⬚ C. B:{1, 2, 3}
⬚ D. {1, 2, 3}
⬚ E. C:[1, 2, 3]
⬚ F. [1, 2, 3]
⬚ G. D:(Error)
⬚ H. (Error)

