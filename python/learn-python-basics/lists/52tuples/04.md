Quiz: What will be the output of the following code?

```python
even_numbers = (2, 4, 6, 8)
print(even_numbers[2])
```

Tap an option below to answer:

##### Incorrect, give it another shot!

Tuple indexing starts at 0. So, even_numbers[2] gives the third item, not the first item.

Tuple indexing starts at 0. So, even_numbers[2] gives the third item, not the first item.

##### Incorrect, give it another shot!

Tuple index starts from 0, not 1. Hence, even_numbers[2] gives the third item, not the second item.

Tuple index starts from 0, not 1. Hence, even_numbers[2] gives the third item, not the second item.

##### Correct Answer!

even_numbers[2] gives the third item because tuple index starts at 0.

even_numbers[2] gives the third item because tuple index starts at 0.

##### Incorrect, give it another shot!

Tuple indexing starts from 0. So, even_numbers[2] gives the third item, not the fourth item.

Tuple indexing starts from 0. So, even_numbers[2] gives the third item, not the fourth item.

##### Incorrect, give it another shot!

Tuple indexing starts at 0. So, even_numbers[2] gives the third item, not the first item.

Tuple indexing starts at 0. So, even_numbers[2] gives the third item, not the first item.

##### Incorrect, give it another shot!

Tuple index starts from 0, not 1. Hence, even_numbers[2] gives the third item, not the second item.

Tuple index starts from 0, not 1. Hence, even_numbers[2] gives the third item, not the second item.

##### Correct Answer!

even_numbers[2] gives the third item because tuple index starts at 0.

even_numbers[2] gives the third item because tuple index starts at 0.

##### Incorrect, give it another shot!

Tuple indexing starts from 0. So, even_numbers[2] gives the third item, not the fourth item.

Tuple indexing starts from 0. So, even_numbers[2] gives the third item, not the fourth item.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:2
⬚ B. 2
⬚ C. B:4
⬚ D. 4
⬚ E. C:6
⬚ F. 6
⬚ G. D:8
⬚ H. 8

