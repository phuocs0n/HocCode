Quiz: What's the variable in the following code?

```python
jack = "name"
```

Tap an option below to answer:

jack

##### Correct Answer!

In the above code, jack is a variable. In Python, the value on the right of the = symbol ("name") is assigned to the variable on the left of the = symbol (jack).

In the above code, jack is a variable.

In Python, the value on the right of the = symbol ("name") is assigned to the variable on the left of the = symbol (jack).

"name"

##### Incorrect, give it another shot!

"name" is not a variable. It's a string since it's enclosed inside quotation marks.

"name" is not a variable. It's a string since it's enclosed inside quotation marks.

name

##### Incorrect, give it another shot!

name is not a variable; it's the value of the string assigned to the variable.

name is not a variable; it's the value of the string assigned to the variable.

##### Incorrect, give it another shot!

The = sign is the assignment operator, not a variable.

The = sign is the assignment operator, not a variable.

##### Correct Answer!

In the above code, jack is a variable. In Python, the value on the right of the = symbol ("name") is assigned to the variable on the left of the = symbol (jack).

In the above code, jack is a variable.

In Python, the value on the right of the = symbol ("name") is assigned to the variable on the left of the = symbol (jack).

##### Incorrect, give it another shot!

"name" is not a variable. It's a string since it's enclosed inside quotation marks.

"name" is not a variable. It's a string since it's enclosed inside quotation marks.

##### Incorrect, give it another shot!

name is not a variable; it's the value of the string assigned to the variable.

name is not a variable; it's the value of the string assigned to the variable.

##### Incorrect, give it another shot!

The = sign is the assignment operator, not a variable.

The = sign is the assignment operator, not a variable.

## 📝 Quiz

**Quiz: What's the variable in the following code?**

⬚ A. A:jack
⬚ B. jack
⬚ C. B:"name"
⬚ D. "name"
⬚ E. C:name
⬚ F. name
⬚ G. D:=
⬚ H. =

