Quiz: What is the output of the following code?

```python
favorite_actor = "Andrew"
print("Favorite actor:", favorite_actor)
```

Tap an option below to answer:

Favorite actor:, Andrew

##### Incorrect, give it another shot!

The comma is used to separate items in print(). It is not displayed in the output.

The comma is used to separate items in print(). It is not displayed in the output.

Favorite actor: favorite_actor

##### Incorrect, give it another shot!

favorite_actor is a variable, so its value (Andrew) is printed.

favorite_actor is a variable, so its value (Andrew) is printed.

"Favorite actor:" Andrew

##### Incorrect, give it another shot!

The quotation marks " inside the print() function are not displayed in the output. Only the string text and variable value are printed.

The quotation marks " inside the print() function are not displayed in the output. Only the string text and variable value are printed.

Favorite actor: Andrew

##### Correct Answer!

The code outputs the "Favorite actor" string, followed by the value stored in the favorite_actor variable.

The code outputs the "Favorite actor" string, followed by the value stored in the favorite_actor variable.

## 📝 Quiz

**Quiz: What is the output of the following code?**

⬚ A. A:Favorite actor:, Andrew
⬚ B. Favorite actor:, Andrew
⬚ C. B:Favorite actor: favorite_actor
⬚ D. Favorite actor: favorite_actor
⬚ E. C:"Favorite actor:" Andrew
⬚ F. "Favorite actor:" Andrew
⬚ G. D:Favorite actor: Andrew
⬚ H. Favorite actor: Andrew

