Quiz: Which of the following codes has quadratic time complexity?

Tap an option below to answer:

```python
def print_numbers(n):
    for i in range(n):
        for j in range(n):
            print(j)
```

##### Correct Answer!

This code's time complexity is quadratic due to the nested loop.

This code's time complexity is quadratic due to the nested loop.

```python
def print_numbers(n):
    i = 0
    j = 0 
    while i < n:
        print(i)
        i = i + 1
    
    while j < n:
        print(j)
        j = j + 1
```

##### Incorrect, give it another shot!

Nested loops do cause quadratic time complexity, but note that the code here contains two separate while loops.

Nested loops do cause quadratic time complexity, but note that the code here contains two separate while loops.

```python
def print_numbers(n):
    for i in range(n):
        if i < n:
            print(n)
```

##### Incorrect, give it another shot!

The code doesn't contain a nested loop. Instead, it contains an if statement inside a for loop. So, the time complexity is linear.

The code doesn't contain a nested loop. Instead, it contains an if statement inside a for loop. So, the time complexity is linear.

All of the above.

##### Incorrect, give it another shot!

Only one of the above options is correct.

Only one of the above options is correct.

## 📝 Quiz

**Quiz: Which of the following codes has quadratic time complexity?**

⬚ A. A:def print_numbers(n):
    for i in range(n):
        for j in range(n):
            print(j)
⬚ B. def print_numbers(n):
    for i in range(n):
        for j in range(n):
            print(j)
⬚ C. B:def print_numbers(n):
    i = 0
    j = 0 
    while i < n:
        print(i)
        i = i + 1
    
    while j < n:
        print(j)
        j = j + 1
⬚ D. def print_numbers(n):
    i = 0
    j = 0 
    while i < n:
        print(i)
        i = i + 1
    
    while j < n:
        print(j)
        j = j + 1
⬚ E. C:def print_numbers(n):
    for i in range(n):
        if i < n:
            print(n)
⬚ F. def print_numbers(n):
    for i in range(n):
        if i < n:
            print(n)
⬚ G. D:All of the above.
⬚ H. All of the above.

