Quiz: What is the time complexity of the following code?

```python
def print_numbers(n):
    i = 0 
    while i < n: 
        print(i)
        i += 1
```

Tap an option below to answer:

O(1)

##### Incorrect, give it another shot!

The time complexity of the above code is not constant because the execution time depends upon the input size.

The time complexity of the above code is not constant because the execution time depends upon the input size.

O(n + 2)

##### Incorrect, give it another shot!

The time complexity doesn't include two terms.

The time complexity doesn't include two terms.

O(n / 2)

##### Incorrect, give it another shot!

Coefficients aren't included in complexity.

Coefficients aren't included in complexity.

O(n)

##### Correct Answer!

Yes, the time complexity of our code is O(n)– linear time complexity.

Yes, the time complexity of our code is O(n)– linear time complexity.

##### Incorrect, give it another shot!

The time complexity of the above code is not constant because the execution time depends upon the input size.

The time complexity of the above code is not constant because the execution time depends upon the input size.

##### Incorrect, give it another shot!

The time complexity doesn't include two terms.

The time complexity doesn't include two terms.

##### Incorrect, give it another shot!

Coefficients aren't included in complexity.

Coefficients aren't included in complexity.

##### Correct Answer!

Yes, the time complexity of our code is O(n)– linear time complexity.

Yes, the time complexity of our code is O(n)– linear time complexity.

## 📝 Quiz

**Quiz: What is the time complexity of the following code?**

⬚ A. A:O(1)
⬚ B. O(1)
⬚ C. B:O(n + 2)
⬚ D. O(n + 2)
⬚ E. C:O(n / 2)
⬚ F. O(n / 2)
⬚ G. D:O(n)
⬚ H. O(n)

