Quiz: How many units of time does this function take to run if n = 3?

```python
def print_num(n):
    if n > 1:
        print(n)
```

Tap an option below to answer:

n+1

##### Incorrect, give it another shot!

The if statement is not a loop. It only takes constant unit of time to execute.

The if statement is not a loop. It only takes constant unit of time to execute.

2n

##### Incorrect, give it another shot!

There are no loops inside the code, so it takes a constant time to run regardless of the input size.

There are no loops inside the code, so it takes a constant time to run regardless of the input size.

##### Incorrect, give it another shot!

Since the if condition evaluates to True, we must consider the time taken by print() inside its body as well.

Since the if condition evaluates to True, we must consider the time taken by print() inside its body as well.

##### Correct Answer!

The if condition evaluates to True, and both the if and print() statements each take a single unit of time to run. Thus, the total unit of time taken is 2.

The if condition evaluates to True, and both the if and print() statements each take a single unit of time to run. Thus, the total unit of time taken is 2.

##### Incorrect, give it another shot!

The if statement is not a loop. It only takes constant unit of time to execute.

The if statement is not a loop. It only takes constant unit of time to execute.

##### Incorrect, give it another shot!

There are no loops inside the code, so it takes a constant time to run regardless of the input size.

There are no loops inside the code, so it takes a constant time to run regardless of the input size.

##### Incorrect, give it another shot!

Since the if condition evaluates to True, we must consider the time taken by print() inside its body as well.

Since the if condition evaluates to True, we must consider the time taken by print() inside its body as well.

##### Correct Answer!

The if condition evaluates to True, and both the if and print() statements each take a single unit of time to run. Thus, the total unit of time taken is 2.

The if condition evaluates to True, and both the if and print() statements each take a single unit of time to run. Thus, the total unit of time taken is 2.

## 📝 Quiz

**Quiz: How many units of time does this function take to run if n = 3?**

⬚ A. A:n+1
⬚ B. n+1
⬚ C. B:2n
⬚ D. 2n
⬚ E. C:1
⬚ F. 1
⬚ G. D:2
⬚ H. 2

