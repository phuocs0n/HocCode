Quiz: How many steps does it take for the following Python code to run?

```python
def print_num(n):
    print(n)
    print(n+1)
```

Tap an option below to answer:

##### Incorrect, give it another shot!

A tricky one! As there are no loops, the code takes a fixed number of steps.

A tricky one! As there are no loops, the code takes a fixed number of steps.

##### Incorrect, give it another shot!

Each print() statement counts as a step.

Each print() statement counts as a step.

##### Correct Answer!

As there are two print() statements in the code and no loops, the code takes two steps to run.

As there are two print() statements in the code and no loops, the code takes two steps to run.

n+1

##### Incorrect, give it another shot!

As there are no loops, the code takes a fixed number of steps.

As there are no loops, the code takes a fixed number of steps.

##### Incorrect, give it another shot!

A tricky one! As there are no loops, the code takes a fixed number of steps.

A tricky one! As there are no loops, the code takes a fixed number of steps.

##### Incorrect, give it another shot!

Each print() statement counts as a step.

Each print() statement counts as a step.

##### Correct Answer!

As there are two print() statements in the code and no loops, the code takes two steps to run.

As there are two print() statements in the code and no loops, the code takes two steps to run.

##### Incorrect, give it another shot!

As there are no loops, the code takes a fixed number of steps.

As there are no loops, the code takes a fixed number of steps.

## 📝 Quiz

**Quiz: How many steps does it take for the following Python code to run?**

⬚ A. A:n
⬚ B. n
⬚ C. B:1
⬚ D. 1
⬚ E. C:2
⬚ F. 2
⬚ G. D:n+1
⬚ H. n+1

