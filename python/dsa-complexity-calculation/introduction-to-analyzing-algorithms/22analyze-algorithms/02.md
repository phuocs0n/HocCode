# Optimizing Code

#### Course: Complexity Calculation

## Optimizing Code

Let's optimize the code we wrote previously using a simple formula to calculate the sum of natural numbers.

```python
Sum = n * (n + 1) / 2
```

Now, let's rewrite the code using the formula:

```python
1n = 1000
2
3total = n * (n + 1) / 2
4print(total)
```

Output

```text
500500.0
```

Assuming it takes 1 second to run 1 step, our optimized code executes in 1 second.

In our previous approach, the loop took 16 minutes to compute the sum. However, this optimized formula, regardless of the value of n, executes in just 1 second.

Even if we calculate the sum of 10¹¹ (100 billion) natural numbers, our optimized code still runs in 1 second.

Now, our code is faster and more scalable, showing why analyzing and optimizing algorithms is so important.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


