# Solving Recurrence Relation with Master Theorem

#### Course: Complexity Calculation

## Solving Recurrence Relation with Master Theorem

To solve an equation using recurrence relation for a dividing function, the function must be in the following format:

```text
T(n)=ab+f(n)T\lparen n \rparen = \cfrac{a}{b} + f(n)T(n)=ba​+f(n)where,where,where,f(n)=Ө(nk(log⁡n)p),    f\lparen n \rparen = Ө(n^k (\log n)^p), f(n)=Ө(nk(logn)p),a≥1,b>1,    a \geq 1, b > 1,a≥1,b>1,k≥1,    k \geq 1,k≥1,p=real number    p = real\ numberp=real number
```

Next, let's apply the equation to three different cases:

- Case 1: log⁡ba>k\log_ba > klogb​a>k
- Case 2: log⁡ba=k\log_ba = klogb​a=k
- Case 3: log⁡ba<k\log_ba < klogb​a<k

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


