# Example 2

#### Course: Complexity Calculation

## Example 2

Suppose we are given the following recurrence relation:

```text
T(n)=4T(n2)+nT\lparen n\rparen = 4T \left( \cfrac{n}{2} \right) +nT(n)=4T(2n​)+n
```

Let's calculate its time complexity using the master theorem.

Step 1: Compare the recurrence relation with the general form.

The general form of T(n) is:

```text
T(n)=aT(nb)+Ө(nk(log⁡n)p)T\lparen n\rparen = aT \left( \cfrac{n}{b} \right) + Ө(n^k \lparen \log n \rparen ^p)T(n)=aT(bn​)+Ө(nk(logn)p)
```

Step 2: Rewrite the recurrence relation to match the general form.

```text
T(n)=4T(n2)+Ө(n1(log⁡n)0)T\lparen n\rparen = 4T \left( \cfrac{n}{2} \right) + Ө(n^1 \lparen \log n \rparen ^0)T(n)=4T(2n​)+Ө(n1(logn)0)
```

Step 3: Identify a, b, k and p and check the conditions for the master theorem.

```python
a = 4
b = 2
k = 1
p = 0
```

Since a≥1a \geq 1a≥1, b > 1, k≥0k \geq 0k≥0, and p is a real number, the master theorem is possible.

Step 4: Compare log⁡ba\log_balogb​a and k and applying the matching rule.

Here,

```text
log⁡ba=log⁡24=2\log_ba = \log_24 = 2logb​a=log2​4=2k=1k = 1 k=1
```

Since log⁡ba>k\log_ba > klogb​a>k, let's apply the formula to find the time complexity for this scenario.

```text
T(n)=Ө(nlog⁡ba)T\lparen n \rparen = Ө(n ^{\log_ba})T(n)=Ө(nlogb​a)T(n)=Ө(n2)\hphantom{T\lparen n \rparen } = Ө(n^2)T(n)=Ө(n2)
```

Next, let's look at another example.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


