# log⁡ba=k\log_ba = klogb​a=k

#### Course: Complexity Calculation

## log⁡ba=k\log_ba = klogb​a=k

If log⁡ba=k\log_ba = klogb​a=k, then we should check values of p for three cases:

1. if p > -1, then T(n)=Ө(nklog⁡p+1n)T\lparen n\rparen = Ө \lparen n^k \log^{p +1} n \rparenT(n)=Ө(nklogp+1n)
2. if p = -1, then T(n)=Ө(nklog⁡log⁡n))T\lparen n\rparen = Ө \lparen n^k \log\log n \rparen)T(n)=Ө(nkloglogn))
3. if p < -1, then T(n)=Ө(nk)T\lparen n\rparen = Ө \lparen n^k \rparenT(n)=Ө(nk)

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


