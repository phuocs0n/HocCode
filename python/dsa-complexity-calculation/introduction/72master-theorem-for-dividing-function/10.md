Quiz: Which case should be considered when finding the time complexity of the following recurrence relation?

```text
T(n)=16T(n4)+nT\lparen n\rparen = 16T \left( \cfrac{n}{4} \right) + nT(n)=16T(4n​)+n
```

Tap an option below to answer:

log⁡ba>k\log_ba > klogb​a>k

##### Correct Answer!

Since log⁡416>1\log_416 > 1log4​16>1.

Since log⁡416>1\log_416 > 1log4​16>1.

log⁡ba=k\log_ba = klogb​a=k

##### Incorrect, give it another shot!

Since log⁡416!=1\log_416 != 1log4​16!=1.

Since log⁡416!=1\log_416 != 1log4​16!=1.

log⁡ba<k\log_ba < klogb​a<k

##### Incorrect, give it another shot!

Since log⁡416\log_416log4​16 is not less than 1.

Since log⁡416\log_416log4​16 is not less than 1.

log⁡ba>=k\log_ba >= klogb​a>=k

##### Incorrect, give it another shot!

Compound inequalities, such as (>= and <=), are not suitable for defining a case in the master theorem.

Compound inequalities, such as (>= and <=), are not suitable for defining a case in the master theorem.

##### Correct Answer!

Since log⁡416>1\log_416 > 1log4​16>1.

Since log⁡416>1\log_416 > 1log4​16>1.

##### Incorrect, give it another shot!

Since log⁡416!=1\log_416 != 1log4​16!=1.

Since log⁡416!=1\log_416 != 1log4​16!=1.

##### Incorrect, give it another shot!

Since log⁡416\log_416log4​16 is not less than 1.

Since log⁡416\log_416log4​16 is not less than 1.

##### Incorrect, give it another shot!

Compound inequalities, such as (>= and <=), are not suitable for defining a case in the master theorem.

Compound inequalities, such as (>= and <=), are not suitable for defining a case in the master theorem.

## 📝 Quiz

**Quiz: Which case should be considered when finding the time complexity of the following recurrence relation?**

⬚ A. A:log⁡ba>k\log_ba > klogb​a>k
⬚ B. log⁡ba>k\log_ba > klogb​a>k
⬚ C. B:log⁡ba=k\log_ba = klogb​a=k
⬚ D. log⁡ba=k\log_ba = klogb​a=k
⬚ E. C:log⁡ba<k\log_ba < klogb​a<k
⬚ F. log⁡ba<k\log_ba < klogb​a<k
⬚ G. D:log⁡ba>=k\log_ba >= klogb​a>=k
⬚ H. log⁡ba>=k\log_ba >= klogb​a>=k

