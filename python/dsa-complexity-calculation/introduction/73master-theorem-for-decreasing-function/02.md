# Solving Recurrence Relation with Master Theorem

#### Course: Complexity Calculation

## Solving Recurrence Relation with Master Theorem

To solve an equation using recurrence relation for a decreasing function, the function must be in the following format:

```text
T(n)=aT(n−b)+f(n)T\lparen n\rparen = aT\lparen n - b\rparen +f(n) T(n)=aT(n−b)+f(n)where,where,where,f(n)=O(nk),    f(n) = O(n^k), f(n)=O(nk),a>0, b>0,    a > 0,\ b > 0, a>0, b>0,k≥0    k \geq 0k≥0
```

Now, let's observe the equation for three different cases:

Case 1: a = 1

```text
T(n)=O(nk+1)T\lparen n\rparen =O(n^{k+1})T(n)=O(nk+1)
```

Case 2: a > 1

```text
T(n)=O(nkanb)T\lparen n\rparen =O(n^ka ^ \frac{n}{b})T(n)=O(nkabn​)
```

Case 3: a < 1

```text
T(n)=O(nk)T\lparen n\rparen =O(n^k)T(n)=O(nk)
```

Let's see some examples of the calculating time complexity of decreasing function.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


