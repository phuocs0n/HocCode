# Example 3

#### Course: Complexity Calculation

## Example 3

Consider the following recurrence relation:

```text
T(n)=12T(n−1)+n2T\lparen n\rparen = \cfrac{1}{2} T\lparen n - 1\rparen + n^2T(n)=21​T(n−1)+n2
```

Now let's calculate its time complexity using the master theorem.

Step 1: Compare the recurrence relation with the general form.

The general form of T(n) is:

```text
T(n)=aT(n−b)+O(nk)T\lparen n\rparen = aT\lparen n - b\rparen + O(n^k)T(n)=aT(n−b)+O(nk)
```

Step 2: Modify recurrence relation to match the general form.

```text
T(n)=12∗T(n−1)+n2T\lparen n\rparen = \cfrac{1}{2}*T\lparen n - 1\rparen + n^2T(n)=21​∗T(n−1)+n2
```

Step 3: Find a, b, and k check the conditions for the master theorem.

```text
a=12a = \cfrac{1}{2}a=21​b=1b = 1b=1k=2k = 2k=2
```

Since a > 0, b > 0, and k≥0k \geq 0k≥0, the master theorem is possible.

Step 4: Compare a and 1 and applying the matching rule.

Here,

```text
a=12a = \cfrac{1}{2}a=21​k=2k = 2k=2
```

Since a < 1, let's apply the formula to find the time complexity for this scenario.

```text
T(n)=O(nk)T\lparen n \rparen = O(n^k)T(n)=O(nk)T(n)=O(n2)\hphantom{T\lparen n \rparen }= O(n^2)T(n)=O(n2)
```

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


