# Example 1

#### Course: Complexity Calculation

## Example 1

Remember the following program we discussed earlier:

```python
def test(n):
    if n > 0:
        for i in range(1, n+1):
            print(n)
        test(n-1)
```

If the test(n) function takes T(n) time to execute, then its recurrence relation is:

```text
T(n)=T(n−1)+nT\lparen n\rparen =T\lparen n - 1\rparen + n T(n)=T(n−1)+n
```

Let's calculate its time complexity using the master theorem.

Step 1: Compare the recurrence relation with the general form.

The general form of T(n) is:

```text
T(n)=aT(n−b)+O(nk)T\lparen n\rparen = aT\lparen n - b\rparen + O(n^k)T(n)=aT(n−b)+O(nk)
```

Step 2: Modify recurrence relation to match the general form.

```text
T(n)=1∗T(n−1)+O(n1)T\lparen n\rparen = 1 * T\lparen n - 1\rparen + O(n^1)T(n)=1∗T(n−1)+O(n1)
```

Step 3: Find a, b, and k check the conditions for the master theorem.

```python
a = 1
b = 1
k = 1
```

Since a > 0, b > 0, and k≥0k \geq 0k≥0, the master theorem is possible.

Step 4: Compare a and 1 and apply: the matching rule.

Here,

```python
a = 1
k = 1
```

Since a = 1, let's apply the formula to find the time complexity for this scenario.

```text
T(n)=O(nk+1)T\lparen n\rparen = O(n^{k+1})T(n)=O(nk+1)T(n)=O(n1+1)\hphantom{T\lparen n\rparen} = O(n^{1+1})T(n)=O(n1+1)T(n)=O(n2)\hphantom{T\lparen n\rparen} = O(n^2)T(n)=O(n2)
```

Next, let's consider a slightly different recurrence.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


