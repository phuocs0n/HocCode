Quiz: What is the time complexity of the following code?

```python
def print_hello(n):
    for i in range(n):
        print("hello")
    
    j = 0
    while j < n:
        print("hello")
        j = j + 1
```

Tap an option below to answer:

O(n2)O(n^2)O(n2)

##### Incorrect, give it another shot!

The while loop is not inside the for loop. Hence, the time complexity is not O(n2)O(n^2)O(n2).

The while loop is not inside the for loop. Hence, the time complexity is not O(n2)O(n^2)O(n2).

O(n)

##### Correct Answer!

The two loops are independent of each other. Hence, the time complexity is O(n).

The two loops are independent of each other. Hence, the time complexity is O(n).

O(logn)

##### Incorrect, give it another shot!

The program's execution time changes linearly (not logarithmically) with the change in its input size.

The program's execution time changes linearly (not logarithmically) with the change in its input size.

O(1)

##### Incorrect, give it another shot!

As there are loops in the program, its execution time doesn't remain constant.

As there are loops in the program, its execution time doesn't remain constant.

##### Incorrect, give it another shot!

The while loop is not inside the for loop. Hence, the time complexity is not O(n2)O(n^2)O(n2).

The while loop is not inside the for loop. Hence, the time complexity is not O(n2)O(n^2)O(n2).

##### Correct Answer!

The two loops are independent of each other. Hence, the time complexity is O(n).

The two loops are independent of each other. Hence, the time complexity is O(n).

##### Incorrect, give it another shot!

The program's execution time changes linearly (not logarithmically) with the change in its input size.

The program's execution time changes linearly (not logarithmically) with the change in its input size.

##### Incorrect, give it another shot!

As there are loops in the program, its execution time doesn't remain constant.

As there are loops in the program, its execution time doesn't remain constant.

## 📝 Quiz

**Quiz: What is the time complexity of the following code?**

⬚ A. A:O(n2)O(n^2)O(n2)
⬚ B. O(n2)O(n^2)O(n2)
⬚ C. B:O(n)
⬚ D. O(n)
⬚ E. C:O(logn)
⬚ F. O(logn)
⬚ G. D:O(1)
⬚ H. O(1)

