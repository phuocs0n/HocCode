Quiz: If a program has O(n) time complexity, it means the program may execute with ___ complexity.

Tap an option below to answer:

O(n)

##### Incorrect, give it another shot!

It's a correct answer. But it's not the only correct answer.

It's a correct answer. But it's not the only correct answer.

O(log n)

##### Incorrect, give it another shot!

It's a correct answer. But it's not the only correct answer.

It's a correct answer. But it's not the only correct answer.

Neither of the options.

##### Incorrect, give it another shot!

Big O is the upper bound. Meaning a program with O(n) complexity can take O(n) or less time to execute like O(log n).

Big O is the upper bound. Meaning a program with O(n) complexity can take O(n) or less time to execute like O(log n).

Both first and second options.

##### Correct Answer!

A program with O(n) time complexity may execute in O(n) time or faster (since Big O denotes an upper bound).

A program with O(n) time complexity may execute in O(n) time or faster (since Big O denotes an upper bound).

## 📝 Quiz

**Quiz: If a program has O(n) time complexity, it means the program may execute with ___ complexity.**

⬚ A. A:O(n)
⬚ B. O(n)
⬚ C. B:O(log n)
⬚ D. O(log n)
⬚ E. C:Neither of the options.
⬚ F. Neither of the options.
⬚ G. D:Both first and second options.
⬚ H. Both first and second options.

