Quiz: If a program has θ(n) time complexity, it means, the program may execute with ___ complexity.

Tap an option below to answer:

θ(n)

##### Correct Answer!

Theta is the exact bound. A program with θ(n) complexity will take θ(n) complexity to execute.

Theta is the exact bound. A program with θ(n) complexity will take θ(n) complexity to execute.

θ(n3)θ(n^3)θ(n3)

##### Incorrect, give it another shot!

Theta is the exact bound. A program with θ(n) complexity will not take more time to execute (like θ(n3)θ(n^3)θ(n3)).

Theta is the exact bound. A program with θ(n) complexity will not take more time to execute (like θ(n3)θ(n^3)θ(n3)).

θ(n2)θ(n^2)θ(n2)

##### Incorrect, give it another shot!

Theta is the exact bound. A program with θ(n) complexity will not take more time to execute (like θ(n2)θ(n^2)θ(n2)).

Theta is the exact bound. A program with θ(n) complexity will not take more time to execute (like θ(n2)θ(n^2)θ(n2)).

θ(1)

##### Incorrect, give it another shot!

Theta is the exact bound. A program with θ(n) complexity will not take less time to execute (like θ(1)).

Theta is the exact bound. A program with θ(n) complexity will not take less time to execute (like θ(1)).

##### Correct Answer!

Theta is the exact bound. A program with θ(n) complexity will take θ(n) complexity to execute.

Theta is the exact bound. A program with θ(n) complexity will take θ(n) complexity to execute.

##### Incorrect, give it another shot!

Theta is the exact bound. A program with θ(n) complexity will not take more time to execute (like θ(n3)θ(n^3)θ(n3)).

Theta is the exact bound. A program with θ(n) complexity will not take more time to execute (like θ(n3)θ(n^3)θ(n3)).

##### Incorrect, give it another shot!

Theta is the exact bound. A program with θ(n) complexity will not take more time to execute (like θ(n2)θ(n^2)θ(n2)).

Theta is the exact bound. A program with θ(n) complexity will not take more time to execute (like θ(n2)θ(n^2)θ(n2)).

##### Incorrect, give it another shot!

Theta is the exact bound. A program with θ(n) complexity will not take less time to execute (like θ(1)).

Theta is the exact bound. A program with θ(n) complexity will not take less time to execute (like θ(1)).

## 📝 Quiz

**Quiz: If a program has θ(n) time complexity, it means, the program may execute with ___ complexity.**

⬚ A. A:θ(n)
⬚ B. θ(n)
⬚ C. B:θ(n3)θ(n^3)θ(n3)
⬚ D. θ(n3)θ(n^3)θ(n3)
⬚ E. C:θ(n2)θ(n^2)θ(n2)
⬚ F. θ(n2)θ(n^2)θ(n2)
⬚ G. D:θ(1)
⬚ H. θ(1)

