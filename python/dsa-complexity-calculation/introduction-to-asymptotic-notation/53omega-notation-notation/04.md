Quiz: If a program has Ω(n) time complexity, it means the program may execute with ___ complexity.

Tap an option below to answer:

Ω(n)

##### Incorrect, give it another shot!

It's a correct answer. But it's not the only correct answer.

It's a correct answer. But it's not the only correct answer.

Ω(n2)Ω(n^2)Ω(n2)

##### Incorrect, give it another shot!

Omega is lower bound. Meaning a program with Ω(n) complexity can take Ω(n) or more time to execute like Ω(n2)Ω(n^2)Ω(n2).

Omega is lower bound. Meaning a program with Ω(n) complexity can take Ω(n) or more time to execute like Ω(n2)Ω(n^2)Ω(n2).

Ω(logn)Ω(logn)Ω(logn)

##### Incorrect, give it another shot!

Omega is lower bound. Meaning a program with Ω(n) complexity cannot take less than Ω(n) time to execute.

Omega is lower bound. Meaning a program with Ω(n) complexity cannot take less than Ω(n) time to execute.

Both first and second options.

##### Correct Answer!

A program with Ω(n) time complexity may take more time than it to execute (lower bound).

A program with Ω(n) time complexity may take more time than it to execute (lower bound).

## 📝 Quiz

**Quiz: If a program has Ω(n) time complexity, it means the program may execute with ___ complexity.**

⬚ A. A:Ω(n)
⬚ B. Ω(n)
⬚ C. B:Ω(n2)Ω(n^2)Ω(n2)
⬚ D. Ω(n2)Ω(n^2)Ω(n2)
⬚ E. C:Ω(logn)Ω(logn)Ω(logn)
⬚ F. Ω(logn)Ω(logn)Ω(logn)
⬚ G. D:Both first and second options.
⬚ H. Both first and second options.

