# Constant Space Complexity – O(1)

#### Course: Complexity Calculation

## Constant Space Complexity – O(1)

An algorithm has a constant space complexity when the memory it uses doesn't depend on the change in its input size.

In other words, even if the input size grows, the memory required to execute the program remains the same.

Let's take an example.

```python
1def find_sum(n1, n2, n3):
2    result = n1 + n2 + n3
3    print(result)
4
5find_sum(1,2,3)
```

Output

```text
6
```

Let's find the space complexity of the find_sum() function.

Let's look at variables and their sizes again to understand memory usage.

In the above program,

| Variables | Input Size |
| --- | --- |
| n1 | 1 |
| n2 | 1 |
| n3 | 1 |
| result | 1 |

```text
Space Complexity=1+1+1+1Space\ Complexity = 1 + 1 + 1 + 1Space Complexity=1+1+1+1Space Complexity=4\hphantom{Space\ Complexity }= 4 Space Complexity=4Space Complexity=4∗n0 (Because n0=1)\hphantom{Space\ Complexity }= 4 * n^0\ (Because\ n^0 = 1)Space Complexity=4∗n0 (Because n0=1)Space Complexity=n0(By frequency count method)\hphantom{Space\ Complexity }= n^0(By\ frequency\ count\ method)Space Complexity=n0(By frequency count method)
```

By the frequency count method, constants are dropped.

```text
Space Complexity=1\hphantom{Space\ Complexity }= 1 Space Complexity=1Space Complexity=O(1)\hphantom{Space\ Complexity }= O(1) Space Complexity=O(1)
```

O(1) thus means that, even if the input size increases, the space complexity doesn't change—just like in the case of time complexity.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


