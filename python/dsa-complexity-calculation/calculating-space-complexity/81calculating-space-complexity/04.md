Quiz: What is the space complexity of the following code?

```python
def print_sum(n1):
    print(n1)
```

Tap an option below to answer:

O(n)

##### Incorrect, give it another shot!

The space complexity remains constant even when the size of the input changes. Therefore, the space complexity is not linear.

The space complexity remains constant even when the size of the input changes. Therefore, the space complexity is not linear.

O(n1)

##### Incorrect, give it another shot!

We don't represent space complexity with variable names.

We don't represent space complexity with variable names.

O(1)

##### Correct Answer!

The space complexity remains constant even when the size of the input changes.

The space complexity remains constant even when the size of the input changes.

O(logn)

##### Incorrect, give it another shot!

The space complexity remains constant even if the size of the input changes. Therefore, the space complexity is not logarithmic.

The space complexity remains constant even if the size of the input changes. Therefore, the space complexity is not logarithmic.

##### Incorrect, give it another shot!

The space complexity remains constant even when the size of the input changes. Therefore, the space complexity is not linear.

The space complexity remains constant even when the size of the input changes. Therefore, the space complexity is not linear.

##### Incorrect, give it another shot!

We don't represent space complexity with variable names.

We don't represent space complexity with variable names.

##### Correct Answer!

The space complexity remains constant even when the size of the input changes.

The space complexity remains constant even when the size of the input changes.

##### Incorrect, give it another shot!

The space complexity remains constant even if the size of the input changes. Therefore, the space complexity is not logarithmic.

The space complexity remains constant even if the size of the input changes. Therefore, the space complexity is not logarithmic.

## 📝 Quiz

**Quiz: What is the space complexity of the following code?**

⬚ A. A:O(n)
⬚ B. O(n)
⬚ C. B:O(n1)
⬚ D. O(n1)
⬚ E. C:O(1)
⬚ F. O(1)
⬚ G. D:O(logn)
⬚ H. O(logn)

