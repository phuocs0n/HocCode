Quiz: In the code below, where do you think the box element will move when we use translate: 50% 50%;?

```css
<div class="parent">
    <div class="box">I move with translate(50%, 50%)!</div>
</div>
```

Tap an option below to answer:

It moves 50% of the parent element's width and height.

##### Incorrect, give it another shot!

The translate property does not calculate percentages based on the parent element. It only considers the size of the element being moved.

The translate property does not calculate percentages based on the parent element. It only considers the size of the element being moved.

It moves 50% of the screen's width and height.

##### Incorrect, give it another shot!

The translate property does not use screen size when calculating percentages.

The translate property does not use screen size when calculating percentages.

It moves 50% of the box element's own width and height.

##### Correct Answer!

translate(50%, 50%) moves the box element based on its own dimensions. So, it shifts by 50% of its width and 50% of its height.

translate(50%, 50%) moves the box element based on its own dimensions. So, it shifts by 50% of its width and 50% of its height.

It moves to the center of the parent element.

##### Incorrect, give it another shot!

This does not necessarily center the .box element inside the parent container, as the movement depends solely on the size of the .box element.

This does not necessarily center the .box element inside the parent container, as the movement depends solely on the size of the .box element.

## 📝 Quiz

**Quiz: In the code below, where do you think the box element will move when we use translate: 50% 50%;?**

⬚ A. A:It moves 50% of the parent element's width and height.
⬚ B. It moves 50% of the parent element's width and height.
⬚ C. B:It moves 50% of the screen's width and height.
⬚ D. It moves 50% of the screen's width and height.
⬚ E. C:It moves 50% of the box element's own width and height.
⬚ F. It moves 50% of the box element's own width and height.
⬚ G. D:It moves to the center of the parent element.
⬚ H. It moves to the center of the parent element.

