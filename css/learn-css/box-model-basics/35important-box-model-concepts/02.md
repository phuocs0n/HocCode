Quiz: Given the following code snippet:

```css
div {
    width: 100px;
    height: 100px;
    padding: 30px;
    border: 5px;
    margin: 30px;
    box-sizing: content-box;
}
```

What is the total width and height of the <div> element?

Tap an option below to answer:

width is 100px and height is 100px.

##### Incorrect, give it another shot!

Since the box-sizing property is set to content-box, the total width and height of the <div> also includes padding and border, which isn't the case here.

Since the box-sizing property is set to content-box, the total width and height of the <div> also includes padding and border, which isn't the case here.

width is 135px and height is 135px.

##### Incorrect, give it another shot!

As you know, applying padding and borders affects all sides of an element. This means that the width will need to account for both the left and right padding and borders, while the height will include the top and bottom padding and borders as well.

As you know, applying padding and borders affects all sides of an element. This means that the width will need to account for both the left and right padding and borders, while the height will include the top and bottom padding and borders as well.

width is 170px and height is 170px.

##### Correct Answer!

Setting box-sizing to content-box includes padding and border in the total width and height of the element.

Setting box-sizing to content-box includes padding and border in the total width and height of the element.

width is 230px and height is 230px.

##### Incorrect, give it another shot!

margin is never included in the total width and height of an element, regardless of whether the box-sizing value is applied or not.

margin is never included in the total width and height of an element, regardless of whether the box-sizing value is applied or not.

## 📝 Quiz

**Quiz: Given the following code snippet:**

⬚ A. A:width is 100px and height is 100px.
⬚ B. width is 100px and height is 100px.
⬚ C. B:width is 135px and height is 135px.
⬚ D. width is 135px and height is 135px.
⬚ E. C:width is 170px and height is 170px.
⬚ F. width is 170px and height is 170px.
⬚ G. D:width is 230px and height is 230px.
⬚ H. width is 230px and height is 230px.

