Quiz: You want to correctly position an icon 20px down from its original position. Which of the following code snippets can help you achieve this?

Tap an option below to answer:

```css
.icon {
    top: 20px; 
}
```

##### Incorrect, give it another shot!

This code doesn't specify a position type, so it won't work as intended.

This code doesn't specify a position type, so it won't work as intended.

```css
.icon {
    position: static;
    top: 20px; 
}
```

##### Incorrect, give it another shot!

position: static ignores the top value so the icon won't move.

position: static ignores the top value so the icon won't move.

```css
.icon {
    position: relative;
    top: -20px; 
}
```

##### Incorrect, give it another shot!

This code moves the icon up instead of down due to the negative value of displacement.

This code moves the icon up instead of down due to the negative value of displacement.

```css
.icon {
    position: relative;
    top: 20px;
}
```

##### Correct Answer!

This code properly uses position: relative to move the icon down by 20px.

This code properly uses position: relative to move the icon down by 20px.

## 📝 Quiz

**Quiz: You want to correctly position an icon 20px down from its original position. Which of the following code snippets can help you achieve this?**

⬚ A. A:.icon {
    top: 20px; 
}
⬚ B. .icon {
    top: 20px; 
}
⬚ C. B:.icon {
    position: static;
    top: 20px; 
}
⬚ D. .icon {
    position: static;
    top: 20px; 
}
⬚ E. C:.icon {
    position: relative;
    top: -20px; 
}
⬚ F. .icon {
    position: relative;
    top: -20px; 
}
⬚ G. D:.icon {
    position: relative;
    top: 20px;
}
⬚ H. .icon {
    position: relative;
    top: 20px;
}

