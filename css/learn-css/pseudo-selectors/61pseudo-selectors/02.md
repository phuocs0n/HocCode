Quiz: What is the effect of the CSS code below?

```css
div:hover { 
    background-color: black; 
}
```

Tap an option below to answer:

It changes the background color of the <div> element to black when the mouse pointer is over it.

##### Correct Answer!

The :hover pseudo-class applies a style when a user's mouse pointer hovers over an element.

The :hover pseudo-class applies a style when a user's mouse pointer hovers over an element.

It changes the color of the text in the <div> element to black.

##### Incorrect, give it another shot!

This CSS rule deals with the background color of the <div>, not the color of the text inside it.

This CSS rule deals with the background color of the <div>, not the color of the text inside it.

It always sets the background color of the <div> element to black, regardless of any action by the user.

##### Incorrect, give it another shot!

The :hover pseudo-class applies a style only when a user's mouse pointer hovers over the specified element.

The :hover pseudo-class applies a style only when a user's mouse pointer hovers over the specified element.

It hides the <div> element when the mouse is not over it.

##### Incorrect, give it another shot!

The :hover pseudo-class doesn't hide or show elements. It applies styles when the user's mouse pointer hovers over the specified element.

The :hover pseudo-class doesn't hide or show elements. It applies styles when the user's mouse pointer hovers over the specified element.

## 📝 Quiz

**Quiz: What is the effect of the CSS code below?**

⬚ A. A:It changes the background color of the <div> element to black when the mouse pointer is over it.
⬚ B. It changes the background color of the <div> element to black when the mouse pointer is over it.
⬚ C. B:It changes the color of the text in the <div> element to black.
⬚ D. It changes the color of the text in the <div> element to black.
⬚ E. C:It always sets the background color of the <div> element to black, regardless of any action by the user.
⬚ F. It always sets the background color of the <div> element to black, regardless of any action by the user.
⬚ G. D:It hides the <div> element when the mouse is not over it.
⬚ H. It hides the <div> element when the mouse is not over it.

