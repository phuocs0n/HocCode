Quiz: Which of the following shows a good practice for CSS naming conventions?

Tap an option below to answer:

Using unclear, generic names for classes and IDs.

##### Incorrect, give it another shot!

For maintainable CSS, choosing clear, descriptive names for classes and IDs is recommended.

For maintainable CSS, choosing clear, descriptive names for classes and IDs is recommended.

Using different naming styles for classes and IDs.

##### Incorrect, give it another shot!

Sticking to a consistent naming style contributes to better readability of your CSS code.

Sticking to a consistent naming style contributes to better readability of your CSS code.

Using clear, descriptive names for classes and IDs.

##### Correct Answer!

Clear, descriptive names for classes and IDs give insight into the functionality or appearance of an element.

Clear, descriptive names for classes and IDs give insight into the functionality or appearance of an element.

Adding comments in the CSS file without any particular order.

##### Incorrect, give it another shot!

While it is a good practice to use comments in your CSS file, they should be organized and relate directly to the subsequent CSS rule(s).

While it is a good practice to use comments in your CSS file, they should be organized and relate directly to the subsequent CSS rule(s).

## 📝 Quiz

**Quiz: Which of the following shows a good practice for CSS naming conventions?**

⬚ A. A:Using unclear, generic names for classes and IDs.
⬚ B. Using unclear, generic names for classes and IDs.
⬚ C. B:Using different naming styles for classes and IDs.
⬚ D. Using different naming styles for classes and IDs.
⬚ E. C:Using clear, descriptive names for classes and IDs.
⬚ F. Using clear, descriptive names for classes and IDs.
⬚ G. D:Adding comments in the CSS file without any particular order.
⬚ H. Adding comments in the CSS file without any particular order.

