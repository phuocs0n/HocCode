Quiz: What does the given CSS code specify?

```css
#intro {
    color: red;
}
```

Tap an option below to answer:

The HTML element with an ID of intro will have the text color red.

##### Correct Answer!

The CSS code specifies that the style will be applied to the HTML element with the ID intro.

The CSS code specifies that the style will be applied to the HTML element with the ID intro.

Any HTML element with a class of intro will have the text color red.

##### Incorrect, give it another shot!

In CSS, the hash symbol # selects HTML elements by their ID attribute, not their class attribute.

In CSS, the hash symbol # selects HTML elements by their ID attribute, not their class attribute.

The whole webpage will have a red background color.

##### Incorrect, give it another shot!

This CSS code applies the color to the text of the HTML element with the ID intro, not to the page background.

This CSS code applies the color to the text of the HTML element with the ID intro, not to the page background.

All text on the webpage will be red.

##### Incorrect, give it another shot!

The CSS code does not modify the color of all text on the webpage. It only changes the color of the text inside the HTML element with the ID intro.

The CSS code does not modify the color of all text on the webpage. It only changes the color of the text inside the HTML element with the ID intro.

## 📝 Quiz

**Quiz: What does the given CSS code specify?**

⬚ A. A:The HTML element with an ID of intro will have the text color red.
⬚ B. The HTML element with an ID of intro will have the text color red.
⬚ C. B:Any HTML element with a class of intro will have the text color red.
⬚ D. Any HTML element with a class of intro will have the text color red.
⬚ E. C:The whole webpage will have a red background color.
⬚ F. The whole webpage will have a red background color.
⬚ G. D:All text on the webpage will be red.
⬚ H. All text on the webpage will be red.

