Quiz: How do you link an external CSS file to your HTML document?

Tap an option below to answer:

By using the <style> tag.

##### Incorrect, give it another shot!

The <style> tag is used for adding CSS internally in HTML, not for linking external CSS files.

The <style> tag is used for adding CSS internally in HTML, not for linking external CSS files.

By using the <script> tag.

##### Incorrect, give it another shot!

The <script> tag is used for adding JavaScript to HTML documents, not for adding external CSS files.

The <script> tag is used for adding JavaScript to HTML documents, not for adding external CSS files.

By using the <link> tag.

##### Correct Answer!

The <link> tag is used with the rel="stylesheet" and href="filename.css" attributes for linking external CSS files in the HTML file.

The <link> tag is used with the rel="stylesheet" and href="filename.css" attributes for linking external CSS files in the HTML file.

By using the <meta> tag.

##### Incorrect, give it another shot!

The <meta> tag provides metadata about the HTML document and is not used for linking external CSS files.

The <meta> tag provides metadata about the HTML document and is not used for linking external CSS files.

##### Incorrect, give it another shot!

The <style> tag is used for adding CSS internally in HTML, not for linking external CSS files.

The <style> tag is used for adding CSS internally in HTML, not for linking external CSS files.

##### Incorrect, give it another shot!

The <script> tag is used for adding JavaScript to HTML documents, not for adding external CSS files.

The <script> tag is used for adding JavaScript to HTML documents, not for adding external CSS files.

##### Correct Answer!

The <link> tag is used with the rel="stylesheet" and href="filename.css" attributes for linking external CSS files in the HTML file.

The <link> tag is used with the rel="stylesheet" and href="filename.css" attributes for linking external CSS files in the HTML file.

##### Incorrect, give it another shot!

The <meta> tag provides metadata about the HTML document and is not used for linking external CSS files.

The <meta> tag provides metadata about the HTML document and is not used for linking external CSS files.

## 📝 Quiz

**Quiz: How do you link an external CSS file to your HTML document?**

⬚ A. A:By using the <style> tag.
⬚ B. By using the <style> tag.
⬚ C. B:By using the <script> tag.
⬚ D. By using the <script> tag.
⬚ E. C:By using the <link> tag.
⬚ F. By using the <link> tag.
⬚ G. D:By using the <meta> tag.
⬚ H. By using the <meta> tag.

