Quiz: Given the following code snippet:

```css
:root {
    font-size: 20px;
}

.container {
    font-size: 10px;
}

.p1 {
    font-size: 2em;
}

.p2 {
    font-size: 3rem;
}
```

What are the pixel values for the font sizes of p1 and p2 elements?

Tap an option below to answer:

The font sizes of p1 and p2 are 40px and 30px, respectively.

##### Incorrect, give it another shot!

Consider how em relates to its parent element's font size and how rem is based on the root font size.

Consider how em relates to its parent element's font size and how rem is based on the root font size.

The font sizes of p1 and p2 are both 32px.

##### Incorrect, give it another shot!

The root and parent elements have different font sizes, making it unlikely for em and rem units to yield the same pixel values.

The root and parent elements have different font sizes, making it unlikely for em and rem units to yield the same pixel values.

The font sizes of p1 and p2 are 2em and 3rem, respectively.

##### Incorrect, give it another shot!

The answer must be in pixel values.

The answer must be in pixel values.

The font sizes of p1 and p2 are 20px and 60px, respectively.

##### Correct Answer!

The font size of p1 is 2em (2 x 10px) and p2 is 3rem (3 x 20px).

The font size of p1 is 2em (2 x 10px) and p2 is 3rem (3 x 20px).

## 📝 Quiz

**Quiz: Given the following code snippet:**

⬚ A. A:The font sizes of p1 and p2 are 40px and 30px, respectively.
⬚ B. The font sizes of p1 and p2 are 40px and 30px, respectively.
⬚ C. B:The font sizes of p1 and p2 are both 32px.
⬚ D. The font sizes of p1 and p2 are both 32px.
⬚ E. C:The font sizes of p1 and p2 are 2em and 3rem, respectively.
⬚ F. The font sizes of p1 and p2 are 2em and 3rem, respectively.
⬚ G. D:The font sizes of p1 and p2 are 20px and 60px, respectively.
⬚ H. The font sizes of p1 and p2 are 20px and 60px, respectively.

