Quiz: Which of the following media query syntax is correct?

Tap an option below to answer:

```css
body {
    @media (min-width: 600px) {
        background-color: lightblue;
    }
}
```

##### Incorrect, give it another shot!

The CSS rulesets must be defined inside the @media keyword, not outside.

The CSS rulesets must be defined inside the @media keyword, not outside.

```css
media (min-width: 600px) {
    body {
        background-color: lightblue;
    }
}
```

##### Incorrect, give it another shot!

Using only media is incorrect as it's missing the @ symbol. Instead, the correct keyword is @media.

Using only media is incorrect as it's missing the @ symbol. Instead, the correct keyword is @media.

```css
@media (min-width: 600px) {
    body {
        background-color: lightblue;
    }
}
```

##### Correct Answer!

The @media keyword indicates the start of a media query, followed by the CSS rulesets that define the changes to take place in the given condition.

The @media keyword indicates the start of a media query, followed by the CSS rulesets that define the changes to take place in the given condition.

```css
@media {
    body {
        background-color: lightblue;
    }
}
```

##### Incorrect, give it another shot!

A media query is started, but no condition is provided to specify when the CSS rules should apply.

A media query is started, but no condition is provided to specify when the CSS rules should apply.

## 📝 Quiz

**Quiz: Which of the following media query syntax is correct?**

⬚ A. A:body {
    @media (min-width: 600px) {
        background-color: lightblue;
    }
}
⬚ B. body {
    @media (min-width: 600px) {
        background-color: lightblue;
    }
}
⬚ C. B:media (min-width: 600px) {
    body {
        background-color: lightblue;
    }
}
⬚ D. media (min-width: 600px) {
    body {
        background-color: lightblue;
    }
}
⬚ E. C:@media (min-width: 600px) {
    body {
        background-color: lightblue;
    }
}
⬚ F. @media (min-width: 600px) {
    body {
        background-color: lightblue;
    }
}
⬚ G. D:@media {
    body {
        background-color: lightblue;
    }
}
⬚ H. @media {
    body {
        background-color: lightblue;
    }
}

