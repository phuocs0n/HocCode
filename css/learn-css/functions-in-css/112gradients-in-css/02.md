Quiz: How would you create a linear gradient that transitions from red at the top to blue at the bottom?

Tap an option below to answer:

```css
.gradient-box {
    background: linear-gradient(to bottom, red, blue);
}
```

##### Correct Answer!

This code will create a vertical gradient that transitions from red at the top to blue at the bottom by specifying to bottom in the linear-gradient() function.

This code will create a vertical gradient that transitions from red at the top to blue at the bottom by specifying to bottom in the linear-gradient() function.

```css
.gradient-box {
    background: linear-gradient(to left, red, blue);
}
```

##### Incorrect, give it another shot!

This code will create a gradient that transitions from left to right, not top to bottom.

This code will create a gradient that transitions from left to right, not top to bottom.

```css
.gradient-box {
    background: radial-gradient(circle, red, blue);
}
```

##### Incorrect, give it another shot!

With this code, you'll create a radial gradient, not a linear gradient.

With this code, you'll create a radial gradient, not a linear gradient.

```css
.gradient-box {
    background: linear-gradient(to top, red, blue);
}
```

##### Incorrect, give it another shot!

While the code specifies a linear gradient, the correct direction for a top-to-bottom gradient in CSS is to bottom, not to top.

While the code specifies a linear gradient, the correct direction for a top-to-bottom gradient in CSS is to bottom, not to top.

## 📝 Quiz

**Quiz: How would you create a linear gradient that transitions from red at the top to blue at the bottom?**

⬚ A. A:.gradient-box {
    background: linear-gradient(to bottom, red, blue);
}
⬚ B. .gradient-box {
    background: linear-gradient(to bottom, red, blue);
}
⬚ C. B:.gradient-box {
    background: linear-gradient(to left, red, blue);
}
⬚ D. .gradient-box {
    background: linear-gradient(to left, red, blue);
}
⬚ E. C:.gradient-box {
    background: radial-gradient(circle, red, blue);
}
⬚ F. .gradient-box {
    background: radial-gradient(circle, red, blue);
}
⬚ G. D:.gradient-box {
    background: linear-gradient(to top, red, blue);
}
⬚ H. .gradient-box {
    background: linear-gradient(to top, red, blue);
}

