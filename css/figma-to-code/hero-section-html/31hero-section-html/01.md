# Add the hero section wrapper

## Add the hero section wrapper

In this step, you'll create the basic wrapper for the hero. You're not adding the hero content yet.

You're just setting up the structure so everything stays organized.

Add the hero section wrapper inside the main content area of the page.

In index.html:

- Add a main element after the header
- Inside main, add a section with the class hero
- Inside the hero section, add a div with the class container

- index.html
- style.css

```unknown
<!DOCTYPE html>
```

