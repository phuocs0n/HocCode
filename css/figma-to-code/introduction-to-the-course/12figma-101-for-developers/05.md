Quiz: In Figma, if clicking selects the container instead of the element inside it, what should you do?

Tap an option below to answer:

Right-click and select child element.

##### Incorrect, give it another shot!

Right-click menus can help in some cases, but it's not the core workflow here. The simplest method is clicking to drill down.

Right-click menus can help in some cases, but it's not the core workflow here. The simplest method is clicking to drill down.

Keep clicking until the exact element is selected.

##### Correct Answer!

Each click selects the next nested layer until you reach the exact element you want.

Each click selects the next nested layer until you reach the exact element you want.

Zoom in closer.

##### Incorrect, give it another shot!

Zooming can make clicking easier, but it doesn't change how nested selection works in Figma.

Zooming can make clicking easier, but it doesn't change how nested selection works in Figma.

Hold Shift while clicking.

##### Incorrect, give it another shot!

Shift-click is for selecting multiple items, not for drilling into elements inside a container.

Shift-click is for selecting multiple items, not for drilling into elements inside a container.

## 📝 Quiz

**Quiz: In Figma, if clicking selects the container instead of the element inside it, what should you do?**

⬚ A. A:Right-click and select child element.
⬚ B. Right-click and select child element.
⬚ C. B:Keep clicking until the exact element is selected.
⬚ D. Keep clicking until the exact element is selected.
⬚ E. C:Zoom in closer.
⬚ F. Zoom in closer.
⬚ G. D:Hold Shift while clicking.
⬚ H. Hold Shift while clicking.

