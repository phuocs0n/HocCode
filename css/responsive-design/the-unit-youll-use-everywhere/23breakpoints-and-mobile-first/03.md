Quiz: What does mobile-first CSS mean?

Tap an option below to answer:

Writing CSS only for mobile and ignoring desktop.

##### Incorrect, give it another shot!

Using max-width media queries only.

Using max-width media queries only.

Making the desktop layout look like mobile.

##### Incorrect, give it another shot!

Base styles for the smallest screen, add layout for larger viewports with min-width media queries.

Base styles for the smallest screen, add layout for larger viewports with min-width media queries.

Mobile-first absolutely supports desktop. It just reaches desktop through min-width layers rather than starting there. Base styles run on all viewports, only the additions are gated.

##### Incorrect, give it another shot!

max-width is actually the desktop-first approach. You start wide and use max-width to narrow things back down. Mobile-first flips it, start narrow and add complexity upward with min-width.

max-width is actually the desktop-first approach. You start wide and use max-width to narrow things back down. Mobile-first flips it, start narrow and add complexity upward with min-width.

That would just be a bad desktop experience. Mobile-first means you write for mobile first in the cascade, then add more layout as space allows.

##### Correct Answer!

Mobile-first means base styles target the smallest viewport and you layer on larger layouts with min-width, not max-width.

Mobile-first means base styles target the smallest viewport and you layer on larger layouts with min-width, not max-width.

## 📝 Quiz

**Quiz: What does mobile-first CSS mean?**

⬚ A. A:Writing CSS only for mobile and ignoring desktop.
⬚ B. Writing CSS only for mobile and ignoring desktop.
⬚ C. B:Making the desktop layout look like mobile.
⬚ D. Making the desktop layout look like mobile.
⬚ E. C:Mobile-first absolutely supports desktop. It just reaches desktop through min-width layers rather than starting there. Base styles run on all viewports, only the additions are gated.
⬚ F. Mobile-first absolutely supports desktop. It just reaches desktop through min-width layers rather than starting there. Base styles run on all viewports, only the additions are gated.
⬚ G. D:That would just be a bad desktop experience. Mobile-first means you write for mobile first in the cascade, then add more layout as space allows.
⬚ H. That would just be a bad desktop experience. Mobile-first means you write for mobile first in the cascade, then add more layout as space allows.

