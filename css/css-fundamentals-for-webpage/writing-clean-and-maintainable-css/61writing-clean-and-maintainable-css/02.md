Quiz: In a real project, why do developers often create a reusable .container class and apply it across sections (header, main, forms, footer)?

Tap an option below to answer:

To make every section look identical.

##### Incorrect, give it another shot!

Incorrect. A container standardizes layout boundaries (width/centering/padding), not the visual design of every component.

Incorrect. A container standardizes layout boundaries (width/centering/padding), not the visual design of every component.

To centralize layout rules (max-width, horizontal padding, centering) in one reusable place.

##### Correct Answer!

Correct. This keeps spacing consistent across the site and makes global layout changes safe and fast.

Correct. This keeps spacing consistent across the site and makes global layout changes safe and fast.

To replace Flexbox and Grid completely.

##### Incorrect, give it another shot!

Incorrect. Containers work alongside Flexbox/Grid. They handle page width and alignment, while Flexbox/Grid handle internal layout.

Incorrect. Containers work alongside Flexbox/Grid. They handle page width and alignment, while Flexbox/Grid handle internal layout.

Because responsive design cannot work without a .container.

##### Incorrect, give it another shot!

Incorrect. Responsiveness is possible without it, but .container makes it much easier to keep layouts consistent across screen sizes.

Incorrect. Responsiveness is possible without it, but .container makes it much easier to keep layouts consistent across screen sizes.

## 📝 Quiz

**Quiz: In a real project, why do developers often create a reusable .container class and apply it across sections (header, main, forms, footer)?**

⬚ A. A:To make every section look identical.
⬚ B. To make every section look identical.
⬚ C. B:To centralize layout rules (max-width, horizontal padding, centering) in one reusable place.
⬚ D. To centralize layout rules (max-width, horizontal padding, centering) in one reusable place.
⬚ E. C:To replace Flexbox and Grid completely.
⬚ F. To replace Flexbox and Grid completely.
⬚ G. D:Because responsive design cannot work without a .container.
⬚ H. Because responsive design cannot work without a .container.

