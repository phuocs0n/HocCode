Quiz: Why do many developers build responsive layouts mobile-first (start small, then add @media rules for larger screens)?

Tap an option below to answer:

Because desktop users don't matter anymore.

##### Incorrect, give it another shot!

Incorrect. Mobile-first is a workflow choice, not a statement that desktop is irrelevant.

Incorrect. Mobile-first is a workflow choice, not a statement that desktop is irrelevant.

Because media queries only work on phones.

##### Incorrect, give it another shot!

Incorrect. Media queries work everywhere — they respond to viewport size on any device.

Incorrect. Media queries work everywhere — they respond to viewport size on any device.

Because it forces you to handle the hardest constraints first, which usually creates a cleaner layout that scales up.

##### Correct Answer!

Correct. If your layout works on small screens, it's typically easier to enhance it for tablets and desktops.

Correct. If your layout works on small screens, it's typically easier to enhance it for tablets and desktops.

Because it automatically reduces the CSS file size.

##### Incorrect, give it another shot!

Incorrect. Mobile-first can be efficient, but smaller file size isn't the main reason teams adopt it.

Incorrect. Mobile-first can be efficient, but smaller file size isn't the main reason teams adopt it.

## 📝 Quiz

**Quiz: Why do many developers build responsive layouts mobile-first (start small, then add @media rules for larger screens)?**

⬚ A. A:Because desktop users don't matter anymore.
⬚ B. Because desktop users don't matter anymore.
⬚ C. B:Because media queries only work on phones.
⬚ D. Because media queries only work on phones.
⬚ E. C:Because it forces you to handle the hardest constraints first, which usually creates a cleaner layout that scales up.
⬚ F. Because it forces you to handle the hardest constraints first, which usually creates a cleaner layout that scales up.
⬚ G. D:Because it automatically reduces the CSS file size.
⬚ H. Because it automatically reduces the CSS file size.

