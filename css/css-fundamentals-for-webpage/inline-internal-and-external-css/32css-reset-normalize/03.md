Quiz: You open a brand-new HTML page (no CSS file linked) and the heading still has space around it. Where is that spacing coming from?

Tap an option below to answer:

HTML automatically adds margins and padding.

##### Incorrect, give it another shot!

This is wrong. HTML defines structure and meaning, not presentation rules.

This is wrong. HTML defines structure and meaning, not presentation rules.

Your CSS file is being loaded automatically by the browser.

##### Incorrect, give it another shot!

Incorrect. External CSS only applies if you link it with a <link> tag (or add styles another way).

Incorrect. External CSS only applies if you link it with a <link> tag (or add styles another way).

The browser's default stylesheet (user agent styles) is applying built-in margins/padding.

##### Correct Answer!

Correct. Browsers include built-in styles so HTML looks readable by default.

Correct. Browsers include built-in styles so HTML looks readable by default.

The spacing is only visible in DevTools, not on real webpages.

##### Incorrect, give it another shot!

Incorrect. DevTools can show the spacing clearly, but the spacing exists in normal page rendering too.

Incorrect. DevTools can show the spacing clearly, but the spacing exists in normal page rendering too.

## 📝 Quiz

**Quiz: You open a brand-new HTML page (no CSS file linked) and the heading still has space around it. Where is that spacing coming from?**

⬚ A. A:HTML automatically adds margins and padding.
⬚ B. HTML automatically adds margins and padding.
⬚ C. B:Your CSS file is being loaded automatically by the browser.
⬚ D. Your CSS file is being loaded automatically by the browser.
⬚ E. C:The browser's default stylesheet (user agent styles) is applying built-in margins/padding.
⬚ F. The browser's default stylesheet (user agent styles) is applying built-in margins/padding.
⬚ G. D:The spacing is only visible in DevTools, not on real webpages.
⬚ H. The spacing is only visible in DevTools, not on real webpages.

