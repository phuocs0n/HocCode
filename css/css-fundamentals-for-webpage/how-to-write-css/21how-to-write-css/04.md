Quiz: Which of the following is a valid CSS code?

Tap an option below to answer:

```css
h1 {
    color: blue
    font-size: 24px;
}
```

##### Incorrect, give it another shot!

A semicolon is missing after color: blue. Semicolons separate CSS properties.

A semicolon is missing after color: blue. Semicolons separate CSS properties.

```css
h1 {
    color, font-size: blue, 24px;
}
```

##### Incorrect, give it another shot!

This is the wrong syntax for CSS properties. Each property should be assigned a value after a colon (:).

This is the wrong syntax for CSS properties. Each property should be assigned a value after a colon (:).

```css
h1 {
    color: blue;
    font-size: 24px;
}
```

##### Correct Answer!

In CSS, multiple declarations are separated by semicolons. This rule correctly sets the color and font size for the <h1> element.

In CSS, multiple declarations are separated by semicolons. This rule correctly sets the color and font size for the <h1> element.

```css
h1
    color: blue;
    font-size: 24px;
```

##### Incorrect, give it another shot!

The opening and closing curly braces are missing around the CSS properties.

The opening and closing curly braces are missing around the CSS properties.

## 📝 Quiz

**Quiz: Which of the following is a valid CSS code?**

⬚ A. A:h1 {
    color: blue
    font-size: 24px;
}
⬚ B. h1 {
    color: blue
    font-size: 24px;
}
⬚ C. B:h1 {
    color, font-size: blue, 24px;
}
⬚ D. h1 {
    color, font-size: blue, 24px;
}
⬚ E. C:h1 {
    color: blue;
    font-size: 24px;
}
⬚ F. h1 {
    color: blue;
    font-size: 24px;
}
⬚ G. D:h1
    color: blue;
    font-size: 24px;
⬚ H. h1
    color: blue;
    font-size: 24px;

