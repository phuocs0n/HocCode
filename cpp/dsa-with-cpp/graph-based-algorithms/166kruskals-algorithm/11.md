Quiz: What is the primary goal of Kruskal's algorithm?

Tap an option below to answer:

Finding the shortest path between two vertices.

##### Incorrect, give it another shot!

Kruskal's algorithm isn't designed to find the shortest path between two specific vertices.

Kruskal's algorithm isn't designed to find the shortest path between two specific vertices.

Finding a minimum spanning tree of a graph.

##### Correct Answer!

Kruskal's algorithm seeks to find a subgraph that touches all the vertices without forming any cycles and does so with the least total edge weight.

Kruskal's algorithm seeks to find a subgraph that touches all the vertices without forming any cycles and does so with the least total edge weight.

Finding strongly connected components in a directed graph.

##### Incorrect, give it another shot!

Kruskal's algorithm isn't designed to find the strongly connected components in a graph.

Kruskal's algorithm isn't designed to find the strongly connected components in a graph.

Finding the maximum flow in a network.

##### Incorrect, give it another shot!

Kruskal's algorithm isn't designed to find the maximum flow in a network.

Kruskal's algorithm isn't designed to find the maximum flow in a network.

## 📝 Quiz

**Quiz: What is the primary goal of Kruskal's algorithm?**

⬚ A. A:Finding the shortest path between two vertices.
⬚ B. Finding the shortest path between two vertices.
⬚ C. B:Finding a minimum spanning tree of a graph.
⬚ D. Finding a minimum spanning tree of a graph.
⬚ E. C:Finding strongly connected components in a directed graph.
⬚ F. Finding strongly connected components in a directed graph.
⬚ G. D:Finding the maximum flow in a network.
⬚ H. Finding the maximum flow in a network.

