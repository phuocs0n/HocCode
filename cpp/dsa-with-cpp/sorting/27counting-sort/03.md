Quiz: What is the primary purpose of the counting vector in counting sort?

Tap an option below to answer:

To store the original vector.

##### Incorrect, give it another shot!

The counting vector doesn't contain the items of the original vector but their frequency of occurrence within the vector.

The counting vector doesn't contain the items of the original vector but their frequency of occurrence within the vector.

To replace the original vector.

##### Incorrect, give it another shot!

The counting vector doesn't replace the original vector. It's a new vector with the frequency of occurrence of items in the original vector.

The counting vector doesn't replace the original vector. It's a new vector with the frequency of occurrence of items in the original vector.

To store the maximum element of the vector.

##### Incorrect, give it another shot!

The counting vector doesn't just store the maximum element. It stores all the elements starting from 0 to the maximum element; it has a size of max + 1.

The counting vector doesn't just store the maximum element. It stores all the elements starting from 0 to the maximum element; it has a size of max + 1.

To record the occurrence of each element in the original vector.

##### Correct Answer!

The counting vector is used to keep track of the occurrence of each element from the original vector.

The counting vector is used to keep track of the occurrence of each element from the original vector.

## 📝 Quiz

**Quiz: What is the primary purpose of the counting vector in counting sort?**

⬚ A. A:To store the original vector.
⬚ B. To store the original vector.
⬚ C. B:To replace the original vector.
⬚ D. To replace the original vector.
⬚ E. C:To store the maximum element of the vector.
⬚ F. To store the maximum element of the vector.
⬚ G. D:To record the occurrence of each element in the original vector.
⬚ H. To record the occurrence of each element in the original vector.

