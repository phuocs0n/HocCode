Quiz: Which of the following represents a min-heap?

Tap an option below to answer:

Each child is smaller than or equal to its parent.

##### Incorrect, give it another shot!

This describes a max-heap.

This describes a max-heap.

Each child is greater than or equal to its parent.

##### Correct Answer!

This describes a min-heap.

This describes a min-heap.

The nodes in the heap are sorted in ascending order.

##### Incorrect, give it another shot!

The nodes in a min-heap are not necessarily sorted in ascending order.

The nodes in a min-heap are not necessarily sorted in ascending order.

The nodes in the heap are sorted in descending order.

##### Incorrect, give it another shot!

The nodes in a min-heap are not sorted in descending order.

The nodes in a min-heap are not sorted in descending order.

## 📝 Quiz

**Quiz: Which of the following represents a min-heap?**

⬚ A. A:Each child is smaller than or equal to its parent.
⬚ B. Each child is smaller than or equal to its parent.
⬚ C. B:Each child is greater than or equal to its parent.
⬚ D. Each child is greater than or equal to its parent.
⬚ E. C:The nodes in the heap are sorted in ascending order.
⬚ F. The nodes in the heap are sorted in ascending order.
⬚ G. D:The nodes in the heap are sorted in descending order.
⬚ H. The nodes in the heap are sorted in descending order.

