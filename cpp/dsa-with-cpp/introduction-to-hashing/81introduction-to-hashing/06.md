Quiz: Suppose we have a hash function, H(x) = (x - 5) mod 10.

If we apply this function to the keys: 11, 12, and 20, what hash values will we get?

Tap an option below to answer:

6, 7 and 5

##### Correct Answer!

The hash value of 11 will be 6.The hash value of 12 will be 7.The hash value of 20 will be 5.

The hash value of 11 will be 6.

The hash value of 12 will be 7.

The hash value of 20 will be 5.

1, 2 and 0

##### Incorrect, give it another shot!

5 is subtracted from each key before the mod takes place.

5 is subtracted from each key before the mod takes place.

6, 7 and 15

##### Incorrect, give it another shot!

The hash function mods the key by 10 after subtraction.

The hash function mods the key by 10 after subtraction.

11, 12 and 20

##### Incorrect, give it another shot!

The hash values and keys are not the same.

The hash values and keys are not the same.

##### Correct Answer!

The hash value of 11 will be 6.The hash value of 12 will be 7.The hash value of 20 will be 5.

The hash value of 11 will be 6.

The hash value of 12 will be 7.

The hash value of 20 will be 5.

##### Incorrect, give it another shot!

5 is subtracted from each key before the mod takes place.

5 is subtracted from each key before the mod takes place.

##### Incorrect, give it another shot!

The hash function mods the key by 10 after subtraction.

The hash function mods the key by 10 after subtraction.

##### Incorrect, give it another shot!

The hash values and keys are not the same.

The hash values and keys are not the same.

## 📝 Quiz

**Quiz: Suppose we have a hash function, H(x) = (x - 5) mod 10.**

⬚ A. A:6, 7 and 5
⬚ B. 6, 7 and 5
⬚ C. B:1, 2 and 0
⬚ D. 1, 2 and 0
⬚ E. C:6, 7 and 15
⬚ F. 6, 7 and 15
⬚ G. D:11, 12 and 20
⬚ H. 11, 12 and 20

