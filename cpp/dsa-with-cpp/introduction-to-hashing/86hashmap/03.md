Quiz: Which of the following is true about a hashmap?

Tap an option below to answer:

A hashmap is a type of data structure that is always sorted.

##### Incorrect, give it another shot!

Typically, hashmaps are not sorted in any order.

Typically, hashmaps are not sorted in any order.

Hashmaps support faster lookups, insertion, and deletion.

##### Correct Answer!

Lookups, insertion, and deletion in hashmaps are indeed faster.

Lookups, insertion, and deletion in hashmaps are indeed faster.

Hashmaps are like arrays; they are used to store a collection of values.

##### Incorrect, give it another shot!

Hashmaps store values as a key value pair; they are totally different from arrays.

Hashmaps store values as a key value pair; they are totally different from arrays.

Hashmaps are poor at handling data collision, so they have high chances of data corruption.

##### Incorrect, give it another shot!

Hashmaps handle collisions excellently, thus preventing the risk of data corruption.

Hashmaps handle collisions excellently, thus preventing the risk of data corruption.

## 📝 Quiz

**Quiz: Which of the following is true about a hashmap?**

⬚ A. A:A hashmap is a type of data structure that is always sorted.
⬚ B. A hashmap is a type of data structure that is always sorted.
⬚ C. B:Hashmaps support faster lookups, insertion, and deletion.
⬚ D. Hashmaps support faster lookups, insertion, and deletion.
⬚ E. C:Hashmaps are like arrays; they are used to  store a collection of values.
⬚ F. Hashmaps are like arrays; they are used to  store a collection of values.
⬚ G. D:Hashmaps are poor at handling data collision, so they have high chances of data corruption.
⬚ H. Hashmaps are poor at handling data collision, so they have high chances of data corruption.

