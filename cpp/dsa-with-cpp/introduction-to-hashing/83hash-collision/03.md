Quiz: What is a collision in hashing?

Tap an option below to answer:

When a hash function fails to produce an output.

##### Incorrect, give it another shot!

Generally, we choose a hash function in such a way that applying the hash function doesn't result in an error.

Generally, we choose a hash function in such a way that applying the hash function doesn't result in an error.

When two different keys produce the same hash output.

##### Correct Answer!

Collisions occur when different keys produce the same hash code in a hash function.

Collisions occur when different keys produce the same hash code in a hash function.

When a hash table becomes too large to handle.

##### Incorrect, give it another shot!

Collisions are not directly related to the size of the hash table, though a larger hash table typically has a lower probability of collision.

Collisions are not directly related to the size of the hash table, though a larger hash table typically has a lower probability of collision.

When a hash function produces negative values.

##### Incorrect, give it another shot!

Collisions are not related to the sign of the hash code produced by the function.

Collisions are not related to the sign of the hash code produced by the function.

## 📝 Quiz

**Quiz: What is a collision in hashing?**

⬚ A. A:When a hash function fails to produce an output.
⬚ B. When a hash function fails to produce an output.
⬚ C. B:When two different keys produce the same hash output.
⬚ D. When two different keys produce the same hash output.
⬚ E. C:When a hash table becomes too large to handle.
⬚ F. When a hash table becomes too large to handle.
⬚ G. D:When a hash function produces negative values.
⬚ H. When a hash function produces negative values.

