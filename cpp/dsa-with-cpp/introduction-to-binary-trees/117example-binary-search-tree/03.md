Quiz: Given the property of BST (left child < root < right child), performing __________ Traversal on BST returns elements in sorted order.

Tap an option below to answer:

Preorder

##### Incorrect, give it another shot!

The values of elements in a BST are in the order: right > root > left. Since preorder traversal returns the values in root-left-right manner, the elements are not sorted.

The values of elements in a BST are in the order: right > root > left. Since preorder traversal returns the values in root-left-right manner, the elements are not sorted.

Postorder

##### Incorrect, give it another shot!

The values of elements in a BST are in the order: right > root > left. Since postorder traversal returns the values in left-right-root manner, the elements are not sorted.

The values of elements in a BST are in the order: right > root > left. Since postorder traversal returns the values in left-right-root manner, the elements are not sorted.

Inorder

##### Correct Answer!

Inorder traversal visits the BST in left-root-right manner, returning elements in ascending order.

Inorder traversal visits the BST in left-root-right manner, returning elements in ascending order.

Level Order

##### Incorrect, give it another shot!

Since level order traversal returns elements level by level, the elements are not sorted in ascending order.

Since level order traversal returns elements level by level, the elements are not sorted in ascending order.

##### Incorrect, give it another shot!

The values of elements in a BST are in the order: right > root > left. Since preorder traversal returns the values in root-left-right manner, the elements are not sorted.

The values of elements in a BST are in the order: right > root > left. Since preorder traversal returns the values in root-left-right manner, the elements are not sorted.

##### Incorrect, give it another shot!

The values of elements in a BST are in the order: right > root > left. Since postorder traversal returns the values in left-right-root manner, the elements are not sorted.

The values of elements in a BST are in the order: right > root > left. Since postorder traversal returns the values in left-right-root manner, the elements are not sorted.

##### Correct Answer!

Inorder traversal visits the BST in left-root-right manner, returning elements in ascending order.

Inorder traversal visits the BST in left-root-right manner, returning elements in ascending order.

##### Incorrect, give it another shot!

Since level order traversal returns elements level by level, the elements are not sorted in ascending order.

Since level order traversal returns elements level by level, the elements are not sorted in ascending order.

## 📝 Quiz

**Quiz: Given the property of BST (left child < root < right child), performing __________ Traversal on BST returns elements in sorted order.**

⬚ A. A:Preorder
⬚ B. Preorder
⬚ C. B:Postorder
⬚ D. Postorder
⬚ E. C:Inorder
⬚ F. Inorder
⬚ G. D:Level Order
⬚ H. Level Order

