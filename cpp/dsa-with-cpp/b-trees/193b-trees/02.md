Quiz: Why is latency high for range queries in a B-tree?

Tap an option below to answer:

They require pointers to be rearranged frequently.

##### Incorrect, give it another shot!

Rearrangement of pointers is part of tree traversal. However, B-trees require multiple disk accesses due to data being stored in the primary and secondary memories.

Rearrangement of pointers is part of tree traversal. However, B-trees require multiple disk accesses due to data being stored in the primary and secondary memories.

The same node is accessed repeatedly.

##### Incorrect, give it another shot!

While a node might be accessed multiple times, the main reason behind high latency is the need to move data between the primary and secondary memories.

While a node might be accessed multiple times, the main reason behind high latency is the need to move data between the primary and secondary memories.

Data is transferred frequently between the primary and secondary memories.

##### Correct Answer!

Accessing data in B-trees requires frequent disk transfers between the primary and secondary memories, which increases the overall time required for the operation.

Accessing data in B-trees requires frequent disk transfers between the primary and secondary memories, which increases the overall time required for the operation.

All nodes of the tree are processed at once.

##### Incorrect, give it another shot!

B-tree operations are typically executed on individual nodes, not on all nodes at once.

B-tree operations are typically executed on individual nodes, not on all nodes at once.

## 📝 Quiz

**Quiz: Why is latency high for range queries in a B-tree?**

⬚ A. A:They require pointers to be rearranged frequently.
⬚ B. They require pointers to be rearranged frequently.
⬚ C. B:The same node is accessed repeatedly.
⬚ D. The same node is accessed repeatedly.
⬚ E. C:Data is transferred frequently between the primary and secondary memories.
⬚ F. Data is transferred frequently between the primary and secondary memories.
⬚ G. D:All nodes of the tree are processed at once.
⬚ H. All nodes of the tree are processed at once.

