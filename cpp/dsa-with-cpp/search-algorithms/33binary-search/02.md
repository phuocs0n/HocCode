Quiz: Why can't we use binary search on an unsorted vector?

Tap an option below to answer:

Because binary search relies on dividing the vector.

##### Incorrect, give it another shot!

While binary search involves dividing the vector, simply dividing isn't the reason binary search can't be used on an unsorted vector.

While binary search involves dividing the vector, simply dividing isn't the reason binary search can't be used on an unsorted vector.

Because binary search requires repeatedly comparing the middle element with the target.

##### Correct Answer!

Binary search repeatedly compares the middle element with the target and uses the sorted nature of the vector to eliminate half of the remaining elements.

Binary search repeatedly compares the middle element with the target and uses the sorted nature of the vector to eliminate half of the remaining elements.

Because the vector has too many elements.

##### Incorrect, give it another shot!

The number of elements in the vector is irrelevant to whether binary search can be applied.

The number of elements in the vector is irrelevant to whether binary search can be applied.

Because it's less efficient.

##### Incorrect, give it another shot!

Whatever its efficiency might be, binary search is simply not applicable for finding the target element in an unsorted vector.

Whatever its efficiency might be, binary search is simply not applicable for finding the target element in an unsorted vector.

## 📝 Quiz

**Quiz: Why can't we use binary search on an unsorted vector?**

⬚ A. A:Because binary search relies on dividing the vector.
⬚ B. Because binary search relies on dividing the vector.
⬚ C. B:Because binary search requires repeatedly comparing the middle element with the target.
⬚ D. Because binary search requires repeatedly comparing the middle element with the target.
⬚ E. C:Because the vector has too many elements.
⬚ F. Because the vector has too many elements.
⬚ G. D:Because it's less efficient.
⬚ H. Because it's less efficient.

