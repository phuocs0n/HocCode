Quiz: What is the correct way to create an array in C++?

Tap an option below to answer:

int numbers[4] = {3, 4, 5, 6};

##### Correct Answer!

This creates an integer array named numbers with 4 elements.

This creates an integer array named numbers with 4 elements.

int numbers[4] = [3, 4, 5, 6];

##### Incorrect, give it another shot!

We use the curly braces {} for array elements not square brackets.

We use the curly braces {} for array elements not square brackets.

int[4] numbers = {3, 4, 5, 6};

##### Incorrect, give it another shot!

[4] is placed after the array name.

[4] is placed after the array name.

int[4] numbers = [3, 4, 5, 6];

##### Incorrect, give it another shot!

[4] is placed after the array name and we use curly braces {} for array elements.

[4] is placed after the array name and we use curly braces {} for array elements.

## 📝 Quiz

**Quiz: What is the correct way to create an array in C++?**

⬚ A. A:int numbers[4] = {3, 4, 5, 6};
⬚ B. int numbers[4] = {3, 4, 5, 6};
⬚ C. B:int numbers[4] = [3, 4, 5, 6];
⬚ D. int numbers[4] = [3, 4, 5, 6];
⬚ E. C:int[4] numbers = {3, 4, 5, 6};
⬚ F. int[4] numbers = {3, 4, 5, 6};
⬚ G. D:int[4] numbers = [3, 4, 5, 6];
⬚ H. int[4] numbers = [3, 4, 5, 6];

