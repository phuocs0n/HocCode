Quiz: Which of the following is the correct way to take input of the x variable?

```cpp
int x;
```

Tap an option below to answer:

cin >> x;

##### Correct Answer!

Here, cin and >> takes input from the user and stores it in the x variable.

Here, cin and >> takes input from the user and stores it in the x variable.

cin << x;

##### Incorrect, give it another shot!

The << operator is used with cout to print the output.

The << operator is used with cout to print the output.

cout >> x;

##### Incorrect, give it another shot!

We use cout to print output to the screen.

We use cout to print output to the screen.

scanf("%d", &x);

##### Incorrect, give it another shot!

The scanf() function is used to take input value in C programming.

The scanf() function is used to take input value in C programming.

##### Correct Answer!

Here, cin and >> takes input from the user and stores it in the x variable.

Here, cin and >> takes input from the user and stores it in the x variable.

##### Incorrect, give it another shot!

The << operator is used with cout to print the output.

The << operator is used with cout to print the output.

##### Incorrect, give it another shot!

We use cout to print output to the screen.

We use cout to print output to the screen.

##### Incorrect, give it another shot!

The scanf() function is used to take input value in C programming.

The scanf() function is used to take input value in C programming.

## 📝 Quiz

**Quiz: Which of the following is the correct way to take input of the x variable?**

⬚ A. A:cin >> x;
⬚ B. cin >> x;
⬚ C. B:cin << x;
⬚ D. cin << x;
⬚ E. C:cout >> x;
⬚ F. cout >> x;
⬚ G. D:scanf("%d", &x);
⬚ H. scanf("%d", &x);

