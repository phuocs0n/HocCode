Quiz: Which value can be assigned to the test variable in the given case?

```cpp
int test;
```

Tap an option below to answer:

19

##### Correct Answer!

19 is an integer value that can be stored in the int variable.

19 is an integer value that can be stored in the int variable.

19.5

##### Incorrect, give it another shot!

19.5 is a decimal value which is not correct for the int variable.

19.5 is a decimal value which is not correct for the int variable.

"nineteen"

##### Incorrect, give it another shot!

"nineteen" is a textual value that cannot be stored in the int variable.

"nineteen" is a textual value that cannot be stored in the int variable.

"19"

##### Incorrect, give it another shot!

"19" is enclosed inside double quotes, so it is a textual value.

"19" is enclosed inside double quotes, so it is a textual value.

##### Correct Answer!

19 is an integer value that can be stored in the int variable.

19 is an integer value that can be stored in the int variable.

##### Incorrect, give it another shot!

19.5 is a decimal value which is not correct for the int variable.

19.5 is a decimal value which is not correct for the int variable.

##### Incorrect, give it another shot!

"nineteen" is a textual value that cannot be stored in the int variable.

"nineteen" is a textual value that cannot be stored in the int variable.

##### Incorrect, give it another shot!

"19" is enclosed inside double quotes, so it is a textual value.

"19" is enclosed inside double quotes, so it is a textual value.

## 📝 Quiz

**Quiz: Which value can be assigned to the test variable in the given case?**

⬚ A. A:19
⬚ B. 19
⬚ C. B:19.5
⬚ D. 19.5
⬚ E. C:"nineteen"
⬚ F. "nineteen"
⬚ G. D:"19"
⬚ H. "19"

