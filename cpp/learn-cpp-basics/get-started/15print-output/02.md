Quiz: What is the output of the following code?

```cpp
#include <iostream>
using namespace std;

int main() {
    
    int number = 18;
    cout << "Number is " << number;
    
    return 0;
}
```

Tap an option below to answer:

Number is number

##### Incorrect, give it another shot!

The print statement prints the string Number is and the value of the number variable.

The print statement prints the string Number is and the value of the number variable.

Number is 18

##### Correct Answer!

The print statement prints the string Number is and 18 (the value of the number variable).

The print statement prints the string Number is and 18 (the value of the number variable).

Number is

18

##### Incorrect, give it another shot!

We have used the single print statement to print the string Number is and the variable number without endl. So, they won't be printed in two different lines.

We have used the single print statement to print the string Number is and the variable number without endl. So, they won't be printed in two different lines.

Error

##### Incorrect, give it another shot!

The above code runs without any error.

The above code runs without any error.

## 📝 Quiz

**Quiz: What is the output of the following code?**

⬚ A. A:Number is number
⬚ B. Number is number
⬚ C. B:Number is 18
⬚ D. Number is 18
⬚ E. C:Number is18
⬚ F. Number is18
⬚ G. D:Error
⬚ H. Error

