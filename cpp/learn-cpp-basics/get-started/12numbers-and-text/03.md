Quiz: 0.0 is a ___?

Tap an option below to answer:

double

##### Correct Answer!

0.0 is a floating-point number.

0.0 is a floating-point number.

int

##### Incorrect, give it another shot!

0.0 is not an integer. Integers don't have decimal parts.

0.0 is not an integer. Integers don't have decimal parts.

string

##### Incorrect, give it another shot!

0.0 is not a string. Strings are wrapped inside double quotation marks.

0.0 is not a string. Strings are wrapped inside double quotation marks.

None of the above

##### Incorrect, give it another shot!

One of the above answers is correct.

One of the above answers is correct.

##### Correct Answer!

0.0 is a floating-point number.

0.0 is a floating-point number.

##### Incorrect, give it another shot!

0.0 is not an integer. Integers don't have decimal parts.

0.0 is not an integer. Integers don't have decimal parts.

##### Incorrect, give it another shot!

0.0 is not a string. Strings are wrapped inside double quotation marks.

0.0 is not a string. Strings are wrapped inside double quotation marks.

##### Incorrect, give it another shot!

One of the above answers is correct.

One of the above answers is correct.

## 📝 Quiz

**Quiz: 0.0 is a ___?**

⬚ A. A:double
⬚ B. double
⬚ C. B:int
⬚ D. int
⬚ E. C:string
⬚ F. string
⬚ G. D:None of the above
⬚ H. None of the above

