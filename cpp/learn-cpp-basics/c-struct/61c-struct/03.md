Quiz: What is a struct in C++?

Tap an option below to answer:

Collection of functions

##### Incorrect, give it another shot!

A struct can contain variables and arrays as well.

A struct can contain variables and arrays as well.

Collection of arrays

##### Incorrect, give it another shot!

A struct can contain variables and functions as well.

A struct can contain variables and functions as well.

Collection of variables, functions, and arrays

##### Correct Answer!

A struct can contain variables, functions, and arrays of all types.

A struct can contain variables, functions, and arrays of all types.

Collection of same types of variables and arrays

##### Incorrect, give it another shot!

A struct can contain variables and arrays of all types.

A struct can contain variables and arrays of all types.

## 📝 Quiz

**Quiz: What is a struct in C++?**

⬚ A. A:Collection of functions
⬚ B. Collection of functions
⬚ C. B:Collection of arrays
⬚ D. Collection of arrays
⬚ E. C:Collection of variables, functions, and arrays
⬚ F. Collection of variables, functions, and arrays
⬚ G. D:Collection of same types of variables and arrays
⬚ H. Collection of same types of variables and arrays

