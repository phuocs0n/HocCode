Quiz: What keyword do we use to create a boolean type variable?

Tap an option below to answer:

Boolean

##### Incorrect, give it another shot!

There is no keyword named Boolean.

There is no keyword named Boolean.

boolean

##### Incorrect, give it another shot!

boolean is the data type, not a keyword.

boolean is the data type, not a keyword.

bool

##### Correct Answer!

bool is used to create boolean type variables.

bool is used to create boolean type variables.

Bool

##### Incorrect, give it another shot!

C++ is case-sensitive, so Bool is not the same as bool.

C++ is case-sensitive, so Bool is not the same as bool.

##### Incorrect, give it another shot!

There is no keyword named Boolean.

There is no keyword named Boolean.

##### Incorrect, give it another shot!

boolean is the data type, not a keyword.

boolean is the data type, not a keyword.

##### Correct Answer!

bool is used to create boolean type variables.

bool is used to create boolean type variables.

##### Incorrect, give it another shot!

C++ is case-sensitive, so Bool is not the same as bool.

C++ is case-sensitive, so Bool is not the same as bool.

## 📝 Quiz

**Quiz: What keyword do we use to create a boolean type variable?**

⬚ A. A:Boolean
⬚ B. Boolean
⬚ C. B:boolean
⬚ D. boolean
⬚ E. C:bool
⬚ F. bool
⬚ G. D:Bool
⬚ H. Bool

