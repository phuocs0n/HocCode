Quiz: What will be the output of the following code?

```cpp
#include <iostream>
using namespace std;

void find_product() {
    int result = 4 * 9;
}

int main() {

    cout << result;

    return 0;
}
```

Tap an option below to answer:

36

##### Incorrect, give it another shot!

The result variable inside the main() function is not defined. Remember, the int result = 4 * 9; is local to the find_product() function only. It doesn't exist outside the function.

The result variable inside the main() function is not defined. Remember, the int result = 4 * 9; is local to the find_product() function only. It doesn't exist outside the function.

(Error)

##### Correct Answer!

The result variable is not defined. It only exists inside of the find_product() function.

The result variable is not defined. It only exists inside of the find_product() function.

result

##### Incorrect, give it another shot!

We are printing the result variable not the text "result".

We are printing the result variable not the text "result".

None of the above

##### Incorrect, give it another shot!

One of the options is correct

One of the options is correct

##### Incorrect, give it another shot!

The result variable inside the main() function is not defined. Remember, the int result = 4 * 9; is local to the find_product() function only. It doesn't exist outside the function.

The result variable inside the main() function is not defined. Remember, the int result = 4 * 9; is local to the find_product() function only. It doesn't exist outside the function.

##### Correct Answer!

The result variable is not defined. It only exists inside of the find_product() function.

The result variable is not defined. It only exists inside of the find_product() function.

##### Incorrect, give it another shot!

We are printing the result variable not the text "result".

We are printing the result variable not the text "result".

##### Incorrect, give it another shot!

One of the options is correct

One of the options is correct

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:36
⬚ B. 36
⬚ C. B:(Error)
⬚ D. (Error)
⬚ E. C:result
⬚ F. result
⬚ G. D:None of the above
⬚ H. None of the above

