Quiz: Which C++ header file has the definition of the sqrt() function?

Tap an option below to answer:

iostream

##### Incorrect, give it another shot!

The sqrt() function isn't defined in the iostream header file.

The sqrt() function isn't defined in the iostream header file.

stdio

##### Incorrect, give it another shot!

The stdio header contains the standard input/output functions for the C programming language.

The stdio header contains the standard input/output functions for the C programming language.

cmath

##### Correct Answer!

The C++ sqrt() function is defined in the cmath header file.

The C++ sqrt() function is defined in the cmath header file.

math

##### Incorrect, give it another shot!

The math file is a C language header file that cannot be used in C++.

The math file is a C language header file that cannot be used in C++.

##### Incorrect, give it another shot!

The sqrt() function isn't defined in the iostream header file.

The sqrt() function isn't defined in the iostream header file.

##### Incorrect, give it another shot!

The stdio header contains the standard input/output functions for the C programming language.

The stdio header contains the standard input/output functions for the C programming language.

##### Correct Answer!

The C++ sqrt() function is defined in the cmath header file.

The C++ sqrt() function is defined in the cmath header file.

##### Incorrect, give it another shot!

The math file is a C language header file that cannot be used in C++.

The math file is a C language header file that cannot be used in C++.

## 📝 Quiz

**Quiz: Which C++ header file has the definition of the sqrt() function?**

⬚ A. A:iostream
⬚ B. iostream
⬚ C. B:stdio
⬚ D. stdio
⬚ E. C:cmath
⬚ F. cmath
⬚ G. D:math
⬚ H. math

