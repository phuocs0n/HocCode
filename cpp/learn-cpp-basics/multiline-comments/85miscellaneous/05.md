Quiz: What will be the output of the following code?

```cpp
#include <iostream>
using namespace std;

int main() {

    int n = 10;
    if (n == 10 && 100) {
        cout << "Hello";
    }

    return 0;
}
```

Tap an option below to answer:

(no output)

##### Incorrect, give it another shot!

Here, n == 0 && 100 evaluates to true because both n == 10 and 100 evaluates to true. Hence, the body of the if statement is executed.

Here, n == 0 && 100 evaluates to true because both n == 10 and 100 evaluates to true. Hence, the body of the if statement is executed.

(error code)

##### Incorrect, give it another shot!

The program is valid and will not cause any errors.

The program is valid and will not cause any errors.

Hello

Hello

##### Incorrect, give it another shot!

We have printed Hello only once if the test condition is true.

We have printed Hello only once if the test condition is true.

Hello

##### Correct Answer!

Here, n == 10 && 100 evaluates to true because both n == 10 and 100 evaluates to true.

Here, n == 10 && 100 evaluates to true because both n == 10 and 100 evaluates to true.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:(no output)
⬚ B. (no output)
⬚ C. B:(error code)
⬚ D. (error code)
⬚ E. C:HelloHello
⬚ F. HelloHello
⬚ G. D:Hello
⬚ H. Hello

