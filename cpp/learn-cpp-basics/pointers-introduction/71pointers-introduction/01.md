Quiz: What might be the output of the following code?

```cpp
#include <iostream>
using namespace std;

int main() {
    double number = 9.56;
    cout << &number;
    return 0;
}
```

Tap an option below to answer:

9.56

##### Incorrect, give it another shot!

&number doesn't return the value of the number variable.

&number doesn't return the value of the number variable.

0x7ffce674ed24

##### Correct Answer!

&number returns the memory address of the number variable.

&number returns the memory address of the number variable.

9.56

0x7ffce674ed24

##### Incorrect, give it another shot!

We have only used one cout statement. So there can only be one line of output.

We have only used one cout statement. So there can only be one line of output.

Error

##### Incorrect, give it another shot!

The above code runs without any errors.

The above code runs without any errors.

## 📝 Quiz

**Quiz: What might be the output of the following code?**

⬚ A. A:9.56
⬚ B. 9.56
⬚ C. B:0x7ffce674ed24
⬚ D. 0x7ffce674ed24
⬚ E. C:9.560x7ffce674ed24
⬚ F. 9.560x7ffce674ed24
⬚ G. D:Error
⬚ H. Error

