Quiz: What is the use of the delete operator?

Tap an option below to answer:

To allocate memory dynamically

##### Incorrect, give it another shot!

The new operator is used to dynamically allocate memory.

The new operator is used to dynamically allocate memory.

To create a resizable array

##### Incorrect, give it another shot!

The concept of dynamic memory allocation is used to create a resizable array.

The concept of dynamic memory allocation is used to create a resizable array.

To free the dynamically allocated memory.

##### Correct Answer!

The delete operator is used to free the dynamically allocated memory once it is used.

The delete operator is used to free the dynamically allocated memory once it is used.

All of the above

##### Incorrect, give it another shot!

Only one of the above options is the correct answer.

Only one of the above options is the correct answer.

## 📝 Quiz

**Quiz: What is the use of the delete operator?**

⬚ A. A:To allocate memory dynamically
⬚ B. To allocate memory dynamically
⬚ C. B:To create a resizable array
⬚ D. To create a resizable array
⬚ E. C:To free the dynamically allocated memory.
⬚ F. To free the dynamically allocated memory.
⬚ G. D:All of the above
⬚ H. All of the above

