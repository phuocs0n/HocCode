Quiz: What is the correct way to call the following function?

```cpp
void func(int* pt) {
    ....
}
```

Tap an option below to answer:

int number = 25;

func(number);

##### Incorrect, give it another shot!

The function takes the address of the number variable as its argument, denoted by &number.

The function takes the address of the number variable as its argument, denoted by &number.

int number = 25;

func(*number);

##### Incorrect, give it another shot!

*number doesn't represent the address of the number variable.

*number doesn't represent the address of the number variable.

int number = 25;

func(&number);

##### Correct Answer!

The function takes the address of the number variable as an argument, denoted by &number.

The function takes the address of the number variable as an argument, denoted by &number.

None of the above

##### Incorrect, give it another shot!

One of the above options is the correct answer.

One of the above options is the correct answer.

## 📝 Quiz

**Quiz: What is the correct way to call the following function?**

⬚ A. A:int number = 25;func(number);
⬚ B. int number = 25;func(number);
⬚ C. B:int number = 25;func(*number);
⬚ D. int number = 25;func(*number);
⬚ E. C:int number = 25;func(&number);
⬚ F. int number = 25;func(&number);
⬚ G. D:None of the above
⬚ H. None of the above

