Quiz: What will be the output of the following code?

```cpp
#include <iostream>
using namespace std;

void greet() {
    string greeting = "Hello";  
}

int main() {

    cout << greeting;
    return 0;
}
```

Tap an option below to answer:

Hello

##### Incorrect, give it another shot!

The greeting variable is local to the greet() function. So, it doesn't exist outside that function.

The greeting variable is local to the greet() function. So, it doesn't exist outside that function.

(Error)

##### Correct Answer!

The greeting variable is not defined outside the greet() function. So, printing it outside will result in an error.

The greeting variable is not defined outside the greet() function. So, printing it outside will result in an error.

greeting

##### Incorrect, give it another shot!

We have printed the greeting variable, not the "greeting" string.

We have printed the greeting variable, not the "greeting" string.

Empty Output

##### Incorrect, give it another shot!

The program does not compile since the greeting variable can only be used inside the greet() function. Thus, we won't get any output—empty or otherwise.

The program does not compile since the greeting variable can only be used inside the greet() function. Thus, we won't get any output—empty or otherwise.

##### Incorrect, give it another shot!

The greeting variable is local to the greet() function. So, it doesn't exist outside that function.

The greeting variable is local to the greet() function. So, it doesn't exist outside that function.

##### Correct Answer!

The greeting variable is not defined outside the greet() function. So, printing it outside will result in an error.

The greeting variable is not defined outside the greet() function. So, printing it outside will result in an error.

##### Incorrect, give it another shot!

We have printed the greeting variable, not the "greeting" string.

We have printed the greeting variable, not the "greeting" string.

##### Incorrect, give it another shot!

The program does not compile since the greeting variable can only be used inside the greet() function. Thus, we won't get any output—empty or otherwise.

The program does not compile since the greeting variable can only be used inside the greet() function. Thus, we won't get any output—empty or otherwise.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:Hello
⬚ B. Hello
⬚ C. B:(Error)
⬚ D. (Error)
⬚ E. C:greeting
⬚ F. greeting
⬚ G. D:Empty Output
⬚ H. Empty Output

