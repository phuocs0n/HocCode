Quiz: What is the correct syntax to overload the binary - operator?

Tap an option below to answer:

Class_Name operator -- () {...}

##### Incorrect, give it another shot!

This is the syntax for overloading the prefix -- operator.

This is the syntax for overloading the prefix -- operator.

Class_Name operator -- (int) {...}

##### Incorrect, give it another shot!

This is the syntax for overloading the postfix -- operator.

This is the syntax for overloading the postfix -- operator.

Class_Name operator - (Class_Name obj) {...}

##### Correct Answer!

This is the correct syntax for overloading the binary - operator.

This is the correct syntax for overloading the binary - operator.

Class_Name operator - () {...}

##### Incorrect, give it another shot!

We must supply a parameter to the operator() function when overloading a binary operator.

We must supply a parameter to the operator() function when overloading a binary operator.

## 📝 Quiz

**Quiz: What is the correct syntax to overload the binary - operator?**

⬚ A. A:Class_Name operator -- () {...}
⬚ B. Class_Name operator -- () {...}
⬚ C. B:Class_Name operator -- (int) {...}
⬚ D. Class_Name operator -- (int) {...}
⬚ E. C:Class_Name operator - (Class_Name obj) {...}
⬚ F. Class_Name operator - (Class_Name obj) {...}
⬚ G. D:Class_Name operator - () {...}
⬚ H. Class_Name operator - () {...}

