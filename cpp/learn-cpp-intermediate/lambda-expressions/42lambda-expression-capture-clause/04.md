Quiz: Which of the following statements is true for lambda capture clause by value?

Tap an option below to answer:

We can change the value of the captured variable.

##### Incorrect, give it another shot!

We cannot change the value of the captured variable when capturing by value.

We cannot change the value of the captured variable when capturing by value.

We pass the address of the variable we want to capture.

##### Incorrect, give it another shot!

False. When capturing by value, we pass a copy of the value of the variable we want to capture, not its address.

False. When capturing by value, we pass a copy of the value of the variable we want to capture, not its address.

We pass a copy of the value of the variable we want to capture.

##### Correct Answer!

This statement is true.

This statement is true.

All of the above

##### Incorrect, give it another shot!

Only one of the above statements is true.

Only one of the above statements is true.

## 📝 Quiz

**Quiz: Which of the following statements is true for lambda capture clause by value?**

⬚ A. A:We can change the value of the captured variable.
⬚ B. We can change the value of the captured variable.
⬚ C. B:We pass the address of the variable we want to capture.
⬚ D. We pass the address of the variable we want to capture.
⬚ E. C:We pass a copy of the value of the variable we want to capture.
⬚ F. We pass a copy of the value of the variable we want to capture.
⬚ G. D:All of the above
⬚ H. All of the above

