Quiz: Which of the following statements is correct?

Tap an option below to answer:

We put the code that might cause an exception inside the try block.

##### Correct Answer!

This statement is correct.

This statement is correct.

It is compulsory to use the throw statement when handling exceptions.

##### Incorrect, give it another shot!

We can skip the throw statement in some selected cases.

We can skip the throw statement in some selected cases.

We need to supply a parameter to the try block.

##### Incorrect, give it another shot!

The try block doesn't take arguments.

The try block doesn't take arguments.

We put the code that might cause an exception inside the catch block.

##### Incorrect, give it another shot!

We put the code to handle the exception in the catch block.

We put the code to handle the exception in the catch block.

## 📝 Quiz

**Quiz: Which of the following statements is correct?**

⬚ A. A:We put the code that might cause an exception inside the try block.
⬚ B. We put the code that might cause an exception inside the try block.
⬚ C. B:It is compulsory to use the throw statement when handling exceptions.
⬚ D. It is compulsory to use the throw statement when handling exceptions.
⬚ E. C:We need to supply a parameter to the try block.
⬚ F. We need to supply a parameter to the try block.
⬚ G. D:We put the code that might cause an exception inside the catch block.
⬚ H. We put the code that might cause an exception inside the catch block.

