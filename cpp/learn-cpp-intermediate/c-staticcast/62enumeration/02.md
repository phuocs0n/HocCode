Quiz: What will be the output of the following code?

```cpp
enum names {Tim, Allen, Burton, Pool, Zahn} names1;

int main() {

    names1 = Pool;
    cout << names1;
 
    return 0;
}
```

Tap an option below to answer:

Pool

##### Incorrect, give it another shot!

enum variables store integer values. So printing them prints the stored integers.

enum variables store integer values. So printing them prints the stored integers.

##### Correct Answer!

Each enum element is assigned an integer value starting from 0 i.e. the first element is represented by 0, the second element is represented by 1, and so on.

Each enum element is assigned an integer value starting from 0 i.e. the first element is represented by 0, the second element is represented by 1, and so on.

##### Incorrect, give it another shot!

Each enum element is assigned an integer value starting from 0 i.e. the first element is represented by 0, the second element is represented by 1, and so on.

Each enum element is assigned an integer value starting from 0 i.e. the first element is represented by 0, the second element is represented by 1, and so on.

Error

##### Incorrect, give it another shot!

This code runs without any errors.

This code runs without any errors.

##### Incorrect, give it another shot!

enum variables store integer values. So printing them prints the stored integers.

enum variables store integer values. So printing them prints the stored integers.

##### Correct Answer!

Each enum element is assigned an integer value starting from 0 i.e. the first element is represented by 0, the second element is represented by 1, and so on.

Each enum element is assigned an integer value starting from 0 i.e. the first element is represented by 0, the second element is represented by 1, and so on.

##### Incorrect, give it another shot!

Each enum element is assigned an integer value starting from 0 i.e. the first element is represented by 0, the second element is represented by 1, and so on.

Each enum element is assigned an integer value starting from 0 i.e. the first element is represented by 0, the second element is represented by 1, and so on.

##### Incorrect, give it another shot!

This code runs without any errors.

This code runs without any errors.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:Pool
⬚ B. Pool
⬚ C. B:3
⬚ D. 3
⬚ E. C:4
⬚ F. 4
⬚ G. D:Error
⬚ H. Error

