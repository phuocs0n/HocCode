Quiz: Which of the following statements is false?

Tap an option below to answer:

A namespace is any scope inside a C++ program.

##### Correct Answer!

A namespace is not just any scope; it is actually a named scope inside a C++ program.

A namespace is not just any scope; it is actually a named scope inside a C++ program.

A namespace is a named scope inside a C++ program.

##### Incorrect, give it another shot!

This statement is true.

This statement is true.

A namespace can contain variables, arrays, functions, classes, etc.

##### Incorrect, give it another shot!

This statement is true.

This statement is true.

We can use the same name for multiple entities as long as they are in different scopes or namespaces.

##### Incorrect, give it another shot!

This statement is true.

This statement is true.

## 📝 Quiz

**Quiz: Which of the following statements is false?**

⬚ A. A:A namespace is any scope inside a C++ program.
⬚ B. A namespace is any scope inside a C++ program.
⬚ C. B:A namespace is a named scope inside a C++ program.
⬚ D. A namespace is a named scope inside a C++ program.
⬚ E. C:A namespace can contain variables, arrays,  functions, classes, etc.
⬚ F. A namespace can contain variables, arrays,  functions, classes, etc.
⬚ G. D:We can use the same name for multiple entities as long as they are in different scopes or namespaces.
⬚ H. We can use the same name for multiple entities as long as they are in different scopes or namespaces.

