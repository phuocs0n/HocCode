Quiz: Which of the following is an invalid way of opening a file in read mode?

Tap an option below to answer:

fstream my_file;

my_file.open("sample.txt");

##### Incorrect, give it another shot!

This is a valid C++ code that opens a file in read mode.

This is a valid C++ code that opens a file in read mode.

fstream my_file("sample.txt");

##### Incorrect, give it another shot!

This is a valid C++ code that opens a file in read mode.

This is a valid C++ code that opens a file in read mode.

ifstream my_file("sample.txt");

##### Incorrect, give it another shot!

This is a valid C++ code that opens a file in read mode.

This is a valid C++ code that opens a file in read mode.

fstream my_file;

my_file("sample.txt");

##### Correct Answer!

In order to avoid using the open() function, the file must be opened during the object declaration itself, and not after creating the object.

In order to avoid using the open() function, the file must be opened during the object declaration itself, and not after creating the object.

## 📝 Quiz

**Quiz: Which of the following is an invalid way of opening a file in read mode?**

⬚ A. A:fstream my_file;my_file.open("sample.txt");
⬚ B. fstream my_file;my_file.open("sample.txt");
⬚ C. B:fstream my_file("sample.txt");
⬚ D. fstream my_file("sample.txt");
⬚ E. C:ifstream my_file("sample.txt");
⬚ F. ifstream my_file("sample.txt");
⬚ G. D:fstream my_file;my_file("sample.txt");
⬚ H. fstream my_file;my_file("sample.txt");

