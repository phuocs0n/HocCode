Quiz: Which of the following arguments is used to open a file for read operations?

Tap an option below to answer:

ios::in

##### Correct Answer!

The ios::in argument is used for opening a file for read operations.

The ios::in argument is used for opening a file for read operations.

ios::out

##### Incorrect, give it another shot!

The ios::out argument is used for opening a file for write operations.

The ios::out argument is used for opening a file for write operations.

ios::app

##### Incorrect, give it another shot!

The ios::app argument is used for adding content to a file.

The ios::app argument is used for adding content to a file.

None of the above

##### Incorrect, give it another shot!

One of the above options is correct.

One of the above options is correct.

##### Correct Answer!

The ios::in argument is used for opening a file for read operations.

The ios::in argument is used for opening a file for read operations.

##### Incorrect, give it another shot!

The ios::out argument is used for opening a file for write operations.

The ios::out argument is used for opening a file for write operations.

##### Incorrect, give it another shot!

The ios::app argument is used for adding content to a file.

The ios::app argument is used for adding content to a file.

##### Incorrect, give it another shot!

One of the above options is correct.

One of the above options is correct.

## 📝 Quiz

**Quiz: Which of the following arguments is used to open a file for read operations?**

⬚ A. A:ios::in
⬚ B. ios::in
⬚ C. B:ios::out
⬚ D. ios::out
⬚ E. C:ios::app
⬚ F. ios::app
⬚ G. D:None of the above
⬚ H. None of the above

