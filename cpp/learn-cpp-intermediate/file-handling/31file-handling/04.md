Quiz: What header file is required to read and write files in C++?

Tap an option below to answer:

<fstream>

##### Correct Answer!

The <fstream> file is used to read from and write to a file.

The <fstream> file is used to read from and write to a file.

<ifstream>

##### Incorrect, give it another shot!

ifstream is a class inside the <fstream> file and is used to read from a file.

ifstream is a class inside the <fstream> file and is used to read from a file.

<ofstream>

##### Incorrect, give it another shot!

ofstream is a class inside the <fstream> file and is used to write to a file.

ofstream is a class inside the <fstream> file and is used to write to a file.

<iostream>

##### Incorrect, give it another shot!

The <iostream> file is used to handle basic input-output operations in C++. It is not used for file handling.

The <iostream> file is used to handle basic input-output operations in C++. It is not used for file handling.

##### Correct Answer!

The <fstream> file is used to read from and write to a file.

The <fstream> file is used to read from and write to a file.

##### Incorrect, give it another shot!

ifstream is a class inside the <fstream> file and is used to read from a file.

ifstream is a class inside the <fstream> file and is used to read from a file.

##### Incorrect, give it another shot!

ofstream is a class inside the <fstream> file and is used to write to a file.

ofstream is a class inside the <fstream> file and is used to write to a file.

##### Incorrect, give it another shot!

The <iostream> file is used to handle basic input-output operations in C++. It is not used for file handling.

The <iostream> file is used to handle basic input-output operations in C++. It is not used for file handling.

## 📝 Quiz

**Quiz: What header file is required to read and write files in C++?**

⬚ A. A:<fstream>
⬚ B. <fstream>
⬚ C. B:<ifstream>
⬚ D. <ifstream>
⬚ E. C:<ofstream>
⬚ F. <ofstream>
⬚ G. D:<iostream>
⬚ H. <iostream>

