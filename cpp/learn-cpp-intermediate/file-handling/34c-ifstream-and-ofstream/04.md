Quiz: Which of these following classes cannot be used to write to a file?

Tap an option below to answer:

fstream

##### Incorrect, give it another shot!

The fstream class can both read from and write to a file.

The fstream class can both read from and write to a file.

ofstream

##### Incorrect, give it another shot!

The ofstream class is used for writing to a file.

The ofstream class is used for writing to a file.

ifstream

##### Correct Answer!

The ifstream class is only used for reading from a file.

The ifstream class is only used for reading from a file.

None of the above

##### Incorrect, give it another shot!

One of the above options is correct.

One of the above options is correct.

##### Incorrect, give it another shot!

The fstream class can both read from and write to a file.

The fstream class can both read from and write to a file.

##### Incorrect, give it another shot!

The ofstream class is used for writing to a file.

The ofstream class is used for writing to a file.

##### Correct Answer!

The ifstream class is only used for reading from a file.

The ifstream class is only used for reading from a file.

##### Incorrect, give it another shot!

One of the above options is correct.

One of the above options is correct.

## 📝 Quiz

**Quiz: Which of these following classes cannot be used to write to a file?**

⬚ A. A:fstream
⬚ B. fstream
⬚ C. B:ofstream
⬚ D. ofstream
⬚ E. C:ifstream
⬚ F. ifstream
⬚ G. D:None of the above
⬚ H. None of the above

