Quiz: Suppose you want to copy the contents of the file connected to the file2 object to the one connected to the file1 object.

Assuming the files are opened in the correct modes, how would you copy the file?

Tap an option below to answer:

file1.rdbuf(file2);

##### Incorrect, give it another shot!

This is an invalid way of copying files in C++.

This is an invalid way of copying files in C++.

file2.rdbuf(file1);

##### Incorrect, give it another shot!

This is an invalid way of copying files in C++.

This is an invalid way of copying files in C++.

file2 << file1.rdbuf();

##### Incorrect, give it another shot!

This copies the contents of the file opened by file1 to the one opened by file2.

This copies the contents of the file opened by file1 to the one opened by file2.

file1 << file2.rdbuf();

##### Correct Answer!

This copies the contents of the file opened by file2 to the one opened by file1.

This copies the contents of the file opened by file2 to the one opened by file1.

##### Incorrect, give it another shot!

This is an invalid way of copying files in C++.

This is an invalid way of copying files in C++.

##### Incorrect, give it another shot!

This is an invalid way of copying files in C++.

This is an invalid way of copying files in C++.

##### Incorrect, give it another shot!

This copies the contents of the file opened by file1 to the one opened by file2.

This copies the contents of the file opened by file1 to the one opened by file2.

##### Correct Answer!

This copies the contents of the file opened by file2 to the one opened by file1.

This copies the contents of the file opened by file2 to the one opened by file1.

## 📝 Quiz

**Quiz: Suppose you want to copy the contents of the file connected to the file2 object to the one connected to the file1 object.**

⬚ A. A:file1.rdbuf(file2);
⬚ B. file1.rdbuf(file2);
⬚ C. B:file2.rdbuf(file1);
⬚ D. file2.rdbuf(file1);
⬚ E. C:file2 << file1.rdbuf();
⬚ F. file2 << file1.rdbuf();
⬚ G. D:file1 << file2.rdbuf();
⬚ H. file1 << file2.rdbuf();

