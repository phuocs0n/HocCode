# Extending the Project

#### Course: Learn C++ OOP

## Extending the Project

### Run the Game N Times

Suppose we want to run our game 5 times. We can do that using a for loop in the main() function.

```cpp
Game game;
    
for(int i = 1; i <= 5; ++i) {
      
    string user_pick = game.get_user_pick();
    string computer_pick = game.get_computer_pick();
    string result = game.get_result();

    cout << "User Pick: " << user_pick << endl;
    cout << "Computer Pick: " << computer_pick << endl;
    cout << "You " << result << endl;      
}
```

### Ask the User to Play the Game Again

Suppose we want to ask the user if they want to play the game again. Here's how we can do it.

```cpp
while (1) {

    Game game;
    char play_again;

    string user_pick = game.get_user_pick();
    string computer_pick = game.get_computer_pick();
    string result = game.get_result();

    cout << "User Pick: " << user_pick << endl;
    cout << "Computer Pick: " << computer_pick << endl;
    cout << "You " << result << endl;
     
    cout << "Do you want to play again? (y/n): ";      
    cin>> play_again;
      
    // if user enter any other character other than y, the game ends
    if (play_again != 'y') {
        break;
    }
}
```

Now, each time a game is completed, the user is asked whether they want to play the game again or not. If the user enters 'y', the game will run again because of the infinite loop.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


