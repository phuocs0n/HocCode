# Step 4: Make User Input a Valid String

#### Course: Learn C++ OOP

## Step 4: Make User Input a Valid String

### Project Changes

- change the get_user_pick() function to take valid input

### Function Objectives

The purpose of this function is to run an infinite loop that breaks only when the user provides a valid input.

```cpp
string get_user_pick() {

    char user_input;


    // get input until user enters 'r', 'p', or 's'
    while (1) {

        cout << "Enter Your Choice (r for rock, p for paper, s for scissors): ";
        cin >> user_input;

        // convert user_pick to lowercase
        user_input = tolower(user_input);

        // assign appropriate string to user_pick
        if (user_input == 'r') {
            user_pick = "rock";

            // terminate while loop
            break;
        }
        else if (user_input == 'p') {
            user_pick = "paper";

            // terminate while loop
            break;
        }
        else if (user_input == 's') {
            user_pick = "scissors";

            // terminate while loop
            break;
        }
    }


    return user_pick;
}
```

Inside the get_user_pick() Function

We have created an infinite while loop inside this function. Inside the loop, we have asked for user input.

After the user enters their choice, we have converted the input character to lowercase so that both uppercase and lowercase inputs are valid.

```cpp
user_input = tolower(user_input);
```

If this converted value is either 'r' or 'p' or 's', we assign the appropriate string to the user_pick variable. This action is followed by a break statement, which terminates the infinite while loop.

However, if the user input is invalid, the loop is continued and the user is asked for the input again.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


