Quiz: Which of the following is an invalid way of creating a constructor for the Student class?

Tap an option below to answer:

Student() {...}

##### Incorrect, give it another shot!

A constructor does not have a return type and must have the same name as the class. So this is a valid way of creating a constructor.

A constructor does not have a return type and must have the same name as the class. So this is a valid way of creating a constructor.

Student(int num) {...}

##### Incorrect, give it another shot!

A constructor does not have a return type, must have the same name as the class, and can have a parameter. So this is a valid way of creating a constructor.

A constructor does not have a return type, must have the same name as the class, and can have a parameter. So this is a valid way of creating a constructor.

Student(string text, char alpha) {...}

##### Incorrect, give it another shot!

A constructor does not have a return type, must have the same name as the class, and can have multiple parameters. So this is a valid way of creating a constructor.

A constructor does not have a return type, must have the same name as the class, and can have multiple parameters. So this is a valid way of creating a constructor.

None of the above

##### Correct Answer!

All the above codes are valid ways of creating a Student() constructor.

All the above codes are valid ways of creating a Student() constructor.

## 📝 Quiz

**Quiz: Which of the following is an invalid way of creating a constructor for the Student class?**

⬚ A. A:Student() {...}
⬚ B. Student() {...}
⬚ C. B:Student(int num) {...}
⬚ D. Student(int num) {...}
⬚ E. C:Student(string text, char alpha) {...}
⬚ F. Student(string text, char alpha) {...}
⬚ G. D:None of the above
⬚ H. None of the above

