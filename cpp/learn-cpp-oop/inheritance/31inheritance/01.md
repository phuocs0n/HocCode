Quiz: Suppose a Student class is derived from a Person class. Then, which of the following statements is correct?

Tap an option below to answer:

The Student class inherits all the variables and functions of the Person class.

##### Correct Answer!

Derived classes (child classes) inherit the variables and functions of the base class (parent class).

Derived classes (child classes) inherit the variables and functions of the base class (parent class).

Both classes inherit each other's variables and functions.

##### Incorrect, give it another shot!

Only the derived class inherits the base class's variables and functions.

Only the derived class inherits the base class's variables and functions.

The Person class inherits all the variables and functions of the Student class.

##### Incorrect, give it another shot!

Derived classes (child classes) inherit the variables and functions of the base class (parent class), not the other way around.

Derived classes (child classes) inherit the variables and functions of the base class (parent class), not the other way around.

None of the above.

##### Incorrect, give it another shot!

One of the answers is correct.

One of the answers is correct.

## 📝 Quiz

**Quiz: Suppose a Student class is derived from a Person class. Then, which of the following statements is correct?**

⬚ A. A:The Student class inherits all the variables and functions of the Person class.
⬚ B. The Student class inherits all the variables and functions of the Person class.
⬚ C. B:Both classes inherit each other's variables and functions.
⬚ D. Both classes inherit each other's variables and functions.
⬚ E. C:The Person class inherits all the variables and functions of the Student class.
⬚ F. The Person class inherits all the variables and functions of the Student class.
⬚ G. D:None of the above.
⬚ H. None of the above.

