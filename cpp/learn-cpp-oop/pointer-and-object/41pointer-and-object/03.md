Quiz: What is the output of the following code?

```cpp
class Laptop {
    public:
        string brand;
};

int main() {

    Laptop laptop1;
    Laptop* ptr = &laptop1;
    laptop1.brand = "HP";
    cout << laptop1.brand << endl;

    ptr.brand = "Lenovo";
    cout << ptr.brand;
    return 0;
}
```

Tap an option below to answer:

HP

Lenovo

##### Incorrect, give it another shot!

The code ptr.brand causes an error because we must use the -> operator with pointers to access class objects.

The code ptr.brand causes an error because we must use the -> operator with pointers to access class objects.

HP

HP

##### Incorrect, give it another shot!

The code ptr.brand causes an error because we must use the -> operator with pointers to access class objects.

The code ptr.brand causes an error because we must use the -> operator with pointers to access class objects.

HP

0x7ffd37c6e3a0

##### Incorrect, give it another shot!

The code ptr.brand causes an error because we must use the -> operator with pointers to access class objects.

The code ptr.brand causes an error because we must use the -> operator with pointers to access class objects.

Error

##### Correct Answer!

The code ptr.brand causes an error because we must use the -> operator with pointers to access class objects.

The code ptr.brand causes an error because we must use the -> operator with pointers to access class objects.

## 📝 Quiz

**Quiz: What is the output of the following code?**

⬚ A. A:HPLenovo
⬚ B. HPLenovo
⬚ C. B:HPHP
⬚ D. HPHP
⬚ E. C:HP0x7ffd37c6e3a0
⬚ F. HP0x7ffd37c6e3a0
⬚ G. D:Error
⬚ H. Error

