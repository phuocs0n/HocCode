Quiz: Which of the following is true for Encapsulation in C++?

Tap an option below to answer:

Encapsulation means inheriting variables and functions from the base class.

##### Incorrect, give it another shot!

The process of inheriting variables and functions is known as inheritance.

The process of inheriting variables and functions is known as inheritance.

Encapsulation is the bundling of variables and functions inside a single class.

##### Correct Answer!

Encapsulation is the bundling of variables and functions inside a single class.

Encapsulation is the bundling of variables and functions inside a single class.

Encapsulation is a form of function overriding.

##### Incorrect, give it another shot!

Encapsulation is not a form of function overriding.

Encapsulation is not a form of function overriding.

Encapsulation is having many forms.

##### Incorrect, give it another shot!

Encapsulation is not having many forms.

Encapsulation is not having many forms.

## 📝 Quiz

**Quiz: Which of the following is true for Encapsulation in C++?**

⬚ A. A:Encapsulation means inheriting variables and functions from the base class.
⬚ B. Encapsulation means inheriting variables and functions from the base class.
⬚ C. B:Encapsulation is the bundling of variables and functions inside a single class.
⬚ D. Encapsulation is the bundling of variables and functions inside a single class.
⬚ E. C:Encapsulation is a form of function overriding.
⬚ F. Encapsulation is a form of function overriding.
⬚ G. D:Encapsulation is having many forms.
⬚ H. Encapsulation is having many forms.

