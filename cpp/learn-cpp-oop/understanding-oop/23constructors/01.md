Quiz: Find the constructor in this code.

```cpp
class Person {    
    public:

        string name = "Marty McFly";

        Person() {
            cout << name << endl;
        }
    
        void display() {
            cout << name << endl;
        }      
};
```

Tap an option below to answer:

Person

##### Incorrect, give it another shot!

Person is the name of the class.

Person is the name of the class.

display()

##### Incorrect, give it another shot!

display() is a member function of the class, not its constructor.

display() is a member function of the class, not its constructor.

name

##### Incorrect, give it another shot!

name is a member variable of the class, not its constructor.

name is a member variable of the class, not its constructor.

Person()

##### Correct Answer!

The name of a constructor is the same as the class followed by parentheses.

The name of a constructor is the same as the class followed by parentheses.

##### Incorrect, give it another shot!

Person is the name of the class.

Person is the name of the class.

##### Incorrect, give it another shot!

display() is a member function of the class, not its constructor.

display() is a member function of the class, not its constructor.

##### Incorrect, give it another shot!

name is a member variable of the class, not its constructor.

name is a member variable of the class, not its constructor.

##### Correct Answer!

The name of a constructor is the same as the class followed by parentheses.

The name of a constructor is the same as the class followed by parentheses.

## 📝 Quiz

**Quiz: Find the constructor in this code.**

⬚ A. A:Person
⬚ B. Person
⬚ C. B:display()
⬚ D. display()
⬚ E. C:name
⬚ F. name
⬚ G. D:Person()
⬚ H. Person()

