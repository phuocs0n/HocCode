Quiz: How do we create an object of the Circle class dynamically?

Tap an option below to answer:

Circle circle1;

##### Incorrect, give it another shot!

This is not how we create an object dynamically in C++.

This is not how we create an object dynamically in C++.

Circle* circle1 = new Circle();

##### Correct Answer!

This is the correct way to create an object dynamically in C++.

This is the correct way to create an object dynamically in C++.

new Circle* circle1;

##### Incorrect, give it another shot!

This is the wrong way to dynamically create an object in C++.

This is the wrong way to dynamically create an object in C++.

new Circle* circle1();

##### Incorrect, give it another shot!

This is the wrong way to dynamically create an object in C++.

This is the wrong way to dynamically create an object in C++.

## 📝 Quiz

**Quiz: How do we create an object of the Circle class dynamically?**

⬚ A. A:Circle circle1;
⬚ B. Circle circle1;
⬚ C. B:Circle* circle1 = new Circle();
⬚ D. Circle* circle1 = new Circle();
⬚ E. C:new Circle* circle1;
⬚ F. new Circle* circle1;
⬚ G. D:new Circle* circle1();
⬚ H. new Circle* circle1();

