Quiz: Suppose you have a class named Rectangle and you want to pass its object to the calculate_area() function, which returns a double value. How do you create the function definition?

Tap an option below to answer:

double calculate_area(Rectangle) {...}

##### Incorrect, give it another shot!

We need both the class name and object name for the function definition.

We need both the class name and object name for the function definition.

Rectangle calculate_area(object) {...};

##### Incorrect, give it another shot!

We are passing the object as a parameter, so we need to specify the class name as the function parameter.

We are passing the object as a parameter, so we need to specify the class name as the function parameter.

Rectangle calculate_area(Rectangle rect) {...}

##### Incorrect, give it another shot!

The given function returns a double value, not a Rectangle object.

The given function returns a double value, not a Rectangle object.

double calculate_area(Rectangle rect) {...}

##### Correct Answer!

We need to specify the class name and object name inside the parentheses.

We need to specify the class name and object name inside the parentheses.

## 📝 Quiz

**Quiz: Suppose you have a class named Rectangle and you want to pass its object to the calculate_area() function, which returns a double value. How do you create the function definition?**

⬚ A. A:double calculate_area(Rectangle) {...}
⬚ B. double calculate_area(Rectangle) {...}
⬚ C. B:Rectangle calculate_area(object) {...};
⬚ D. Rectangle calculate_area(object) {...};
⬚ E. C:Rectangle calculate_area(Rectangle rect) {...}
⬚ F. Rectangle calculate_area(Rectangle rect) {...}
⬚ G. D:double calculate_area(Rectangle rect) {...}
⬚ H. double calculate_area(Rectangle rect) {...}

