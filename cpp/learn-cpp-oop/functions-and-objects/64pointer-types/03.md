Quiz: What will be the output of the following code?

```cpp
#include <iostream>
using namespace std; 

// define the Test class
class Test {
    public:
        int number;

        void display_number() {
            cout << this->number << endl;
        }
};

int main() {

    Test t1;
    t1.number = 3;
    Test t2;
    t2.number = 5;

    t1.display_number();

    return 0;
}
```

Tap an option below to answer:

##### Incorrect, give it another shot!

We are calling the display_number() function only once.

We are calling the display_number() function only once.

##### Incorrect, give it another shot!

We are calling the display_number() function only once.

We are calling the display_number() function only once.

##### Incorrect, give it another shot!

We are calling the function using the t1 object and 5 is the value of number for the t2 object.

We are calling the function using the t1 object and 5 is the value of number for the t2 object.

##### Correct Answer!

3 is the value of number for the t1 object and we are calling the function using t1.

3 is the value of number for the t1 object and we are calling the function using t1.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:53
⬚ B. 53
⬚ C. B:35
⬚ D. 35
⬚ E. C:5
⬚ F. 5
⬚ G. D:3
⬚ H. 3

