Quiz: Which of the following is the correct way to create a vector?

Tap an option below to answer:

vector<> ages = {25, 36};

##### Incorrect, give it another shot!

We must define the type of data we want to store in the vector inside <>.

We must define the type of data we want to store in the vector inside <>.

vector<ages> int = {25, 36};

##### Incorrect, give it another shot!

We define the data type inside <> while declaring the vector name outside of it.

We define the data type inside <> while declaring the vector name outside of it.

vector<int> ages = {25, 36};

##### Correct Answer!

This is the correct way to define a vector.

This is the correct way to define a vector.

vector ages = {25, 36};

##### Incorrect, give it another shot!

We must define the type of data we want to store in the vector inside <>.

We must define the type of data we want to store in the vector inside <>.

##### Incorrect, give it another shot!

We must define the type of data we want to store in the vector inside <>.

We must define the type of data we want to store in the vector inside <>.

##### Incorrect, give it another shot!

We define the data type inside <> while declaring the vector name outside of it.

We define the data type inside <> while declaring the vector name outside of it.

##### Correct Answer!

This is the correct way to define a vector.

This is the correct way to define a vector.

##### Incorrect, give it another shot!

We must define the type of data we want to store in the vector inside <>.

We must define the type of data we want to store in the vector inside <>.

## 📝 Quiz

**Quiz: Which of the following is the correct way to create a vector?**

⬚ A. A:vector<> ages = {25, 36};
⬚ B. vector<> ages = {25, 36};
⬚ C. B:vector<ages> int = {25, 36};
⬚ D. vector<ages> int = {25, 36};
⬚ E. C:vector<int> ages = {25, 36};
⬚ F. vector<int> ages = {25, 36};
⬚ G. D:vector ages = {25, 36};
⬚ H. vector ages = {25, 36};

