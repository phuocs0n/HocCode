Quiz: Which of the following is the correct way to create a std::array?

Tap an option below to answer:

array<int> ages = {25, 36};

##### Incorrect, give it another shot!

std::array needs to know the size of an array

std::array needs to know the size of an array

array<5> ages = {25, 36};

##### Incorrect, give it another shot!

std::array needs to know about the type of the element it stores.

std::array needs to know about the type of the element it stores.

array ages = {25, 36}

##### Incorrect, give it another shot!

std::array needs to know about the type and the size of the array.

std::array needs to know about the type and the size of the array.

array<int, 2> ages = {25, 36}

##### Correct Answer!

This is the correct way to define a std::array in C++

This is the correct way to define a std::array in C++

## 📝 Quiz

**Quiz: Which of the following is the correct way to create a std::array?**

⬚ A. A:array<int> ages = {25, 36};
⬚ B. array<int> ages = {25, 36};
⬚ C. B:array<5> ages = {25, 36};
⬚ D. array<5> ages = {25, 36};
⬚ E. C:array ages = {25, 36}
⬚ F. array ages = {25, 36}
⬚ G. D:array<int, 2> ages = {25, 36}
⬚ H. array<int, 2> ages = {25, 36}

