Quiz: Which of the following expressions is valid?

Tap an option below to answer:

```cpp
stack<int> s = {1, 2, 3};
```

##### Incorrect, give it another shot!

This syntax works for other containers like vector, but not for stack.

This syntax works for other containers like vector, but not for stack.

```cpp
stack<int> s({1, 2, 3});
```

##### Incorrect, give it another shot!

This is an incorrect syntax for initializing a stack. Stack doesn't directly accept an initializer list.

This is an incorrect syntax for initializing a stack. Stack doesn't directly accept an initializer list.

```cpp
stack<int> s;
```

##### Correct Answer!

Correct! stack is initialized empty, and elements are added using push().

Correct! stack is initialized empty, and elements are added using push().

```cpp
stack<int> s = stack<int>({1, 2, 3});
```

##### Incorrect, give it another shot!

This might look correct, but stack doesn't accept initializer lists in this way.

This might look correct, but stack doesn't accept initializer lists in this way.

## 📝 Quiz

**Quiz: Which of the following expressions is valid?**

⬚ A. A:stack<int> s = {1, 2, 3};
⬚ B. stack<int> s = {1, 2, 3};
⬚ C. B:stack<int> s({1, 2, 3});
⬚ D. stack<int> s({1, 2, 3});
⬚ E. C:stack<int> s;
⬚ F. stack<int> s;
⬚ G. D:stack<int> s = stack<int>({1, 2, 3});
⬚ H. stack<int> s = stack<int>({1, 2, 3});

