Quiz: Which of the following statements is true?

Tap an option below to answer:

We use the push() method to push an element to any position.

##### Incorrect, give it another shot!

We can only use the push() method to add an element after the last element.

We can only use the push() method to add an element after the last element.

We use the pop() method to remove elements from any position.

##### Incorrect, give it another shot!

We can only use the pop() method to remove the first element.

We can only use the pop() method to remove the first element.

The first element to get inserted in the queue is the first element to be removed.

##### Correct Answer!

Yes, the queue data structure follows the first in first out principle.

Yes, the queue data structure follows the first in first out principle.

None of the above.

##### Incorrect, give it another shot!

One of the above options is true.

One of the above options is true.

## 📝 Quiz

**Quiz: Which of the following statements is true?**

⬚ A. A:We use the push() method to push an element to any position.
⬚ B. We use the push() method to push an element to any position.
⬚ C. B:We use the pop() method to remove elements from any position.
⬚ D. We use the pop() method to remove elements from any position.
⬚ E. C:The first element to get inserted in the queue is the first element to be removed.
⬚ F. The first element to get inserted in the queue is the first element to be removed.
⬚ G. D:None of the above.
⬚ H. None of the above.

