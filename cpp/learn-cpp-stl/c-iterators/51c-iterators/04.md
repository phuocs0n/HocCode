Quiz: Our programmer, Stuti, wants to access the last element of the vector. But she couldn't correctly get the value. What should she do here?

```cpp
vector<int> number {5, 10, 15, 20, 25, 30, 35};
auto iter = number.end();

cout << "The last element is " << iter << endl;
```

Tap an option below to answer:

She should dereference the iter to get the value.

```cpp
cout << "The last element is " << *iter << endl;
```

##### Incorrect, give it another shot!

This change compiles but this doesn't print the last element of the vector because end() returns an iterator to the element one past the last element.

This change compiles but this doesn't print the last element of the vector because end() returns an iterator to the element one past the last element.

She should subtract one from the iterator returned by end()

```cpp
cout << "The last element is " << iter - 1 << endl;
```

##### Incorrect, give it another shot!

This doesn't compile. Subtracting by one to get the last element is a good idea. But did you forget to dereference ?

This doesn't compile. Subtracting by one to get the last element is a good idea. But did you forget to dereference ?

She should subtract one from the iterator returned by end() and dereference the value.

```cpp
cout << "The last element is " << *(iter - 1 ) << endl;
```

##### Correct Answer!

This is correct, subtracting one from the iterator returned by end() points to the last element and dereferencing it prints the element at the last.

This is correct, subtracting one from the iterator returned by end() points to the last element and dereferencing it prints the element at the last.

She should dereference the iterator returned by end() and then subtract one from it.

```cpp
cout << "The last element is " << *iter - 1 << endl;
```

##### Incorrect, give it another shot!

This subtracts one from the value pointed by end() which is not what we want.

This subtracts one from the value pointed by end() which is not what we want.

## 📝 Quiz

**Quiz: Our programmer, Stuti, wants to access the last element of the vector. But she couldn't correctly get the value. What should she do here?**

⬚ A. A:She should dereference the iter to get the value.cout << "The last element is " << *iter << endl;
⬚ B. She should dereference the iter to get the value.cout << "The last element is " << *iter << endl;
⬚ C. B:She should subtract one from the iterator returned by end()cout << "The last element is " << iter - 1 << endl;
⬚ D. She should subtract one from the iterator returned by end()cout << "The last element is " << iter - 1 << endl;
⬚ E. C:She should subtract one from the iterator returned by end() and dereference the value.cout << "The last element is " << *(iter - 1 ) << endl;
⬚ F. She should subtract one from the iterator returned by end() and dereference the value.cout << "The last element is " << *(iter - 1 ) << endl;
⬚ G. D:She should dereference the iterator returned by end() and then subtract one from it.cout << "The last element is " << *iter - 1 << endl;
⬚ H. She should dereference the iterator returned by end() and then subtract one from it.cout << "The last element is " << *iter - 1 << endl;

