Quiz: What is a struct in C?

Tap an option below to answer:

Collection of pointers

##### Incorrect, give it another shot!

A struct can contain both arrays and variables (which includes pointers).

A struct can contain both arrays and variables (which includes pointers).

Collection of arrays

##### Incorrect, give it another shot!

A struct can contain variables as well.

A struct can contain variables as well.

Collection of variables and arrays

##### Correct Answer!

A struct can contain variables and arrays of all types.

A struct can contain variables and arrays of all types.

Collection of same types of variables and arrays

##### Incorrect, give it another shot!

A struct can contain variables and arrays of all types.

A struct can contain variables and arrays of all types.

## 📝 Quiz

**Quiz: What is a struct in C?**

⬚ A. A:Collection of pointers
⬚ B. Collection of pointers
⬚ C. B:Collection of arrays
⬚ D. Collection of arrays
⬚ E. C:Collection of variables and arrays
⬚ F. Collection of variables and arrays
⬚ G. D:Collection of same types of variables and arrays
⬚ H. Collection of same types of variables and arrays

