Quiz: Suppose we have a struct variable struct Student student1;. What is the correct way to call a function with this variable as an argument?

Tap an option below to answer:

func(Student);

##### Incorrect, give it another shot!

Student is the name of the struct, not the struct variable.

Student is the name of the struct, not the struct variable.

func(struct);

##### Incorrect, give it another shot!

The struct keyword is used to create a struct.

The struct keyword is used to create a struct.

func(student1);

##### Correct Answer!

student1 is the struct variable that is passed as an argument to a function.

student1 is the struct variable that is passed as an argument to a function.

func(struct Student student1);

##### Incorrect, give it another shot!

struct Student student1 is used to declare a struct variable.

struct Student student1 is used to declare a struct variable.

## 📝 Quiz

**Quiz: Suppose we have a struct variable struct Student student1;. What is the correct way to call a function with this variable as an argument?**

⬚ A. A:func(Student);
⬚ B. func(Student);
⬚ C. B:func(struct);
⬚ D. func(struct);
⬚ E. C:func(student1);
⬚ F. func(student1);
⬚ G. D:func(struct Student student1);
⬚ H. func(struct Student student1);

