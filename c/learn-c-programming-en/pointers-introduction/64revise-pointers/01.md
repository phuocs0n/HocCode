# Add 10 to Each Element of the Array

#### Course: Learn C Programming

## Add 10 to Each Element of the Array

Suppose we have an array with elements: {8, 7, 21, 13}. Now, we need to add 10 to each element of the array so our output looks like this: {18, 17, 31, 23}.

### Thought Process

First, we need to access each array element using the loop. Inside the loop, we need to add 10 to each element and assign the result to the respective position.

```c
1#include <stdio.h>
2
3int main() {
4
5  int numbers[4] = {8, 7, 21, 13};
6
7  // loop to access each array element
8  for (int i = 0; i < 4; ++i) {
9    *(numbers + i) = *(numbers + i) + 10;
10  }
11
12  // print the array
13  printf("Array Elements: ");
14  for (int i = 0; i < 4; ++i) {
15    printf("%d ", *(numbers + i));
16  }
17
18  return 0;
19}
```

In the above example, notice the line:

```c
*(numbers + i) = *(numbers + i) + 10;
```

Here, *(numbers + i) accesses the array element at position i, adds 10 to it and assigns the result to the same position.

Quick Remainder:

- numbers - gives the address of first array element
- *numbers - gives the value of the first element
- numbers + 1 - gives the address of the second element
- *(numbers + 1) - gives the value of the second element
- ....
- numbers + i - gives the address of the ith element
- *(numbers + i) - gives the value of the ith element

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


