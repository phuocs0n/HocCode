Quiz: Which of the following is a standard library function?

Tap an option below to answer:

printf()

##### Incorrect, give it another shot!

printf() is a standard library function that allows us to print text to output.

printf() is a standard library function that allows us to print text to output.

scanf()

##### Incorrect, give it another shot!

scanf() is a standard library function that allows us to get input from the user.

scanf() is a standard library function that allows us to get input from the user.

Both A & B

##### Correct Answer!

Both printf() and scanf() are standard library functions.

Both printf() and scanf() are standard library functions.

None of the above

##### Incorrect, give it another shot!

Both printf() and scanf() are standard library functions..

Both printf() and scanf() are standard library functions..

##### Incorrect, give it another shot!

printf() is a standard library function that allows us to print text to output.

printf() is a standard library function that allows us to print text to output.

##### Incorrect, give it another shot!

scanf() is a standard library function that allows us to get input from the user.

scanf() is a standard library function that allows us to get input from the user.

##### Correct Answer!

Both printf() and scanf() are standard library functions.

Both printf() and scanf() are standard library functions.

##### Incorrect, give it another shot!

Both printf() and scanf() are standard library functions..

Both printf() and scanf() are standard library functions..

## 📝 Quiz

**Quiz: Which of the following is a standard library function?**

⬚ A. A:printf()
⬚ B. printf()
⬚ C. B:scanf()
⬚ D. scanf()
⬚ E. C:Both A & B
⬚ F. Both A & B
⬚ G. D:None of the above
⬚ H. None of the above

