Quiz: In this for loop, which one is the update statement?

```c
for (int i = 1; i < 5; i++) {...}
```

Tap an option below to answer:

int i = 1

##### Incorrect, give it another shot!

int i = 1 is an initialization expression which initializes the value of i to 1.

int i = 1 is an initialization expression which initializes the value of i to 1.

i < 5

##### Incorrect, give it another shot!

i < 5 is a boolean expression which checks if the value of i is less than 5.

i < 5 is a boolean expression which checks if the value of i is less than 5.

i++

##### Correct Answer!

i++ is an update expression which increases the value of i by 1.

i++ is an update expression which increases the value of i by 1.

for

##### Incorrect, give it another shot!

for is a keyword which indicates the start of a for loop.

for is a keyword which indicates the start of a for loop.

##### Incorrect, give it another shot!

int i = 1 is an initialization expression which initializes the value of i to 1.

int i = 1 is an initialization expression which initializes the value of i to 1.

##### Incorrect, give it another shot!

i < 5 is a boolean expression which checks if the value of i is less than 5.

i < 5 is a boolean expression which checks if the value of i is less than 5.

##### Correct Answer!

i++ is an update expression which increases the value of i by 1.

i++ is an update expression which increases the value of i by 1.

##### Incorrect, give it another shot!

for is a keyword which indicates the start of a for loop.

for is a keyword which indicates the start of a for loop.

## 📝 Quiz

**Quiz: In this for loop, which one is the update statement?**

⬚ A. A:int i = 1
⬚ B. int i = 1
⬚ C. B:i < 5
⬚ D. i < 5
⬚ E. C:i++
⬚ F. i++
⬚ G. D:for
⬚ H. for

