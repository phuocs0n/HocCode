Quiz: What will be the output of the following code?

```c
#include <stdio.h>

int main() {

  int i = 1;
  int n = 6;

  while (i < n) {
    printf("%d\n", i);

    i = i + 2; // Increase i by 2
  }
  
  return 0;
}
```

Tap an option below to answer:

##### Incorrect, give it another shot!

This would have been the answer if we had used i = i + 1; instead of i = i + 2; inside the loop.

This would have been the answer if we had used i = i + 1; instead of i = i + 2; inside the loop.

##### Correct Answer!

The initial value of i is 1. Then, its value is increased to 3, then to 5. When i becomes 7, the loop terminates.

The initial value of i is 1. Then, its value is increased to 3, then to 5. When i becomes 7, the loop terminates.

##### Incorrect, give it another shot!

The loop iterates 3 times before i < n becomes false. Hence, the value of i is printed 3 times.

The loop iterates 3 times before i < n becomes false. Hence, the value of i is printed 3 times.

##### Incorrect, give it another shot!

The loop iterates 3 times before i < n becomes false. Hence, the value of i is printed 3 times.

The loop iterates 3 times before i < n becomes false. Hence, the value of i is printed 3 times.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:12345
⬚ B. 12345
⬚ C. B:135
⬚ D. 135
⬚ E. C:1
⬚ F. 1
⬚ G. D:13
⬚ H. 13

