Quiz: What is the size of the given array?

```c
int arr[3][5];
```

Tap an option below to answer:

15

##### Correct Answer!

The array can store 3 single dimensional array of size 5 so the total size is 15 (3 * 5).

The array can store 3 single dimensional array of size 5 so the total size is 15 (3 * 5).

##### Incorrect, give it another shot!

3 represents the number of single dimensional arrays present inside this 2D array.

3 represents the number of single dimensional arrays present inside this 2D array.

##### Incorrect, give it another shot!

5 is the number of elements present inside the inner single dimensional array.

5 is the number of elements present inside the inner single dimensional array.

(undefined)

##### Incorrect, give it another shot!

The size of a 2D array can be identified by multiplying rows and columns.

The size of a 2D array can be identified by multiplying rows and columns.

##### Correct Answer!

The array can store 3 single dimensional array of size 5 so the total size is 15 (3 * 5).

The array can store 3 single dimensional array of size 5 so the total size is 15 (3 * 5).

##### Incorrect, give it another shot!

3 represents the number of single dimensional arrays present inside this 2D array.

3 represents the number of single dimensional arrays present inside this 2D array.

##### Incorrect, give it another shot!

5 is the number of elements present inside the inner single dimensional array.

5 is the number of elements present inside the inner single dimensional array.

##### Incorrect, give it another shot!

The size of a 2D array can be identified by multiplying rows and columns.

The size of a 2D array can be identified by multiplying rows and columns.

## 📝 Quiz

**Quiz: What is the size of the given array?**

⬚ A. A:15
⬚ B. 15
⬚ C. B:3
⬚ D. 3
⬚ E. C:5
⬚ F. 5
⬚ G. D:(undefined)
⬚ H. (undefined)

