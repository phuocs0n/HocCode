Quiz: In C, single-line comments use ___ and multi-line comments use ____.

Tap an option below to answer:

# and /* */

##### Incorrect, give it another shot!

Using /* */ for multi-line comments is correct, but # is not used for single-line comments in C.

Using /* */ for multi-line comments is correct, but # is not used for single-line comments in C.

// and { }

##### Incorrect, give it another shot!

The single-line comment syntax (//) is correct, but { } does not function as a comment in C.

The single-line comment syntax (//) is correct, but { } does not function as a comment in C.

{} and /* */

##### Incorrect, give it another shot!

The multi-line comment syntax (/* */) is correct, but the single-line comment syntax is incorrect.

The multi-line comment syntax (/* */) is correct, but the single-line comment syntax is incorrect.

// and /* */

##### Correct Answer!

Single-line comments in C begin with //. Multi-line comments start with /* and end with */, allowing comments to span multiple lines.

Single-line comments in C begin with //. Multi-line comments start with /* and end with */, allowing comments to span multiple lines.

##### Incorrect, give it another shot!

Using /* */ for multi-line comments is correct, but # is not used for single-line comments in C.

Using /* */ for multi-line comments is correct, but # is not used for single-line comments in C.

##### Incorrect, give it another shot!

The single-line comment syntax (//) is correct, but { } does not function as a comment in C.

The single-line comment syntax (//) is correct, but { } does not function as a comment in C.

##### Incorrect, give it another shot!

The multi-line comment syntax (/* */) is correct, but the single-line comment syntax is incorrect.

The multi-line comment syntax (/* */) is correct, but the single-line comment syntax is incorrect.

##### Correct Answer!

Single-line comments in C begin with //. Multi-line comments start with /* and end with */, allowing comments to span multiple lines.

Single-line comments in C begin with //. Multi-line comments start with /* and end with */, allowing comments to span multiple lines.

## 📝 Quiz

**Quiz: In C, single-line comments use ___ and multi-line comments use ____.**

⬚ A. A:# and /* */
⬚ B. # and /* */
⬚ C. B:// and { }
⬚ D. // and { }
⬚ E. C:{} and /* */
⬚ F. {} and /* */
⬚ G. D:// and /* */
⬚ H. // and /* */

