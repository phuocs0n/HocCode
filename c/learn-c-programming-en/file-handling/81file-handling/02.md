Quiz: How to declare a pointer to work with files in C?

Tap an option below to answer:

file* pointer;

##### Incorrect, give it another shot!

file is not a valid keyword in C.

file is not a valid keyword in C.

FILE* pointer;

##### Correct Answer!

We use the FILE type pointer to work with files.

We use the FILE type pointer to work with files.

int* pointer;

##### Incorrect, give it another shot!

We don't use the int type pointer to work with files.

We don't use the int type pointer to work with files.

FILE pointer;

##### Incorrect, give it another shot!

We use the pointer variable of FILE type not the regular variable.

We use the pointer variable of FILE type not the regular variable.

##### Incorrect, give it another shot!

file is not a valid keyword in C.

file is not a valid keyword in C.

##### Correct Answer!

We use the FILE type pointer to work with files.

We use the FILE type pointer to work with files.

##### Incorrect, give it another shot!

We don't use the int type pointer to work with files.

We don't use the int type pointer to work with files.

##### Incorrect, give it another shot!

We use the pointer variable of FILE type not the regular variable.

We use the pointer variable of FILE type not the regular variable.

## 📝 Quiz

**Quiz: How to declare a pointer to work with files in C?**

⬚ A. A:file* pointer;
⬚ B. file* pointer;
⬚ C. B:FILE* pointer;
⬚ D. FILE* pointer;
⬚ E. C:int* pointer;
⬚ F. int* pointer;
⬚ G. D:FILE pointer;
⬚ H. FILE pointer;

