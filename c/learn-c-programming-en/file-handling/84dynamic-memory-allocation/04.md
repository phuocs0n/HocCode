Quiz: What is the use of the free() function?

Tap an option below to answer:

To allocate memory dynamically

##### Incorrect, give it another shot!

The malloc() function is used to dynamically allocate memory.

The malloc() function is used to dynamically allocate memory.

To create a resizable array

##### Incorrect, give it another shot!

The concept of dynamic memory allocation is used to create a resizable array.

The concept of dynamic memory allocation is used to create a resizable array.

To free dynamically allocated memory.

##### Correct Answer!

The free() function is used to free the dynamically allocated memory once it is used.

The free() function is used to free the dynamically allocated memory once it is used.

All of the above

##### Incorrect, give it another shot!

Only one of the above options is the correct answer.

Only one of the above options is the correct answer.

## 📝 Quiz

**Quiz: What is the use of the free() function?**

⬚ A. A:To allocate memory dynamically
⬚ B. To allocate memory dynamically
⬚ C. B:To create a resizable array
⬚ D. To create a resizable array
⬚ E. C:To free dynamically allocated memory.
⬚ F. To free dynamically allocated memory.
⬚ G. D:All of the above
⬚ H. All of the above

