Quiz: What will be the output of the following code?

```c
#include <stdio.h>

void greet() {
    char greeting[] = "Hello";  
}

int main() {

    printf("%s", greeting);  
    return 0;
}
```

Tap an option below to answer:

Hello

##### Incorrect, give it another shot!

The greeting[] array is local to the greet() function. So, it doesn't exist outside that function.

The greeting[] array is local to the greet() function. So, it doesn't exist outside that function.

(Error)

##### Correct Answer!

The greeting[] array is not defined outside the greet() function. So, printing it outside will result in an error.

The greeting[] array is not defined outside the greet() function. So, printing it outside will result in an error.

greeting

##### Incorrect, give it another shot!

We have printed the greeting[] array, not the "greeting" string.

We have printed the greeting[] array, not the "greeting" string.

Empty Output

##### Incorrect, give it another shot!

The program does not compile since the greeting[] array can only be used inside the greet() function. Thus, we won't get any output—empty or otherwise.

The program does not compile since the greeting[] array can only be used inside the greet() function. Thus, we won't get any output—empty or otherwise.

##### Incorrect, give it another shot!

The greeting[] array is local to the greet() function. So, it doesn't exist outside that function.

The greeting[] array is local to the greet() function. So, it doesn't exist outside that function.

##### Correct Answer!

The greeting[] array is not defined outside the greet() function. So, printing it outside will result in an error.

The greeting[] array is not defined outside the greet() function. So, printing it outside will result in an error.

##### Incorrect, give it another shot!

We have printed the greeting[] array, not the "greeting" string.

We have printed the greeting[] array, not the "greeting" string.

##### Incorrect, give it another shot!

The program does not compile since the greeting[] array can only be used inside the greet() function. Thus, we won't get any output—empty or otherwise.

The program does not compile since the greeting[] array can only be used inside the greet() function. Thus, we won't get any output—empty or otherwise.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:Hello
⬚ B. Hello
⬚ C. B:(Error)
⬚ D. (Error)
⬚ E. C:greeting
⬚ F. greeting
⬚ G. D:Empty Output
⬚ H. Empty Output

