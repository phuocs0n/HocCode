# Base Case for Recursion

#### Course: Learn Recursion With C

## Base Case for Recursion

Consider the following program to print numbers:

```c
1#include <stdio.h>
2
3void print_numbers(int n) {
4
5    printf("%d\n", n);
6    print_numbers(n + 1);  
7}
8
9int main() {
10
11    print_numbers(1);  
12    return 0;
13}
```

Output

```text
1
2
3
...
```

This program displays numbers infinitely because it lacks a stopping condition.

Now, let's add a base case so that the program ends after displaying 2.

```c
1#include <stdio.h>
2
3void print_numbers(int n) {
4
5    // base case to terminate recursion
6    if (n == 3) {
7        return;
8    }
9
10    // execute code if base case is not met
11    printf("%d\n", n);
12    
13    // recursive call with increment
14    print_numbers(n + 1);
15}
16
17int main() {
18
19    print_numbers(1);  
20    return 0;
21}
```

Output

```text
1
2
```

In the program, we have added the following base case:

```c
if (n == 3) {
    return;
}
```

If n is equal to 3, the function encounters the return statement, and the recursive call ends.

Let's understand this step-by-step using an image.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


