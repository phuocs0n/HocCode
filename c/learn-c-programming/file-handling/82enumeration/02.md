Quiz: What is the output of the following code?

```c
#include <stdio.h>

enum Weekends {
  Saturday, Sunday
};

int main() {

  enum Weekends day = Sunday;
  printf("%d", day);

  return 0;
}
```

Tap an option below to answer:

Sunday

##### Incorrect, give it another shot!

Enum constants are by default integral.

Enum constants are by default integral.

##### Incorrect, give it another shot!

Integral values of enum constants start from 0 not 1.

Integral values of enum constants start from 0 not 1.

##### Correct Answer!

Integral values of enum constants start from 0.

Integral values of enum constants start from 0.

Error

##### Incorrect, give it another shot!

Code runs without any error.

Code runs without any error.

##### Incorrect, give it another shot!

Enum constants are by default integral.

Enum constants are by default integral.

##### Incorrect, give it another shot!

Integral values of enum constants start from 0 not 1.

Integral values of enum constants start from 0 not 1.

##### Correct Answer!

Integral values of enum constants start from 0.

Integral values of enum constants start from 0.

##### Incorrect, give it another shot!

Code runs without any error.

Code runs without any error.

## 📝 Quiz

**Quiz: What is the output of the following code?**

⬚ A. A:Sunday
⬚ B. Sunday
⬚ C. B:2
⬚ D. 2
⬚ E. C:1
⬚ F. 1
⬚ G. D:Error
⬚ H. Error

