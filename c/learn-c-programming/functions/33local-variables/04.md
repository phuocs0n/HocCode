Quiz: What will be the output of the following code?

```c
#include <stdio.h>

void findProduct() {

  int result = 4 * 9;
}

int main() {

  printf("%d", result);

  return 0;
}
```

Tap an option below to answer:

36

##### Incorrect, give it another shot!

The result variable inside the main() function is not defined. Remember, the int result = 4 * 9 is local to the findProduct() function only. It doesn't exist outside.

The result variable inside the main() function is not defined. Remember, the int result = 4 * 9 is local to the findProduct() function only. It doesn't exist outside.

(Error)

##### Correct Answer!

The result variable is not defined. It only exists inside the findProduct() function.

The result variable is not defined. It only exists inside the findProduct() function.

result

##### Incorrect, give it another shot!

We are printing the result variable not the text "result".

We are printing the result variable not the text "result".

None of the above

##### Incorrect, give it another shot!

One of the options is correct.

One of the options is correct.

##### Incorrect, give it another shot!

The result variable inside the main() function is not defined. Remember, the int result = 4 * 9 is local to the findProduct() function only. It doesn't exist outside.

The result variable inside the main() function is not defined. Remember, the int result = 4 * 9 is local to the findProduct() function only. It doesn't exist outside.

##### Correct Answer!

The result variable is not defined. It only exists inside the findProduct() function.

The result variable is not defined. It only exists inside the findProduct() function.

##### Incorrect, give it another shot!

We are printing the result variable not the text "result".

We are printing the result variable not the text "result".

##### Incorrect, give it another shot!

One of the options is correct.

One of the options is correct.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:36
⬚ B. 36
⬚ C. B:(Error)
⬚ D. (Error)
⬚ E. C:result
⬚ F. result
⬚ G. D:None of the above
⬚ H. None of the above

