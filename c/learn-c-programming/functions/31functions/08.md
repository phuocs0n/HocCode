Quiz: How'd you call a function named greet?

Tap an option below to answer:

```c
greet;
```

##### Incorrect, give it another shot!

To call a function, we must use parentheses () after its name.

To call a function, we must use parentheses () after its name.

```c
();
```

##### Incorrect, give it another shot!

To call a function, we use the function name followed by ().

To call a function, we use the function name followed by ().

```c
greet();
```

##### Correct Answer!

In C, a function is called by using its name followed by parentheses ().

In C, a function is called by using its name followed by parentheses ().

```c
void greet();
```

##### Incorrect, give it another shot!

When calling a function, we do not include its return type (void, int, etc.)—we only use its name followed by ().

When calling a function, we do not include its return type (void, int, etc.)—we only use its name followed by ().

## 📝 Quiz

**Quiz: How'd you call a function named greet?**

⬚ A. A:greet;
⬚ B. greet;
⬚ C. B:();
⬚ D. ();
⬚ E. C:greet();
⬚ F. greet();
⬚ G. D:void greet();
⬚ H. void greet();

