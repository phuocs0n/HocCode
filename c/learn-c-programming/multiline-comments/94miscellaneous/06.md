Quiz: What will be the output of the following code?

```c
#include <stdio.h>

int main() {
  int n = 10;
  if (n == 10 && 100) {
    printf("Hello");
  }

  return 0;
}
```

Tap an option below to answer:

(No Output)

##### Incorrect, give it another shot!

Here, n == 0 && 100 evaluates to true because both n == 10 and 100 evaluate to true. Hence, the body of the if statement is executed.

Here, n == 0 && 100 evaluates to true because both n == 10 and 100 evaluate to true. Hence, the body of the if statement is executed.

(Error Code)

##### Incorrect, give it another shot!

The program is valid and will not cause any errors.

The program is valid and will not cause any errors.

Hello

Hello

##### Incorrect, give it another shot!

We have printed Hello only once if the test condition is true.

We have printed Hello only once if the test condition is true.

Hello

##### Correct Answer!

Here, n == 10 && 100 evaluates to true because both n == 10 and 100 evaluate to true.

Here, n == 10 && 100 evaluates to true because both n == 10 and 100 evaluate to true.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:(No Output)
⬚ B. (No Output)
⬚ C. B:(Error Code)
⬚ D. (Error Code)
⬚ E. C:HelloHello
⬚ F. HelloHello
⬚ G. D:Hello
⬚ H. Hello

