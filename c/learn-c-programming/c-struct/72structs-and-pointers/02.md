Quiz: What is the output of the following code?

```c
#include <stdio.h>

typedef struct Student {
  int marks;
} students;

int main() {
  students *ptr, st1;
  ptr = &st1;
  st1.marks = 70;
  printf("%d\n", ptr->marks);
  printf("%d", (*ptr).marks);
  return 0;
}
```

Tap an option below to answer:

70

70

##### Correct Answer!

Since ptr points to st1, both ptr-> marks and (*ptr).marks give the value of st1.marks.

Since ptr points to st1, both ptr-> marks and (*ptr).marks give the value of st1.marks.

70

##### Incorrect, give it another shot!

(*ptr).marks is not 0 since it gives the value of str1.marks i.e. 70.

(*ptr).marks is not 0 since it gives the value of str1.marks i.e. 70.

70

0x7ffd121b258a

##### Incorrect, give it another shot!

(*ptr).marks gives the data stored in str1.marks and not its address.

(*ptr).marks gives the data stored in str1.marks and not its address.

Error

##### Incorrect, give it another shot!

The code runs without any error.

The code runs without any error.

## 📝 Quiz

**Quiz: What is the output of the following code?**

⬚ A. A:7070
⬚ B. 7070
⬚ C. B:700
⬚ D. 700
⬚ E. C:700x7ffd121b258a
⬚ F. 700x7ffd121b258a
⬚ G. D:Error
⬚ H. Error

