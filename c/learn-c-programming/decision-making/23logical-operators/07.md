Quiz: What will be the output of the following code?

```c
#include <stdio.h>

int main() {

  int result1 = (6 > 2) && (7 > 10);
  printf("%d\n", result1);

  int result2 = (6 > 2) || (7 > 10);
  printf("%d", result2);
  
  return 0;
}
```

Tap an option below to answer:

##### Incorrect, give it another shot!

The && operator returns 1 (true) only when both operands are true.

The && operator returns 1 (true) only when both operands are true.

##### Incorrect, give it another shot!

The || operator returns 1 (true) if either of the operands is true.

The || operator returns 1 (true) if either of the operands is true.

##### Incorrect, give it another shot!

The first statement is 1 (true) only when both operands are true because it involves the && operator. And, the second is 0 (false) only when both operands are false because it involves the || operator.

The first statement is 1 (true) only when both operands are true because it involves the && operator. And, the second is 0 (false) only when both operands are false because it involves the || operator.

##### Correct Answer!

The first statement is 0 (false) because 7 > 10 is false. The second statement is 1 (true) because 6 > 2 is true.

The first statement is 0 (false) because 7 > 10 is false. The second statement is 1 (true) because 6 > 2 is true.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:11
⬚ B. 11
⬚ C. B:00
⬚ D. 00
⬚ E. C:10
⬚ F. 10
⬚ G. D:01
⬚ H. 01

