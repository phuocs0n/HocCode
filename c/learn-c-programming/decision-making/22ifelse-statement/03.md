Quiz: What will be the output of the following code?

```c
#include <stdio.h>

int main() {

  int number = 0;

  if (number < 0) {
    printf("Number is negative");
  }

  printf("This is always executed");
  
  return 0;
}
```

Tap an option below to answer:

Number is negative.

This is always executed.

##### Incorrect, give it another shot!

The test condition of the if statement is false. Hence, the body of the if statement will not be executed.

The test condition of the if statement is false. Hence, the body of the if statement will not be executed.

(No Output)

##### Incorrect, give it another shot!

There is a statement outside of if. So that statement is printed no matter what.

There is a statement outside of if. So that statement is printed no matter what.

This is always executed.

##### Correct Answer!

The test condition of the if statement is false. Hence, the body of the if statement is not executed.

The test condition of the if statement is false. Hence, the body of the if statement is not executed.

(Error Code)

##### Incorrect, give it another shot!

The program is valid and will not cause any errors.

The program is valid and will not cause any errors.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:Number is negative.This is always executed.
⬚ B. Number is negative.This is always executed.
⬚ C. B:(No Output)
⬚ D. (No Output)
⬚ E. C:This is always executed.
⬚ F. This is always executed.
⬚ G. D:(Error Code)
⬚ H. (Error Code)

