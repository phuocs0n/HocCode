Quiz: What might be the output of the following code?

```c
#include <stdio.h>

int main() {
  int number = 24;
  printf("%p", &number);
  return 0;
}
```

Tap an option below to answer:

24

##### Incorrect, give it another shot!

&number doesn't return the value of the number variable.

&number doesn't return the value of the number variable.

0x7ffce674ed24

##### Correct Answer!

&number returns the memory address of the number variable.

&number returns the memory address of the number variable.

%p

##### Incorrect, give it another shot!

The %p is the format specifier to print the memory address.

The %p is the format specifier to print the memory address.

Error

##### Incorrect, give it another shot!

The above code runs without any error

The above code runs without any error

##### Incorrect, give it another shot!

&number doesn't return the value of the number variable.

&number doesn't return the value of the number variable.

##### Correct Answer!

&number returns the memory address of the number variable.

&number returns the memory address of the number variable.

##### Incorrect, give it another shot!

The %p is the format specifier to print the memory address.

The %p is the format specifier to print the memory address.

##### Incorrect, give it another shot!

The above code runs without any error

The above code runs without any error

## 📝 Quiz

**Quiz: What might be the output of the following code?**

⬚ A. A:24
⬚ B. 24
⬚ C. B:0x7ffce674ed24
⬚ D. 0x7ffce674ed24
⬚ E. C:%p
⬚ F. %p
⬚ G. D:Error
⬚ H. Error

