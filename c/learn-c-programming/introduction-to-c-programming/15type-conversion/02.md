Quiz: What value does the variable x after the following statement:

```c
int x = 57.47;
```

Tap an option below to answer:

This is an error, we cannot assign a floating point value to an int.

##### Incorrect, give it another shot!

This is not an error. It is possible due to implicit type conversion.

This is not an error. It is possible due to implicit type conversion.

57

##### Correct Answer!

The C compiler implicitly converts the floating point value to an int value. So x holds 57.

The C compiler implicitly converts the floating point value to an int value. So x holds 57.

57.47

##### Incorrect, give it another shot!

An integer can only represent numbers without a decimal point, so the values after the decimal point are omitted.

An integer can only represent numbers without a decimal point, so the values after the decimal point are omitted.

5747

##### Incorrect, give it another shot!

We are not assigning a value of 5747 so there is no way x holds this value.

We are not assigning a value of 5747 so there is no way x holds this value.

## 📝 Quiz

**Quiz: What value does the variable x after the following statement:**

⬚ A. A:This is an error, we cannot assign a floating point value to an int.
⬚ B. This is an error, we cannot assign a floating point value to an int.
⬚ C. B:57
⬚ D. 57
⬚ E. C:57.47
⬚ F. 57.47
⬚ G. D:5747
⬚ H. 5747

