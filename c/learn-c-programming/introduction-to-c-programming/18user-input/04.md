Quiz: Which of the following is the correct way to take input for the variable x ?

```c
int x;
```

Tap an option below to answer:

```c
scanf("%d", x);
```

##### Incorrect, give it another shot!

We use &x with scanf() to take integer input.

We use &x with scanf() to take integer input.

```c
scanf("%d", &x);
```

##### Correct Answer!

We use the %d format specifier and &x with scanf() to take integer input.

We use the %d format specifier and &x with scanf() to take integer input.

```c
scanf("%d" &x);
```

##### Incorrect, give it another shot!

"%d" and &x should be separated by a comma.

"%d" and &x should be separated by a comma.

```c
scanf("%c", &x);
```

##### Incorrect, give it another shot!

We are taking the input value for the int variable, so the format specifier is not %c.

We are taking the input value for the int variable, so the format specifier is not %c.

## 📝 Quiz

**Quiz: Which of the following is the correct way to take input for the variable x ?**

⬚ A. A:scanf("%d", x);
⬚ B. scanf("%d", x);
⬚ C. B:scanf("%d", &x);
⬚ D. scanf("%d", &x);
⬚ E. C:scanf("%d" &x);
⬚ F. scanf("%d" &x);
⬚ G. D:scanf("%c", &x);
⬚ H. scanf("%c", &x);

