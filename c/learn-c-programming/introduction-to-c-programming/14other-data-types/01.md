Quiz: Suppose we have a double variable named salary. How can we print it?

Tap an option below to answer:

```c
printf(salary);
```

##### Incorrect, give it another shot!

To print a variable, we need to use the appropriate format specifier enclosed within double quotes "".

To print a variable, we need to use the appropriate format specifier enclosed within double quotes "".

```c
printf("%d", salary);
```

##### Incorrect, give it another shot!

We use the %d format specifier with int variables, not with double variables.

We use the %d format specifier with int variables, not with double variables.

```c
printf(%lf, salary);
```

##### Incorrect, give it another shot!

We must enclose the format specifier within double quotes "".

We must enclose the format specifier within double quotes "".

```c
printf("%lf", salary);
```

##### Correct Answer!

We use the %lf format specifier to print a double variable. The format specifier %lf must be enclosed within double quotes "".

We use the %lf format specifier to print a double variable. The format specifier %lf must be enclosed within double quotes "".

## 📝 Quiz

**Quiz: Suppose we have a double variable named salary. How can we print it?**

⬚ A. A:printf(salary);
⬚ B. printf(salary);
⬚ C. B:printf("%d", salary);
⬚ D. printf("%d", salary);
⬚ E. C:printf(%lf, salary);
⬚ F. printf(%lf, salary);
⬚ G. D:printf("%lf", salary);
⬚ H. printf("%lf", salary);

