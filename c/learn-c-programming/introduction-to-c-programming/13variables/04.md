Quiz: How do you create a variable named value of the integer type?

Tap an option below to answer:

```c
value;
```

##### Incorrect, give it another shot!

You need to mention the data type while declaring a variable.

You need to mention the data type while declaring a variable.

```c
int;
```

##### Incorrect, give it another shot!

You need to mention the name of the variable while declaring a variable.

You need to mention the name of the variable while declaring a variable.

```c
int value;
```

##### Correct Answer!

A variable declaration consists of two parts – data type and the variable name.

A variable declaration consists of two parts – data type and the variable name.

```c
value int;
```

##### Incorrect, give it another shot!

You must place the data type first and then the name of the variable while declaring a variable.

You must place the data type first and then the name of the variable while declaring a variable.

## 📝 Quiz

**Quiz: How do you create a variable named value of the integer type?**

⬚ A. A:value;
⬚ B. value;
⬚ C. B:int;
⬚ D. int;
⬚ E. C:int value;
⬚ F. int value;
⬚ G. D:value int;
⬚ H. value int;

