Quiz: How do you print this variable with 3 digits after the decimal point?

```c
double num = 32.4;
```

Tap an option below to answer:

```c
printf("%lf", num);
```

##### Incorrect, give it another shot!

This prints the variable with 6 digits after the decimal point.

This prints the variable with 6 digits after the decimal point.

```c
printf("%.2lf", num);
```

##### Incorrect, give it another shot!

This prints the variable with 2 digits after the decimal point.

This prints the variable with 2 digits after the decimal point.

```c
printf("%.1lf", num);
```

##### Incorrect, give it another shot!

This prints the variable with only 1 digit after the decimal point.

This prints the variable with only 1 digit after the decimal point.

```c
printf("%.3lf", num);
```

##### Correct Answer!

This prints the variable with 3 digits after the decimal point.

This prints the variable with 3 digits after the decimal point.

## 📝 Quiz

**Quiz: How do you print this variable with 3 digits after the decimal point?**

⬚ A. A:printf("%lf", num);
⬚ B. printf("%lf", num);
⬚ C. B:printf("%.2lf", num);
⬚ D. printf("%.2lf", num);
⬚ E. C:printf("%.1lf", num);
⬚ F. printf("%.1lf", num);
⬚ G. D:printf("%.3lf", num);
⬚ H. printf("%.3lf", num);

