Quiz: What is the purpose of using @Override annotation?

Tap an option below to answer:

@Override annotation overrides the method of the superclass.

##### Incorrect, give it another shot!

@Override makes sure that you are actually overriding the method from the superclass. It doesn't override the method from the superclass.

@Override makes sure that you are actually overriding the method from the superclass. It doesn't override the method from the superclass.

@Override annotation does nothing to the code.

##### Incorrect, give it another shot!

@Override annotation makes sure that we are overriding the method of the superclass. If the method we are overriding is not correct, we get an error.

@Override annotation makes sure that we are overriding the method of the superclass. If the method we are overriding is not correct, we get an error.

@Override annotation makes sure that we are overriding the method of the superclass.

##### Correct Answer!

True, @Override annotation makes sure that we are overriding the method of the superclass.

True, @Override annotation makes sure that we are overriding the method of the superclass.

None of the above

##### Incorrect, give it another shot!

One of the above given answers is true.

One of the above given answers is true.

## 📝 Quiz

**Quiz: What is the purpose of using @Override annotation?**

⬚ A. A:@Override annotation overrides the method of the superclass.
⬚ B. @Override annotation overrides the method of the superclass.
⬚ C. B:@Override annotation does nothing to the code.
⬚ D. @Override annotation does nothing to the code.
⬚ E. C:@Override annotation makes sure that we are overriding the method of the superclass.
⬚ F. @Override annotation makes sure that we are overriding the method of the superclass.
⬚ G. D:None of the above
⬚ H. None of the above

