Quiz: Which is the invalid wrapper class for primitive types?

Tap an option below to answer:

Integer

##### Incorrect, give it another shot!

Integer is the valid wrapper class for the primitive type int.

Integer is the valid wrapper class for the primitive type int.

Char

##### Correct Answer!

The corresponding wrapper class for char is Character.

The corresponding wrapper class for char is Character.

Boolean

##### Incorrect, give it another shot!

Boolean is the valid wrapper class for the primitive type boolean.

Boolean is the valid wrapper class for the primitive type boolean.

Double

##### Incorrect, give it another shot!

Double is the valid wrapper class for the primitive type double.

Double is the valid wrapper class for the primitive type double.

##### Incorrect, give it another shot!

Integer is the valid wrapper class for the primitive type int.

Integer is the valid wrapper class for the primitive type int.

##### Correct Answer!

The corresponding wrapper class for char is Character.

The corresponding wrapper class for char is Character.

##### Incorrect, give it another shot!

Boolean is the valid wrapper class for the primitive type boolean.

Boolean is the valid wrapper class for the primitive type boolean.

##### Incorrect, give it another shot!

Double is the valid wrapper class for the primitive type double.

Double is the valid wrapper class for the primitive type double.

## 📝 Quiz

**Quiz: Which is the invalid wrapper class for primitive types?**

⬚ A. A:Integer
⬚ B. Integer
⬚ C. B:Char
⬚ D. Char
⬚ E. C:Boolean
⬚ F. Boolean
⬚ G. D:Double
⬚ H. Double

