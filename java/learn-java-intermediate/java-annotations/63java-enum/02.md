Quiz: What is the output of the program?

```java
enum Size {
    SMALL, MEDIUM, LARGE
}

class Main {
    public static void main(String[] args) {
        System.out.println(Size.SMALL);
  }
}
```

Tap an option below to answer:

Error

##### Incorrect, give it another shot!

The program runs without any error.

The program runs without any error.

MEDIUM

##### Incorrect, give it another shot!

Size.SMALL accesses the enum constant SMALL not MEDIUM.

Size.SMALL accesses the enum constant SMALL not MEDIUM.

SMALL

##### Correct Answer!

Size.SMALL accesses the enum constant SMALL.

Size.SMALL accesses the enum constant SMALL.

LARGE

##### Incorrect, give it another shot!

Size.SMALL accesses the enum constant SMALL not LARGE.

Size.SMALL accesses the enum constant SMALL not LARGE.

##### Incorrect, give it another shot!

The program runs without any error.

The program runs without any error.

##### Incorrect, give it another shot!

Size.SMALL accesses the enum constant SMALL not MEDIUM.

Size.SMALL accesses the enum constant SMALL not MEDIUM.

##### Correct Answer!

Size.SMALL accesses the enum constant SMALL.

Size.SMALL accesses the enum constant SMALL.

##### Incorrect, give it another shot!

Size.SMALL accesses the enum constant SMALL not LARGE.

Size.SMALL accesses the enum constant SMALL not LARGE.

## 📝 Quiz

**Quiz: What is the output of the program?**

⬚ A. A:Error
⬚ B. Error
⬚ C. B:MEDIUM
⬚ D. MEDIUM
⬚ E. C:SMALL
⬚ F. SMALL
⬚ G. D:LARGE
⬚ H. LARGE

