Quiz: What is the correct way to create a HashMap?

Tap an option below to answer:

HashMap<String, Integer> hashMap = new HashMap<>();

##### Correct Answer!

HashMap stores elements in key/value pairs. Hence, we need to specify both keys and values during the creation of a HashMap.

HashMap stores elements in key/value pairs. Hence, we need to specify both keys and values during the creation of a HashMap.

HashMap<String> hashMap = new HashMap<>();

##### Incorrect, give it another shot!

We need to specify both keys and values during the creation of a HashMap.

We need to specify both keys and values during the creation of a HashMap.

HashMap<Integer> hashMap = new HashMap<>();

##### Incorrect, give it another shot!

We need to specify both keys and values during the creation of a HashMap.

We need to specify both keys and values during the creation of a HashMap.

HashMap<> hashmap = new HashMap<String, Integer>();

##### Incorrect, give it another shot!

This is the invalid way to create a HashMap in Java.

This is the invalid way to create a HashMap in Java.

## 📝 Quiz

**Quiz: What is the correct way to create a HashMap?**

⬚ A. A:HashMap<String, Integer> hashMap = new HashMap<>();
⬚ B. HashMap<String, Integer> hashMap = new HashMap<>();
⬚ C. B:HashMap<String> hashMap = new HashMap<>();
⬚ D. HashMap<String> hashMap = new HashMap<>();
⬚ E. C:HashMap<Integer> hashMap = new HashMap<>();
⬚ F. HashMap<Integer> hashMap = new HashMap<>();
⬚ G. D:HashMap<> hashmap = new HashMap<String, Integer>();
⬚ H. HashMap<> hashmap = new HashMap<String, Integer>();

