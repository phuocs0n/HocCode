Quiz: What will be the output of the following code?

```java
import java.util.HashSet;

class Main {
    public static void main(String[] args) {
        
        HashSet<String> item = new HashSet<>();

        item.add("Eva");
        item.add("Eva");
        item.add("Eva");

        System.out.println(item);
    }
}
```

Tap an option below to answer:

[Eva, Eva, Eva]

##### Incorrect, give it another shot!

HashSet doesn't store duplicate elements.

HashSet doesn't store duplicate elements.

[]

##### Incorrect, give it another shot!

HashSet doesn't store duplicate elements. However, it does store elements.

HashSet doesn't store duplicate elements. However, it does store elements.

[Eva]

##### Correct Answer!

HashSet doesn't store duplicate elements, so the element will be stored only once.

HashSet doesn't store duplicate elements, so the element will be stored only once.

None of the above.

##### Incorrect, give it another shot!

One of the above answers is correct.

One of the above answers is correct.

##### Incorrect, give it another shot!

HashSet doesn't store duplicate elements.

HashSet doesn't store duplicate elements.

##### Incorrect, give it another shot!

HashSet doesn't store duplicate elements. However, it does store elements.

HashSet doesn't store duplicate elements. However, it does store elements.

##### Correct Answer!

HashSet doesn't store duplicate elements, so the element will be stored only once.

HashSet doesn't store duplicate elements, so the element will be stored only once.

##### Incorrect, give it another shot!

One of the above answers is correct.

One of the above answers is correct.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:[Eva, Eva, Eva]
⬚ B. [Eva, Eva, Eva]
⬚ C. B:[]
⬚ D. []
⬚ E. C:[Eva]
⬚ F. [Eva]
⬚ G. D:None of the above.
⬚ H. None of the above.

