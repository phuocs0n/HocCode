Quiz: What is the correct way to create a generic class?

Tap an option below to answer:

class Main<T> {...}

##### Correct Answer!

We use the <T> parameter to specify the generic class.

We use the <T> parameter to specify the generic class.

generic class Main {...}

##### Incorrect, give it another shot!

There is no keyword named generic in Java.

There is no keyword named generic in Java.

class Main extends Generics {..}

##### Incorrect, give it another shot!

We don't extend any Generics class to create a generic class.

We don't extend any Generics class to create a generic class.

generic class Main<T> {..}

##### Incorrect, give it another shot!

We only use the class keyword to create a generic class.

We only use the class keyword to create a generic class.

## 📝 Quiz

**Quiz: What is the correct way to create a generic class?**

⬚ A. A:class Main<T> {...}
⬚ B. class Main<T> {...}
⬚ C. B:generic class Main {...}
⬚ D. generic class Main {...}
⬚ E. C:class Main extends Generics {..}
⬚ F. class Main extends Generics {..}
⬚ G. D:generic class Main<T> {..}
⬚ H. generic class Main<T> {..}

