Quiz: Which of the following statements about Java packages is not true?

Tap an option below to answer:

We can have more than one Java file in a package.

##### Incorrect, give it another shot!

We can have as many files as we want in a package.

We can have as many files as we want in a package.

We cannot access private members of a package outside the package.

##### Incorrect, give it another shot!

True, private members of a package cannot be accessed outside of the package.

True, private members of a package cannot be accessed outside of the package.

We must import the class of the package to use it outside the package.

##### Incorrect, give it another shot!

True, we must import the class of the package to use it outside the package.

True, we must import the class of the package to use it outside the package.

We can use only built-in packages in our Java files.

##### Correct Answer!

We can use both user-defined and built-in packages in our Java file.

We can use both user-defined and built-in packages in our Java file.

## 📝 Quiz

**Quiz: Which of the following statements about Java packages is not true?**

⬚ A. A:We can have more than one Java file in a package.
⬚ B. We can have more than one Java file in a package.
⬚ C. B:We cannot access private members of a package outside the package.
⬚ D. We cannot access private members of a package outside the package.
⬚ E. C:We must import the class of the package to use it outside the package.
⬚ F. We must import the class of the package to use it outside the package.
⬚ G. D:We can use only built-in packages in our Java files.
⬚ H. We can use only built-in packages in our Java files.

