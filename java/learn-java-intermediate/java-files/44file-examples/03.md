Quiz: How do we copy a file in Java?

Tap an option below to answer:

Using the copy() method

##### Incorrect, give it another shot!

There is no method named copy() in the File class.

There is no method named copy() in the File class.

Read the content of a file to an array and write the content from the array to another file

##### Correct Answer!

To copy a file, we first read the content of a file and then write the content to another file.

To copy a file, we first read the content of a file and then write the content to another file.

Using read() method

##### Incorrect, give it another shot!

The read() method is used to read the content of a file.

The read() method is used to read the content of a file.

Using write() method

##### Incorrect, give it another shot!

The write() method is used to write to a file.

The write() method is used to write to a file.

## 📝 Quiz

**Quiz: How do we copy a file in Java?**

⬚ A. A:Using the copy() method
⬚ B. Using the copy() method
⬚ C. B:Read the content of a file to an array and write the content from the array to another file
⬚ D. Read the content of a file to an array and write the content from the array to another file
⬚ E. C:Using read() method
⬚ F. Using read() method
⬚ G. D:Using write() method
⬚ H. Using write() method

