Quiz: Which method is used to create a directory in Java?

Tap an option below to answer:

mkdirectory()

##### Incorrect, give it another shot!

mkdirectory() is an invalid method name to create a directory.

mkdirectory() is an invalid method name to create a directory.

createDirectory()

##### Incorrect, give it another shot!

There is no method called createDirectory() in Java.

There is no method called createDirectory() in Java.

mkdir()

##### Correct Answer!

The mkdir() method is used to create a directory in Java.

The mkdir() method is used to create a directory in Java.

File()

##### Incorrect, give it another shot!

File() is a constructor that is used to create an object that will be associated with the directory.

File() is a constructor that is used to create an object that will be associated with the directory.

##### Incorrect, give it another shot!

mkdirectory() is an invalid method name to create a directory.

mkdirectory() is an invalid method name to create a directory.

##### Incorrect, give it another shot!

There is no method called createDirectory() in Java.

There is no method called createDirectory() in Java.

##### Correct Answer!

The mkdir() method is used to create a directory in Java.

The mkdir() method is used to create a directory in Java.

##### Incorrect, give it another shot!

File() is a constructor that is used to create an object that will be associated with the directory.

File() is a constructor that is used to create an object that will be associated with the directory.

## 📝 Quiz

**Quiz: Which method is used to create a directory in Java?**

⬚ A. A:mkdirectory()
⬚ B. mkdirectory()
⬚ C. B:createDirectory()
⬚ D. createDirectory()
⬚ E. C:mkdir()
⬚ F. mkdir()
⬚ G. D:File()
⬚ H. File()

