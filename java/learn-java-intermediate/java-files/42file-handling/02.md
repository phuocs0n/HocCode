Quiz: How do we create a file in Java?

Tap an option below to answer:

File file = new File("newFile.txt");

##### Incorrect, give it another shot!

We use the File class to create an object of the file. Creating an object doesn't mean creating a file.

We use the File class to create an object of the file. Creating an object doesn't mean creating a file.

String value = file.createNewFile();

##### Incorrect, give it another shot!

The createNewFile() method returns true or false, so we must assign its value to a boolean variable.

The createNewFile() method returns true or false, so we must assign its value to a boolean variable.

boolean value = file.createNewFile();

##### Correct Answer!

The createNewFile() method is used with the object of File to create a new file. As it returns a boolean value we have assigned its value to a boolean variable.

The createNewFile() method is used with the object of File to create a new file. As it returns a boolean value we have assigned its value to a boolean variable.

None of the above.

##### Incorrect, give it another shot!

One of the above answers is correct.

One of the above answers is correct.

## 📝 Quiz

**Quiz: How do we create a file in Java?**

⬚ A. A:File file = new File("newFile.txt");
⬚ B. File file = new File("newFile.txt");
⬚ C. B:String value = file.createNewFile();
⬚ D. String value = file.createNewFile();
⬚ E. C:boolean value = file.createNewFile();
⬚ F. boolean value = file.createNewFile();
⬚ G. D:None of the above.
⬚ H. None of the above.

