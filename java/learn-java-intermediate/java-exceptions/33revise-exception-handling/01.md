Quiz: The code that might cause exception is placed inside the

Tap an option below to answer:

catch block

##### Incorrect, give it another shot!

The code inside the catch block is executed if an exception occurs.

The code inside the catch block is executed if an exception occurs.

finally block

##### Incorrect, give it another shot!

The code inside the finally block is always executed no matter if the exception occurs or not.

The code inside the finally block is always executed no matter if the exception occurs or not.

try block

##### Correct Answer!

We use the try block to store code that might cause an exception.

We use the try block to store code that might cause an exception.

if block

##### Incorrect, give it another shot!

We include code that will run if the condition is true inside the if block.

We include code that will run if the condition is true inside the if block.

##### Incorrect, give it another shot!

The code inside the catch block is executed if an exception occurs.

The code inside the catch block is executed if an exception occurs.

##### Incorrect, give it another shot!

The code inside the finally block is always executed no matter if the exception occurs or not.

The code inside the finally block is always executed no matter if the exception occurs or not.

##### Correct Answer!

We use the try block to store code that might cause an exception.

We use the try block to store code that might cause an exception.

##### Incorrect, give it another shot!

We include code that will run if the condition is true inside the if block.

We include code that will run if the condition is true inside the if block.

## 📝 Quiz

**Quiz: The code that might cause exception is placed inside the**

⬚ A. A:catch block
⬚ B. catch block
⬚ C. B:finally block
⬚ D. finally block
⬚ E. C:try block
⬚ F. try block
⬚ G. D:if block
⬚ H. if block

