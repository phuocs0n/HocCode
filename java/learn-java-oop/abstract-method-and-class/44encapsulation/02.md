Quiz: Which of the following is true for Encapsulation in Java?

Tap an option below to answer:

Encapsulation is inheriting fields and methods from superclass.

##### Incorrect, give it another shot!

Inheriting fields and methods is known as inheritance.

Inheriting fields and methods is known as inheritance.

Encapsulation is the bundling of fields and methods inside a single class.

##### Correct Answer!

Encapsulation is the bundling of fields and methods inside a single class.

Encapsulation is the bundling of fields and methods inside a single class.

Encapsulation is a form of abstraction.

##### Incorrect, give it another shot!

Encapsulation is not a form of abstraction.

Encapsulation is not a form of abstraction.

Encapsulation is having many forms.

##### Incorrect, give it another shot!

Encapsulation is not having many forms.

Encapsulation is not having many forms.

## 📝 Quiz

**Quiz: Which of the following is true for Encapsulation in Java?**

⬚ A. A:Encapsulation is inheriting fields and methods from superclass.
⬚ B. Encapsulation is inheriting fields and methods from superclass.
⬚ C. B:Encapsulation is the bundling of fields and methods inside a single class.
⬚ D. Encapsulation is the bundling of fields and methods inside a single class.
⬚ E. C:Encapsulation is a form of abstraction.
⬚ F. Encapsulation is a form of abstraction.
⬚ G. D:Encapsulation is having many forms.
⬚ H. Encapsulation is having many forms.

