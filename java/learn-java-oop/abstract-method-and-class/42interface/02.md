Quiz: What is the correct way to implement an interface?

Tap an option below to answer:

Using the extends keyword

##### Incorrect, give it another shot!

The extends keyword is used to perform inheritance in Java.

The extends keyword is used to perform inheritance in Java.

Using the implements keyword

##### Correct Answer!

The implements keyword is used to implement an interface by another class.

The implements keyword is used to implement an interface by another class.

Using the interface keyword

##### Incorrect, give it another shot!

The interface keyword is used to create an interface in Java.

The interface keyword is used to create an interface in Java.

Using the abstract keyword

##### Incorrect, give it another shot!

The abstract keyword is used to create abstract methods and classes in Java.

The abstract keyword is used to create abstract methods and classes in Java.

##### Incorrect, give it another shot!

The extends keyword is used to perform inheritance in Java.

The extends keyword is used to perform inheritance in Java.

##### Correct Answer!

The implements keyword is used to implement an interface by another class.

The implements keyword is used to implement an interface by another class.

##### Incorrect, give it another shot!

The interface keyword is used to create an interface in Java.

The interface keyword is used to create an interface in Java.

##### Incorrect, give it another shot!

The abstract keyword is used to create abstract methods and classes in Java.

The abstract keyword is used to create abstract methods and classes in Java.

## 📝 Quiz

**Quiz: What is the correct way to implement an interface?**

⬚ A. A:Using the extends keyword
⬚ B. Using the extends keyword
⬚ C. B:Using the implements keyword
⬚ D. Using the implements keyword
⬚ E. C:Using the interface keyword
⬚ F. Using the interface keyword
⬚ G. D:Using the abstract keyword
⬚ H. Using the abstract keyword

