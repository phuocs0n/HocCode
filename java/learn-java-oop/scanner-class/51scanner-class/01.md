Quiz: Which of the following is the correct syntax to create Scanner object?

Tap an option below to answer:

Scanner input = new Scanner();

##### Incorrect, give it another shot!

While creating an object of Scanner, we must provide a System.in parameter.

While creating an object of Scanner, we must provide a System.in parameter.

Scanner input = new Scanner(System);

##### Incorrect, give it another shot!

System is not the valid parameter passed to the Scanner constructor.

System is not the valid parameter passed to the Scanner constructor.

Scanner input = Scanner(System.in);

##### Incorrect, give it another shot!

We use the new keyword along with the constructor while creating the object of Scanner.

We use the new keyword along with the constructor while creating the object of Scanner.

Scanner input = new Scanner(System.in);

##### Correct Answer!

The System.in parameter is used to take input from the standard input (keyword).

The System.in parameter is used to take input from the standard input (keyword).

## 📝 Quiz

**Quiz: Which of the following is the correct syntax to create Scanner object?**

⬚ A. A:Scanner input = new Scanner();
⬚ B. Scanner input = new Scanner();
⬚ C. B:Scanner input = new Scanner(System);
⬚ D. Scanner input = new Scanner(System);
⬚ E. C:Scanner input = Scanner(System.in);
⬚ F. Scanner input = Scanner(System.in);
⬚ G. D:Scanner input = new Scanner(System.in);
⬚ H. Scanner input = new Scanner(System.in);

