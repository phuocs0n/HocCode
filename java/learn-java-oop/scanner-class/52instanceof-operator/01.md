Quiz: What is the correct way of checking if obj1 is the instance of a Hello class?

Tap an option below to answer:

obj1.instanceof Hello

##### Incorrect, give it another shot!

We don't use . with instanceof.

We don't use . with instanceof.

obj1 instanceof Hello

##### Correct Answer!

This code checks if obj1 is the instance of Hello.

This code checks if obj1 is the instance of Hello.

obj1 instanceof(Hello)

##### Incorrect, give it another shot!

We don't keep the class name inside the bracket.

We don't keep the class name inside the bracket.

obj1 instanceOf Hello

##### Incorrect, give it another shot!

The syntax is incorrect. It is instanceof not instanceOf.

The syntax is incorrect. It is instanceof not instanceOf.

##### Incorrect, give it another shot!

We don't use . with instanceof.

We don't use . with instanceof.

##### Correct Answer!

This code checks if obj1 is the instance of Hello.

This code checks if obj1 is the instance of Hello.

##### Incorrect, give it another shot!

We don't keep the class name inside the bracket.

We don't keep the class name inside the bracket.

##### Incorrect, give it another shot!

The syntax is incorrect. It is instanceof not instanceOf.

The syntax is incorrect. It is instanceof not instanceOf.

## 📝 Quiz

**Quiz: What is the correct way of checking if obj1 is the instance of a Hello class?**

⬚ A. A: obj1.instanceof Hello
⬚ B. obj1.instanceof Hello
⬚ C. B:obj1 instanceof Hello
⬚ D. obj1 instanceof Hello
⬚ E. C:obj1 instanceof(Hello)
⬚ F. obj1 instanceof(Hello)
⬚ G. D:obj1 instanceOf Hello
⬚ H. obj1 instanceOf Hello

