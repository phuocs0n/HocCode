Quiz: What is the correct way to call the constructor of the Animal class from the constructor of Dog?

```java
class Animal {
    Animal() {
    }
}

class Dog extends Animal {
    Dog() {
    }
}
```

Tap an option below to answer:

Animal obj = new Animal();

obj.super();

##### Incorrect, give it another shot!

We don't need to create an object of the Animal class to call its constructor from the constructor of its child class.

We don't need to create an object of the Animal class to call its constructor from the constructor of its child class.

super.Animal();

##### Incorrect, give it another shot!

We don't need to specify the name of the parent class to call its constructor.

We don't need to specify the name of the parent class to call its constructor.

super();

##### Correct Answer!

super() calls the constructor of the Animal class from the constructor of the Dog class.

super() calls the constructor of the Animal class from the constructor of the Dog class.

All of the above.

##### Incorrect, give it another shot!

Only one of the above options is correct.

Only one of the above options is correct.

## 📝 Quiz

**Quiz: What is the correct way to call the constructor of the Animal class from the constructor of Dog?**

⬚ A. A:Animal obj = new Animal();obj.super();
⬚ B. Animal obj = new Animal();obj.super();
⬚ C. B:super.Animal();
⬚ D. super.Animal();
⬚ E. C:super();
⬚ F. super();
⬚ G. D:All of the above.
⬚ H. All of the above.

