Quiz: What is the correct way to create constants in Java?

Tap an option below to answer:

const int identity = 34;

##### Incorrect, give it another shot!

There is nothing such as const keyword in Java.

There is nothing such as const keyword in Java.

public int identity = 34;

##### Incorrect, give it another shot!

The public keyword makes the field public not constant.

The public keyword makes the field public not constant.

final int identity = 34;

##### Correct Answer!

We use the final keyword to create constants in Java.

We use the final keyword to create constants in Java.

static int identity = 34;

##### Incorrect, give it another shot!

The static keyword makes the field static not constant.

The static keyword makes the field static not constant.

##### Incorrect, give it another shot!

There is nothing such as const keyword in Java.

There is nothing such as const keyword in Java.

##### Incorrect, give it another shot!

The public keyword makes the field public not constant.

The public keyword makes the field public not constant.

##### Correct Answer!

We use the final keyword to create constants in Java.

We use the final keyword to create constants in Java.

##### Incorrect, give it another shot!

The static keyword makes the field static not constant.

The static keyword makes the field static not constant.

## 📝 Quiz

**Quiz: What is the correct way to create constants in Java?**

⬚ A. A:const int identity = 34;
⬚ B. const int identity = 34;
⬚ C. B:public int identity = 34;
⬚ D. public int identity = 34;
⬚ E. C:final int identity = 34;
⬚ F. final int identity = 34;
⬚ G. D:static int identity = 34;
⬚ H. static int identity = 34;

