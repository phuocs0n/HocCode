Quiz: What is the correct way to access the age variable?

```java
class Test {
    static int age;
}
```

Tap an option below to answer:

obj.age = 31;

System.out.println(obj.age);

##### Incorrect, give it another shot!

age is a static variable, so we don't have to use an object to modify and access it.

age is a static variable, so we don't have to use an object to modify and access it.

obj.age = 31;

System.out.println(age);

##### Incorrect, give it another shot!

age is a static variable, so we can directly assign value to it without using an object.

age is a static variable, so we can directly assign value to it without using an object.

age = 31;

System.out.println(age);

##### Correct Answer!

age is a static variable, so we can both modify its value and access it without using an object.

age is a static variable, so we can both modify its value and access it without using an object.

age = 31;

System.out.println(obj.age);

##### Incorrect, give it another shot!

age is a static variable, so we don't have to use an object to access it.

age is a static variable, so we don't have to use an object to access it.

## 📝 Quiz

**Quiz: What is the correct way to access the age variable?**

⬚ A. A:obj.age = 31;System.out.println(obj.age);
⬚ B. obj.age = 31;System.out.println(obj.age);
⬚ C. B:obj.age = 31;System.out.println(age);
⬚ D. obj.age = 31;System.out.println(age);
⬚ E. C:age = 31;System.out.println(age);
⬚ F. age = 31;System.out.println(age);
⬚ G. D:age = 31;System.out.println(obj.age);
⬚ H. age = 31;System.out.println(obj.age);

