Quiz: Find the constructor in this code?

```java
class Main {

    String construct = "Constructor";

    Main() {
        // code
    }

    public void display() {
        // code
    }
}
```

Tap an option below to answer:

Main

##### Incorrect, give it another shot!

Main is the name of the class.

Main is the name of the class.

display()

##### Incorrect, give it another shot!

display() is a method inside the class.

display() is a method inside the class.

construct

##### Incorrect, give it another shot!

construct is a field inside the class.

construct is a field inside the class.

Main()

##### Correct Answer!

The name of a constructor is the same as the class followed by a parenthesis.

The name of a constructor is the same as the class followed by a parenthesis.

##### Incorrect, give it another shot!

Main is the name of the class.

Main is the name of the class.

##### Incorrect, give it another shot!

display() is a method inside the class.

display() is a method inside the class.

##### Incorrect, give it another shot!

construct is a field inside the class.

construct is a field inside the class.

##### Correct Answer!

The name of a constructor is the same as the class followed by a parenthesis.

The name of a constructor is the same as the class followed by a parenthesis.

## 📝 Quiz

**Quiz: Find the constructor in this code?**

⬚ A. A:Main
⬚ B. Main
⬚ C. B:display()
⬚ D. display()
⬚ E. C:construct
⬚ F. construct
⬚ G. D:Main()
⬚ H. Main()

