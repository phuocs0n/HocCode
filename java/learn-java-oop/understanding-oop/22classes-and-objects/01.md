Quiz: What is the correct way to create a class in Java?

Tap an option below to answer:

Class ClassName {...}

##### Incorrect, give it another shot!

Java doesn't have any keyword named Class.

Java doesn't have any keyword named Class.

class ClassName() {...}

##### Incorrect, give it another shot!

The parentheses () denote a method in Java.

The parentheses () denote a method in Java.

class = ClassName;

##### Incorrect, give it another shot!

We use = to assign value to a variable.

We use = to assign value to a variable.

class ClassName {...}

##### Correct Answer!

The class keyword is used to create a class in Java.

The class keyword is used to create a class in Java.

##### Incorrect, give it another shot!

Java doesn't have any keyword named Class.

Java doesn't have any keyword named Class.

##### Incorrect, give it another shot!

The parentheses () denote a method in Java.

The parentheses () denote a method in Java.

##### Incorrect, give it another shot!

We use = to assign value to a variable.

We use = to assign value to a variable.

##### Correct Answer!

The class keyword is used to create a class in Java.

The class keyword is used to create a class in Java.

## 📝 Quiz

**Quiz: What is the correct way to create a class in Java?**

⬚ A. A:Class ClassName {...}
⬚ B. Class ClassName {...}
⬚ C. B:class ClassName() {...}
⬚ D. class ClassName() {...}
⬚ E. C:class = ClassName;
⬚ F. class = ClassName;
⬚ G. D:class ClassName {...}
⬚ H. class ClassName {...}

