Quiz: What is the correct way to create an array in Java?

Tap an option below to answer:

int[] numbers = {1, 2, 3, 4, 5};

##### Correct Answer!

This creates an integer array named numbers with 5 elements.

This creates an integer array named numbers with 5 elements.

int numbers = {1, 2, 3, 4, 5};

##### Incorrect, give it another shot!

We use the [] symbol while creating an array.

We use the [] symbol while creating an array.

int[] numbers = [1, 2, 3, 4, 5];

##### Incorrect, give it another shot!

We use the curly braces for array elements, not square brackets.

We use the curly braces for array elements, not square brackets.

All of the above

##### Incorrect, give it another shot!

Only one of the above options is the correct answer.

Only one of the above options is the correct answer.

## 📝 Quiz

**Quiz: What is the correct way to create an array in Java?**

⬚ A. A:int[] numbers = {1, 2, 3, 4, 5};
⬚ B. int[] numbers = {1, 2, 3, 4, 5};
⬚ C. B:int numbers = {1, 2, 3, 4, 5};
⬚ D. int numbers = {1, 2, 3, 4, 5};
⬚ E. C:int[] numbers = [1, 2, 3, 4, 5];
⬚ F. int[] numbers = [1, 2, 3, 4, 5];
⬚ G. D:All of the above
⬚ H. All of the above

