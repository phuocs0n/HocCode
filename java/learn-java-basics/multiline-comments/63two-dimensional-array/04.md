Quiz: What is the value of the x variable in the following code?

```java
int[][] arr = { {1, 2, 3}, {4, 5, 6} };

x = arr[1][1];
```

Tap an option below to answer:

##### Incorrect, give it another shot!

1 is the first element of the first array and we can access it using arr[0][0].

1 is the first element of the first array and we can access it using arr[0][0].

##### Incorrect, give it another shot!

The position of the 4 inside the second inner array is 1st so it is accessed using arr[1][0].

The position of the 4 inside the second inner array is 1st so it is accessed using arr[1][0].

##### Incorrect, give it another shot!

2 lies in the second position of the first array so it can be accessed using arr[0][1].

2 lies in the second position of the first array so it can be accessed using arr[0][1].

##### Correct Answer!

5 lies in the second position of the second array and since array index starts from 0, we can access it using arr[1][1].

5 lies in the second position of the second array and since array index starts from 0, we can access it using arr[1][1].

##### Incorrect, give it another shot!

1 is the first element of the first array and we can access it using arr[0][0].

1 is the first element of the first array and we can access it using arr[0][0].

##### Incorrect, give it another shot!

The position of the 4 inside the second inner array is 1st so it is accessed using arr[1][0].

The position of the 4 inside the second inner array is 1st so it is accessed using arr[1][0].

##### Incorrect, give it another shot!

2 lies in the second position of the first array so it can be accessed using arr[0][1].

2 lies in the second position of the first array so it can be accessed using arr[0][1].

##### Correct Answer!

5 lies in the second position of the second array and since array index starts from 0, we can access it using arr[1][1].

5 lies in the second position of the second array and since array index starts from 0, we can access it using arr[1][1].

## 📝 Quiz

**Quiz: What is the value of the x variable in the following code?**

⬚ A. A:1
⬚ B. 1
⬚ C. B:4
⬚ D. 4
⬚ E. C:2
⬚ F. 2
⬚ G. D:5
⬚ H. 5

