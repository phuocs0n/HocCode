Quiz: What is the value of the result variable?

```java
int result = '4' + 5;
```

Tap an option below to answer:

##### Incorrect, give it another shot!

Here, '4' is a character, not an integer value.

Here, '4' is a character, not an integer value.

##### Incorrect, give it another shot!

We are adding the integer 5 with '4', so the output will be more than 5.

We are adding the integer 5 with '4', so the output will be more than 5.

57

##### Correct Answer!

The ASCII value of '4' is 52, so the sum of 52 and 5 is 57.

The ASCII value of '4' is 52, so the sum of 52 and 5 is 57.

(Error Code)

##### Incorrect, give it another shot!

The code runs without any issue.

The code runs without any issue.

##### Incorrect, give it another shot!

Here, '4' is a character, not an integer value.

Here, '4' is a character, not an integer value.

##### Incorrect, give it another shot!

We are adding the integer 5 with '4', so the output will be more than 5.

We are adding the integer 5 with '4', so the output will be more than 5.

##### Correct Answer!

The ASCII value of '4' is 52, so the sum of 52 and 5 is 57.

The ASCII value of '4' is 52, so the sum of 52 and 5 is 57.

##### Incorrect, give it another shot!

The code runs without any issue.

The code runs without any issue.

## 📝 Quiz

**Quiz: What is the value of the result variable?**

⬚ A. A:9
⬚ B. 9
⬚ C. B:5
⬚ D. 5
⬚ E. C:57
⬚ F. 57
⬚ G. D:(Error Code)
⬚ H. (Error Code)

