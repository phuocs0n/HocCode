Quiz: Which of the following variables is assigned a boolean value in this Java code?

Tap an option below to answer:

Boolean result = true;

##### Incorrect, give it another shot!

There is no keyword Boolean in Java.

There is no keyword Boolean in Java.

boolean result = true;

##### Correct Answer!

We use the boolean keyword to create boolean types in Java.

We use the boolean keyword to create boolean types in Java.

boolean result = "true";

##### Incorrect, give it another shot!

Boolean values true and false are represented without quotations.

Boolean values true and false are represented without quotations.

boolean result = True;

##### Incorrect, give it another shot!

Boolean values true and false should be in lowercase.

Boolean values true and false should be in lowercase.

##### Incorrect, give it another shot!

There is no keyword Boolean in Java.

There is no keyword Boolean in Java.

##### Correct Answer!

We use the boolean keyword to create boolean types in Java.

We use the boolean keyword to create boolean types in Java.

##### Incorrect, give it another shot!

Boolean values true and false are represented without quotations.

Boolean values true and false are represented without quotations.

##### Incorrect, give it another shot!

Boolean values true and false should be in lowercase.

Boolean values true and false should be in lowercase.

## 📝 Quiz

**Quiz: Which of the following variables is assigned a boolean value in this Java code?**

⬚ A. A:Boolean result = true;
⬚ B. Boolean result = true;
⬚ C. B:boolean result = true;
⬚ D. boolean result = true;
⬚ E. C:boolean result = "true";
⬚ F. boolean result = "true";
⬚ G. D:boolean result = True;
⬚ H. boolean result = True;

