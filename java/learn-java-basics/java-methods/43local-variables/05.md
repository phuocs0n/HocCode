Quiz: What will be the output of the following code?

```java
class Main {

    void getNumber() {
        int number = 1;
    }
  
    public static void main(String[] args) {

        Main obj = new Main();
        System.out.println(number);
    }
}
```

Tap an option below to answer:

##### Incorrect, give it another shot!

The number variable inside the System.out.println() method is not defined. Remember, the int number = 1; is local to the getNumber() method only. It doesn't exist outside.

The number variable inside the System.out.println() method is not defined. Remember, the int number = 1; is local to the getNumber() method only. It doesn't exist outside.

(Error)

##### Correct Answer!

The number variable is not defined. It only exists inside of the getNumber() method.

The number variable is not defined. It only exists inside of the getNumber() method.

number

##### Incorrect, give it another shot!

We are printing the number variable not the 'number' string.

We are printing the number variable not the 'number' string.

None of the above

##### Incorrect, give it another shot!

One of the options is correct

One of the options is correct

##### Incorrect, give it another shot!

The number variable inside the System.out.println() method is not defined. Remember, the int number = 1; is local to the getNumber() method only. It doesn't exist outside.

The number variable inside the System.out.println() method is not defined. Remember, the int number = 1; is local to the getNumber() method only. It doesn't exist outside.

##### Correct Answer!

The number variable is not defined. It only exists inside of the getNumber() method.

The number variable is not defined. It only exists inside of the getNumber() method.

##### Incorrect, give it another shot!

We are printing the number variable not the 'number' string.

We are printing the number variable not the 'number' string.

##### Incorrect, give it another shot!

One of the options is correct

One of the options is correct

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:1
⬚ B. 1
⬚ C. B:(Error)
⬚ D. (Error)
⬚ E. C:number
⬚ F. number
⬚ G. D:None of the above
⬚ H. None of the above

