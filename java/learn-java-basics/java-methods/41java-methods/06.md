Quiz: What is the correct way to call the following method?

```java
class Main {
    void method() {
        ...
    }
}
```

Tap an option below to answer:

Main obj = new Main();

obj.method;

##### Incorrect, give it another shot!

We need to include the () at the end of the method name while calling the method.

We need to include the () at the end of the method name while calling the method.

Main obj = new Main();

obj.method();

##### Correct Answer!

We use the object along with the dot operator to call the method.

We use the object along with the dot operator to call the method.

method();

##### Incorrect, give it another shot!

We use an object of the class to call the method.

We use an object of the class to call the method.

Method obj = new Method();

obj.method()

##### Incorrect, give it another shot!

method() is defined inside the Main class, so we need to use the object of Main to call the method.

method() is defined inside the Main class, so we need to use the object of Main to call the method.

## 📝 Quiz

**Quiz: What is the correct way to call the following method?**

⬚ A. A:Main obj = new Main();obj.method;
⬚ B. Main obj = new Main();obj.method;
⬚ C. B:Main obj = new Main();obj.method();
⬚ D. Main obj = new Main();obj.method();
⬚ E. C:method();
⬚ F. method();
⬚ G. D:Method obj = new Method();obj.method()
⬚ H. Method obj = new Method();obj.method()

