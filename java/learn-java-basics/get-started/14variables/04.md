Quiz: What's the variable in the following code?

```java
int score = 35;
```

Tap an option below to answer:

int

##### Incorrect, give it another shot!

int specifies the data type of the variable but is not a variable in itself.

int specifies the data type of the variable but is not a variable in itself.

35

##### Incorrect, give it another shot!

35 is the integer value stored in the variable, not the variable itself.

35 is the integer value stored in the variable, not the variable itself.

score

##### Correct Answer!

In Java, the value to the right of the = symbol is a variable.

In Java, the value to the right of the = symbol is a variable.

##### Incorrect, give it another shot!

The = sign is the assignment operator, not a variable.

The = sign is the assignment operator, not a variable.

##### Incorrect, give it another shot!

int specifies the data type of the variable but is not a variable in itself.

int specifies the data type of the variable but is not a variable in itself.

##### Incorrect, give it another shot!

35 is the integer value stored in the variable, not the variable itself.

35 is the integer value stored in the variable, not the variable itself.

##### Correct Answer!

In Java, the value to the right of the = symbol is a variable.

In Java, the value to the right of the = symbol is a variable.

##### Incorrect, give it another shot!

The = sign is the assignment operator, not a variable.

The = sign is the assignment operator, not a variable.

## 📝 Quiz

**Quiz: What's the variable in the following code?**

⬚ A. A:int
⬚ B. int
⬚ C. B:35
⬚ D. 35
⬚ E. C:score
⬚ F. score
⬚ G. D:=
⬚ H. =

