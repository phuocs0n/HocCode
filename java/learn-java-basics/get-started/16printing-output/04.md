Quiz: What is the output of the following code?

```java
class Main {
    public static void main(String[] args) {
        
        int number = 18;
        System.out.println("Number is " + number);
    }
}
```

Tap an option below to answer:

Number is number

##### Incorrect, give it another shot!

The + operator joins the string Number is with the value of the number variable.

The + operator joins the string Number is with the value of the number variable.

Number is 18

##### Correct Answer!

The + operator joins the string Number is with 18 (the value of the number variable).

The + operator joins the string Number is with 18 (the value of the number variable).

Number is

18

##### Incorrect, give it another shot!

We have used a single print statement to print the string Number is and the variable number. So, they won't be printed in two different lines.

We have used a single print statement to print the string Number is and the variable number. So, they won't be printed in two different lines.

Number is18

##### Incorrect, give it another shot!

There is a space at the end of our string within the quotation marks which is carried on to the output.

There is a space at the end of our string within the quotation marks which is carried on to the output.

## 📝 Quiz

**Quiz: What is the output of the following code?**

⬚ A. A:Number is number
⬚ B. Number is number
⬚ C. B:Number is 18
⬚ D. Number is 18
⬚ E. C:Number is18
⬚ F. Number is18
⬚ G. D:Number is18
⬚ H. Number is18

