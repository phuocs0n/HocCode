Quiz: What will be the output of the following code?

```java
class Main {

    public static void greet() {
        String greeting = "Hello";
    }

    public static void main(String[] args) {
        System.out.println(greeting);
    }
}
```

Tap an option below to answer:

Hello

##### Incorrect, give it another shot!

The greeting variable is local to the greet() method. So, it doesn't exist outside that method.

The greeting variable is local to the greet() method. So, it doesn't exist outside that method.

(Error)

##### Correct Answer!

The greeting variable is not defined outside the greet() method. So, printing it outside will result in an error.

The greeting variable is not defined outside the greet() method. So, printing it outside will result in an error.

greeting

##### Incorrect, give it another shot!

We have printed the greeting variable, not the "greeting" string.

We have printed the greeting variable, not the "greeting" string.

(Nothing is printed)

##### Incorrect, give it another shot!

While the void function returns nothing, the program either has an output or throws an error.

While the void function returns nothing, the program either has an output or throws an error.

##### Incorrect, give it another shot!

The greeting variable is local to the greet() method. So, it doesn't exist outside that method.

The greeting variable is local to the greet() method. So, it doesn't exist outside that method.

##### Correct Answer!

The greeting variable is not defined outside the greet() method. So, printing it outside will result in an error.

The greeting variable is not defined outside the greet() method. So, printing it outside will result in an error.

##### Incorrect, give it another shot!

We have printed the greeting variable, not the "greeting" string.

We have printed the greeting variable, not the "greeting" string.

##### Incorrect, give it another shot!

While the void function returns nothing, the program either has an output or throws an error.

While the void function returns nothing, the program either has an output or throws an error.

## 📝 Quiz

**Quiz: What will be the output of the following code?**

⬚ A. A:Hello
⬚ B. Hello
⬚ C. B:(Error)
⬚ D. (Error)
⬚ E. C:greeting
⬚ F. greeting
⬚ G. D:(Nothing is printed)
⬚ H. (Nothing is printed)

