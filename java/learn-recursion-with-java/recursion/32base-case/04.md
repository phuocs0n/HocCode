# Example 1: Recursion

#### Course: Learn Recursion With Java

## Example 1: Recursion

Let's modify our previous program so that the recursive methods return a value.

```java
1class Main {
2
3    public static String printNumbers(int n) {
4
5        // Base case with return value
6        if (n == 3) {
7            return "Done";
8        }
9
10        System.out.println(n); 
11
12        return printNumbers(n + 1);
13    }
14
15
16    public static void main(String[] args) {
17
18        String output = printNumbers(1);  
19        System.out.println(output); 
20    }
21}
```

Output

```text
1
2
Done
```

Here, we have used a recursive call as a return statement:

```java
return printNumbers(n + 1);
```

Let's understand how this program works.

##### How does it work?

Use this space for your doubts about this lesson. Ask us anything that's confusing and our experts will provide explanations to help you understand.

## 📝 Quiz

****


